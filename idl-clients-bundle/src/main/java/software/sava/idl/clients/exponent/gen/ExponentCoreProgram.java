// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.idl.clients.exponent.gen.types.AdminAction;
import software.sava.idl.clients.exponent.gen.types.Amount;
import software.sava.idl.clients.exponent.gen.types.CollectTreasuryEmissionKind;
import software.sava.idl.clients.exponent.gen.types.CollectTreasuryInterestKind;
import software.sava.idl.clients.exponent.gen.types.CpiAccounts;
import software.sava.idl.clients.exponent.gen.types.MarketAdminAction;
import software.sava.idl.clients.exponent.gen.types.Number;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class ExponentCoreProgram {

  public static final Discriminator ADD_EMISSION_DISCRIMINATOR = toDiscriminator(18);

  /// @param robotTokenAccountKey Assert that the new token account is owned by the vault
  /// @param yieldPositionKey Increase the robot's position size
  public static List<AccountMeta> addEmissionKeys(final PublicKey authorityKey,
                                                  final PublicKey feePayerKey,
                                                  final PublicKey vaultKey,
                                                  final PublicKey adminKey,
                                                  final PublicKey syProgramKey,
                                                  final PublicKey addressLookupTableKey,
                                                  final PublicKey robotTokenAccountKey,
                                                  final PublicKey treasuryTokenAccountKey,
                                                  final PublicKey yieldPositionKey,
                                                  final PublicKey systemProgramKey) {
    return List.of(
      createReadOnlySigner(authorityKey),
      createWritableSigner(feePayerKey),
      createWrite(vaultKey),
      createRead(adminKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createRead(robotTokenAccountKey),
      createRead(treasuryTokenAccountKey),
      createWrite(yieldPositionKey),
      createRead(systemProgramKey)
    );
  }

  /// @param robotTokenAccountKey Assert that the new token account is owned by the vault
  /// @param yieldPositionKey Increase the robot's position size
  /// @param treasuryFeeBps: u16
  public static Instruction addEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                        final PublicKey authorityKey,
                                        final PublicKey feePayerKey,
                                        final PublicKey vaultKey,
                                        final PublicKey adminKey,
                                        final PublicKey syProgramKey,
                                        final PublicKey addressLookupTableKey,
                                        final PublicKey robotTokenAccountKey,
                                        final PublicKey treasuryTokenAccountKey,
                                        final PublicKey yieldPositionKey,
                                        final PublicKey systemProgramKey,
                                        final CpiAccounts cpiAccounts,
                                        final int treasuryFeeBps) {
    final var keys = addEmissionKeys(
      authorityKey,
      feePayerKey,
      vaultKey,
      adminKey,
      syProgramKey,
      addressLookupTableKey,
      robotTokenAccountKey,
      treasuryTokenAccountKey,
      yieldPositionKey,
      systemProgramKey
    );
    return addEmission(invokedExponentCoreProgramMeta, keys, cpiAccounts, treasuryFeeBps);
  }

  /// @param treasuryFeeBps: u16
  public static Instruction addEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                        final List<AccountMeta> keys,
                                        final CpiAccounts cpiAccounts,
                                        final int treasuryFeeBps) {
    final byte[] _data = new byte[3 + cpiAccounts.l()];
    int i = ADD_EMISSION_DISCRIMINATOR.write(_data, 0);
    i += cpiAccounts.write(_data, i);
    putInt16LE(_data, i, treasuryFeeBps);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param treasuryFeeBps: u16
  public record AddEmissionIxData(Discriminator discriminator, CpiAccounts cpiAccounts, int treasuryFeeBps) implements SerDe {

    public static AddEmissionIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int CPI_ACCOUNTS_OFFSET = 1;

    public static AddEmissionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var cpiAccounts = CpiAccounts.read(_data, i);
      i += cpiAccounts.l();
      final var treasuryFeeBps = Short.toUnsignedInt(getInt16LE(_data, i));
      return new AddEmissionIxData(discriminator, cpiAccounts, treasuryFeeBps);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += cpiAccounts.write(_data, i);
      putInt16LE(_data, i, treasuryFeeBps);
      i += 2;
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + cpiAccounts.l() + 2;
    }
  }

  public static final Discriminator ADD_FARM_DISCRIMINATOR = toDiscriminator(22);

  public static List<AccountMeta> addFarmKeys(final PublicKey marketKey,
                                              final PublicKey signerKey,
                                              final PublicKey feePayerKey,
                                              final PublicKey mintNewKey,
                                              final PublicKey adminStateKey,
                                              final PublicKey tokenSourceKey,
                                              final PublicKey tokenFarmKey,
                                              final PublicKey tokenProgramKey,
                                              final PublicKey systemProgramKey) {
    return List.of(
      createWrite(marketKey),
      createReadOnlySigner(signerKey),
      createWritableSigner(feePayerKey),
      createRead(mintNewKey),
      createRead(adminStateKey),
      createWrite(tokenSourceKey),
      createWrite(tokenFarmKey),
      createRead(tokenProgramKey),
      createRead(systemProgramKey)
    );
  }

  /// @param tokenRate: u64
  /// @param untilTimestamp: u32
  public static Instruction addFarm(final AccountMeta invokedExponentCoreProgramMeta,
                                    final PublicKey marketKey,
                                    final PublicKey signerKey,
                                    final PublicKey feePayerKey,
                                    final PublicKey mintNewKey,
                                    final PublicKey adminStateKey,
                                    final PublicKey tokenSourceKey,
                                    final PublicKey tokenFarmKey,
                                    final PublicKey tokenProgramKey,
                                    final PublicKey systemProgramKey,
                                    final long tokenRate,
                                    final long untilTimestamp) {
    final var keys = addFarmKeys(
      marketKey,
      signerKey,
      feePayerKey,
      mintNewKey,
      adminStateKey,
      tokenSourceKey,
      tokenFarmKey,
      tokenProgramKey,
      systemProgramKey
    );
    return addFarm(invokedExponentCoreProgramMeta, keys, tokenRate, untilTimestamp);
  }

  /// @param tokenRate: u64
  /// @param untilTimestamp: u32
  public static Instruction addFarm(final AccountMeta invokedExponentCoreProgramMeta,
                                    final List<AccountMeta> keys,
                                    final long tokenRate,
                                    final long untilTimestamp) {
    final byte[] _data = new byte[13];
    int i = ADD_FARM_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, tokenRate);
    i += 8;
    putInt32LE(_data, i, (int) untilTimestamp);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param tokenRate: u64
  /// @param untilTimestamp: u32
  public record AddFarmIxData(Discriminator discriminator, long tokenRate, long untilTimestamp) implements SerDe {

    public static AddFarmIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 13;

    public static final int TOKEN_RATE_OFFSET = 1;
    public static final int UNTIL_TIMESTAMP_OFFSET = 9;

    public static AddFarmIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var tokenRate = getInt64LE(_data, i);
      i += 8;
      final var untilTimestamp = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new AddFarmIxData(discriminator, tokenRate, untilTimestamp);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, tokenRate);
      i += 8;
      putInt32LE(_data, i, (int) untilTimestamp);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator ADD_LP_TOKENS_METADATA_DISCRIMINATOR = toDiscriminator(41);

  public static List<AccountMeta> addLpTokensMetadataKeys(final PublicKey payerKey,
                                                          final PublicKey adminKey,
                                                          final PublicKey marketKey,
                                                          final PublicKey mintLpKey,
                                                          final PublicKey metadataKey,
                                                          final PublicKey tokenMetadataProgramKey,
                                                          final PublicKey systemProgramKey) {
    return List.of(
      createWritableSigner(payerKey),
      createRead(adminKey),
      createRead(marketKey),
      createRead(mintLpKey),
      createWrite(metadataKey),
      createRead(tokenMetadataProgramKey),
      createRead(systemProgramKey)
    );
  }

  public static Instruction addLpTokensMetadata(final AccountMeta invokedExponentCoreProgramMeta,
                                                final PublicKey payerKey,
                                                final PublicKey adminKey,
                                                final PublicKey marketKey,
                                                final PublicKey mintLpKey,
                                                final PublicKey metadataKey,
                                                final PublicKey tokenMetadataProgramKey,
                                                final PublicKey systemProgramKey,
                                                final String name,
                                                final String symbol,
                                                final String uri) {
    final var keys = addLpTokensMetadataKeys(
      payerKey,
      adminKey,
      marketKey,
      mintLpKey,
      metadataKey,
      tokenMetadataProgramKey,
      systemProgramKey
    );
    return addLpTokensMetadata(
      invokedExponentCoreProgramMeta,
      keys,
      name,
      symbol,
      uri
    );
  }

  public static Instruction addLpTokensMetadata(final AccountMeta invokedExponentCoreProgramMeta,
                                                final List<AccountMeta> keys,
                                                final String name,
                                                final String symbol,
                                                final String uri) {
    final byte[] _name = SerDeUtil.encodeString(name);
    final byte[] _symbol = SerDeUtil.encodeString(symbol);
    final byte[] _uri = SerDeUtil.encodeString(uri);
    final byte[] _data = new byte[13 + _name.length + _symbol.length + _uri.length];
    int i = ADD_LP_TOKENS_METADATA_DISCRIMINATOR.write(_data, 0);
    i += SerDeUtil.writeVector(4, _name, _data, i);
    i += SerDeUtil.writeVector(4, _symbol, _data, i);
    SerDeUtil.writeVector(4, _uri, _data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record AddLpTokensMetadataIxData(Discriminator discriminator,
                                          String name, byte[] _name,
                                          String symbol, byte[] _symbol,
                                          String uri, byte[] _uri) implements SerDe {

    public static AddLpTokensMetadataIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int NAME_OFFSET = 1;

    public static AddLpTokensMetadataIxData createRecord(final Discriminator discriminator,
                                                         final String name,
                                                         final String symbol,
                                                         final String uri) {
      return new AddLpTokensMetadataIxData(discriminator, name, name == null ? null : SerDeUtil.encodeString(name), symbol, symbol == null ? null : SerDeUtil.encodeString(symbol), uri, uri == null ? null : SerDeUtil.encodeString(uri));
    }

    public static AddLpTokensMetadataIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final byte[] _name = SerDeUtil.readbyteVector(4, _data, i);
      final var name = SerDeUtil.decodeString(_name);
      i += 4 + _name.length;
      final byte[] _symbol = SerDeUtil.readbyteVector(4, _data, i);
      final var symbol = SerDeUtil.decodeString(_symbol);
      i += 4 + _symbol.length;
      final byte[] _uri = SerDeUtil.readbyteVector(4, _data, i);
      final var uri = SerDeUtil.decodeString(_uri);
      return new AddLpTokensMetadataIxData(discriminator, name, name == null ? null : SerDeUtil.encodeString(name), symbol, symbol == null ? null : SerDeUtil.encodeString(symbol), uri, uri == null ? null : SerDeUtil.encodeString(uri));
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeVector(4, _name, _data, i);
      i += SerDeUtil.writeVector(4, _symbol, _data, i);
      i += SerDeUtil.writeVector(4, _uri, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + 4 + _name.length + 4 + _symbol.length + 4 + _uri.length;
    }
  }

  public static final Discriminator ADD_MARKET_EMISSION_DISCRIMINATOR = toDiscriminator(25);

  public static List<AccountMeta> addMarketEmissionKeys(final PublicKey marketKey,
                                                        final PublicKey signerKey,
                                                        final PublicKey feePayerKey,
                                                        final PublicKey mintNewKey,
                                                        final PublicKey adminStateKey,
                                                        final PublicKey tokenEmissionKey,
                                                        final PublicKey tokenProgramKey,
                                                        final PublicKey systemProgramKey) {
    return List.of(
      createWrite(marketKey),
      createReadOnlySigner(signerKey),
      createWritableSigner(feePayerKey),
      createRead(mintNewKey),
      createRead(adminStateKey),
      createWrite(tokenEmissionKey),
      createRead(tokenProgramKey),
      createRead(systemProgramKey)
    );
  }

  public static Instruction addMarketEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                              final PublicKey marketKey,
                                              final PublicKey signerKey,
                                              final PublicKey feePayerKey,
                                              final PublicKey mintNewKey,
                                              final PublicKey adminStateKey,
                                              final PublicKey tokenEmissionKey,
                                              final PublicKey tokenProgramKey,
                                              final PublicKey systemProgramKey,
                                              final CpiAccounts cpiAccounts) {
    final var keys = addMarketEmissionKeys(
      marketKey,
      signerKey,
      feePayerKey,
      mintNewKey,
      adminStateKey,
      tokenEmissionKey,
      tokenProgramKey,
      systemProgramKey
    );
    return addMarketEmission(invokedExponentCoreProgramMeta, keys, cpiAccounts);
  }

  public static Instruction addMarketEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                              final List<AccountMeta> keys,
                                              final CpiAccounts cpiAccounts) {
    final byte[] _data = new byte[1 + cpiAccounts.l()];
    int i = ADD_MARKET_EMISSION_DISCRIMINATOR.write(_data, 0);
    cpiAccounts.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record AddMarketEmissionIxData(Discriminator discriminator, CpiAccounts cpiAccounts) implements SerDe {

    public static AddMarketEmissionIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int CPI_ACCOUNTS_OFFSET = 1;

    public static AddMarketEmissionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var cpiAccounts = CpiAccounts.read(_data, i);
      return new AddMarketEmissionIxData(discriminator, cpiAccounts);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += cpiAccounts.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + cpiAccounts.l();
    }
  }

  public static final Discriminator BUY_YT_DISCRIMINATOR = toDiscriminator(0);

  /// Buy YT with SY
  ///
  /// @param tokenSyTraderKey Source account for the trader's SY tokens
  /// @param tokenYtTraderKey Destination for receiving YT tokens
  /// @param tokenSyEscrowKey Temporary SY account owned by market
  /// @param tokenPtEscrowKey PT liquidity owned by market
  public static List<AccountMeta> buyYtKeys(final PublicKey traderKey,
                                            final PublicKey marketKey,
                                            final PublicKey tokenSyTraderKey,
                                            final PublicKey tokenYtTraderKey,
                                            final PublicKey tokenPtTraderKey,
                                            final PublicKey tokenSyEscrowKey,
                                            final PublicKey tokenPtEscrowKey,
                                            final PublicKey tokenFeeTreasurySyKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey vaultAuthorityKey,
                                            final PublicKey vaultKey,
                                            final PublicKey tokenSyEscrowVaultKey,
                                            final PublicKey mintYtKey,
                                            final PublicKey mintPtKey,
                                            final PublicKey addressLookupTableVaultKey,
                                            final PublicKey yieldPositionKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey) {
    return List.of(
      createWritableSigner(traderKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenYtTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(tokenProgramKey),
      createRead(addressLookupTableKey),
      createRead(syProgramKey),
      createWrite(vaultAuthorityKey),
      createWrite(vaultKey),
      createWrite(tokenSyEscrowVaultKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(addressLookupTableVaultKey),
      createWrite(yieldPositionKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Buy YT with SY
  ///
  /// @param tokenSyTraderKey Source account for the trader's SY tokens
  /// @param tokenYtTraderKey Destination for receiving YT tokens
  /// @param tokenSyEscrowKey Temporary SY account owned by market
  /// @param tokenPtEscrowKey PT liquidity owned by market
  /// @param syIn: u64
  /// @param ytOut: u64
  public static Instruction buyYt(final AccountMeta invokedExponentCoreProgramMeta,
                                  final PublicKey traderKey,
                                  final PublicKey marketKey,
                                  final PublicKey tokenSyTraderKey,
                                  final PublicKey tokenYtTraderKey,
                                  final PublicKey tokenPtTraderKey,
                                  final PublicKey tokenSyEscrowKey,
                                  final PublicKey tokenPtEscrowKey,
                                  final PublicKey tokenFeeTreasurySyKey,
                                  final PublicKey tokenProgramKey,
                                  final PublicKey addressLookupTableKey,
                                  final PublicKey syProgramKey,
                                  final PublicKey vaultAuthorityKey,
                                  final PublicKey vaultKey,
                                  final PublicKey tokenSyEscrowVaultKey,
                                  final PublicKey mintYtKey,
                                  final PublicKey mintPtKey,
                                  final PublicKey addressLookupTableVaultKey,
                                  final PublicKey yieldPositionKey,
                                  final PublicKey eventAuthorityKey,
                                  final PublicKey programKey,
                                  final long syIn,
                                  final long ytOut) {
    final var keys = buyYtKeys(
      traderKey,
      marketKey,
      tokenSyTraderKey,
      tokenYtTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      tokenFeeTreasurySyKey,
      tokenProgramKey,
      addressLookupTableKey,
      syProgramKey,
      vaultAuthorityKey,
      vaultKey,
      tokenSyEscrowVaultKey,
      mintYtKey,
      mintPtKey,
      addressLookupTableVaultKey,
      yieldPositionKey,
      eventAuthorityKey,
      programKey
    );
    return buyYt(invokedExponentCoreProgramMeta, keys, syIn, ytOut);
  }

  /// Buy YT with SY
  ///
  /// @param syIn: u64
  /// @param ytOut: u64
  public static Instruction buyYt(final AccountMeta invokedExponentCoreProgramMeta,
                                  final List<AccountMeta> keys,
                                  final long syIn,
                                  final long ytOut) {
    final byte[] _data = new byte[17];
    int i = BUY_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, syIn);
    i += 8;
    putInt64LE(_data, i, ytOut);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param syIn: u64
  /// @param ytOut: u64
  public record BuyYtIxData(Discriminator discriminator, long syIn, long ytOut) implements SerDe {

    public static BuyYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 17;

    public static final int SY_IN_OFFSET = 1;
    public static final int YT_OUT_OFFSET = 9;

    public static BuyYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var syIn = getInt64LE(_data, i);
      i += 8;
      final var ytOut = getInt64LE(_data, i);
      return new BuyYtIxData(discriminator, syIn, ytOut);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, syIn);
      i += 8;
      putInt64LE(_data, i, ytOut);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator CLAIM_FARM_EMISSIONS_DISCRIMINATOR = toDiscriminator(24);

  public static List<AccountMeta> claimFarmEmissionsKeys(final PublicKey ownerKey,
                                                         final PublicKey marketKey,
                                                         final PublicKey lpPositionKey,
                                                         final PublicKey tokenDstKey,
                                                         final PublicKey mintKey,
                                                         final PublicKey tokenFarmKey,
                                                         final PublicKey tokenProgramKey,
                                                         final PublicKey eventAuthorityKey,
                                                         final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createRead(marketKey),
      createWrite(lpPositionKey),
      createWrite(tokenDstKey),
      createRead(mintKey),
      createWrite(tokenFarmKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction claimFarmEmissions(final AccountMeta invokedExponentCoreProgramMeta,
                                               final PublicKey ownerKey,
                                               final PublicKey marketKey,
                                               final PublicKey lpPositionKey,
                                               final PublicKey tokenDstKey,
                                               final PublicKey mintKey,
                                               final PublicKey tokenFarmKey,
                                               final PublicKey tokenProgramKey,
                                               final PublicKey eventAuthorityKey,
                                               final PublicKey programKey,
                                               final Amount amount) {
    final var keys = claimFarmEmissionsKeys(
      ownerKey,
      marketKey,
      lpPositionKey,
      tokenDstKey,
      mintKey,
      tokenFarmKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return claimFarmEmissions(invokedExponentCoreProgramMeta, keys, amount);
  }

  public static Instruction claimFarmEmissions(final AccountMeta invokedExponentCoreProgramMeta,
                                               final List<AccountMeta> keys,
                                               final Amount amount) {
    final byte[] _data = new byte[1 + amount.l()];
    int i = CLAIM_FARM_EMISSIONS_DISCRIMINATOR.write(_data, 0);
    amount.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record ClaimFarmEmissionsIxData(Discriminator discriminator, Amount amount) implements SerDe {

    public static ClaimFarmEmissionsIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int AMOUNT_OFFSET = 1;

    public static ClaimFarmEmissionsIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = Amount.read(_data, i);
      return new ClaimFarmEmissionsIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += amount.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + amount.l();
    }
  }

  public static final Discriminator COLLECT_EMISSION_DISCRIMINATOR = toDiscriminator(19);

  public static List<AccountMeta> collectEmissionKeys(final PublicKey ownerKey,
                                                      final PublicKey vaultKey,
                                                      final PublicKey positionKey,
                                                      final PublicKey syProgramKey,
                                                      final PublicKey authorityKey,
                                                      final PublicKey emissionEscrowKey,
                                                      final PublicKey emissionDstKey,
                                                      final PublicKey addressLookupTableKey,
                                                      final PublicKey treasuryEmissionTokenAccountKey,
                                                      final PublicKey tokenProgramKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(vaultKey),
      createWrite(positionKey),
      createRead(syProgramKey),
      createRead(authorityKey),
      createWrite(emissionEscrowKey),
      createWrite(emissionDstKey),
      createRead(addressLookupTableKey),
      createWrite(treasuryEmissionTokenAccountKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param index: u16
  public static Instruction collectEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                            final PublicKey ownerKey,
                                            final PublicKey vaultKey,
                                            final PublicKey positionKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey authorityKey,
                                            final PublicKey emissionEscrowKey,
                                            final PublicKey emissionDstKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey treasuryEmissionTokenAccountKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final int index,
                                            final Amount amount) {
    final var keys = collectEmissionKeys(
      ownerKey,
      vaultKey,
      positionKey,
      syProgramKey,
      authorityKey,
      emissionEscrowKey,
      emissionDstKey,
      addressLookupTableKey,
      treasuryEmissionTokenAccountKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return collectEmission(invokedExponentCoreProgramMeta, keys, index, amount);
  }

  /// @param index: u16
  public static Instruction collectEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                            final List<AccountMeta> keys,
                                            final int index,
                                            final Amount amount) {
    final byte[] _data = new byte[3 + amount.l()];
    int i = COLLECT_EMISSION_DISCRIMINATOR.write(_data, 0);
    putInt16LE(_data, i, index);
    i += 2;
    amount.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param index: u16
  public record CollectEmissionIxData(Discriminator discriminator, int index, Amount amount) implements SerDe {

    public static CollectEmissionIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int INDEX_OFFSET = 1;
    public static final int AMOUNT_OFFSET = 3;

    public static CollectEmissionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var index = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var amount = Amount.read(_data, i);
      return new CollectEmissionIxData(discriminator, index, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt16LE(_data, i, index);
      i += 2;
      i += amount.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + 2 + amount.l();
    }
  }

  public static final Discriminator COLLECT_INTEREST_DISCRIMINATOR = toDiscriminator(6);

  /// @param ownerKey Owner of the YieldTokenPosition
  /// @param yieldPositionKey Owner's position data for YT deposits
  /// @param vaultKey The vault that holds the SY tokens in escrow
  /// @param tokenSyDstKey The receiving token account for SY withdrawn
  public static List<AccountMeta> collectInterestKeys(final PublicKey ownerKey,
                                                      final PublicKey yieldPositionKey,
                                                      final PublicKey vaultKey,
                                                      final PublicKey tokenSyDstKey,
                                                      final PublicKey escrowSyKey,
                                                      final PublicKey authorityKey,
                                                      final PublicKey tokenProgramKey,
                                                      final PublicKey syProgramKey,
                                                      final PublicKey treasurySyTokenAccountKey,
                                                      final PublicKey addressLookupTableKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(yieldPositionKey),
      createWrite(vaultKey),
      createWrite(tokenSyDstKey),
      createWrite(escrowSyKey),
      createWrite(authorityKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(treasurySyTokenAccountKey),
      createRead(addressLookupTableKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param ownerKey Owner of the YieldTokenPosition
  /// @param yieldPositionKey Owner's position data for YT deposits
  /// @param vaultKey The vault that holds the SY tokens in escrow
  /// @param tokenSyDstKey The receiving token account for SY withdrawn
  public static Instruction collectInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                            final PublicKey ownerKey,
                                            final PublicKey yieldPositionKey,
                                            final PublicKey vaultKey,
                                            final PublicKey tokenSyDstKey,
                                            final PublicKey escrowSyKey,
                                            final PublicKey authorityKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey treasurySyTokenAccountKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final Amount amount) {
    final var keys = collectInterestKeys(
      ownerKey,
      yieldPositionKey,
      vaultKey,
      tokenSyDstKey,
      escrowSyKey,
      authorityKey,
      tokenProgramKey,
      syProgramKey,
      treasurySyTokenAccountKey,
      addressLookupTableKey,
      eventAuthorityKey,
      programKey
    );
    return collectInterest(invokedExponentCoreProgramMeta, keys, amount);
  }

  public static Instruction collectInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                            final List<AccountMeta> keys,
                                            final Amount amount) {
    final byte[] _data = new byte[1 + amount.l()];
    int i = COLLECT_INTEREST_DISCRIMINATOR.write(_data, 0);
    amount.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record CollectInterestIxData(Discriminator discriminator, Amount amount) implements SerDe {

    public static CollectInterestIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int AMOUNT_OFFSET = 1;

    public static CollectInterestIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = Amount.read(_data, i);
      return new CollectInterestIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += amount.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + amount.l();
    }
  }

  public static final Discriminator COLLECT_TREASURY_EMISSION_DISCRIMINATOR = toDiscriminator(20);

  /// @param yieldPositionKey constrained by vault
  /// @param authorityKey constrained by vault
  public static List<AccountMeta> collectTreasuryEmissionKeys(final PublicKey signerKey,
                                                              final PublicKey yieldPositionKey,
                                                              final PublicKey vaultKey,
                                                              final PublicKey syProgramKey,
                                                              final PublicKey authorityKey,
                                                              final PublicKey emissionEscrowKey,
                                                              final PublicKey emissionDstKey,
                                                              final PublicKey addressLookupTableKey,
                                                              final PublicKey tokenProgramKey,
                                                              final PublicKey adminKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(yieldPositionKey),
      createWrite(vaultKey),
      createRead(syProgramKey),
      createRead(authorityKey),
      createWrite(emissionEscrowKey),
      createWrite(emissionDstKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(adminKey)
    );
  }

  /// @param yieldPositionKey constrained by vault
  /// @param authorityKey constrained by vault
  /// @param emissionIndex: u16
  public static Instruction collectTreasuryEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final PublicKey signerKey,
                                                    final PublicKey yieldPositionKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey authorityKey,
                                                    final PublicKey emissionEscrowKey,
                                                    final PublicKey emissionDstKey,
                                                    final PublicKey addressLookupTableKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey adminKey,
                                                    final int emissionIndex,
                                                    final Amount amount,
                                                    final CollectTreasuryEmissionKind kind) {
    final var keys = collectTreasuryEmissionKeys(
      signerKey,
      yieldPositionKey,
      vaultKey,
      syProgramKey,
      authorityKey,
      emissionEscrowKey,
      emissionDstKey,
      addressLookupTableKey,
      tokenProgramKey,
      adminKey
    );
    return collectTreasuryEmission(
      invokedExponentCoreProgramMeta,
      keys,
      emissionIndex,
      amount,
      kind
    );
  }

  /// @param emissionIndex: u16
  public static Instruction collectTreasuryEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final int emissionIndex,
                                                    final Amount amount,
                                                    final CollectTreasuryEmissionKind kind) {
    final byte[] _data = new byte[3 + amount.l() + kind.l()];
    int i = COLLECT_TREASURY_EMISSION_DISCRIMINATOR.write(_data, 0);
    putInt16LE(_data, i, emissionIndex);
    i += 2;
    i += amount.write(_data, i);
    kind.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param emissionIndex: u16
  public record CollectTreasuryEmissionIxData(Discriminator discriminator,
                                              int emissionIndex,
                                              Amount amount,
                                              CollectTreasuryEmissionKind kind) implements SerDe {

    public static CollectTreasuryEmissionIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int EMISSION_INDEX_OFFSET = 1;
    public static final int AMOUNT_OFFSET = 3;

    public static CollectTreasuryEmissionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var amount = Amount.read(_data, i);
      i += amount.l();
      final var kind = CollectTreasuryEmissionKind.read(_data, i);
      return new CollectTreasuryEmissionIxData(discriminator, emissionIndex, amount, kind);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt16LE(_data, i, emissionIndex);
      i += 2;
      i += amount.write(_data, i);
      i += kind.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + 2 + amount.l() + kind.l();
    }
  }

  public static final Discriminator COLLECT_TREASURY_INTEREST_DISCRIMINATOR = toDiscriminator(21);

  /// @param vaultKey The vault that holds the SY tokens in escrow
  /// @param syDstKey The receiving token account for SY withdrawn
  public static List<AccountMeta> collectTreasuryInterestKeys(final PublicKey signerKey,
                                                              final PublicKey yieldPositionKey,
                                                              final PublicKey vaultKey,
                                                              final PublicKey syDstKey,
                                                              final PublicKey escrowSyKey,
                                                              final PublicKey authorityKey,
                                                              final PublicKey tokenProgramKey,
                                                              final PublicKey syProgramKey,
                                                              final PublicKey addressLookupTableKey,
                                                              final PublicKey adminKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(yieldPositionKey),
      createWrite(vaultKey),
      createWrite(syDstKey),
      createWrite(escrowSyKey),
      createRead(authorityKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createRead(adminKey)
    );
  }

  /// @param vaultKey The vault that holds the SY tokens in escrow
  /// @param syDstKey The receiving token account for SY withdrawn
  public static Instruction collectTreasuryInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final PublicKey signerKey,
                                                    final PublicKey yieldPositionKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey syDstKey,
                                                    final PublicKey escrowSyKey,
                                                    final PublicKey authorityKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey addressLookupTableKey,
                                                    final PublicKey adminKey,
                                                    final Amount amount,
                                                    final CollectTreasuryInterestKind kind) {
    final var keys = collectTreasuryInterestKeys(
      signerKey,
      yieldPositionKey,
      vaultKey,
      syDstKey,
      escrowSyKey,
      authorityKey,
      tokenProgramKey,
      syProgramKey,
      addressLookupTableKey,
      adminKey
    );
    return collectTreasuryInterest(invokedExponentCoreProgramMeta, keys, amount, kind);
  }

  public static Instruction collectTreasuryInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final Amount amount,
                                                    final CollectTreasuryInterestKind kind) {
    final byte[] _data = new byte[1 + amount.l() + kind.l()];
    int i = COLLECT_TREASURY_INTEREST_DISCRIMINATOR.write(_data, 0);
    i += amount.write(_data, i);
    kind.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record CollectTreasuryInterestIxData(Discriminator discriminator, Amount amount, CollectTreasuryInterestKind kind) implements SerDe {

    public static CollectTreasuryInterestIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int AMOUNT_OFFSET = 1;

    public static CollectTreasuryInterestIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = Amount.read(_data, i);
      i += amount.l();
      final var kind = CollectTreasuryInterestKind.read(_data, i);
      return new CollectTreasuryInterestIxData(discriminator, amount, kind);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += amount.write(_data, i);
      i += kind.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + amount.l() + kind.l();
    }
  }

  public static final Discriminator DEPOSIT_YT_DISCRIMINATOR = toDiscriminator(7);

  /// Deposit YT into escrow in order to earn rewards & SY interest
  ///
  /// @param depositorKey Permissionless depositor - not necessarily the owner of the YieldTokenPosition
  /// @param vaultKey Vault that manages YT
  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param ytSrcKey Depositor's YT token account
  /// @param escrowYtKey Vault-owned escrow account for YT
  /// @param syProgramKey The SY interface implementation for the vault's token
  /// @param yieldPositionKey Vault-owned yield token position
  public static List<AccountMeta> depositYtKeys(final PublicKey depositorKey,
                                                final PublicKey vaultKey,
                                                final PublicKey userYieldPositionKey,
                                                final PublicKey ytSrcKey,
                                                final PublicKey escrowYtKey,
                                                final PublicKey tokenProgramKey,
                                                final PublicKey syProgramKey,
                                                final PublicKey addressLookupTableKey,
                                                final PublicKey yieldPositionKey,
                                                final PublicKey systemProgramKey,
                                                final PublicKey eventAuthorityKey,
                                                final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(vaultKey),
      createWrite(userYieldPositionKey),
      createWrite(ytSrcKey),
      createWrite(escrowYtKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createWrite(yieldPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Deposit YT into escrow in order to earn rewards & SY interest
  ///
  /// @param depositorKey Permissionless depositor - not necessarily the owner of the YieldTokenPosition
  /// @param vaultKey Vault that manages YT
  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param ytSrcKey Depositor's YT token account
  /// @param escrowYtKey Vault-owned escrow account for YT
  /// @param syProgramKey The SY interface implementation for the vault's token
  /// @param yieldPositionKey Vault-owned yield token position
  /// @param amount: u64
  public static Instruction depositYt(final AccountMeta invokedExponentCoreProgramMeta,
                                      final PublicKey depositorKey,
                                      final PublicKey vaultKey,
                                      final PublicKey userYieldPositionKey,
                                      final PublicKey ytSrcKey,
                                      final PublicKey escrowYtKey,
                                      final PublicKey tokenProgramKey,
                                      final PublicKey syProgramKey,
                                      final PublicKey addressLookupTableKey,
                                      final PublicKey yieldPositionKey,
                                      final PublicKey systemProgramKey,
                                      final PublicKey eventAuthorityKey,
                                      final PublicKey programKey,
                                      final long amount) {
    final var keys = depositYtKeys(
      depositorKey,
      vaultKey,
      userYieldPositionKey,
      ytSrcKey,
      escrowYtKey,
      tokenProgramKey,
      syProgramKey,
      addressLookupTableKey,
      yieldPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return depositYt(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// Deposit YT into escrow in order to earn rewards & SY interest
  ///
  /// @param amount: u64
  public static Instruction depositYt(final AccountMeta invokedExponentCoreProgramMeta,
                                      final List<AccountMeta> keys,
                                      final long amount) {
    final byte[] _data = new byte[9];
    int i = DEPOSIT_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record DepositYtIxData(Discriminator discriminator, long amount) implements SerDe {

    public static DepositYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static DepositYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new DepositYtIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator INIT_LP_POSITION_DISCRIMINATOR = toDiscriminator(13);

  /// Initialize a LP position for a user to deposit LP tokens into
  ///
  public static List<AccountMeta> initLpPositionKeys(final PublicKey feePayerKey,
                                                     final PublicKey ownerKey,
                                                     final PublicKey marketKey,
                                                     final PublicKey lpPositionKey,
                                                     final PublicKey systemProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey) {
    return List.of(
      createWritableSigner(feePayerKey),
      createRead(ownerKey),
      createRead(marketKey),
      createWrite(lpPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Initialize a LP position for a user to deposit LP tokens into
  ///
  public static Instruction initLpPosition(final AccountMeta invokedExponentCoreProgramMeta,
                                           final PublicKey feePayerKey,
                                           final PublicKey ownerKey,
                                           final PublicKey marketKey,
                                           final PublicKey lpPositionKey,
                                           final PublicKey systemProgramKey,
                                           final PublicKey eventAuthorityKey,
                                           final PublicKey programKey) {
    final var keys = initLpPositionKeys(
      feePayerKey,
      ownerKey,
      marketKey,
      lpPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return initLpPosition(invokedExponentCoreProgramMeta, keys);
  }

  /// Initialize a LP position for a user to deposit LP tokens into
  ///
  public static Instruction initLpPosition(final AccountMeta invokedExponentCoreProgramMeta,
                                           final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, INIT_LP_POSITION_DISCRIMINATOR);
  }

  public static final Discriminator INIT_MARKET_TWO_DISCRIMINATOR = toDiscriminator(10);

  /// @param marketKey There is 1 market per vault
  /// @param vaultKey Links the mint_sy & mint_pt & sy_program together
  /// @param escrowSyKey This account for SY is only a temporary pass-through account
  ///                    It is used to transfer SY tokens from the signer to the market
  ///                    And then from the market to the SY program's escrow
  /// @param escrowLpKey Holds activated LP tokens for farming & SY emissions
  /// @param ptSrcKey Signer's PT token account
  /// @param sySrcKey Signer's SY token account
  /// @param lpDstKey Receiving account for LP tokens
  /// @param tokenProgramKey Use the old Token program as the implementation for PT & SY & LP tokens
  public static List<AccountMeta> initMarketTwoKeys(final PublicKey payerKey,
                                                    final PublicKey adminSignerKey,
                                                    final PublicKey marketKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey mintSyKey,
                                                    final PublicKey mintPtKey,
                                                    final PublicKey mintLpKey,
                                                    final PublicKey escrowPtKey,
                                                    final PublicKey escrowSyKey,
                                                    final PublicKey escrowLpKey,
                                                    final PublicKey ptSrcKey,
                                                    final PublicKey sySrcKey,
                                                    final PublicKey lpDstKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey systemProgramKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey associatedTokenProgramKey,
                                                    final PublicKey addressLookupTableKey,
                                                    final PublicKey adminKey,
                                                    final PublicKey tokenTreasuryFeeSyKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(adminSignerKey),
      createWrite(marketKey),
      createRead(vaultKey),
      createRead(mintSyKey),
      createRead(mintPtKey),
      createWrite(mintLpKey),
      createWrite(escrowPtKey),
      createWrite(escrowSyKey),
      createWrite(escrowLpKey),
      createWrite(ptSrcKey),
      createWrite(sySrcKey),
      createWrite(lpDstKey),
      createRead(tokenProgramKey),
      createRead(systemProgramKey),
      createRead(syProgramKey),
      createRead(associatedTokenProgramKey),
      createRead(addressLookupTableKey),
      createRead(adminKey),
      createRead(tokenTreasuryFeeSyKey)
    );
  }

  /// @param marketKey There is 1 market per vault
  /// @param vaultKey Links the mint_sy & mint_pt & sy_program together
  /// @param escrowSyKey This account for SY is only a temporary pass-through account
  ///                    It is used to transfer SY tokens from the signer to the market
  ///                    And then from the market to the SY program's escrow
  /// @param escrowLpKey Holds activated LP tokens for farming & SY emissions
  /// @param ptSrcKey Signer's PT token account
  /// @param sySrcKey Signer's SY token account
  /// @param lpDstKey Receiving account for LP tokens
  /// @param tokenProgramKey Use the old Token program as the implementation for PT & SY & LP tokens
  /// @param ptInit: u64
  /// @param syInit: u64
  /// @param feeTreasurySyBps: u16
  /// @param seedId: u8
  public static Instruction initMarketTwo(final AccountMeta invokedExponentCoreProgramMeta,
                                          final PublicKey payerKey,
                                          final PublicKey adminSignerKey,
                                          final PublicKey marketKey,
                                          final PublicKey vaultKey,
                                          final PublicKey mintSyKey,
                                          final PublicKey mintPtKey,
                                          final PublicKey mintLpKey,
                                          final PublicKey escrowPtKey,
                                          final PublicKey escrowSyKey,
                                          final PublicKey escrowLpKey,
                                          final PublicKey ptSrcKey,
                                          final PublicKey sySrcKey,
                                          final PublicKey lpDstKey,
                                          final PublicKey tokenProgramKey,
                                          final PublicKey systemProgramKey,
                                          final PublicKey syProgramKey,
                                          final PublicKey associatedTokenProgramKey,
                                          final PublicKey addressLookupTableKey,
                                          final PublicKey adminKey,
                                          final PublicKey tokenTreasuryFeeSyKey,
                                          final double lnFeeRateRoot,
                                          final double rateScalarRoot,
                                          final double initRateAnchor,
                                          final Number syExchangeRate,
                                          final long ptInit,
                                          final long syInit,
                                          final int feeTreasurySyBps,
                                          final CpiAccounts cpiAccounts,
                                          final int seedId) {
    final var keys = initMarketTwoKeys(
      payerKey,
      adminSignerKey,
      marketKey,
      vaultKey,
      mintSyKey,
      mintPtKey,
      mintLpKey,
      escrowPtKey,
      escrowSyKey,
      escrowLpKey,
      ptSrcKey,
      sySrcKey,
      lpDstKey,
      tokenProgramKey,
      systemProgramKey,
      syProgramKey,
      associatedTokenProgramKey,
      addressLookupTableKey,
      adminKey,
      tokenTreasuryFeeSyKey
    );
    return initMarketTwo(
      invokedExponentCoreProgramMeta,
      keys,
      lnFeeRateRoot,
      rateScalarRoot,
      initRateAnchor,
      syExchangeRate,
      ptInit,
      syInit,
      feeTreasurySyBps,
      cpiAccounts,
      seedId
    );
  }

  /// @param ptInit: u64
  /// @param syInit: u64
  /// @param feeTreasurySyBps: u16
  /// @param seedId: u8
  public static Instruction initMarketTwo(final AccountMeta invokedExponentCoreProgramMeta,
                                          final List<AccountMeta> keys,
                                          final double lnFeeRateRoot,
                                          final double rateScalarRoot,
                                          final double initRateAnchor,
                                          final Number syExchangeRate,
                                          final long ptInit,
                                          final long syInit,
                                          final int feeTreasurySyBps,
                                          final CpiAccounts cpiAccounts,
                                          final int seedId) {
    final byte[] _data = new byte[44 + syExchangeRate.l() + cpiAccounts.l()];
    int i = INIT_MARKET_TWO_DISCRIMINATOR.write(_data, 0);
    putFloat64LE(_data, i, lnFeeRateRoot);
    i += 8;
    putFloat64LE(_data, i, rateScalarRoot);
    i += 8;
    putFloat64LE(_data, i, initRateAnchor);
    i += 8;
    i += syExchangeRate.write(_data, i);
    putInt64LE(_data, i, ptInit);
    i += 8;
    putInt64LE(_data, i, syInit);
    i += 8;
    putInt16LE(_data, i, feeTreasurySyBps);
    i += 2;
    i += cpiAccounts.write(_data, i);
    _data[i] = (byte) seedId;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ptInit: u64
  /// @param syInit: u64
  /// @param feeTreasurySyBps: u16
  /// @param seedId: u8
  public record InitMarketTwoIxData(Discriminator discriminator,
                                    double lnFeeRateRoot,
                                    double rateScalarRoot,
                                    double initRateAnchor,
                                    Number syExchangeRate,
                                    long ptInit,
                                    long syInit,
                                    int feeTreasurySyBps,
                                    CpiAccounts cpiAccounts,
                                    int seedId) implements SerDe {

    public static InitMarketTwoIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int LN_FEE_RATE_ROOT_OFFSET = 1;
    public static final int RATE_SCALAR_ROOT_OFFSET = 9;
    public static final int INIT_RATE_ANCHOR_OFFSET = 17;
    public static final int SY_EXCHANGE_RATE_OFFSET = 25;
    public static final int PT_INIT_OFFSET = 57;
    public static final int SY_INIT_OFFSET = 65;
    public static final int FEE_TREASURY_SY_BPS_OFFSET = 73;
    public static final int CPI_ACCOUNTS_OFFSET = 75;

    public static InitMarketTwoIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var lnFeeRateRoot = getFloat64LE(_data, i);
      i += 8;
      final var rateScalarRoot = getFloat64LE(_data, i);
      i += 8;
      final var initRateAnchor = getFloat64LE(_data, i);
      i += 8;
      final var syExchangeRate = Number.read(_data, i);
      i += syExchangeRate.l();
      final var ptInit = getInt64LE(_data, i);
      i += 8;
      final var syInit = getInt64LE(_data, i);
      i += 8;
      final var feeTreasurySyBps = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var cpiAccounts = CpiAccounts.read(_data, i);
      i += cpiAccounts.l();
      final var seedId = _data[i] & 0xFF;
      return new InitMarketTwoIxData(discriminator,
                                     lnFeeRateRoot,
                                     rateScalarRoot,
                                     initRateAnchor,
                                     syExchangeRate,
                                     ptInit,
                                     syInit,
                                     feeTreasurySyBps,
                                     cpiAccounts,
                                     seedId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putFloat64LE(_data, i, lnFeeRateRoot);
      i += 8;
      putFloat64LE(_data, i, rateScalarRoot);
      i += 8;
      putFloat64LE(_data, i, initRateAnchor);
      i += 8;
      i += syExchangeRate.write(_data, i);
      putInt64LE(_data, i, ptInit);
      i += 8;
      putInt64LE(_data, i, syInit);
      i += 8;
      putInt16LE(_data, i, feeTreasurySyBps);
      i += 2;
      i += cpiAccounts.write(_data, i);
      _data[i] = (byte) seedId;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + 8
           + 8
           + 8
           + syExchangeRate.l()
           + 8
           + 8
           + 2
           + cpiAccounts.l()
           + 1;
    }
  }

  public static final Discriminator INITIALIZE_VAULT_DISCRIMINATOR = toDiscriminator(2);

  /// High-trust function to init vault
  ///
  /// @param authorityKey The signer for the vault
  public static List<AccountMeta> initializeVaultKeys(final PublicKey payerKey,
                                                      final PublicKey adminKey,
                                                      final PublicKey authorityKey,
                                                      final PublicKey vaultKey,
                                                      final PublicKey mintPtKey,
                                                      final PublicKey mintYtKey,
                                                      final PublicKey escrowYtKey,
                                                      final PublicKey escrowSyKey,
                                                      final PublicKey mintSyKey,
                                                      final PublicKey systemProgramKey,
                                                      final PublicKey tokenProgramKey,
                                                      final PublicKey treasuryTokenAccountKey,
                                                      final PublicKey associatedTokenProgramKey,
                                                      final PublicKey syProgramKey,
                                                      final PublicKey addressLookupTableKey,
                                                      final PublicKey yieldPositionKey,
                                                      final PublicKey metadataKey,
                                                      final PublicKey tokenMetadataProgramKey) {
    return List.of(
      createWritableSigner(payerKey),
      createRead(adminKey),
      createRead(authorityKey),
      createWritableSigner(vaultKey),
      createWrite(mintPtKey),
      createWrite(mintYtKey),
      createWrite(escrowYtKey),
      createWrite(escrowSyKey),
      createRead(mintSyKey),
      createRead(systemProgramKey),
      createRead(tokenProgramKey),
      createRead(treasuryTokenAccountKey),
      createRead(associatedTokenProgramKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createWrite(yieldPositionKey),
      createWrite(metadataKey),
      createRead(tokenMetadataProgramKey)
    );
  }

  /// High-trust function to init vault
  ///
  /// @param authorityKey The signer for the vault
  /// @param startTimestamp: u32
  /// @param duration: u32
  /// @param interestBpsFee: u16
  /// @param minOpSizeStrip: u64
  /// @param minOpSizeMerge: u64
  public static Instruction initializeVault(final AccountMeta invokedExponentCoreProgramMeta,
                                            final PublicKey payerKey,
                                            final PublicKey adminKey,
                                            final PublicKey authorityKey,
                                            final PublicKey vaultKey,
                                            final PublicKey mintPtKey,
                                            final PublicKey mintYtKey,
                                            final PublicKey escrowYtKey,
                                            final PublicKey escrowSyKey,
                                            final PublicKey mintSyKey,
                                            final PublicKey systemProgramKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey treasuryTokenAccountKey,
                                            final PublicKey associatedTokenProgramKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey yieldPositionKey,
                                            final PublicKey metadataKey,
                                            final PublicKey tokenMetadataProgramKey,
                                            final long startTimestamp,
                                            final long duration,
                                            final int interestBpsFee,
                                            final CpiAccounts cpiAccounts,
                                            final long minOpSizeStrip,
                                            final long minOpSizeMerge,
                                            final String ptMetadataName,
                                            final String ptMetadataSymbol,
                                            final String ptMetadataUri) {
    final var keys = initializeVaultKeys(
      payerKey,
      adminKey,
      authorityKey,
      vaultKey,
      mintPtKey,
      mintYtKey,
      escrowYtKey,
      escrowSyKey,
      mintSyKey,
      systemProgramKey,
      tokenProgramKey,
      treasuryTokenAccountKey,
      associatedTokenProgramKey,
      syProgramKey,
      addressLookupTableKey,
      yieldPositionKey,
      metadataKey,
      tokenMetadataProgramKey
    );
    return initializeVault(
      invokedExponentCoreProgramMeta,
      keys,
      startTimestamp,
      duration,
      interestBpsFee,
      cpiAccounts,
      minOpSizeStrip,
      minOpSizeMerge,
      ptMetadataName,
      ptMetadataSymbol,
      ptMetadataUri
    );
  }

  /// High-trust function to init vault
  ///
  /// @param startTimestamp: u32
  /// @param duration: u32
  /// @param interestBpsFee: u16
  /// @param minOpSizeStrip: u64
  /// @param minOpSizeMerge: u64
  public static Instruction initializeVault(final AccountMeta invokedExponentCoreProgramMeta,
                                            final List<AccountMeta> keys,
                                            final long startTimestamp,
                                            final long duration,
                                            final int interestBpsFee,
                                            final CpiAccounts cpiAccounts,
                                            final long minOpSizeStrip,
                                            final long minOpSizeMerge,
                                            final String ptMetadataName,
                                            final String ptMetadataSymbol,
                                            final String ptMetadataUri) {
    final byte[] _ptMetadataName = SerDeUtil.encodeString(ptMetadataName);
    final byte[] _ptMetadataSymbol = SerDeUtil.encodeString(ptMetadataSymbol);
    final byte[] _ptMetadataUri = SerDeUtil.encodeString(ptMetadataUri);
    final byte[] _data = new byte[39 + cpiAccounts.l() + _ptMetadataName.length + _ptMetadataSymbol.length + _ptMetadataUri.length];
    int i = INITIALIZE_VAULT_DISCRIMINATOR.write(_data, 0);
    putInt32LE(_data, i, (int) startTimestamp);
    i += 4;
    putInt32LE(_data, i, (int) duration);
    i += 4;
    putInt16LE(_data, i, interestBpsFee);
    i += 2;
    i += cpiAccounts.write(_data, i);
    putInt64LE(_data, i, minOpSizeStrip);
    i += 8;
    putInt64LE(_data, i, minOpSizeMerge);
    i += 8;
    i += SerDeUtil.writeVector(4, _ptMetadataName, _data, i);
    i += SerDeUtil.writeVector(4, _ptMetadataSymbol, _data, i);
    SerDeUtil.writeVector(4, _ptMetadataUri, _data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param startTimestamp: u32
  /// @param duration: u32
  /// @param interestBpsFee: u16
  /// @param minOpSizeStrip: u64
  /// @param minOpSizeMerge: u64
  public record InitializeVaultIxData(Discriminator discriminator,
                                      long startTimestamp,
                                      long duration,
                                      int interestBpsFee,
                                      CpiAccounts cpiAccounts,
                                      long minOpSizeStrip,
                                      long minOpSizeMerge,
                                      String ptMetadataName, byte[] _ptMetadataName,
                                      String ptMetadataSymbol, byte[] _ptMetadataSymbol,
                                      String ptMetadataUri, byte[] _ptMetadataUri) implements SerDe {

    public static InitializeVaultIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int START_TIMESTAMP_OFFSET = 1;
    public static final int DURATION_OFFSET = 5;
    public static final int INTEREST_BPS_FEE_OFFSET = 9;
    public static final int CPI_ACCOUNTS_OFFSET = 11;

    public static InitializeVaultIxData createRecord(final Discriminator discriminator,
                                                     final long startTimestamp,
                                                     final long duration,
                                                     final int interestBpsFee,
                                                     final CpiAccounts cpiAccounts,
                                                     final long minOpSizeStrip,
                                                     final long minOpSizeMerge,
                                                     final String ptMetadataName,
                                                     final String ptMetadataSymbol,
                                                     final String ptMetadataUri) {
      return new InitializeVaultIxData(discriminator,
                                       startTimestamp,
                                       duration,
                                       interestBpsFee,
                                       cpiAccounts,
                                       minOpSizeStrip,
                                       minOpSizeMerge,
                                       ptMetadataName, ptMetadataName == null ? null : SerDeUtil.encodeString(ptMetadataName),
                                       ptMetadataSymbol, ptMetadataSymbol == null ? null : SerDeUtil.encodeString(ptMetadataSymbol),
                                       ptMetadataUri, ptMetadataUri == null ? null : SerDeUtil.encodeString(ptMetadataUri));
    }

    public static InitializeVaultIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var startTimestamp = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var duration = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var interestBpsFee = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var cpiAccounts = CpiAccounts.read(_data, i);
      i += cpiAccounts.l();
      final var minOpSizeStrip = getInt64LE(_data, i);
      i += 8;
      final var minOpSizeMerge = getInt64LE(_data, i);
      i += 8;
      final byte[] _ptMetadataName = SerDeUtil.readbyteVector(4, _data, i);
      final var ptMetadataName = SerDeUtil.decodeString(_ptMetadataName);
      i += 4 + _ptMetadataName.length;
      final byte[] _ptMetadataSymbol = SerDeUtil.readbyteVector(4, _data, i);
      final var ptMetadataSymbol = SerDeUtil.decodeString(_ptMetadataSymbol);
      i += 4 + _ptMetadataSymbol.length;
      final byte[] _ptMetadataUri = SerDeUtil.readbyteVector(4, _data, i);
      final var ptMetadataUri = SerDeUtil.decodeString(_ptMetadataUri);
      return new InitializeVaultIxData(discriminator,
                                       startTimestamp,
                                       duration,
                                       interestBpsFee,
                                       cpiAccounts,
                                       minOpSizeStrip,
                                       minOpSizeMerge,
                                       ptMetadataName, ptMetadataName == null ? null : SerDeUtil.encodeString(ptMetadataName),
                                       ptMetadataSymbol, ptMetadataSymbol == null ? null : SerDeUtil.encodeString(ptMetadataSymbol),
                                       ptMetadataUri, ptMetadataUri == null ? null : SerDeUtil.encodeString(ptMetadataUri));
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt32LE(_data, i, (int) startTimestamp);
      i += 4;
      putInt32LE(_data, i, (int) duration);
      i += 4;
      putInt16LE(_data, i, interestBpsFee);
      i += 2;
      i += cpiAccounts.write(_data, i);
      putInt64LE(_data, i, minOpSizeStrip);
      i += 8;
      putInt64LE(_data, i, minOpSizeMerge);
      i += 8;
      i += SerDeUtil.writeVector(4, _ptMetadataName, _data, i);
      i += SerDeUtil.writeVector(4, _ptMetadataSymbol, _data, i);
      i += SerDeUtil.writeVector(4, _ptMetadataUri, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + 4
           + 4
           + 2
           + cpiAccounts.l()
           + 8
           + 8
           + 4 + _ptMetadataName.length
           + 4 + _ptMetadataSymbol.length
           + 4 + _ptMetadataUri.length;
    }
  }

  public static final Discriminator INITIALIZE_YIELD_POSITION_DISCRIMINATOR = toDiscriminator(3);

  /// @param ownerKey Owner of the new YieldTokenPosition
  ///                 ie - the "owner" is not a Signer
  public static List<AccountMeta> initializeYieldPositionKeys(final PublicKey ownerKey,
                                                              final PublicKey vaultKey,
                                                              final PublicKey yieldPositionKey,
                                                              final PublicKey systemProgramKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createRead(vaultKey),
      createWrite(yieldPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param ownerKey Owner of the new YieldTokenPosition
  ///                 ie - the "owner" is not a Signer
  public static Instruction initializeYieldPosition(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final PublicKey ownerKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey yieldPositionKey,
                                                    final PublicKey systemProgramKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    final var keys = initializeYieldPositionKeys(
      ownerKey,
      vaultKey,
      yieldPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return initializeYieldPosition(invokedExponentCoreProgramMeta, keys);
  }

  public static Instruction initializeYieldPosition(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, INITIALIZE_YIELD_POSITION_DISCRIMINATOR);
  }

  public static final Discriminator MARKET_COLLECT_EMISSION_DISCRIMINATOR = toDiscriminator(16);

  /// Collect a staged emission
  ///
  public static List<AccountMeta> marketCollectEmissionKeys(final PublicKey ownerKey,
                                                            final PublicKey marketKey,
                                                            final PublicKey lpPositionKey,
                                                            final PublicKey tokenEmissionEscrowKey,
                                                            final PublicKey tokenEmissionDstKey,
                                                            final PublicKey tokenProgramKey,
                                                            final PublicKey addressLookupTableKey,
                                                            final PublicKey syProgramKey,
                                                            final PublicKey eventAuthorityKey,
                                                            final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(marketKey),
      createWrite(lpPositionKey),
      createWrite(tokenEmissionEscrowKey),
      createWrite(tokenEmissionDstKey),
      createRead(tokenProgramKey),
      createRead(addressLookupTableKey),
      createRead(syProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Collect a staged emission
  ///
  /// @param emissionIndex: u16
  public static Instruction marketCollectEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                                  final PublicKey ownerKey,
                                                  final PublicKey marketKey,
                                                  final PublicKey lpPositionKey,
                                                  final PublicKey tokenEmissionEscrowKey,
                                                  final PublicKey tokenEmissionDstKey,
                                                  final PublicKey tokenProgramKey,
                                                  final PublicKey addressLookupTableKey,
                                                  final PublicKey syProgramKey,
                                                  final PublicKey eventAuthorityKey,
                                                  final PublicKey programKey,
                                                  final int emissionIndex) {
    final var keys = marketCollectEmissionKeys(
      ownerKey,
      marketKey,
      lpPositionKey,
      tokenEmissionEscrowKey,
      tokenEmissionDstKey,
      tokenProgramKey,
      addressLookupTableKey,
      syProgramKey,
      eventAuthorityKey,
      programKey
    );
    return marketCollectEmission(invokedExponentCoreProgramMeta, keys, emissionIndex);
  }

  /// Collect a staged emission
  ///
  /// @param emissionIndex: u16
  public static Instruction marketCollectEmission(final AccountMeta invokedExponentCoreProgramMeta,
                                                  final List<AccountMeta> keys,
                                                  final int emissionIndex) {
    final byte[] _data = new byte[3];
    int i = MARKET_COLLECT_EMISSION_DISCRIMINATOR.write(_data, 0);
    putInt16LE(_data, i, emissionIndex);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param emissionIndex: u16
  public record MarketCollectEmissionIxData(Discriminator discriminator, int emissionIndex) implements SerDe {

    public static MarketCollectEmissionIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 3;

    public static final int EMISSION_INDEX_OFFSET = 1;

    public static MarketCollectEmissionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
      return new MarketCollectEmissionIxData(discriminator, emissionIndex);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt16LE(_data, i, emissionIndex);
      i += 2;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MARKET_DEPOSIT_LP_DISCRIMINATOR = toDiscriminator(14);

  /// Deposit LP tokens into a personal LP position account
  ///
  public static List<AccountMeta> marketDepositLpKeys(final PublicKey ownerKey,
                                                      final PublicKey marketKey,
                                                      final PublicKey lpPositionKey,
                                                      final PublicKey tokenLpSrcKey,
                                                      final PublicKey tokenLpEscrowKey,
                                                      final PublicKey mintLpKey,
                                                      final PublicKey syProgramKey,
                                                      final PublicKey addressLookupTableKey,
                                                      final PublicKey tokenProgramKey,
                                                      final PublicKey systemProgramKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(marketKey),
      createWrite(lpPositionKey),
      createWrite(tokenLpSrcKey),
      createWrite(tokenLpEscrowKey),
      createRead(mintLpKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Deposit LP tokens into a personal LP position account
  ///
  /// @param amount: u64
  public static Instruction marketDepositLp(final AccountMeta invokedExponentCoreProgramMeta,
                                            final PublicKey ownerKey,
                                            final PublicKey marketKey,
                                            final PublicKey lpPositionKey,
                                            final PublicKey tokenLpSrcKey,
                                            final PublicKey tokenLpEscrowKey,
                                            final PublicKey mintLpKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey systemProgramKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final long amount) {
    final var keys = marketDepositLpKeys(
      ownerKey,
      marketKey,
      lpPositionKey,
      tokenLpSrcKey,
      tokenLpEscrowKey,
      mintLpKey,
      syProgramKey,
      addressLookupTableKey,
      tokenProgramKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return marketDepositLp(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// Deposit LP tokens into a personal LP position account
  ///
  /// @param amount: u64
  public static Instruction marketDepositLp(final AccountMeta invokedExponentCoreProgramMeta,
                                            final List<AccountMeta> keys,
                                            final long amount) {
    final byte[] _data = new byte[9];
    int i = MARKET_DEPOSIT_LP_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record MarketDepositLpIxData(Discriminator discriminator, long amount) implements SerDe {

    public static MarketDepositLpIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static MarketDepositLpIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new MarketDepositLpIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MARKET_TWO_DEPOSIT_LIQUIDITY_DISCRIMINATOR = toDiscriminator(11);

  /// @param addressLookupTableKey Address lookup table for vault
  public static List<AccountMeta> marketTwoDepositLiquidityKeys(final PublicKey depositorKey,
                                                                final PublicKey marketKey,
                                                                final PublicKey tokenPtSrcKey,
                                                                final PublicKey tokenSySrcKey,
                                                                final PublicKey tokenPtEscrowKey,
                                                                final PublicKey tokenSyEscrowKey,
                                                                final PublicKey tokenLpDstKey,
                                                                final PublicKey mintLpKey,
                                                                final PublicKey addressLookupTableKey,
                                                                final PublicKey tokenProgramKey,
                                                                final PublicKey syProgramKey,
                                                                final PublicKey eventAuthorityKey,
                                                                final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(marketKey),
      createWrite(tokenPtSrcKey),
      createWrite(tokenSySrcKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpDstKey),
      createWrite(mintLpKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param addressLookupTableKey Address lookup table for vault
  /// @param ptIntent: u64
  /// @param syIntent: u64
  /// @param minLpOut: u64
  public static Instruction marketTwoDepositLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                      final PublicKey depositorKey,
                                                      final PublicKey marketKey,
                                                      final PublicKey tokenPtSrcKey,
                                                      final PublicKey tokenSySrcKey,
                                                      final PublicKey tokenPtEscrowKey,
                                                      final PublicKey tokenSyEscrowKey,
                                                      final PublicKey tokenLpDstKey,
                                                      final PublicKey mintLpKey,
                                                      final PublicKey addressLookupTableKey,
                                                      final PublicKey tokenProgramKey,
                                                      final PublicKey syProgramKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey,
                                                      final long ptIntent,
                                                      final long syIntent,
                                                      final long minLpOut) {
    final var keys = marketTwoDepositLiquidityKeys(
      depositorKey,
      marketKey,
      tokenPtSrcKey,
      tokenSySrcKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpDstKey,
      mintLpKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      eventAuthorityKey,
      programKey
    );
    return marketTwoDepositLiquidity(
      invokedExponentCoreProgramMeta,
      keys,
      ptIntent,
      syIntent,
      minLpOut
    );
  }

  /// @param ptIntent: u64
  /// @param syIntent: u64
  /// @param minLpOut: u64
  public static Instruction marketTwoDepositLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                      final List<AccountMeta> keys,
                                                      final long ptIntent,
                                                      final long syIntent,
                                                      final long minLpOut) {
    final byte[] _data = new byte[25];
    int i = MARKET_TWO_DEPOSIT_LIQUIDITY_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ptIntent);
    i += 8;
    putInt64LE(_data, i, syIntent);
    i += 8;
    putInt64LE(_data, i, minLpOut);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ptIntent: u64
  /// @param syIntent: u64
  /// @param minLpOut: u64
  public record MarketTwoDepositLiquidityIxData(Discriminator discriminator,
                                                long ptIntent,
                                                long syIntent,
                                                long minLpOut) implements SerDe {

    public static MarketTwoDepositLiquidityIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 25;

    public static final int PT_INTENT_OFFSET = 1;
    public static final int SY_INTENT_OFFSET = 9;
    public static final int MIN_LP_OUT_OFFSET = 17;

    public static MarketTwoDepositLiquidityIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var ptIntent = getInt64LE(_data, i);
      i += 8;
      final var syIntent = getInt64LE(_data, i);
      i += 8;
      final var minLpOut = getInt64LE(_data, i);
      return new MarketTwoDepositLiquidityIxData(discriminator, ptIntent, syIntent, minLpOut);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ptIntent);
      i += 8;
      putInt64LE(_data, i, syIntent);
      i += 8;
      putInt64LE(_data, i, minLpOut);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MARKET_TWO_WITHDRAW_LIQUIDITY_DISCRIMINATOR = toDiscriminator(12);

  /// @param tokenPtEscrowKey Market PT liquidity account
  /// @param tokenSyEscrowKey Market-owned interchange account for SY
  /// @param addressLookupTableKey Address lookup table for vault
  public static List<AccountMeta> marketTwoWithdrawLiquidityKeys(final PublicKey withdrawerKey,
                                                                 final PublicKey marketKey,
                                                                 final PublicKey tokenPtDstKey,
                                                                 final PublicKey tokenSyDstKey,
                                                                 final PublicKey tokenPtEscrowKey,
                                                                 final PublicKey tokenSyEscrowKey,
                                                                 final PublicKey tokenLpSrcKey,
                                                                 final PublicKey mintLpKey,
                                                                 final PublicKey addressLookupTableKey,
                                                                 final PublicKey tokenProgramKey,
                                                                 final PublicKey syProgramKey,
                                                                 final PublicKey eventAuthorityKey,
                                                                 final PublicKey programKey) {
    return List.of(
      createWritableSigner(withdrawerKey),
      createWrite(marketKey),
      createWrite(tokenPtDstKey),
      createWrite(tokenSyDstKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpSrcKey),
      createWrite(mintLpKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenPtEscrowKey Market PT liquidity account
  /// @param tokenSyEscrowKey Market-owned interchange account for SY
  /// @param addressLookupTableKey Address lookup table for vault
  /// @param lpIn: u64
  /// @param minPtOut: u64
  /// @param minSyOut: u64
  public static Instruction marketTwoWithdrawLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                       final PublicKey withdrawerKey,
                                                       final PublicKey marketKey,
                                                       final PublicKey tokenPtDstKey,
                                                       final PublicKey tokenSyDstKey,
                                                       final PublicKey tokenPtEscrowKey,
                                                       final PublicKey tokenSyEscrowKey,
                                                       final PublicKey tokenLpSrcKey,
                                                       final PublicKey mintLpKey,
                                                       final PublicKey addressLookupTableKey,
                                                       final PublicKey tokenProgramKey,
                                                       final PublicKey syProgramKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey,
                                                       final long lpIn,
                                                       final long minPtOut,
                                                       final long minSyOut) {
    final var keys = marketTwoWithdrawLiquidityKeys(
      withdrawerKey,
      marketKey,
      tokenPtDstKey,
      tokenSyDstKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpSrcKey,
      mintLpKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      eventAuthorityKey,
      programKey
    );
    return marketTwoWithdrawLiquidity(
      invokedExponentCoreProgramMeta,
      keys,
      lpIn,
      minPtOut,
      minSyOut
    );
  }

  /// @param lpIn: u64
  /// @param minPtOut: u64
  /// @param minSyOut: u64
  public static Instruction marketTwoWithdrawLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                       final List<AccountMeta> keys,
                                                       final long lpIn,
                                                       final long minPtOut,
                                                       final long minSyOut) {
    final byte[] _data = new byte[25];
    int i = MARKET_TWO_WITHDRAW_LIQUIDITY_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, lpIn);
    i += 8;
    putInt64LE(_data, i, minPtOut);
    i += 8;
    putInt64LE(_data, i, minSyOut);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param lpIn: u64
  /// @param minPtOut: u64
  /// @param minSyOut: u64
  public record MarketTwoWithdrawLiquidityIxData(Discriminator discriminator,
                                                 long lpIn,
                                                 long minPtOut,
                                                 long minSyOut) implements SerDe {

    public static MarketTwoWithdrawLiquidityIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 25;

    public static final int LP_IN_OFFSET = 1;
    public static final int MIN_PT_OUT_OFFSET = 9;
    public static final int MIN_SY_OUT_OFFSET = 17;

    public static MarketTwoWithdrawLiquidityIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var lpIn = getInt64LE(_data, i);
      i += 8;
      final var minPtOut = getInt64LE(_data, i);
      i += 8;
      final var minSyOut = getInt64LE(_data, i);
      return new MarketTwoWithdrawLiquidityIxData(discriminator, lpIn, minPtOut, minSyOut);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, lpIn);
      i += 8;
      putInt64LE(_data, i, minPtOut);
      i += 8;
      putInt64LE(_data, i, minSyOut);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MARKET_WITHDRAW_LP_DISCRIMINATOR = toDiscriminator(15);

  /// Withdraw LP tokens from personal LP position account
  ///
  public static List<AccountMeta> marketWithdrawLpKeys(final PublicKey ownerKey,
                                                       final PublicKey marketKey,
                                                       final PublicKey mintLpKey,
                                                       final PublicKey lpPositionKey,
                                                       final PublicKey tokenLpDstKey,
                                                       final PublicKey tokenLpEscrowKey,
                                                       final PublicKey syProgramKey,
                                                       final PublicKey addressLookupTableKey,
                                                       final PublicKey tokenProgramKey,
                                                       final PublicKey systemProgramKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(marketKey),
      createRead(mintLpKey),
      createWrite(lpPositionKey),
      createWrite(tokenLpDstKey),
      createWrite(tokenLpEscrowKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Withdraw LP tokens from personal LP position account
  ///
  /// @param amount: u64
  public static Instruction marketWithdrawLp(final AccountMeta invokedExponentCoreProgramMeta,
                                             final PublicKey ownerKey,
                                             final PublicKey marketKey,
                                             final PublicKey mintLpKey,
                                             final PublicKey lpPositionKey,
                                             final PublicKey tokenLpDstKey,
                                             final PublicKey tokenLpEscrowKey,
                                             final PublicKey syProgramKey,
                                             final PublicKey addressLookupTableKey,
                                             final PublicKey tokenProgramKey,
                                             final PublicKey systemProgramKey,
                                             final PublicKey eventAuthorityKey,
                                             final PublicKey programKey,
                                             final long amount) {
    final var keys = marketWithdrawLpKeys(
      ownerKey,
      marketKey,
      mintLpKey,
      lpPositionKey,
      tokenLpDstKey,
      tokenLpEscrowKey,
      syProgramKey,
      addressLookupTableKey,
      tokenProgramKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return marketWithdrawLp(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// Withdraw LP tokens from personal LP position account
  ///
  /// @param amount: u64
  public static Instruction marketWithdrawLp(final AccountMeta invokedExponentCoreProgramMeta,
                                             final List<AccountMeta> keys,
                                             final long amount) {
    final byte[] _data = new byte[9];
    int i = MARKET_WITHDRAW_LP_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record MarketWithdrawLpIxData(Discriminator discriminator, long amount) implements SerDe {

    public static MarketWithdrawLpIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static MarketWithdrawLpIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new MarketWithdrawLpIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MERGE_DISCRIMINATOR = toDiscriminator(5);

  /// Merge PT + YT into SY
  /// Redeems & burns them, in exchange for SY
  ///
  /// @param authorityKey This authority owns the escrow_sy account & the robot account with the SY program
  ///                     Needs to be mutable to be used in deposit_sy CPI
  /// @param syDstKey Destination account for SY withdrawn from vault
  /// @param escrowSyKey Vault-owned account for SY tokens
  /// @param ytSrcKey The owner's YT token account
  /// @param ptSrcKey The owner's PT token account
  /// @param mintYtKey Mint for YT -- needed for burning
  /// @param mintPtKey Mint for PT -- needed for burning
  /// @param yieldPositionKey Yield position for the vault robot account
  public static List<AccountMeta> mergeKeys(final PublicKey ownerKey,
                                            final PublicKey authorityKey,
                                            final PublicKey vaultKey,
                                            final PublicKey syDstKey,
                                            final PublicKey escrowSyKey,
                                            final PublicKey ytSrcKey,
                                            final PublicKey ptSrcKey,
                                            final PublicKey mintYtKey,
                                            final PublicKey mintPtKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey yieldPositionKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(authorityKey),
      createWrite(vaultKey),
      createWrite(syDstKey),
      createWrite(escrowSyKey),
      createWrite(ytSrcKey),
      createWrite(ptSrcKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createWrite(yieldPositionKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Merge PT + YT into SY
  /// Redeems & burns them, in exchange for SY
  ///
  /// @param authorityKey This authority owns the escrow_sy account & the robot account with the SY program
  ///                     Needs to be mutable to be used in deposit_sy CPI
  /// @param syDstKey Destination account for SY withdrawn from vault
  /// @param escrowSyKey Vault-owned account for SY tokens
  /// @param ytSrcKey The owner's YT token account
  /// @param ptSrcKey The owner's PT token account
  /// @param mintYtKey Mint for YT -- needed for burning
  /// @param mintPtKey Mint for PT -- needed for burning
  /// @param yieldPositionKey Yield position for the vault robot account
  /// @param amount: u64
  public static Instruction merge(final AccountMeta invokedExponentCoreProgramMeta,
                                  final PublicKey ownerKey,
                                  final PublicKey authorityKey,
                                  final PublicKey vaultKey,
                                  final PublicKey syDstKey,
                                  final PublicKey escrowSyKey,
                                  final PublicKey ytSrcKey,
                                  final PublicKey ptSrcKey,
                                  final PublicKey mintYtKey,
                                  final PublicKey mintPtKey,
                                  final PublicKey tokenProgramKey,
                                  final PublicKey syProgramKey,
                                  final PublicKey addressLookupTableKey,
                                  final PublicKey yieldPositionKey,
                                  final PublicKey eventAuthorityKey,
                                  final PublicKey programKey,
                                  final long amount) {
    final var keys = mergeKeys(
      ownerKey,
      authorityKey,
      vaultKey,
      syDstKey,
      escrowSyKey,
      ytSrcKey,
      ptSrcKey,
      mintYtKey,
      mintPtKey,
      tokenProgramKey,
      syProgramKey,
      addressLookupTableKey,
      yieldPositionKey,
      eventAuthorityKey,
      programKey
    );
    return merge(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// Merge PT + YT into SY
  /// Redeems & burns them, in exchange for SY
  ///
  /// @param amount: u64
  public static Instruction merge(final AccountMeta invokedExponentCoreProgramMeta,
                                  final List<AccountMeta> keys,
                                  final long amount) {
    final byte[] _data = new byte[9];
    int i = MERGE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record MergeIxData(Discriminator discriminator, long amount) implements SerDe {

    public static MergeIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static MergeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new MergeIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MODIFY_FARM_DISCRIMINATOR = toDiscriminator(23);

  public static List<AccountMeta> modifyFarmKeys(final PublicKey marketKey,
                                                 final PublicKey signerKey,
                                                 final PublicKey mintKey,
                                                 final PublicKey adminStateKey,
                                                 final PublicKey tokenSourceKey,
                                                 final PublicKey tokenFarmKey,
                                                 final PublicKey tokenProgramKey) {
    return List.of(
      createWrite(marketKey),
      createReadOnlySigner(signerKey),
      createRead(mintKey),
      createRead(adminStateKey),
      createWrite(tokenSourceKey),
      createWrite(tokenFarmKey),
      createRead(tokenProgramKey)
    );
  }

  /// @param untilTimestamp: u32
  /// @param newRate: u64
  public static Instruction modifyFarm(final AccountMeta invokedExponentCoreProgramMeta,
                                       final PublicKey marketKey,
                                       final PublicKey signerKey,
                                       final PublicKey mintKey,
                                       final PublicKey adminStateKey,
                                       final PublicKey tokenSourceKey,
                                       final PublicKey tokenFarmKey,
                                       final PublicKey tokenProgramKey,
                                       final long untilTimestamp,
                                       final long newRate) {
    final var keys = modifyFarmKeys(
      marketKey,
      signerKey,
      mintKey,
      adminStateKey,
      tokenSourceKey,
      tokenFarmKey,
      tokenProgramKey
    );
    return modifyFarm(invokedExponentCoreProgramMeta, keys, untilTimestamp, newRate);
  }

  /// @param untilTimestamp: u32
  /// @param newRate: u64
  public static Instruction modifyFarm(final AccountMeta invokedExponentCoreProgramMeta,
                                       final List<AccountMeta> keys,
                                       final long untilTimestamp,
                                       final long newRate) {
    final byte[] _data = new byte[13];
    int i = MODIFY_FARM_DISCRIMINATOR.write(_data, 0);
    putInt32LE(_data, i, (int) untilTimestamp);
    i += 4;
    putInt64LE(_data, i, newRate);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param untilTimestamp: u32
  /// @param newRate: u64
  public record ModifyFarmIxData(Discriminator discriminator, long untilTimestamp, long newRate) implements SerDe {

    public static ModifyFarmIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 13;

    public static final int UNTIL_TIMESTAMP_OFFSET = 1;
    public static final int NEW_RATE_OFFSET = 5;

    public static ModifyFarmIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var untilTimestamp = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var newRate = getInt64LE(_data, i);
      return new ModifyFarmIxData(discriminator, untilTimestamp, newRate);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt32LE(_data, i, (int) untilTimestamp);
      i += 4;
      putInt64LE(_data, i, newRate);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MODIFY_MARKET_SETTING_DISCRIMINATOR = toDiscriminator(27);

  public static List<AccountMeta> modifyMarketSettingKeys(final PublicKey marketKey,
                                                          final PublicKey signerKey,
                                                          final PublicKey adminStateKey,
                                                          final PublicKey systemProgramKey) {
    return List.of(
      createWrite(marketKey),
      createWritableSigner(signerKey),
      createRead(adminStateKey),
      createRead(systemProgramKey)
    );
  }

  public static Instruction modifyMarketSetting(final AccountMeta invokedExponentCoreProgramMeta,
                                                final PublicKey marketKey,
                                                final PublicKey signerKey,
                                                final PublicKey adminStateKey,
                                                final PublicKey systemProgramKey,
                                                final MarketAdminAction action) {
    final var keys = modifyMarketSettingKeys(
      marketKey,
      signerKey,
      adminStateKey,
      systemProgramKey
    );
    return modifyMarketSetting(invokedExponentCoreProgramMeta, keys, action);
  }

  public static Instruction modifyMarketSetting(final AccountMeta invokedExponentCoreProgramMeta,
                                                final List<AccountMeta> keys,
                                                final MarketAdminAction action) {
    final byte[] _data = new byte[1 + action.l()];
    int i = MODIFY_MARKET_SETTING_DISCRIMINATOR.write(_data, 0);
    action.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record ModifyMarketSettingIxData(Discriminator discriminator, MarketAdminAction action) implements SerDe {

    public static ModifyMarketSettingIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int ACTION_OFFSET = 1;

    public static ModifyMarketSettingIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var action = MarketAdminAction.read(_data, i);
      return new ModifyMarketSettingIxData(discriminator, action);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += action.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + action.l();
    }
  }

  public static final Discriminator MODIFY_VAULT_SETTING_DISCRIMINATOR = toDiscriminator(26);

  public static List<AccountMeta> modifyVaultSettingKeys(final PublicKey vaultKey,
                                                         final PublicKey signerKey,
                                                         final PublicKey adminStateKey,
                                                         final PublicKey systemProgramKey) {
    return List.of(
      createWrite(vaultKey),
      createWritableSigner(signerKey),
      createRead(adminStateKey),
      createRead(systemProgramKey)
    );
  }

  public static Instruction modifyVaultSetting(final AccountMeta invokedExponentCoreProgramMeta,
                                               final PublicKey vaultKey,
                                               final PublicKey signerKey,
                                               final PublicKey adminStateKey,
                                               final PublicKey systemProgramKey,
                                               final AdminAction action) {
    final var keys = modifyVaultSettingKeys(
      vaultKey,
      signerKey,
      adminStateKey,
      systemProgramKey
    );
    return modifyVaultSetting(invokedExponentCoreProgramMeta, keys, action);
  }

  public static Instruction modifyVaultSetting(final AccountMeta invokedExponentCoreProgramMeta,
                                               final List<AccountMeta> keys,
                                               final AdminAction action) {
    final byte[] _data = new byte[1 + action.l()];
    int i = MODIFY_VAULT_SETTING_DISCRIMINATOR.write(_data, 0);
    action.write(_data, i);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record ModifyVaultSettingIxData(Discriminator discriminator, AdminAction action) implements SerDe {

    public static ModifyVaultSettingIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int ACTION_OFFSET = 1;

    public static ModifyVaultSettingIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var action = AdminAction.read(_data, i);
      return new ModifyVaultSettingIxData(discriminator, action);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += action.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 1 + action.l();
    }
  }

  public static final Discriminator REALLOC_MARKET_DISCRIMINATOR = toDiscriminator(40);

  public static List<AccountMeta> reallocMarketKeys(final PublicKey marketKey,
                                                    final PublicKey signerKey,
                                                    final PublicKey adminStateKey,
                                                    final PublicKey systemProgramKey,
                                                    final PublicKey rentKey) {
    return List.of(
      createWrite(marketKey),
      createWritableSigner(signerKey),
      createRead(adminStateKey),
      createRead(systemProgramKey),
      createRead(rentKey)
    );
  }

  /// @param additionalBytes: u64
  public static Instruction reallocMarket(final AccountMeta invokedExponentCoreProgramMeta,
                                          final PublicKey marketKey,
                                          final PublicKey signerKey,
                                          final PublicKey adminStateKey,
                                          final PublicKey systemProgramKey,
                                          final PublicKey rentKey,
                                          final long additionalBytes) {
    final var keys = reallocMarketKeys(
      marketKey,
      signerKey,
      adminStateKey,
      systemProgramKey,
      rentKey
    );
    return reallocMarket(invokedExponentCoreProgramMeta, keys, additionalBytes);
  }

  /// @param additionalBytes: u64
  public static Instruction reallocMarket(final AccountMeta invokedExponentCoreProgramMeta,
                                          final List<AccountMeta> keys,
                                          final long additionalBytes) {
    final byte[] _data = new byte[9];
    int i = REALLOC_MARKET_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, additionalBytes);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param additionalBytes: u64
  public record ReallocMarketIxData(Discriminator discriminator, long additionalBytes) implements SerDe {

    public static ReallocMarketIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int ADDITIONAL_BYTES_OFFSET = 1;

    public static ReallocMarketIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var additionalBytes = getInt64LE(_data, i);
      return new ReallocMarketIxData(discriminator, additionalBytes);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, additionalBytes);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SELL_YT_DISCRIMINATOR = toDiscriminator(1);

  /// Sell YT for SY
  ///
  /// @param tokenSyEscrowKey Account owned by market
  ///                         This is a temporary account that receives SY tokens from the user, after which it transfers them to the SY program
  /// @param tokenPtEscrowKey PT liquidity account
  /// @param authorityVaultKey Vault-robot authority
  /// @param tokenSyEscrowVaultKey Vault-robot owned escrow account for SY
  /// @param addressLookupTableVaultKey ALT owned by vault
  /// @param yieldPositionVaultKey Yield position owned by vault robot
  public static List<AccountMeta> sellYtKeys(final PublicKey traderKey,
                                             final PublicKey marketKey,
                                             final PublicKey tokenYtTraderKey,
                                             final PublicKey tokenPtTraderKey,
                                             final PublicKey tokenSyTraderKey,
                                             final PublicKey tokenSyEscrowKey,
                                             final PublicKey tokenPtEscrowKey,
                                             final PublicKey addressLookupTableKey,
                                             final PublicKey tokenFeeTreasurySyKey,
                                             final PublicKey tokenProgramKey,
                                             final PublicKey vaultKey,
                                             final PublicKey authorityVaultKey,
                                             final PublicKey tokenSyEscrowVaultKey,
                                             final PublicKey mintYtKey,
                                             final PublicKey mintPtKey,
                                             final PublicKey addressLookupTableVaultKey,
                                             final PublicKey yieldPositionVaultKey,
                                             final PublicKey syProgramKey,
                                             final PublicKey eventAuthorityKey,
                                             final PublicKey programKey) {
    return List.of(
      createWritableSigner(traderKey),
      createWrite(marketKey),
      createWrite(tokenYtTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(tokenProgramKey),
      createWrite(vaultKey),
      createWrite(authorityVaultKey),
      createWrite(tokenSyEscrowVaultKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(addressLookupTableVaultKey),
      createWrite(yieldPositionVaultKey),
      createRead(syProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Sell YT for SY
  ///
  /// @param tokenSyEscrowKey Account owned by market
  ///                         This is a temporary account that receives SY tokens from the user, after which it transfers them to the SY program
  /// @param tokenPtEscrowKey PT liquidity account
  /// @param authorityVaultKey Vault-robot authority
  /// @param tokenSyEscrowVaultKey Vault-robot owned escrow account for SY
  /// @param addressLookupTableVaultKey ALT owned by vault
  /// @param yieldPositionVaultKey Yield position owned by vault robot
  /// @param ytIn: u64
  /// @param minSyOut: u64
  public static Instruction sellYt(final AccountMeta invokedExponentCoreProgramMeta,
                                   final PublicKey traderKey,
                                   final PublicKey marketKey,
                                   final PublicKey tokenYtTraderKey,
                                   final PublicKey tokenPtTraderKey,
                                   final PublicKey tokenSyTraderKey,
                                   final PublicKey tokenSyEscrowKey,
                                   final PublicKey tokenPtEscrowKey,
                                   final PublicKey addressLookupTableKey,
                                   final PublicKey tokenFeeTreasurySyKey,
                                   final PublicKey tokenProgramKey,
                                   final PublicKey vaultKey,
                                   final PublicKey authorityVaultKey,
                                   final PublicKey tokenSyEscrowVaultKey,
                                   final PublicKey mintYtKey,
                                   final PublicKey mintPtKey,
                                   final PublicKey addressLookupTableVaultKey,
                                   final PublicKey yieldPositionVaultKey,
                                   final PublicKey syProgramKey,
                                   final PublicKey eventAuthorityKey,
                                   final PublicKey programKey,
                                   final long ytIn,
                                   final long minSyOut) {
    final var keys = sellYtKeys(
      traderKey,
      marketKey,
      tokenYtTraderKey,
      tokenPtTraderKey,
      tokenSyTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenFeeTreasurySyKey,
      tokenProgramKey,
      vaultKey,
      authorityVaultKey,
      tokenSyEscrowVaultKey,
      mintYtKey,
      mintPtKey,
      addressLookupTableVaultKey,
      yieldPositionVaultKey,
      syProgramKey,
      eventAuthorityKey,
      programKey
    );
    return sellYt(invokedExponentCoreProgramMeta, keys, ytIn, minSyOut);
  }

  /// Sell YT for SY
  ///
  /// @param ytIn: u64
  /// @param minSyOut: u64
  public static Instruction sellYt(final AccountMeta invokedExponentCoreProgramMeta,
                                   final List<AccountMeta> keys,
                                   final long ytIn,
                                   final long minSyOut) {
    final byte[] _data = new byte[17];
    int i = SELL_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ytIn);
    i += 8;
    putInt64LE(_data, i, minSyOut);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ytIn: u64
  /// @param minSyOut: u64
  public record SellYtIxData(Discriminator discriminator, long ytIn, long minSyOut) implements SerDe {

    public static SellYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 17;

    public static final int YT_IN_OFFSET = 1;
    public static final int MIN_SY_OUT_OFFSET = 9;

    public static SellYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var ytIn = getInt64LE(_data, i);
      i += 8;
      final var minSyOut = getInt64LE(_data, i);
      return new SellYtIxData(discriminator, ytIn, minSyOut);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ytIn);
      i += 8;
      putInt64LE(_data, i, minSyOut);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator STAGE_YT_YIELD_DISCRIMINATOR = toDiscriminator(9);

  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param yieldPositionKey Yield position for the vault robot account
  public static List<AccountMeta> stageYtYieldKeys(final PublicKey payerKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey userYieldPositionKey,
                                                   final PublicKey yieldPositionKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey addressLookupTableKey,
                                                   final PublicKey systemProgramKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createWrite(vaultKey),
      createWrite(userYieldPositionKey),
      createWrite(yieldPositionKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param yieldPositionKey Yield position for the vault robot account
  public static Instruction stageYtYield(final AccountMeta invokedExponentCoreProgramMeta,
                                         final PublicKey payerKey,
                                         final PublicKey vaultKey,
                                         final PublicKey userYieldPositionKey,
                                         final PublicKey yieldPositionKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey addressLookupTableKey,
                                         final PublicKey systemProgramKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey) {
    final var keys = stageYtYieldKeys(
      payerKey,
      vaultKey,
      userYieldPositionKey,
      yieldPositionKey,
      syProgramKey,
      addressLookupTableKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return stageYtYield(invokedExponentCoreProgramMeta, keys);
  }

  public static Instruction stageYtYield(final AccountMeta invokedExponentCoreProgramMeta,
                                         final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, STAGE_YT_YIELD_DISCRIMINATOR);
  }

  public static final Discriminator STRIP_DISCRIMINATOR = toDiscriminator(4);

  /// Strip SY into PT + YT
  ///
  /// @param authorityKey This account owns the mints for PT & YT
  ///                     And owns the robot account with the SY program
  ///                     Needs to be mutable to be used in deposit_sy CPI
  /// @param sySrcKey Depositor's SY token account
  /// @param escrowSyKey Temporary account owned by the vault for receiving SY tokens before depositing into the SY Program's escrow account
  /// @param ytDstKey Final destination for receiving YT (probably, but not necessarily, owned by the depositor)
  /// @param ptDstKey Final destination for receiving PT (probably, but not necessarily, owned by the depositor)
  /// @param addressLookupTableKey Address lookup table for vault
  /// @param yieldPositionKey Vault-owned yield position account
  public static List<AccountMeta> stripKeys(final PublicKey depositorKey,
                                            final PublicKey authorityKey,
                                            final PublicKey vaultKey,
                                            final PublicKey sySrcKey,
                                            final PublicKey escrowSyKey,
                                            final PublicKey ytDstKey,
                                            final PublicKey ptDstKey,
                                            final PublicKey mintYtKey,
                                            final PublicKey mintPtKey,
                                            final PublicKey tokenProgramKey,
                                            final PublicKey addressLookupTableKey,
                                            final PublicKey syProgramKey,
                                            final PublicKey yieldPositionKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(authorityKey),
      createWrite(vaultKey),
      createWrite(sySrcKey),
      createWrite(escrowSyKey),
      createWrite(ytDstKey),
      createWrite(ptDstKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(tokenProgramKey),
      createRead(addressLookupTableKey),
      createRead(syProgramKey),
      createWrite(yieldPositionKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Strip SY into PT + YT
  ///
  /// @param authorityKey This account owns the mints for PT & YT
  ///                     And owns the robot account with the SY program
  ///                     Needs to be mutable to be used in deposit_sy CPI
  /// @param sySrcKey Depositor's SY token account
  /// @param escrowSyKey Temporary account owned by the vault for receiving SY tokens before depositing into the SY Program's escrow account
  /// @param ytDstKey Final destination for receiving YT (probably, but not necessarily, owned by the depositor)
  /// @param ptDstKey Final destination for receiving PT (probably, but not necessarily, owned by the depositor)
  /// @param addressLookupTableKey Address lookup table for vault
  /// @param yieldPositionKey Vault-owned yield position account
  /// @param amount: u64
  public static Instruction strip(final AccountMeta invokedExponentCoreProgramMeta,
                                  final PublicKey depositorKey,
                                  final PublicKey authorityKey,
                                  final PublicKey vaultKey,
                                  final PublicKey sySrcKey,
                                  final PublicKey escrowSyKey,
                                  final PublicKey ytDstKey,
                                  final PublicKey ptDstKey,
                                  final PublicKey mintYtKey,
                                  final PublicKey mintPtKey,
                                  final PublicKey tokenProgramKey,
                                  final PublicKey addressLookupTableKey,
                                  final PublicKey syProgramKey,
                                  final PublicKey yieldPositionKey,
                                  final PublicKey eventAuthorityKey,
                                  final PublicKey programKey,
                                  final long amount) {
    final var keys = stripKeys(
      depositorKey,
      authorityKey,
      vaultKey,
      sySrcKey,
      escrowSyKey,
      ytDstKey,
      ptDstKey,
      mintYtKey,
      mintPtKey,
      tokenProgramKey,
      addressLookupTableKey,
      syProgramKey,
      yieldPositionKey,
      eventAuthorityKey,
      programKey
    );
    return strip(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// Strip SY into PT + YT
  ///
  /// @param amount: u64
  public static Instruction strip(final AccountMeta invokedExponentCoreProgramMeta,
                                  final List<AccountMeta> keys,
                                  final long amount) {
    final byte[] _data = new byte[9];
    int i = STRIP_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record StripIxData(Discriminator discriminator, long amount) implements SerDe {

    public static StripIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static StripIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new StripIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator TRADE_PT_DISCRIMINATOR = toDiscriminator(17);

  /// @param tokenSyTraderKey Trader's SY token account
  ///                         Mint is constrained by TokenProgram
  /// @param tokenPtTraderKey Receiving PT token account
  ///                         Mint is constrained by TokenProgram
  /// @param tokenSyEscrowKey Account owned by market
  ///                         This is a temporary account that receives SY tokens from the user, after which it transfers them to the SY program
  /// @param tokenPtEscrowKey PT liquidity account
  public static List<AccountMeta> tradePtKeys(final PublicKey traderKey,
                                              final PublicKey marketKey,
                                              final PublicKey tokenSyTraderKey,
                                              final PublicKey tokenPtTraderKey,
                                              final PublicKey tokenSyEscrowKey,
                                              final PublicKey tokenPtEscrowKey,
                                              final PublicKey addressLookupTableKey,
                                              final PublicKey tokenProgramKey,
                                              final PublicKey syProgramKey,
                                              final PublicKey tokenFeeTreasurySyKey,
                                              final PublicKey eventAuthorityKey,
                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(traderKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenSyTraderKey Trader's SY token account
  ///                         Mint is constrained by TokenProgram
  /// @param tokenPtTraderKey Receiving PT token account
  ///                         Mint is constrained by TokenProgram
  /// @param tokenSyEscrowKey Account owned by market
  ///                         This is a temporary account that receives SY tokens from the user, after which it transfers them to the SY program
  /// @param tokenPtEscrowKey PT liquidity account
  public static Instruction tradePt(final AccountMeta invokedExponentCoreProgramMeta,
                                    final PublicKey traderKey,
                                    final PublicKey marketKey,
                                    final PublicKey tokenSyTraderKey,
                                    final PublicKey tokenPtTraderKey,
                                    final PublicKey tokenSyEscrowKey,
                                    final PublicKey tokenPtEscrowKey,
                                    final PublicKey addressLookupTableKey,
                                    final PublicKey tokenProgramKey,
                                    final PublicKey syProgramKey,
                                    final PublicKey tokenFeeTreasurySyKey,
                                    final PublicKey eventAuthorityKey,
                                    final PublicKey programKey,
                                    final long netTraderPt,
                                    final long syConstraint) {
    final var keys = tradePtKeys(
      traderKey,
      marketKey,
      tokenSyTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey,
      programKey
    );
    return tradePt(invokedExponentCoreProgramMeta, keys, netTraderPt, syConstraint);
  }

  public static Instruction tradePt(final AccountMeta invokedExponentCoreProgramMeta,
                                    final List<AccountMeta> keys,
                                    final long netTraderPt,
                                    final long syConstraint) {
    final byte[] _data = new byte[17];
    int i = TRADE_PT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, netTraderPt);
    i += 8;
    putInt64LE(_data, i, syConstraint);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  public record TradePtIxData(Discriminator discriminator, long netTraderPt, long syConstraint) implements SerDe {

    public static TradePtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 17;

    public static final int NET_TRADER_PT_OFFSET = 1;
    public static final int SY_CONSTRAINT_OFFSET = 9;

    public static TradePtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var netTraderPt = getInt64LE(_data, i);
      i += 8;
      final var syConstraint = getInt64LE(_data, i);
      return new TradePtIxData(discriminator, netTraderPt, syConstraint);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, netTraderPt);
      i += 8;
      putInt64LE(_data, i, syConstraint);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WITHDRAW_YT_DISCRIMINATOR = toDiscriminator(8);

  /// @param vaultKey Vault that manages YT
  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param ytDstKey Withdrawer's YT token account
  /// @param escrowYtKey Vault-owned escrow account for YT
  /// @param syProgramKey The SY interface implementation for the vault's token
  public static List<AccountMeta> withdrawYtKeys(final PublicKey ownerKey,
                                                 final PublicKey vaultKey,
                                                 final PublicKey userYieldPositionKey,
                                                 final PublicKey ytDstKey,
                                                 final PublicKey escrowYtKey,
                                                 final PublicKey tokenProgramKey,
                                                 final PublicKey authorityKey,
                                                 final PublicKey syProgramKey,
                                                 final PublicKey addressLookupTableKey,
                                                 final PublicKey yieldPositionKey,
                                                 final PublicKey systemProgramKey,
                                                 final PublicKey eventAuthorityKey,
                                                 final PublicKey programKey) {
    return List.of(
      createWritableSigner(ownerKey),
      createWrite(vaultKey),
      createWrite(userYieldPositionKey),
      createWrite(ytDstKey),
      createWrite(escrowYtKey),
      createRead(tokenProgramKey),
      createRead(authorityKey),
      createRead(syProgramKey),
      createRead(addressLookupTableKey),
      createWrite(yieldPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param vaultKey Vault that manages YT
  /// @param userYieldPositionKey Position data for YT deposits, linked to vault
  /// @param ytDstKey Withdrawer's YT token account
  /// @param escrowYtKey Vault-owned escrow account for YT
  /// @param syProgramKey The SY interface implementation for the vault's token
  /// @param amount: u64
  public static Instruction withdrawYt(final AccountMeta invokedExponentCoreProgramMeta,
                                       final PublicKey ownerKey,
                                       final PublicKey vaultKey,
                                       final PublicKey userYieldPositionKey,
                                       final PublicKey ytDstKey,
                                       final PublicKey escrowYtKey,
                                       final PublicKey tokenProgramKey,
                                       final PublicKey authorityKey,
                                       final PublicKey syProgramKey,
                                       final PublicKey addressLookupTableKey,
                                       final PublicKey yieldPositionKey,
                                       final PublicKey systemProgramKey,
                                       final PublicKey eventAuthorityKey,
                                       final PublicKey programKey,
                                       final long amount) {
    final var keys = withdrawYtKeys(
      ownerKey,
      vaultKey,
      userYieldPositionKey,
      ytDstKey,
      escrowYtKey,
      tokenProgramKey,
      authorityKey,
      syProgramKey,
      addressLookupTableKey,
      yieldPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return withdrawYt(invokedExponentCoreProgramMeta, keys, amount);
  }

  /// @param amount: u64
  public static Instruction withdrawYt(final AccountMeta invokedExponentCoreProgramMeta,
                                       final List<AccountMeta> keys,
                                       final long amount) {
    final byte[] _data = new byte[9];
    int i = WITHDRAW_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record WithdrawYtIxData(Discriminator discriminator, long amount) implements SerDe {

    public static WithdrawYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 9;

    public static final int AMOUNT_OFFSET = 1;

    public static WithdrawYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new WithdrawYtIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_BUY_PT_DISCRIMINATOR = toDiscriminator(29);

  public static List<AccountMeta> wrapperBuyPtKeys(final PublicKey buyerKey,
                                                   final PublicKey marketKey,
                                                   final PublicKey tokenSyTraderKey,
                                                   final PublicKey tokenPtTraderKey,
                                                   final PublicKey tokenSyEscrowKey,
                                                   final PublicKey tokenPtEscrowKey,
                                                   final PublicKey addressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey tokenFeeTreasurySyKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(buyerKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public static Instruction wrapperBuyPt(final AccountMeta invokedExponentCoreProgramMeta,
                                         final PublicKey buyerKey,
                                         final PublicKey marketKey,
                                         final PublicKey tokenSyTraderKey,
                                         final PublicKey tokenPtTraderKey,
                                         final PublicKey tokenSyEscrowKey,
                                         final PublicKey tokenPtEscrowKey,
                                         final PublicKey addressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey tokenFeeTreasurySyKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey,
                                         final long ptAmount,
                                         final long maxBaseAmount,
                                         final int mintSyRemAccountsUntil) {
    final var keys = wrapperBuyPtKeys(
      buyerKey,
      marketKey,
      tokenSyTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperBuyPt(
      invokedExponentCoreProgramMeta,
      keys,
      ptAmount,
      maxBaseAmount,
      mintSyRemAccountsUntil
    );
  }

  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public static Instruction wrapperBuyPt(final AccountMeta invokedExponentCoreProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long ptAmount,
                                         final long maxBaseAmount,
                                         final int mintSyRemAccountsUntil) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_BUY_PT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ptAmount);
    i += 8;
    putInt64LE(_data, i, maxBaseAmount);
    i += 8;
    _data[i] = (byte) mintSyRemAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public record WrapperBuyPtIxData(Discriminator discriminator,
                                   long ptAmount,
                                   long maxBaseAmount,
                                   int mintSyRemAccountsUntil) implements SerDe {

    public static WrapperBuyPtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int PT_AMOUNT_OFFSET = 1;
    public static final int MAX_BASE_AMOUNT_OFFSET = 9;
    public static final int MINT_SY_REM_ACCOUNTS_UNTIL_OFFSET = 17;

    public static WrapperBuyPtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var ptAmount = getInt64LE(_data, i);
      i += 8;
      final var maxBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var mintSyRemAccountsUntil = _data[i] & 0xFF;
      return new WrapperBuyPtIxData(discriminator, ptAmount, maxBaseAmount, mintSyRemAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ptAmount);
      i += 8;
      putInt64LE(_data, i, maxBaseAmount);
      i += 8;
      _data[i] = (byte) mintSyRemAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_BUY_YT_DISCRIMINATOR = toDiscriminator(31);

  public static List<AccountMeta> wrapperBuyYtKeys(final PublicKey buyerKey,
                                                   final PublicKey marketKey,
                                                   final PublicKey tokenSyTraderKey,
                                                   final PublicKey tokenYtTraderKey,
                                                   final PublicKey tokenPtTraderKey,
                                                   final PublicKey tokenSyEscrowKey,
                                                   final PublicKey tokenPtEscrowKey,
                                                   final PublicKey marketAddressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey vaultAuthorityKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey tokenSyEscrowVaultKey,
                                                   final PublicKey mintYtKey,
                                                   final PublicKey mintPtKey,
                                                   final PublicKey vaultAddressLookupTableKey,
                                                   final PublicKey userYieldPositionKey,
                                                   final PublicKey yieldPositionKey,
                                                   final PublicKey escrowYtKey,
                                                   final PublicKey systemProgramKey,
                                                   final PublicKey tokenFeeTreasurySyKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(buyerKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenYtTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(marketAddressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(vaultAuthorityKey),
      createWrite(vaultKey),
      createWrite(tokenSyEscrowVaultKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(vaultAddressLookupTableKey),
      createWrite(userYieldPositionKey),
      createWrite(yieldPositionKey),
      createWrite(escrowYtKey),
      createRead(systemProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param ytOut: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyAccountsLength: u8
  public static Instruction wrapperBuyYt(final AccountMeta invokedExponentCoreProgramMeta,
                                         final PublicKey buyerKey,
                                         final PublicKey marketKey,
                                         final PublicKey tokenSyTraderKey,
                                         final PublicKey tokenYtTraderKey,
                                         final PublicKey tokenPtTraderKey,
                                         final PublicKey tokenSyEscrowKey,
                                         final PublicKey tokenPtEscrowKey,
                                         final PublicKey marketAddressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey vaultAuthorityKey,
                                         final PublicKey vaultKey,
                                         final PublicKey tokenSyEscrowVaultKey,
                                         final PublicKey mintYtKey,
                                         final PublicKey mintPtKey,
                                         final PublicKey vaultAddressLookupTableKey,
                                         final PublicKey userYieldPositionKey,
                                         final PublicKey yieldPositionKey,
                                         final PublicKey escrowYtKey,
                                         final PublicKey systemProgramKey,
                                         final PublicKey tokenFeeTreasurySyKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey,
                                         final long ytOut,
                                         final long maxBaseAmount,
                                         final int mintSyAccountsLength) {
    final var keys = wrapperBuyYtKeys(
      buyerKey,
      marketKey,
      tokenSyTraderKey,
      tokenYtTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      marketAddressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      vaultAuthorityKey,
      vaultKey,
      tokenSyEscrowVaultKey,
      mintYtKey,
      mintPtKey,
      vaultAddressLookupTableKey,
      userYieldPositionKey,
      yieldPositionKey,
      escrowYtKey,
      systemProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperBuyYt(
      invokedExponentCoreProgramMeta,
      keys,
      ytOut,
      maxBaseAmount,
      mintSyAccountsLength
    );
  }

  /// @param ytOut: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyAccountsLength: u8
  public static Instruction wrapperBuyYt(final AccountMeta invokedExponentCoreProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long ytOut,
                                         final long maxBaseAmount,
                                         final int mintSyAccountsLength) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_BUY_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ytOut);
    i += 8;
    putInt64LE(_data, i, maxBaseAmount);
    i += 8;
    _data[i] = (byte) mintSyAccountsLength;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ytOut: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyAccountsLength: u8
  public record WrapperBuyYtIxData(Discriminator discriminator,
                                   long ytOut,
                                   long maxBaseAmount,
                                   int mintSyAccountsLength) implements SerDe {

    public static WrapperBuyYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int YT_OUT_OFFSET = 1;
    public static final int MAX_BASE_AMOUNT_OFFSET = 9;
    public static final int MINT_SY_ACCOUNTS_LENGTH_OFFSET = 17;

    public static WrapperBuyYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var ytOut = getInt64LE(_data, i);
      i += 8;
      final var maxBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var mintSyAccountsLength = _data[i] & 0xFF;
      return new WrapperBuyYtIxData(discriminator, ytOut, maxBaseAmount, mintSyAccountsLength);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ytOut);
      i += 8;
      putInt64LE(_data, i, maxBaseAmount);
      i += 8;
      _data[i] = (byte) mintSyAccountsLength;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_COLLECT_INTEREST_DISCRIMINATOR = toDiscriminator(33);

  /// @param authorityKey The authority owned by the vault for minting PT/YT
  public static List<AccountMeta> wrapperCollectInterestKeys(final PublicKey claimerKey,
                                                             final PublicKey authorityKey,
                                                             final PublicKey vaultKey,
                                                             final PublicKey addressLookupTableKey,
                                                             final PublicKey escrowSyKey,
                                                             final PublicKey syProgramKey,
                                                             final PublicKey tokenProgramKey,
                                                             final PublicKey yieldPositionKey,
                                                             final PublicKey tokenSyDstKey,
                                                             final PublicKey treasurySyTokenAccountKey,
                                                             final PublicKey eventAuthorityKey,
                                                             final PublicKey programKey) {
    return List.of(
      createWritableSigner(claimerKey),
      createWrite(authorityKey),
      createWrite(vaultKey),
      createRead(addressLookupTableKey),
      createWrite(escrowSyKey),
      createRead(syProgramKey),
      createRead(tokenProgramKey),
      createWrite(yieldPositionKey),
      createWrite(tokenSyDstKey),
      createWrite(treasurySyTokenAccountKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param authorityKey The authority owned by the vault for minting PT/YT
  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperCollectInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                                   final PublicKey claimerKey,
                                                   final PublicKey authorityKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey addressLookupTableKey,
                                                   final PublicKey escrowSyKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey yieldPositionKey,
                                                   final PublicKey tokenSyDstKey,
                                                   final PublicKey treasurySyTokenAccountKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey,
                                                   final int redeemSyAccountsLength) {
    final var keys = wrapperCollectInterestKeys(
      claimerKey,
      authorityKey,
      vaultKey,
      addressLookupTableKey,
      escrowSyKey,
      syProgramKey,
      tokenProgramKey,
      yieldPositionKey,
      tokenSyDstKey,
      treasurySyTokenAccountKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperCollectInterest(invokedExponentCoreProgramMeta, keys, redeemSyAccountsLength);
  }

  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperCollectInterest(final AccountMeta invokedExponentCoreProgramMeta,
                                                   final List<AccountMeta> keys,
                                                   final int redeemSyAccountsLength) {
    final byte[] _data = new byte[2];
    int i = WRAPPER_COLLECT_INTEREST_DISCRIMINATOR.write(_data, 0);
    _data[i] = (byte) redeemSyAccountsLength;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param redeemSyAccountsLength: u8
  public record WrapperCollectInterestIxData(Discriminator discriminator, int redeemSyAccountsLength) implements SerDe {

    public static WrapperCollectInterestIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 2;

    public static final int REDEEM_SY_ACCOUNTS_LENGTH_OFFSET = 1;

    public static WrapperCollectInterestIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var redeemSyAccountsLength = _data[i] & 0xFF;
      return new WrapperCollectInterestIxData(discriminator, redeemSyAccountsLength);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      _data[i] = (byte) redeemSyAccountsLength;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_MERGE_DISCRIMINATOR = toDiscriminator(39);

  /// @param tokenSyMergerKey Token account for SY owned by the merger
  public static List<AccountMeta> wrapperMergeKeys(final PublicKey mergerKey,
                                                   final PublicKey tokenSyMergerKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey escrowSyKey,
                                                   final PublicKey tokenYtMergerKey,
                                                   final PublicKey tokenPtMergerKey,
                                                   final PublicKey mintYtKey,
                                                   final PublicKey mintPtKey,
                                                   final PublicKey authorityKey,
                                                   final PublicKey vaultAddressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey vaultRobotYieldPositionKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey systemProgramKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(mergerKey),
      createWrite(tokenSyMergerKey),
      createWrite(vaultKey),
      createWrite(escrowSyKey),
      createWrite(tokenYtMergerKey),
      createWrite(tokenPtMergerKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createWrite(authorityKey),
      createRead(vaultAddressLookupTableKey),
      createRead(tokenProgramKey),
      createWrite(vaultRobotYieldPositionKey),
      createRead(syProgramKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenSyMergerKey Token account for SY owned by the merger
  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperMerge(final AccountMeta invokedExponentCoreProgramMeta,
                                         final PublicKey mergerKey,
                                         final PublicKey tokenSyMergerKey,
                                         final PublicKey vaultKey,
                                         final PublicKey escrowSyKey,
                                         final PublicKey tokenYtMergerKey,
                                         final PublicKey tokenPtMergerKey,
                                         final PublicKey mintYtKey,
                                         final PublicKey mintPtKey,
                                         final PublicKey authorityKey,
                                         final PublicKey vaultAddressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey vaultRobotYieldPositionKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey systemProgramKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey,
                                         final long amountPy,
                                         final int redeemSyAccountsUntil) {
    final var keys = wrapperMergeKeys(
      mergerKey,
      tokenSyMergerKey,
      vaultKey,
      escrowSyKey,
      tokenYtMergerKey,
      tokenPtMergerKey,
      mintYtKey,
      mintPtKey,
      authorityKey,
      vaultAddressLookupTableKey,
      tokenProgramKey,
      vaultRobotYieldPositionKey,
      syProgramKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperMerge(invokedExponentCoreProgramMeta, keys, amountPy, redeemSyAccountsUntil);
  }

  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperMerge(final AccountMeta invokedExponentCoreProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long amountPy,
                                         final int redeemSyAccountsUntil) {
    final byte[] _data = new byte[10];
    int i = WRAPPER_MERGE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountPy);
    i += 8;
    _data[i] = (byte) redeemSyAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public record WrapperMergeIxData(Discriminator discriminator, long amountPy, int redeemSyAccountsUntil) implements SerDe {

    public static WrapperMergeIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 10;

    public static final int AMOUNT_PY_OFFSET = 1;
    public static final int REDEEM_SY_ACCOUNTS_UNTIL_OFFSET = 9;

    public static WrapperMergeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountPy = getInt64LE(_data, i);
      i += 8;
      final var redeemSyAccountsUntil = _data[i] & 0xFF;
      return new WrapperMergeIxData(discriminator, amountPy, redeemSyAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountPy);
      i += 8;
      _data[i] = (byte) redeemSyAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_PROVIDE_LIQUIDITY_DISCRIMINATOR = toDiscriminator(28);

  /// Provide liquidity to a market starting with a base asset
  /// This instruction
  /// - deposits base asset for SY
  /// - strips a portion of the SY into PT & YT
  /// - provides the remaining SY with the PT into the market
  /// - keeps the YT
  ///
  /// # Arguments
  /// - `amount_base` - The amount of base asset to deposit
  /// - `min_lp_out` - The minimum amount of LP tokens to receive
  /// - `mint_base_accounts_until` - The index of the account to use for the base asset mint
  ///
  /// @param authorityKey The authority owned by the vault for minting PT/YT
  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenYtDepositorKey Token account for YT owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  public static List<AccountMeta> wrapperProvideLiquidityKeys(final PublicKey depositorKey,
                                                              final PublicKey authorityKey,
                                                              final PublicKey vaultKey,
                                                              final PublicKey marketKey,
                                                              final PublicKey tokenPtEscrowKey,
                                                              final PublicKey tokenSyEscrowKey,
                                                              final PublicKey tokenLpDstKey,
                                                              final PublicKey mintLpKey,
                                                              final PublicKey tokenSyDepositorKey,
                                                              final PublicKey escrowSyKey,
                                                              final PublicKey tokenYtDepositorKey,
                                                              final PublicKey tokenPtDepositorKey,
                                                              final PublicKey mintYtKey,
                                                              final PublicKey mintPtKey,
                                                              final PublicKey tokenProgramKey,
                                                              final PublicKey vaultAddressLookupTableKey,
                                                              final PublicKey marketAddressLookupTableKey,
                                                              final PublicKey syProgramKey,
                                                              final PublicKey userYieldPositionKey,
                                                              final PublicKey escrowYtKey,
                                                              final PublicKey tokenLpEscrowKey,
                                                              final PublicKey lpPositionKey,
                                                              final PublicKey vaultRobotYieldPositionKey,
                                                              final PublicKey systemProgramKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createRead(authorityKey),
      createWrite(vaultKey),
      createWrite(marketKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpDstKey),
      createWrite(mintLpKey),
      createWrite(tokenSyDepositorKey),
      createWrite(escrowSyKey),
      createWrite(tokenYtDepositorKey),
      createWrite(tokenPtDepositorKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(tokenProgramKey),
      createRead(vaultAddressLookupTableKey),
      createRead(marketAddressLookupTableKey),
      createRead(syProgramKey),
      createWrite(userYieldPositionKey),
      createWrite(escrowYtKey),
      createWrite(tokenLpEscrowKey),
      createWrite(lpPositionKey),
      createWrite(vaultRobotYieldPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// Provide liquidity to a market starting with a base asset
  /// This instruction
  /// - deposits base asset for SY
  /// - strips a portion of the SY into PT & YT
  /// - provides the remaining SY with the PT into the market
  /// - keeps the YT
  ///
  /// # Arguments
  /// - `amount_base` - The amount of base asset to deposit
  /// - `min_lp_out` - The minimum amount of LP tokens to receive
  /// - `mint_base_accounts_until` - The index of the account to use for the base asset mint
  ///
  /// @param authorityKey The authority owned by the vault for minting PT/YT
  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenYtDepositorKey Token account for YT owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintBaseAccountsUntil: u8
  public static Instruction wrapperProvideLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final PublicKey depositorKey,
                                                    final PublicKey authorityKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey marketKey,
                                                    final PublicKey tokenPtEscrowKey,
                                                    final PublicKey tokenSyEscrowKey,
                                                    final PublicKey tokenLpDstKey,
                                                    final PublicKey mintLpKey,
                                                    final PublicKey tokenSyDepositorKey,
                                                    final PublicKey escrowSyKey,
                                                    final PublicKey tokenYtDepositorKey,
                                                    final PublicKey tokenPtDepositorKey,
                                                    final PublicKey mintYtKey,
                                                    final PublicKey mintPtKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey vaultAddressLookupTableKey,
                                                    final PublicKey marketAddressLookupTableKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey userYieldPositionKey,
                                                    final PublicKey escrowYtKey,
                                                    final PublicKey tokenLpEscrowKey,
                                                    final PublicKey lpPositionKey,
                                                    final PublicKey vaultRobotYieldPositionKey,
                                                    final PublicKey systemProgramKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey,
                                                    final long amountBase,
                                                    final long minLpOut,
                                                    final int mintBaseAccountsUntil) {
    final var keys = wrapperProvideLiquidityKeys(
      depositorKey,
      authorityKey,
      vaultKey,
      marketKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpDstKey,
      mintLpKey,
      tokenSyDepositorKey,
      escrowSyKey,
      tokenYtDepositorKey,
      tokenPtDepositorKey,
      mintYtKey,
      mintPtKey,
      tokenProgramKey,
      vaultAddressLookupTableKey,
      marketAddressLookupTableKey,
      syProgramKey,
      userYieldPositionKey,
      escrowYtKey,
      tokenLpEscrowKey,
      lpPositionKey,
      vaultRobotYieldPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperProvideLiquidity(
      invokedExponentCoreProgramMeta,
      keys,
      amountBase,
      minLpOut,
      mintBaseAccountsUntil
    );
  }

  /// Provide liquidity to a market starting with a base asset
  /// This instruction
  /// - deposits base asset for SY
  /// - strips a portion of the SY into PT & YT
  /// - provides the remaining SY with the PT into the market
  /// - keeps the YT
  ///
  /// # Arguments
  /// - `amount_base` - The amount of base asset to deposit
  /// - `min_lp_out` - The minimum amount of LP tokens to receive
  /// - `mint_base_accounts_until` - The index of the account to use for the base asset mint
  ///
  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintBaseAccountsUntil: u8
  public static Instruction wrapperProvideLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final long amountBase,
                                                    final long minLpOut,
                                                    final int mintBaseAccountsUntil) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_PROVIDE_LIQUIDITY_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountBase);
    i += 8;
    putInt64LE(_data, i, minLpOut);
    i += 8;
    _data[i] = (byte) mintBaseAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintBaseAccountsUntil: u8
  public record WrapperProvideLiquidityIxData(Discriminator discriminator,
                                              long amountBase,
                                              long minLpOut,
                                              int mintBaseAccountsUntil) implements SerDe {

    public static WrapperProvideLiquidityIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int AMOUNT_BASE_OFFSET = 1;
    public static final int MIN_LP_OUT_OFFSET = 9;
    public static final int MINT_BASE_ACCOUNTS_UNTIL_OFFSET = 17;

    public static WrapperProvideLiquidityIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountBase = getInt64LE(_data, i);
      i += 8;
      final var minLpOut = getInt64LE(_data, i);
      i += 8;
      final var mintBaseAccountsUntil = _data[i] & 0xFF;
      return new WrapperProvideLiquidityIxData(discriminator, amountBase, minLpOut, mintBaseAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountBase);
      i += 8;
      putInt64LE(_data, i, minLpOut);
      i += 8;
      _data[i] = (byte) mintBaseAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_PROVIDE_LIQUIDITY_BASE_DISCRIMINATOR = toDiscriminator(36);

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  public static List<AccountMeta> wrapperProvideLiquidityBaseKeys(final PublicKey depositorKey,
                                                                  final PublicKey marketKey,
                                                                  final PublicKey tokenPtEscrowKey,
                                                                  final PublicKey tokenSyEscrowKey,
                                                                  final PublicKey tokenLpDstKey,
                                                                  final PublicKey mintLpKey,
                                                                  final PublicKey tokenSyDepositorKey,
                                                                  final PublicKey tokenPtDepositorKey,
                                                                  final PublicKey tokenProgramKey,
                                                                  final PublicKey marketAddressLookupTableKey,
                                                                  final PublicKey syProgramKey,
                                                                  final PublicKey tokenFeeTreasurySyKey,
                                                                  final PublicKey tokenLpEscrowKey,
                                                                  final PublicKey lpPositionKey,
                                                                  final PublicKey systemProgramKey,
                                                                  final PublicKey eventAuthorityKey,
                                                                  final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(marketKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpDstKey),
      createWrite(mintLpKey),
      createWrite(tokenSyDepositorKey),
      createWrite(tokenPtDepositorKey),
      createRead(tokenProgramKey),
      createRead(marketAddressLookupTableKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createWrite(tokenLpEscrowKey),
      createWrite(lpPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  /// @param externalPtToBuy: u64
  /// @param externalSyConstraint: u64
  public static Instruction wrapperProvideLiquidityBase(final AccountMeta invokedExponentCoreProgramMeta,
                                                        final PublicKey depositorKey,
                                                        final PublicKey marketKey,
                                                        final PublicKey tokenPtEscrowKey,
                                                        final PublicKey tokenSyEscrowKey,
                                                        final PublicKey tokenLpDstKey,
                                                        final PublicKey mintLpKey,
                                                        final PublicKey tokenSyDepositorKey,
                                                        final PublicKey tokenPtDepositorKey,
                                                        final PublicKey tokenProgramKey,
                                                        final PublicKey marketAddressLookupTableKey,
                                                        final PublicKey syProgramKey,
                                                        final PublicKey tokenFeeTreasurySyKey,
                                                        final PublicKey tokenLpEscrowKey,
                                                        final PublicKey lpPositionKey,
                                                        final PublicKey systemProgramKey,
                                                        final PublicKey eventAuthorityKey,
                                                        final PublicKey programKey,
                                                        final long amountBase,
                                                        final long minLpOut,
                                                        final int mintSyAccountsUntil,
                                                        final long externalPtToBuy,
                                                        final long externalSyConstraint) {
    final var keys = wrapperProvideLiquidityBaseKeys(
      depositorKey,
      marketKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpDstKey,
      mintLpKey,
      tokenSyDepositorKey,
      tokenPtDepositorKey,
      tokenProgramKey,
      marketAddressLookupTableKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      tokenLpEscrowKey,
      lpPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperProvideLiquidityBase(
      invokedExponentCoreProgramMeta,
      keys,
      amountBase,
      minLpOut,
      mintSyAccountsUntil,
      externalPtToBuy,
      externalSyConstraint
    );
  }

  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  /// @param externalPtToBuy: u64
  /// @param externalSyConstraint: u64
  public static Instruction wrapperProvideLiquidityBase(final AccountMeta invokedExponentCoreProgramMeta,
                                                        final List<AccountMeta> keys,
                                                        final long amountBase,
                                                        final long minLpOut,
                                                        final int mintSyAccountsUntil,
                                                        final long externalPtToBuy,
                                                        final long externalSyConstraint) {
    final byte[] _data = new byte[34];
    int i = WRAPPER_PROVIDE_LIQUIDITY_BASE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountBase);
    i += 8;
    putInt64LE(_data, i, minLpOut);
    i += 8;
    _data[i] = (byte) mintSyAccountsUntil;
    ++i;
    putInt64LE(_data, i, externalPtToBuy);
    i += 8;
    putInt64LE(_data, i, externalSyConstraint);

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountBase: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  /// @param externalPtToBuy: u64
  /// @param externalSyConstraint: u64
  public record WrapperProvideLiquidityBaseIxData(Discriminator discriminator,
                                                  long amountBase,
                                                  long minLpOut,
                                                  int mintSyAccountsUntil,
                                                  long externalPtToBuy,
                                                  long externalSyConstraint) implements SerDe {

    public static WrapperProvideLiquidityBaseIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 34;

    public static final int AMOUNT_BASE_OFFSET = 1;
    public static final int MIN_LP_OUT_OFFSET = 9;
    public static final int MINT_SY_ACCOUNTS_UNTIL_OFFSET = 17;
    public static final int EXTERNAL_PT_TO_BUY_OFFSET = 18;
    public static final int EXTERNAL_SY_CONSTRAINT_OFFSET = 26;

    public static WrapperProvideLiquidityBaseIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountBase = getInt64LE(_data, i);
      i += 8;
      final var minLpOut = getInt64LE(_data, i);
      i += 8;
      final var mintSyAccountsUntil = _data[i] & 0xFF;
      ++i;
      final var externalPtToBuy = getInt64LE(_data, i);
      i += 8;
      final var externalSyConstraint = getInt64LE(_data, i);
      return new WrapperProvideLiquidityBaseIxData(discriminator,
                                                   amountBase,
                                                   minLpOut,
                                                   mintSyAccountsUntil,
                                                   externalPtToBuy,
                                                   externalSyConstraint);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountBase);
      i += 8;
      putInt64LE(_data, i, minLpOut);
      i += 8;
      _data[i] = (byte) mintSyAccountsUntil;
      ++i;
      putInt64LE(_data, i, externalPtToBuy);
      i += 8;
      putInt64LE(_data, i, externalSyConstraint);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_PROVIDE_LIQUIDITY_CLASSIC_DISCRIMINATOR = toDiscriminator(37);

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  public static List<AccountMeta> wrapperProvideLiquidityClassicKeys(final PublicKey depositorKey,
                                                                     final PublicKey marketKey,
                                                                     final PublicKey tokenPtEscrowKey,
                                                                     final PublicKey tokenSyEscrowKey,
                                                                     final PublicKey tokenLpDstKey,
                                                                     final PublicKey mintLpKey,
                                                                     final PublicKey tokenSyDepositorKey,
                                                                     final PublicKey tokenPtDepositorKey,
                                                                     final PublicKey tokenProgramKey,
                                                                     final PublicKey marketAddressLookupTableKey,
                                                                     final PublicKey syProgramKey,
                                                                     final PublicKey tokenLpEscrowKey,
                                                                     final PublicKey lpPositionKey,
                                                                     final PublicKey systemProgramKey,
                                                                     final PublicKey eventAuthorityKey,
                                                                     final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(marketKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpDstKey),
      createWrite(mintLpKey),
      createWrite(tokenSyDepositorKey),
      createWrite(tokenPtDepositorKey),
      createRead(tokenProgramKey),
      createRead(marketAddressLookupTableKey),
      createRead(syProgramKey),
      createWrite(tokenLpEscrowKey),
      createWrite(lpPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param tokenPtDepositorKey Token account for PT owned by the depositor
  /// @param amountBase: u64
  /// @param amountPt: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  public static Instruction wrapperProvideLiquidityClassic(final AccountMeta invokedExponentCoreProgramMeta,
                                                           final PublicKey depositorKey,
                                                           final PublicKey marketKey,
                                                           final PublicKey tokenPtEscrowKey,
                                                           final PublicKey tokenSyEscrowKey,
                                                           final PublicKey tokenLpDstKey,
                                                           final PublicKey mintLpKey,
                                                           final PublicKey tokenSyDepositorKey,
                                                           final PublicKey tokenPtDepositorKey,
                                                           final PublicKey tokenProgramKey,
                                                           final PublicKey marketAddressLookupTableKey,
                                                           final PublicKey syProgramKey,
                                                           final PublicKey tokenLpEscrowKey,
                                                           final PublicKey lpPositionKey,
                                                           final PublicKey systemProgramKey,
                                                           final PublicKey eventAuthorityKey,
                                                           final PublicKey programKey,
                                                           final long amountBase,
                                                           final long amountPt,
                                                           final long minLpOut,
                                                           final int mintSyAccountsUntil) {
    final var keys = wrapperProvideLiquidityClassicKeys(
      depositorKey,
      marketKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpDstKey,
      mintLpKey,
      tokenSyDepositorKey,
      tokenPtDepositorKey,
      tokenProgramKey,
      marketAddressLookupTableKey,
      syProgramKey,
      tokenLpEscrowKey,
      lpPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperProvideLiquidityClassic(
      invokedExponentCoreProgramMeta,
      keys,
      amountBase,
      amountPt,
      minLpOut,
      mintSyAccountsUntil
    );
  }

  /// @param amountBase: u64
  /// @param amountPt: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  public static Instruction wrapperProvideLiquidityClassic(final AccountMeta invokedExponentCoreProgramMeta,
                                                           final List<AccountMeta> keys,
                                                           final long amountBase,
                                                           final long amountPt,
                                                           final long minLpOut,
                                                           final int mintSyAccountsUntil) {
    final byte[] _data = new byte[26];
    int i = WRAPPER_PROVIDE_LIQUIDITY_CLASSIC_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountBase);
    i += 8;
    putInt64LE(_data, i, amountPt);
    i += 8;
    putInt64LE(_data, i, minLpOut);
    i += 8;
    _data[i] = (byte) mintSyAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountBase: u64
  /// @param amountPt: u64
  /// @param minLpOut: u64
  /// @param mintSyAccountsUntil: u8
  public record WrapperProvideLiquidityClassicIxData(Discriminator discriminator,
                                                     long amountBase,
                                                     long amountPt,
                                                     long minLpOut,
                                                     int mintSyAccountsUntil) implements SerDe {

    public static WrapperProvideLiquidityClassicIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 26;

    public static final int AMOUNT_BASE_OFFSET = 1;
    public static final int AMOUNT_PT_OFFSET = 9;
    public static final int MIN_LP_OUT_OFFSET = 17;
    public static final int MINT_SY_ACCOUNTS_UNTIL_OFFSET = 25;

    public static WrapperProvideLiquidityClassicIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountBase = getInt64LE(_data, i);
      i += 8;
      final var amountPt = getInt64LE(_data, i);
      i += 8;
      final var minLpOut = getInt64LE(_data, i);
      i += 8;
      final var mintSyAccountsUntil = _data[i] & 0xFF;
      return new WrapperProvideLiquidityClassicIxData(discriminator,
                                                      amountBase,
                                                      amountPt,
                                                      minLpOut,
                                                      mintSyAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountBase);
      i += 8;
      putInt64LE(_data, i, amountPt);
      i += 8;
      putInt64LE(_data, i, minLpOut);
      i += 8;
      _data[i] = (byte) mintSyAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_SELL_PT_DISCRIMINATOR = toDiscriminator(30);

  public static List<AccountMeta> wrapperSellPtKeys(final PublicKey sellerKey,
                                                    final PublicKey marketKey,
                                                    final PublicKey tokenSyTraderKey,
                                                    final PublicKey tokenPtTraderKey,
                                                    final PublicKey tokenSyEscrowKey,
                                                    final PublicKey tokenPtEscrowKey,
                                                    final PublicKey addressLookupTableKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey tokenFeeTreasurySyKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(sellerKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public static Instruction wrapperSellPt(final AccountMeta invokedExponentCoreProgramMeta,
                                          final PublicKey sellerKey,
                                          final PublicKey marketKey,
                                          final PublicKey tokenSyTraderKey,
                                          final PublicKey tokenPtTraderKey,
                                          final PublicKey tokenSyEscrowKey,
                                          final PublicKey tokenPtEscrowKey,
                                          final PublicKey addressLookupTableKey,
                                          final PublicKey tokenProgramKey,
                                          final PublicKey syProgramKey,
                                          final PublicKey tokenFeeTreasurySyKey,
                                          final PublicKey eventAuthorityKey,
                                          final PublicKey programKey,
                                          final long amountPt,
                                          final long minBaseAmount,
                                          final int redeemSyRemAccountsUntil) {
    final var keys = wrapperSellPtKeys(
      sellerKey,
      marketKey,
      tokenSyTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperSellPt(
      invokedExponentCoreProgramMeta,
      keys,
      amountPt,
      minBaseAmount,
      redeemSyRemAccountsUntil
    );
  }

  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public static Instruction wrapperSellPt(final AccountMeta invokedExponentCoreProgramMeta,
                                          final List<AccountMeta> keys,
                                          final long amountPt,
                                          final long minBaseAmount,
                                          final int redeemSyRemAccountsUntil) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_SELL_PT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountPt);
    i += 8;
    putInt64LE(_data, i, minBaseAmount);
    i += 8;
    _data[i] = (byte) redeemSyRemAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public record WrapperSellPtIxData(Discriminator discriminator,
                                    long amountPt,
                                    long minBaseAmount,
                                    int redeemSyRemAccountsUntil) implements SerDe {

    public static WrapperSellPtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int AMOUNT_PT_OFFSET = 1;
    public static final int MIN_BASE_AMOUNT_OFFSET = 9;
    public static final int REDEEM_SY_REM_ACCOUNTS_UNTIL_OFFSET = 17;

    public static WrapperSellPtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountPt = getInt64LE(_data, i);
      i += 8;
      final var minBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var redeemSyRemAccountsUntil = _data[i] & 0xFF;
      return new WrapperSellPtIxData(discriminator, amountPt, minBaseAmount, redeemSyRemAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountPt);
      i += 8;
      putInt64LE(_data, i, minBaseAmount);
      i += 8;
      _data[i] = (byte) redeemSyRemAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_SELL_YT_DISCRIMINATOR = toDiscriminator(32);

  public static List<AccountMeta> wrapperSellYtKeys(final PublicKey sellerKey,
                                                    final PublicKey marketKey,
                                                    final PublicKey tokenSyTraderKey,
                                                    final PublicKey tokenYtTraderKey,
                                                    final PublicKey tokenPtTraderKey,
                                                    final PublicKey tokenSyEscrowKey,
                                                    final PublicKey tokenPtEscrowKey,
                                                    final PublicKey marketAddressLookupTableKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey vaultAuthorityKey,
                                                    final PublicKey vaultKey,
                                                    final PublicKey tokenSyEscrowVaultKey,
                                                    final PublicKey mintYtKey,
                                                    final PublicKey mintPtKey,
                                                    final PublicKey vaultAddressLookupTableKey,
                                                    final PublicKey yieldPositionKey,
                                                    final PublicKey tokenFeeTreasurySyKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(sellerKey),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenYtTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(marketAddressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(vaultAuthorityKey),
      createWrite(vaultKey),
      createWrite(tokenSyEscrowVaultKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createRead(vaultAddressLookupTableKey),
      createWrite(yieldPositionKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param ytAmount: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperSellYt(final AccountMeta invokedExponentCoreProgramMeta,
                                          final PublicKey sellerKey,
                                          final PublicKey marketKey,
                                          final PublicKey tokenSyTraderKey,
                                          final PublicKey tokenYtTraderKey,
                                          final PublicKey tokenPtTraderKey,
                                          final PublicKey tokenSyEscrowKey,
                                          final PublicKey tokenPtEscrowKey,
                                          final PublicKey marketAddressLookupTableKey,
                                          final PublicKey tokenProgramKey,
                                          final PublicKey syProgramKey,
                                          final PublicKey vaultAuthorityKey,
                                          final PublicKey vaultKey,
                                          final PublicKey tokenSyEscrowVaultKey,
                                          final PublicKey mintYtKey,
                                          final PublicKey mintPtKey,
                                          final PublicKey vaultAddressLookupTableKey,
                                          final PublicKey yieldPositionKey,
                                          final PublicKey tokenFeeTreasurySyKey,
                                          final PublicKey eventAuthorityKey,
                                          final PublicKey programKey,
                                          final long ytAmount,
                                          final long minBaseAmount,
                                          final int redeemSyAccountsUntil) {
    final var keys = wrapperSellYtKeys(
      sellerKey,
      marketKey,
      tokenSyTraderKey,
      tokenYtTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      marketAddressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      vaultAuthorityKey,
      vaultKey,
      tokenSyEscrowVaultKey,
      mintYtKey,
      mintPtKey,
      vaultAddressLookupTableKey,
      yieldPositionKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperSellYt(
      invokedExponentCoreProgramMeta,
      keys,
      ytAmount,
      minBaseAmount,
      redeemSyAccountsUntil
    );
  }

  /// @param ytAmount: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperSellYt(final AccountMeta invokedExponentCoreProgramMeta,
                                          final List<AccountMeta> keys,
                                          final long ytAmount,
                                          final long minBaseAmount,
                                          final int redeemSyAccountsUntil) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_SELL_YT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ytAmount);
    i += 8;
    putInt64LE(_data, i, minBaseAmount);
    i += 8;
    _data[i] = (byte) redeemSyAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param ytAmount: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyAccountsUntil: u8
  public record WrapperSellYtIxData(Discriminator discriminator,
                                    long ytAmount,
                                    long minBaseAmount,
                                    int redeemSyAccountsUntil) implements SerDe {

    public static WrapperSellYtIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int YT_AMOUNT_OFFSET = 1;
    public static final int MIN_BASE_AMOUNT_OFFSET = 9;
    public static final int REDEEM_SY_ACCOUNTS_UNTIL_OFFSET = 17;

    public static WrapperSellYtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var ytAmount = getInt64LE(_data, i);
      i += 8;
      final var minBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var redeemSyAccountsUntil = _data[i] & 0xFF;
      return new WrapperSellYtIxData(discriminator, ytAmount, minBaseAmount, redeemSyAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ytAmount);
      i += 8;
      putInt64LE(_data, i, minBaseAmount);
      i += 8;
      _data[i] = (byte) redeemSyAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_STRIP_DISCRIMINATOR = toDiscriminator(38);

  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  public static List<AccountMeta> wrapperStripKeys(final PublicKey depositorKey,
                                                   final PublicKey tokenSyDepositorKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey escrowSyKey,
                                                   final PublicKey tokenYtDepositorKey,
                                                   final PublicKey tokenPtDepositorKey,
                                                   final PublicKey mintYtKey,
                                                   final PublicKey mintPtKey,
                                                   final PublicKey authorityKey,
                                                   final PublicKey vaultAddressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey escrowYtKey,
                                                   final PublicKey userYieldPositionKey,
                                                   final PublicKey vaultRobotYieldPositionKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey systemProgramKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(depositorKey),
      createWrite(tokenSyDepositorKey),
      createWrite(vaultKey),
      createWrite(escrowSyKey),
      createWrite(tokenYtDepositorKey),
      createWrite(tokenPtDepositorKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createWrite(authorityKey),
      createRead(vaultAddressLookupTableKey),
      createRead(tokenProgramKey),
      createWrite(escrowYtKey),
      createWrite(userYieldPositionKey),
      createWrite(vaultRobotYieldPositionKey),
      createRead(syProgramKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenSyDepositorKey Token account for SY owned by the depositor
  /// @param amountBase: u64
  /// @param mintSyAccountsUntil: u8
  public static Instruction wrapperStrip(final AccountMeta invokedExponentCoreProgramMeta,
                                         final PublicKey depositorKey,
                                         final PublicKey tokenSyDepositorKey,
                                         final PublicKey vaultKey,
                                         final PublicKey escrowSyKey,
                                         final PublicKey tokenYtDepositorKey,
                                         final PublicKey tokenPtDepositorKey,
                                         final PublicKey mintYtKey,
                                         final PublicKey mintPtKey,
                                         final PublicKey authorityKey,
                                         final PublicKey vaultAddressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey escrowYtKey,
                                         final PublicKey userYieldPositionKey,
                                         final PublicKey vaultRobotYieldPositionKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey systemProgramKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey,
                                         final long amountBase,
                                         final int mintSyAccountsUntil) {
    final var keys = wrapperStripKeys(
      depositorKey,
      tokenSyDepositorKey,
      vaultKey,
      escrowSyKey,
      tokenYtDepositorKey,
      tokenPtDepositorKey,
      mintYtKey,
      mintPtKey,
      authorityKey,
      vaultAddressLookupTableKey,
      tokenProgramKey,
      escrowYtKey,
      userYieldPositionKey,
      vaultRobotYieldPositionKey,
      syProgramKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperStrip(invokedExponentCoreProgramMeta, keys, amountBase, mintSyAccountsUntil);
  }

  /// @param amountBase: u64
  /// @param mintSyAccountsUntil: u8
  public static Instruction wrapperStrip(final AccountMeta invokedExponentCoreProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long amountBase,
                                         final int mintSyAccountsUntil) {
    final byte[] _data = new byte[10];
    int i = WRAPPER_STRIP_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountBase);
    i += 8;
    _data[i] = (byte) mintSyAccountsUntil;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountBase: u64
  /// @param mintSyAccountsUntil: u8
  public record WrapperStripIxData(Discriminator discriminator, long amountBase, int mintSyAccountsUntil) implements SerDe {

    public static WrapperStripIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 10;

    public static final int AMOUNT_BASE_OFFSET = 1;
    public static final int MINT_SY_ACCOUNTS_UNTIL_OFFSET = 9;

    public static WrapperStripIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountBase = getInt64LE(_data, i);
      i += 8;
      final var mintSyAccountsUntil = _data[i] & 0xFF;
      return new WrapperStripIxData(discriminator, amountBase, mintSyAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountBase);
      i += 8;
      _data[i] = (byte) mintSyAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_WITHDRAW_LIQUIDITY_DISCRIMINATOR = toDiscriminator(34);

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyWithdrawerKey Token account for SY owned by the depositor
  /// @param tokenPtWithdrawerKey Token account for PT owned by the depositor
  public static List<AccountMeta> wrapperWithdrawLiquidityKeys(final PublicKey withdrawerKey,
                                                               final PublicKey marketKey,
                                                               final PublicKey tokenPtEscrowKey,
                                                               final PublicKey tokenSyEscrowKey,
                                                               final PublicKey tokenLpSrcKey,
                                                               final PublicKey mintLpKey,
                                                               final PublicKey tokenSyWithdrawerKey,
                                                               final PublicKey tokenPtWithdrawerKey,
                                                               final PublicKey tokenProgramKey,
                                                               final PublicKey marketAddressLookupTableKey,
                                                               final PublicKey syProgramKey,
                                                               final PublicKey tokenLpEscrowKey,
                                                               final PublicKey tokenFeeTreasurySyKey,
                                                               final PublicKey lpPositionKey,
                                                               final PublicKey systemProgramKey,
                                                               final PublicKey eventAuthorityKey,
                                                               final PublicKey programKey) {
    return List.of(
      createWritableSigner(withdrawerKey),
      createWrite(marketKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpSrcKey),
      createWrite(mintLpKey),
      createWrite(tokenSyWithdrawerKey),
      createWrite(tokenPtWithdrawerKey),
      createRead(tokenProgramKey),
      createRead(marketAddressLookupTableKey),
      createRead(syProgramKey),
      createWrite(tokenLpEscrowKey),
      createWrite(tokenFeeTreasurySyKey),
      createWrite(lpPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyWithdrawerKey Token account for SY owned by the depositor
  /// @param tokenPtWithdrawerKey Token account for PT owned by the depositor
  /// @param amountLp: u64
  /// @param syConstraint: u64
  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperWithdrawLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                     final PublicKey withdrawerKey,
                                                     final PublicKey marketKey,
                                                     final PublicKey tokenPtEscrowKey,
                                                     final PublicKey tokenSyEscrowKey,
                                                     final PublicKey tokenLpSrcKey,
                                                     final PublicKey mintLpKey,
                                                     final PublicKey tokenSyWithdrawerKey,
                                                     final PublicKey tokenPtWithdrawerKey,
                                                     final PublicKey tokenProgramKey,
                                                     final PublicKey marketAddressLookupTableKey,
                                                     final PublicKey syProgramKey,
                                                     final PublicKey tokenLpEscrowKey,
                                                     final PublicKey tokenFeeTreasurySyKey,
                                                     final PublicKey lpPositionKey,
                                                     final PublicKey systemProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey,
                                                     final long amountLp,
                                                     final long syConstraint,
                                                     final int redeemSyAccountsLength) {
    final var keys = wrapperWithdrawLiquidityKeys(
      withdrawerKey,
      marketKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpSrcKey,
      mintLpKey,
      tokenSyWithdrawerKey,
      tokenPtWithdrawerKey,
      tokenProgramKey,
      marketAddressLookupTableKey,
      syProgramKey,
      tokenLpEscrowKey,
      tokenFeeTreasurySyKey,
      lpPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperWithdrawLiquidity(
      invokedExponentCoreProgramMeta,
      keys,
      amountLp,
      syConstraint,
      redeemSyAccountsLength
    );
  }

  /// @param amountLp: u64
  /// @param syConstraint: u64
  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperWithdrawLiquidity(final AccountMeta invokedExponentCoreProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final long amountLp,
                                                     final long syConstraint,
                                                     final int redeemSyAccountsLength) {
    final byte[] _data = new byte[18];
    int i = WRAPPER_WITHDRAW_LIQUIDITY_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountLp);
    i += 8;
    putInt64LE(_data, i, syConstraint);
    i += 8;
    _data[i] = (byte) redeemSyAccountsLength;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountLp: u64
  /// @param syConstraint: u64
  /// @param redeemSyAccountsLength: u8
  public record WrapperWithdrawLiquidityIxData(Discriminator discriminator,
                                               long amountLp,
                                               long syConstraint,
                                               int redeemSyAccountsLength) implements SerDe {

    public static WrapperWithdrawLiquidityIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 18;

    public static final int AMOUNT_LP_OFFSET = 1;
    public static final int SY_CONSTRAINT_OFFSET = 9;
    public static final int REDEEM_SY_ACCOUNTS_LENGTH_OFFSET = 17;

    public static WrapperWithdrawLiquidityIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountLp = getInt64LE(_data, i);
      i += 8;
      final var syConstraint = getInt64LE(_data, i);
      i += 8;
      final var redeemSyAccountsLength = _data[i] & 0xFF;
      return new WrapperWithdrawLiquidityIxData(discriminator, amountLp, syConstraint, redeemSyAccountsLength);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountLp);
      i += 8;
      putInt64LE(_data, i, syConstraint);
      i += 8;
      _data[i] = (byte) redeemSyAccountsLength;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_WITHDRAW_LIQUIDITY_CLASSIC_DISCRIMINATOR = toDiscriminator(35);

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyWithdrawerKey Token account for SY owned by the depositor
  /// @param tokenPtWithdrawerKey Token account for PT owned by the depositor
  public static List<AccountMeta> wrapperWithdrawLiquidityClassicKeys(final PublicKey withdrawerKey,
                                                                      final PublicKey marketKey,
                                                                      final PublicKey tokenPtEscrowKey,
                                                                      final PublicKey tokenSyEscrowKey,
                                                                      final PublicKey tokenLpSrcKey,
                                                                      final PublicKey mintLpKey,
                                                                      final PublicKey tokenSyWithdrawerKey,
                                                                      final PublicKey tokenPtWithdrawerKey,
                                                                      final PublicKey tokenProgramKey,
                                                                      final PublicKey marketAddressLookupTableKey,
                                                                      final PublicKey syProgramKey,
                                                                      final PublicKey tokenLpEscrowKey,
                                                                      final PublicKey lpPositionKey,
                                                                      final PublicKey systemProgramKey,
                                                                      final PublicKey eventAuthorityKey,
                                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(withdrawerKey),
      createWrite(marketKey),
      createWrite(tokenPtEscrowKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenLpSrcKey),
      createWrite(mintLpKey),
      createWrite(tokenSyWithdrawerKey),
      createWrite(tokenPtWithdrawerKey),
      createRead(tokenProgramKey),
      createRead(marketAddressLookupTableKey),
      createRead(syProgramKey),
      createWrite(tokenLpEscrowKey),
      createWrite(lpPositionKey),
      createRead(systemProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param tokenPtEscrowKey PT liquidity account
  /// @param tokenSyEscrowKey SY token account owned by the market
  /// @param tokenSyWithdrawerKey Token account for SY owned by the depositor
  /// @param tokenPtWithdrawerKey Token account for PT owned by the depositor
  /// @param amountLp: u64
  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperWithdrawLiquidityClassic(final AccountMeta invokedExponentCoreProgramMeta,
                                                            final PublicKey withdrawerKey,
                                                            final PublicKey marketKey,
                                                            final PublicKey tokenPtEscrowKey,
                                                            final PublicKey tokenSyEscrowKey,
                                                            final PublicKey tokenLpSrcKey,
                                                            final PublicKey mintLpKey,
                                                            final PublicKey tokenSyWithdrawerKey,
                                                            final PublicKey tokenPtWithdrawerKey,
                                                            final PublicKey tokenProgramKey,
                                                            final PublicKey marketAddressLookupTableKey,
                                                            final PublicKey syProgramKey,
                                                            final PublicKey tokenLpEscrowKey,
                                                            final PublicKey lpPositionKey,
                                                            final PublicKey systemProgramKey,
                                                            final PublicKey eventAuthorityKey,
                                                            final PublicKey programKey,
                                                            final long amountLp,
                                                            final int redeemSyAccountsLength) {
    final var keys = wrapperWithdrawLiquidityClassicKeys(
      withdrawerKey,
      marketKey,
      tokenPtEscrowKey,
      tokenSyEscrowKey,
      tokenLpSrcKey,
      mintLpKey,
      tokenSyWithdrawerKey,
      tokenPtWithdrawerKey,
      tokenProgramKey,
      marketAddressLookupTableKey,
      syProgramKey,
      tokenLpEscrowKey,
      lpPositionKey,
      systemProgramKey,
      eventAuthorityKey,
      programKey
    );
    return wrapperWithdrawLiquidityClassic(invokedExponentCoreProgramMeta, keys, amountLp, redeemSyAccountsLength);
  }

  /// @param amountLp: u64
  /// @param redeemSyAccountsLength: u8
  public static Instruction wrapperWithdrawLiquidityClassic(final AccountMeta invokedExponentCoreProgramMeta,
                                                            final List<AccountMeta> keys,
                                                            final long amountLp,
                                                            final int redeemSyAccountsLength) {
    final byte[] _data = new byte[10];
    int i = WRAPPER_WITHDRAW_LIQUIDITY_CLASSIC_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountLp);
    i += 8;
    _data[i] = (byte) redeemSyAccountsLength;

    return Instruction.createInstruction(invokedExponentCoreProgramMeta, keys, _data);
  }

  /// @param amountLp: u64
  /// @param redeemSyAccountsLength: u8
  public record WrapperWithdrawLiquidityClassicIxData(Discriminator discriminator, long amountLp, int redeemSyAccountsLength) implements SerDe {

    public static WrapperWithdrawLiquidityClassicIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 10;

    public static final int AMOUNT_LP_OFFSET = 1;
    public static final int REDEEM_SY_ACCOUNTS_LENGTH_OFFSET = 9;

    public static WrapperWithdrawLiquidityClassicIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createDiscriminator(_data, _offset, 1);
      int i = _offset + discriminator.length();
      final var amountLp = getInt64LE(_data, i);
      i += 8;
      final var redeemSyAccountsLength = _data[i] & 0xFF;
      return new WrapperWithdrawLiquidityClassicIxData(discriminator, amountLp, redeemSyAccountsLength);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountLp);
      i += 8;
      _data[i] = (byte) redeemSyAccountsLength;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private ExponentCoreProgram() {
  }
}
