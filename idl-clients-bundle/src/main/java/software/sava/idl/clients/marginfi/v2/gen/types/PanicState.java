// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Panic state for emergency protocol pausing
///
/// @param pauseFlags Whether the protocol is currently paused (1 = paused, 0 = not paused)
/// @param dailyPauseCount Number of times paused today (resets every 24 hours)
/// @param consecutivePauseCount Number of consecutive pauses (resets when unpause happens)
/// @param pauseStartTimestamp Timestamp when the current pause started (0 if not paused)
///                            * When a pause is extended before expiring, this could be in the future.
/// @param lastDailyResetTimestamp Timestamp of the last daily reset (for tracking daily pause count)
/// @param reservedSpace Reserved for future use (making total struct 32 bytes)
public record PanicState(int pauseFlags,
                         int dailyPauseCount,
                         int consecutivePauseCount,
                         byte[] reserved,
                         long pauseStartTimestamp,
                         long lastDailyResetTimestamp,
                         byte[] reservedSpace) implements SerDe {

  public static final int BYTES = 32;
  public static final int RESERVED_LEN = 5;
  public static final int RESERVED_SPACE_LEN = 8;

  public static final int PAUSE_FLAGS_OFFSET = 0;
  public static final int DAILY_PAUSE_COUNT_OFFSET = 1;
  public static final int CONSECUTIVE_PAUSE_COUNT_OFFSET = 2;
  public static final int RESERVED_OFFSET = 3;
  public static final int PAUSE_START_TIMESTAMP_OFFSET = 8;
  public static final int LAST_DAILY_RESET_TIMESTAMP_OFFSET = 16;
  public static final int RESERVED_SPACE_OFFSET = 24;

  public static PanicState read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var pauseFlags = _data[i] & 0xFF;
    ++i;
    final var dailyPauseCount = _data[i] & 0xFF;
    ++i;
    final var consecutivePauseCount = _data[i] & 0xFF;
    ++i;
    final var reserved = new byte[5];
    i += SerDeUtil.readArray(reserved, _data, i);
    final var pauseStartTimestamp = getInt64LE(_data, i);
    i += 8;
    final var lastDailyResetTimestamp = getInt64LE(_data, i);
    i += 8;
    final var reservedSpace = new byte[8];
    SerDeUtil.readArray(reservedSpace, _data, i);
    return new PanicState(pauseFlags,
                          dailyPauseCount,
                          consecutivePauseCount,
                          reserved,
                          pauseStartTimestamp,
                          lastDailyResetTimestamp,
                          reservedSpace);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    _data[i] = (byte) pauseFlags;
    ++i;
    _data[i] = (byte) dailyPauseCount;
    ++i;
    _data[i] = (byte) consecutivePauseCount;
    ++i;
    i += SerDeUtil.writeArrayChecked(reserved, 5, _data, i);
    putInt64LE(_data, i, pauseStartTimestamp);
    i += 8;
    putInt64LE(_data, i, lastDailyResetTimestamp);
    i += 8;
    i += SerDeUtil.writeArrayChecked(reservedSpace, 8, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
