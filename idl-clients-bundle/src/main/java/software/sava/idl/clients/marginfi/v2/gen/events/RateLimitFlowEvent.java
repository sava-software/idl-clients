// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param flowDirection 0 = outflow (withdraw/borrow), 1 = inflow (deposit/repay)
/// @param nativeAmount Amount in native tokens
/// @param currentTimestamp Unix timestamp when the flow was recorded
public record RateLimitFlowEvent(Discriminator discriminator,
                                 PublicKey group,
                                 PublicKey bank,
                                 PublicKey mint,
                                 int flowDirection,
                                 long nativeAmount,
                                 int mintDecimals,
                                 long currentTimestamp) implements MarginfiEvent {

  public static final int BYTES = 122;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(229, 5, 73, 200, 0, 107, 105, 109);

  public static final int GROUP_OFFSET = 8;
  public static final int BANK_OFFSET = 40;
  public static final int MINT_OFFSET = 72;
  public static final int FLOW_DIRECTION_OFFSET = 104;
  public static final int NATIVE_AMOUNT_OFFSET = 105;
  public static final int MINT_DECIMALS_OFFSET = 113;
  public static final int CURRENT_TIMESTAMP_OFFSET = 114;

  public static RateLimitFlowEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var group = readPubKey(_data, i);
    i += 32;
    final var bank = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var flowDirection = _data[i] & 0xFF;
    ++i;
    final var nativeAmount = getInt64LE(_data, i);
    i += 8;
    final var mintDecimals = _data[i] & 0xFF;
    ++i;
    final var currentTimestamp = getInt64LE(_data, i);
    return new RateLimitFlowEvent(discriminator,
                                  group,
                                  bank,
                                  mint,
                                  flowDirection,
                                  nativeAmount,
                                  mintDecimals,
                                  currentTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    group.write(_data, i);
    i += 32;
    bank.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    _data[i] = (byte) flowDirection;
    ++i;
    putInt64LE(_data, i, nativeAmount);
    i += 8;
    _data[i] = (byte) mintDecimals;
    ++i;
    putInt64LE(_data, i, currentTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
