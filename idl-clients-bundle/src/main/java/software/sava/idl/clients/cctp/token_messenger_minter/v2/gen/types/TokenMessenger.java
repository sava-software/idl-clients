// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param messageBodyVersion: u32
/// @param authorityBump: u8
/// @param minFee: u32
public record TokenMessenger(PublicKey _address,
                             Discriminator discriminator,
                             PublicKey denylister,
                             PublicKey owner,
                             PublicKey pendingOwner,
                             long messageBodyVersion,
                             int authorityBump,
                             PublicKey feeRecipient,
                             PublicKey minFeeController,
                             long minFee) implements SerDe {

  public static final int BYTES = 177;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(162, 4, 242, 52, 147, 243, 221, 96);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int DENYLISTER_OFFSET = 8;
  public static final int OWNER_OFFSET = 40;
  public static final int PENDING_OWNER_OFFSET = 72;
  public static final int MESSAGE_BODY_VERSION_OFFSET = 104;
  public static final int AUTHORITY_BUMP_OFFSET = 108;
  public static final int FEE_RECIPIENT_OFFSET = 109;
  public static final int MIN_FEE_CONTROLLER_OFFSET = 141;
  public static final int MIN_FEE_OFFSET = 173;

  public static Filter createDenylisterFilter(final PublicKey denylister) {
    return Filter.createMemCompFilter(DENYLISTER_OFFSET, denylister);
  }

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createPendingOwnerFilter(final PublicKey pendingOwner) {
    return Filter.createMemCompFilter(PENDING_OWNER_OFFSET, pendingOwner);
  }

  public static Filter createMessageBodyVersionFilter(final long messageBodyVersion) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) messageBodyVersion);
    return Filter.createMemCompFilter(MESSAGE_BODY_VERSION_OFFSET, _data);
  }

  public static Filter createAuthorityBumpFilter(final int authorityBump) {
    return Filter.createMemCompFilter(AUTHORITY_BUMP_OFFSET, new byte[]{(byte) authorityBump});
  }

  public static Filter createFeeRecipientFilter(final PublicKey feeRecipient) {
    return Filter.createMemCompFilter(FEE_RECIPIENT_OFFSET, feeRecipient);
  }

  public static Filter createMinFeeControllerFilter(final PublicKey minFeeController) {
    return Filter.createMemCompFilter(MIN_FEE_CONTROLLER_OFFSET, minFeeController);
  }

  public static Filter createMinFeeFilter(final long minFee) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) minFee);
    return Filter.createMemCompFilter(MIN_FEE_OFFSET, _data);
  }

  public static TokenMessenger read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static TokenMessenger read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static TokenMessenger read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static TokenMessenger readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static TokenMessenger readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a TokenMessenger account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], TokenMessenger> FACTORY = TokenMessenger::readChecked;

  public static TokenMessenger read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var denylister = readPubKey(_data, i);
    i += 32;
    final var owner = readPubKey(_data, i);
    i += 32;
    final var pendingOwner = readPubKey(_data, i);
    i += 32;
    final var messageBodyVersion = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var authorityBump = _data[i] & 0xFF;
    ++i;
    final var feeRecipient = readPubKey(_data, i);
    i += 32;
    final var minFeeController = readPubKey(_data, i);
    i += 32;
    final var minFee = Integer.toUnsignedLong(getInt32LE(_data, i));
    return new TokenMessenger(_address,
                              discriminator,
                              denylister,
                              owner,
                              pendingOwner,
                              messageBodyVersion,
                              authorityBump,
                              feeRecipient,
                              minFeeController,
                              minFee);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    denylister.write(_data, i);
    i += 32;
    owner.write(_data, i);
    i += 32;
    pendingOwner.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) messageBodyVersion);
    i += 4;
    _data[i] = (byte) authorityBump;
    ++i;
    feeRecipient.write(_data, i);
    i += 32;
    minFeeController.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) minFee);
    i += 4;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
