// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param remoteDomain: u32
/// @param bump: u8
public record TokenPair(PublicKey _address,
                        Discriminator discriminator,
                        long remoteDomain,
                        PublicKey remoteToken,
                        PublicKey localToken,
                        int bump) implements SerDe {

  public static final int BYTES = 77;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(17, 214, 45, 176, 229, 149, 197, 71);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int REMOTE_DOMAIN_OFFSET = 8;
  public static final int REMOTE_TOKEN_OFFSET = 12;
  public static final int LOCAL_TOKEN_OFFSET = 44;
  public static final int BUMP_OFFSET = 76;

  public static Filter createRemoteDomainFilter(final long remoteDomain) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) remoteDomain);
    return Filter.createMemCompFilter(REMOTE_DOMAIN_OFFSET, _data);
  }

  public static Filter createRemoteTokenFilter(final PublicKey remoteToken) {
    return Filter.createMemCompFilter(REMOTE_TOKEN_OFFSET, remoteToken);
  }

  public static Filter createLocalTokenFilter(final PublicKey localToken) {
    return Filter.createMemCompFilter(LOCAL_TOKEN_OFFSET, localToken);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static TokenPair read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static TokenPair read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static TokenPair read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static TokenPair readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static TokenPair readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a TokenPair account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], TokenPair> FACTORY = TokenPair::readChecked;

  public static TokenPair read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var remoteDomain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var remoteToken = readPubKey(_data, i);
    i += 32;
    final var localToken = readPubKey(_data, i);
    i += 32;
    final var bump = _data[i] & 0xFF;
    return new TokenPair(_address,
                         discriminator,
                         remoteDomain,
                         remoteToken,
                         localToken,
                         bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt32LE(_data, i, (int) remoteDomain);
    i += 4;
    remoteToken.write(_data, i);
    i += 32;
    localToken.write(_data, i);
    i += 32;
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
