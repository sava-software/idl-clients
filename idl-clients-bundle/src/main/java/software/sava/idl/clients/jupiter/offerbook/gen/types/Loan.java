// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param fillIndex: u64
/// @param apy: u32
/// @param duration: u32
/// @param principalAmount: u64
/// @param collateralAmount: u64
/// @param interest: u64
/// @param createdAt: u64
/// @param expiredAt: u64
/// @param updatedAt: u64
public record Loan(PublicKey _address,
                   Discriminator discriminator,
                   PublicKey lender,
                   PublicKey borrower,
                   PublicKey creator,
                   PublicKey offer,
                   LoanStatus status,
                   LoanType loanType,
                   byte[] padding,
                   long fillIndex,
                   Asset principal,
                   byte[] padding1,
                   Asset collateral,
                   byte[] padding2,
                   long apy,
                   long duration,
                   long principalAmount,
                   long collateralAmount,
                   long interest,
                   long createdAt,
                   long expiredAt,
                   long updatedAt,
                   int bump,
                   int collateralAccountBump,
                   byte[] padding4,
                   byte[] reserved) implements SerDe {

  public static final int PADDING_LEN = 6;
  public static final int PADDING_1_LEN = 7;
  public static final int PADDING_2_LEN = 7;
  public static final int PADDING_4_LEN = 6;
  public static final int RESERVED_LEN = 48;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(20, 195, 70, 117, 165, 227, 182, 1);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int LENDER_OFFSET = 8;
  public static final int BORROWER_OFFSET = 40;
  public static final int CREATOR_OFFSET = 72;
  public static final int OFFER_OFFSET = 104;
  public static final int STATUS_OFFSET = 136;
  public static final int LOAN_TYPE_OFFSET = 137;
  public static final int PADDING_OFFSET = 138;
  public static final int FILL_INDEX_OFFSET = 144;
  public static final int PRINCIPAL_OFFSET = 152;

  public static Filter createLenderFilter(final PublicKey lender) {
    return Filter.createMemCompFilter(LENDER_OFFSET, lender);
  }

  public static Filter createBorrowerFilter(final PublicKey borrower) {
    return Filter.createMemCompFilter(BORROWER_OFFSET, borrower);
  }

  public static Filter createCreatorFilter(final PublicKey creator) {
    return Filter.createMemCompFilter(CREATOR_OFFSET, creator);
  }

  public static Filter createOfferFilter(final PublicKey offer) {
    return Filter.createMemCompFilter(OFFER_OFFSET, offer);
  }

  public static Filter createStatusFilter(final LoanStatus status) {
    return Filter.createMemCompFilter(STATUS_OFFSET, status.write());
  }

  public static Filter createLoanTypeFilter(final LoanType loanType) {
    return Filter.createMemCompFilter(LOAN_TYPE_OFFSET, loanType.write());
  }

  public static Filter createFillIndexFilter(final long fillIndex) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, fillIndex);
    return Filter.createMemCompFilter(FILL_INDEX_OFFSET, _data);
  }

  public static Filter createPrincipalFilter(final Asset principal) {
    return Filter.createMemCompFilter(PRINCIPAL_OFFSET, principal.write());
  }

  public static Loan read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Loan read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Loan read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Loan> FACTORY = Loan::read;

  public static Loan read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var lender = readPubKey(_data, i);
    i += 32;
    final var borrower = readPubKey(_data, i);
    i += 32;
    final var creator = readPubKey(_data, i);
    i += 32;
    final var offer = readPubKey(_data, i);
    i += 32;
    final var status = LoanStatus.read(_data, i);
    i += status.l();
    final var loanType = LoanType.read(_data, i);
    i += loanType.l();
    final var padding = new byte[6];
    i += SerDeUtil.readArray(padding, _data, i);
    final var fillIndex = getInt64LE(_data, i);
    i += 8;
    final var principal = Asset.read(_data, i);
    i += principal.l();
    final var padding1 = new byte[7];
    i += SerDeUtil.readArray(padding1, _data, i);
    final var collateral = Asset.read(_data, i);
    i += collateral.l();
    final var padding2 = new byte[7];
    i += SerDeUtil.readArray(padding2, _data, i);
    final var apy = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var duration = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var principalAmount = getInt64LE(_data, i);
    i += 8;
    final var collateralAmount = getInt64LE(_data, i);
    i += 8;
    final var interest = getInt64LE(_data, i);
    i += 8;
    final var createdAt = getInt64LE(_data, i);
    i += 8;
    final var expiredAt = getInt64LE(_data, i);
    i += 8;
    final var updatedAt = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var collateralAccountBump = _data[i] & 0xFF;
    ++i;
    final var padding4 = new byte[6];
    i += SerDeUtil.readArray(padding4, _data, i);
    final var reserved = new byte[48];
    SerDeUtil.readArray(reserved, _data, i);
    return new Loan(_address,
                    discriminator,
                    lender,
                    borrower,
                    creator,
                    offer,
                    status,
                    loanType,
                    padding,
                    fillIndex,
                    principal,
                    padding1,
                    collateral,
                    padding2,
                    apy,
                    duration,
                    principalAmount,
                    collateralAmount,
                    interest,
                    createdAt,
                    expiredAt,
                    updatedAt,
                    bump,
                    collateralAccountBump,
                    padding4,
                    reserved);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    lender.write(_data, i);
    i += 32;
    borrower.write(_data, i);
    i += 32;
    creator.write(_data, i);
    i += 32;
    offer.write(_data, i);
    i += 32;
    i += status.write(_data, i);
    i += loanType.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding, 6, _data, i);
    putInt64LE(_data, i, fillIndex);
    i += 8;
    i += principal.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding1, 7, _data, i);
    i += collateral.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding2, 7, _data, i);
    putInt32LE(_data, i, (int) apy);
    i += 4;
    putInt32LE(_data, i, (int) duration);
    i += 4;
    putInt64LE(_data, i, principalAmount);
    i += 8;
    putInt64LE(_data, i, collateralAmount);
    i += 8;
    putInt64LE(_data, i, interest);
    i += 8;
    putInt64LE(_data, i, createdAt);
    i += 8;
    putInt64LE(_data, i, expiredAt);
    i += 8;
    putInt64LE(_data, i, updatedAt);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    _data[i] = (byte) collateralAccountBump;
    ++i;
    i += SerDeUtil.writeArrayChecked(padding4, 6, _data, i);
    i += SerDeUtil.writeArrayChecked(reserved, 48, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 32
         + 32
         + status.l()
         + loanType.l()
         + SerDeUtil.lenArray(padding)
         + 8
         + principal.l()
         + SerDeUtil.lenArray(padding1)
         + collateral.l()
         + SerDeUtil.lenArray(padding2)
         + 4
         + 4
         + 8
         + 8
         + 8
         + 8
         + 8
         + 8
         + 1
         + 1
         + SerDeUtil.lenArray(padding4)
         + SerDeUtil.lenArray(reserved);
  }
}
