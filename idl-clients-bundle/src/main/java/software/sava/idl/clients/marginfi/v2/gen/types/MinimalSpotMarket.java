// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// Minimal representation of Drift's SpotMarket account
/// Only includes the fields we actually need for marginfi integration
/// https://github.com/drift-labs/protocol-v2/tree/master/programs/drift/src/state/spot_market.rs#L35
///
/// @param pubkey The address of the spot market. It is a pda of the market index
/// @param oracle The oracle used to price the markets deposits/borrows
/// @param mint The token mint of the market
/// @param vault The vault used to store the market's deposits
/// @param padding1: u64[][]
/// @param depositBalance All the fields we need for testing (stored as raw bytes for simplicity)
/// @param padding3: u64[]
/// @param lastInterestTs: u64 Last time the cumulative deposit and borrow interest was updated
///                       Offset: 568 bytes from start of struct (including discriminator)
/// @param padding4: u64[]
/// @param decimals: u32
/// @param marketIndex: u16
/// @param padding5: u16[]
/// @param padding7: u64[] Padding to reach 776 bytes total (including discriminator)
public record MinimalSpotMarket(PublicKey _address,
                                Discriminator discriminator,
                                PublicKey pubkey,
                                PublicKey oracle,
                                PublicKey mint,
                                PublicKey vault,
                                long[][] padding1,
                                byte[] padding2,
                                byte[] depositBalance,
                                byte[] borrowBalance,
                                byte[] cumulativeDepositInterest,
                                byte[] cumulativeBorrowInterest,
                                long[] padding3,
                                long lastInterestTs,
                                long[] padding4,
                                long decimals,
                                int marketIndex,
                                int[] padding5,
                                byte[] padding6,
                                int poolId,
                                long[] padding7) implements SerDe {

  public static final int BYTES = 776;
  public static final int PADDING_1_LEN = 9;
  public static final int PADDING_2_LEN = 8;
  public static final int DEPOSIT_BALANCE_LEN = 16;
  public static final int BORROW_BALANCE_LEN = 16;
  public static final int CUMULATIVE_DEPOSIT_INTEREST_LEN = 16;
  public static final int CUMULATIVE_BORROW_INTEREST_LEN = 16;
  public static final int PADDING_3_LEN = 9;
  public static final int PADDING_4_LEN = 13;
  public static final int PADDING_5_LEN = 24;
  public static final int PADDING_6_LEN = 1;
  public static final int PADDING_7_LEN = 5;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(100, 177, 8, 107, 168, 65, 65, 39);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PUBKEY_OFFSET = 8;
  public static final int ORACLE_OFFSET = 40;
  public static final int MINT_OFFSET = 72;
  public static final int VAULT_OFFSET = 104;
  public static final int PADDING_1_OFFSET = 136;
  public static final int PADDING_2_OFFSET = 424;
  public static final int DEPOSIT_BALANCE_OFFSET = 432;
  public static final int BORROW_BALANCE_OFFSET = 448;
  public static final int CUMULATIVE_DEPOSIT_INTEREST_OFFSET = 464;
  public static final int CUMULATIVE_BORROW_INTEREST_OFFSET = 480;
  public static final int PADDING_3_OFFSET = 496;
  public static final int LAST_INTEREST_TS_OFFSET = 568;
  public static final int PADDING_4_OFFSET = 576;
  public static final int DECIMALS_OFFSET = 680;
  public static final int MARKET_INDEX_OFFSET = 684;
  public static final int PADDING_5_OFFSET = 686;
  public static final int PADDING_6_OFFSET = 734;
  public static final int POOL_ID_OFFSET = 735;
  public static final int PADDING_7_OFFSET = 736;

  public static Filter createPubkeyFilter(final PublicKey pubkey) {
    return Filter.createMemCompFilter(PUBKEY_OFFSET, pubkey);
  }

  public static Filter createOracleFilter(final PublicKey oracle) {
    return Filter.createMemCompFilter(ORACLE_OFFSET, oracle);
  }

  public static Filter createMintFilter(final PublicKey mint) {
    return Filter.createMemCompFilter(MINT_OFFSET, mint);
  }

  public static Filter createVaultFilter(final PublicKey vault) {
    return Filter.createMemCompFilter(VAULT_OFFSET, vault);
  }

  public static Filter createLastInterestTsFilter(final long lastInterestTs) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lastInterestTs);
    return Filter.createMemCompFilter(LAST_INTEREST_TS_OFFSET, _data);
  }

  public static Filter createDecimalsFilter(final long decimals) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) decimals);
    return Filter.createMemCompFilter(DECIMALS_OFFSET, _data);
  }

  public static Filter createMarketIndexFilter(final int marketIndex) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, marketIndex);
    return Filter.createMemCompFilter(MARKET_INDEX_OFFSET, _data);
  }

  public static Filter createPoolIdFilter(final int poolId) {
    return Filter.createMemCompFilter(POOL_ID_OFFSET, new byte[]{(byte) poolId});
  }

  public static MinimalSpotMarket read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static MinimalSpotMarket read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static MinimalSpotMarket read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], MinimalSpotMarket> FACTORY = MinimalSpotMarket::read;

  public static MinimalSpotMarket read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var pubkey = readPubKey(_data, i);
    i += 32;
    final var oracle = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var padding1 = new long[9][4];
    i += SerDeUtil.readArray(padding1, _data, i);
    final var padding2 = new byte[8];
    i += SerDeUtil.readArray(padding2, _data, i);
    final var depositBalance = new byte[16];
    i += SerDeUtil.readArray(depositBalance, _data, i);
    final var borrowBalance = new byte[16];
    i += SerDeUtil.readArray(borrowBalance, _data, i);
    final var cumulativeDepositInterest = new byte[16];
    i += SerDeUtil.readArray(cumulativeDepositInterest, _data, i);
    final var cumulativeBorrowInterest = new byte[16];
    i += SerDeUtil.readArray(cumulativeBorrowInterest, _data, i);
    final var padding3 = new long[9];
    i += SerDeUtil.readArray(padding3, _data, i);
    final var lastInterestTs = getInt64LE(_data, i);
    i += 8;
    final var padding4 = new long[13];
    i += SerDeUtil.readArray(padding4, _data, i);
    final var decimals = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var marketIndex = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var padding5 = new int[24];
    i += SerDeUtil.readUnsignedShortArray(padding5, _data, i);
    final var padding6 = new byte[1];
    i += SerDeUtil.readArray(padding6, _data, i);
    final var poolId = _data[i] & 0xFF;
    ++i;
    final var padding7 = new long[5];
    SerDeUtil.readArray(padding7, _data, i);
    return new MinimalSpotMarket(_address,
                                 discriminator,
                                 pubkey,
                                 oracle,
                                 mint,
                                 vault,
                                 padding1,
                                 padding2,
                                 depositBalance,
                                 borrowBalance,
                                 cumulativeDepositInterest,
                                 cumulativeBorrowInterest,
                                 padding3,
                                 lastInterestTs,
                                 padding4,
                                 decimals,
                                 marketIndex,
                                 padding5,
                                 padding6,
                                 poolId,
                                 padding7);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    pubkey.write(_data, i);
    i += 32;
    oracle.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(padding1, 9, 4, _data, i);
    i += SerDeUtil.writeArrayChecked(padding2, 8, _data, i);
    i += SerDeUtil.writeArrayChecked(depositBalance, 16, _data, i);
    i += SerDeUtil.writeArrayChecked(borrowBalance, 16, _data, i);
    i += SerDeUtil.writeArrayChecked(cumulativeDepositInterest, 16, _data, i);
    i += SerDeUtil.writeArrayChecked(cumulativeBorrowInterest, 16, _data, i);
    i += SerDeUtil.writeArrayChecked(padding3, 9, _data, i);
    putInt64LE(_data, i, lastInterestTs);
    i += 8;
    i += SerDeUtil.writeArrayChecked(padding4, 13, _data, i);
    putInt32LE(_data, i, (int) decimals);
    i += 4;
    putInt16LE(_data, i, marketIndex);
    i += 2;
    i += SerDeUtil.writeUnsignedShortArrayChecked(padding5, 24, _data, i);
    i += SerDeUtil.writeArrayChecked(padding6, 1, _data, i);
    _data[i] = (byte) poolId;
    ++i;
    i += SerDeUtil.writeArrayChecked(padding7, 5, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
