// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record DenylistedAccount(PublicKey _address, Discriminator discriminator, PublicKey account) implements SerDe {

  public static final int BYTES = 40;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(186, 58, 212, 239, 102, 131, 157, 146);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int ACCOUNT_OFFSET = 8;

  public static Filter createAccountFilter(final PublicKey account) {
    return Filter.createMemCompFilter(ACCOUNT_OFFSET, account);
  }

  public static DenylistedAccount read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static DenylistedAccount read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static DenylistedAccount read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static DenylistedAccount readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static DenylistedAccount readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a DenylistedAccount account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], DenylistedAccount> FACTORY = DenylistedAccount::readChecked;

  public static DenylistedAccount read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var account = readPubKey(_data, i);
    return new DenylistedAccount(_address, discriminator, account);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    account.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
