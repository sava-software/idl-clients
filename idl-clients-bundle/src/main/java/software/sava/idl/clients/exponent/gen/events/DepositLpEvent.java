// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amount: u64
/// @param newLpBalance: u64
public record DepositLpEvent(Discriminator discriminator,
                             PublicKey owner,
                             PublicKey market,
                             PublicKey lpPosition,
                             PublicKey tokenLpSrc,
                             PublicKey tokenLpEscrow,
                             PublicKey mintLp,
                             long amount,
                             long newLpBalance,
                             long timestamp) implements ExponentCoreEvent {

  public static final int BYTES = 224;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(118, 20, 164, 8, 112, 130, 3, 62);

  public static final int OWNER_OFFSET = 8;
  public static final int MARKET_OFFSET = 40;
  public static final int LP_POSITION_OFFSET = 72;
  public static final int TOKEN_LP_SRC_OFFSET = 104;
  public static final int TOKEN_LP_ESCROW_OFFSET = 136;
  public static final int MINT_LP_OFFSET = 168;
  public static final int AMOUNT_OFFSET = 200;
  public static final int NEW_LP_BALANCE_OFFSET = 208;
  public static final int TIMESTAMP_OFFSET = 216;

  public static DepositLpEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var market = readPubKey(_data, i);
    i += 32;
    final var lpPosition = readPubKey(_data, i);
    i += 32;
    final var tokenLpSrc = readPubKey(_data, i);
    i += 32;
    final var tokenLpEscrow = readPubKey(_data, i);
    i += 32;
    final var mintLp = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var newLpBalance = getInt64LE(_data, i);
    i += 8;
    final var timestamp = getInt64LE(_data, i);
    return new DepositLpEvent(discriminator,
                              owner,
                              market,
                              lpPosition,
                              tokenLpSrc,
                              tokenLpEscrow,
                              mintLp,
                              amount,
                              newLpBalance,
                              timestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    market.write(_data, i);
    i += 32;
    lpPosition.write(_data, i);
    i += 32;
    tokenLpSrc.write(_data, i);
    i += 32;
    tokenLpEscrow.write(_data, i);
    i += 32;
    mintLp.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    putInt64LE(_data, i, newLpBalance);
    i += 8;
    putInt64LE(_data, i, timestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
