// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.math.BigInteger;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param burnLimitPerMessage: u64
/// @param messagesSent: u64
/// @param messagesReceived: u64
/// @param bump: u8
/// @param custodyBump: u8
public record LocalToken(PublicKey _address,
                         Discriminator discriminator,
                         PublicKey custody,
                         PublicKey mint,
                         long burnLimitPerMessage,
                         long messagesSent,
                         long messagesReceived,
                         BigInteger amountSent,
                         BigInteger amountReceived,
                         int bump,
                         int custodyBump) implements SerDe {

  public static final int BYTES = 130;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(159, 131, 58, 170, 193, 84, 128, 182);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int CUSTODY_OFFSET = 8;
  public static final int MINT_OFFSET = 40;
  public static final int BURN_LIMIT_PER_MESSAGE_OFFSET = 72;
  public static final int MESSAGES_SENT_OFFSET = 80;
  public static final int MESSAGES_RECEIVED_OFFSET = 88;
  public static final int AMOUNT_SENT_OFFSET = 96;
  public static final int AMOUNT_RECEIVED_OFFSET = 112;
  public static final int BUMP_OFFSET = 128;
  public static final int CUSTODY_BUMP_OFFSET = 129;

  public static Filter createCustodyFilter(final PublicKey custody) {
    return Filter.createMemCompFilter(CUSTODY_OFFSET, custody);
  }

  public static Filter createMintFilter(final PublicKey mint) {
    return Filter.createMemCompFilter(MINT_OFFSET, mint);
  }

  public static Filter createBurnLimitPerMessageFilter(final long burnLimitPerMessage) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, burnLimitPerMessage);
    return Filter.createMemCompFilter(BURN_LIMIT_PER_MESSAGE_OFFSET, _data);
  }

  public static Filter createMessagesSentFilter(final long messagesSent) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, messagesSent);
    return Filter.createMemCompFilter(MESSAGES_SENT_OFFSET, _data);
  }

  public static Filter createMessagesReceivedFilter(final long messagesReceived) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, messagesReceived);
    return Filter.createMemCompFilter(MESSAGES_RECEIVED_OFFSET, _data);
  }

  public static Filter createAmountSentFilter(final BigInteger amountSent) {
    final byte[] _data = new byte[16];
    putInt128LE(_data, 0, amountSent);
    return Filter.createMemCompFilter(AMOUNT_SENT_OFFSET, _data);
  }

  public static Filter createAmountReceivedFilter(final BigInteger amountReceived) {
    final byte[] _data = new byte[16];
    putInt128LE(_data, 0, amountReceived);
    return Filter.createMemCompFilter(AMOUNT_RECEIVED_OFFSET, _data);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Filter createCustodyBumpFilter(final int custodyBump) {
    return Filter.createMemCompFilter(CUSTODY_BUMP_OFFSET, new byte[]{(byte) custodyBump});
  }

  public static LocalToken read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static LocalToken read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static LocalToken read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static LocalToken readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static LocalToken readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a LocalToken account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], LocalToken> FACTORY = LocalToken::readChecked;

  public static LocalToken read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var custody = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var burnLimitPerMessage = getInt64LE(_data, i);
    i += 8;
    final var messagesSent = getInt64LE(_data, i);
    i += 8;
    final var messagesReceived = getInt64LE(_data, i);
    i += 8;
    final var amountSent = getUInt128LE(_data, i);
    i += 16;
    final var amountReceived = getUInt128LE(_data, i);
    i += 16;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var custodyBump = _data[i] & 0xFF;
    return new LocalToken(_address,
                          discriminator,
                          custody,
                          mint,
                          burnLimitPerMessage,
                          messagesSent,
                          messagesReceived,
                          amountSent,
                          amountReceived,
                          bump,
                          custodyBump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    custody.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, burnLimitPerMessage);
    i += 8;
    putInt64LE(_data, i, messagesSent);
    i += 8;
    putInt64LE(_data, i, messagesReceived);
    i += 8;
    putInt128LE(_data, i, amountSent);
    i += 16;
    putInt128LE(_data, i, amountReceived);
    i += 16;
    _data[i] = (byte) bump;
    ++i;
    _data[i] = (byte) custodyBump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
