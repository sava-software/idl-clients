// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import java.math.BigInteger;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param managerRedemptionAmount: u64
/// @param partnerRedemptionAmount: u64
public record ManagerWithdrawalWithSplit(Discriminator discriminator,
                                         BigInteger managerPfeeShares,
                                         BigInteger managerMfeeShares,
                                         BigInteger partnerPfeeShares,
                                         BigInteger partnerMfeeShares,
                                         long managerRedemptionAmount,
                                         long partnerRedemptionAmount,
                                         PublicKey bundleAccountKey,
                                         BigInteger totalSharesAfter,
                                         BigInteger managerPfeeSharesAfter,
                                         BigInteger managerMfeeSharesAfter) implements NtbundleEvent {

  public static final int BYTES = 168;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(147, 151, 147, 137, 189, 58, 26, 122);

  public static final int MANAGER_PFEE_SHARES_OFFSET = 8;
  public static final int MANAGER_MFEE_SHARES_OFFSET = 24;
  public static final int PARTNER_PFEE_SHARES_OFFSET = 40;
  public static final int PARTNER_MFEE_SHARES_OFFSET = 56;
  public static final int MANAGER_REDEMPTION_AMOUNT_OFFSET = 72;
  public static final int PARTNER_REDEMPTION_AMOUNT_OFFSET = 80;
  public static final int BUNDLE_ACCOUNT_KEY_OFFSET = 88;
  public static final int TOTAL_SHARES_AFTER_OFFSET = 120;
  public static final int MANAGER_PFEE_SHARES_AFTER_OFFSET = 136;
  public static final int MANAGER_MFEE_SHARES_AFTER_OFFSET = 152;

  public static ManagerWithdrawalWithSplit read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var managerPfeeShares = getUInt128LE(_data, i);
    i += 16;
    final var managerMfeeShares = getUInt128LE(_data, i);
    i += 16;
    final var partnerPfeeShares = getUInt128LE(_data, i);
    i += 16;
    final var partnerMfeeShares = getUInt128LE(_data, i);
    i += 16;
    final var managerRedemptionAmount = getInt64LE(_data, i);
    i += 8;
    final var partnerRedemptionAmount = getInt64LE(_data, i);
    i += 8;
    final var bundleAccountKey = readPubKey(_data, i);
    i += 32;
    final var totalSharesAfter = getUInt128LE(_data, i);
    i += 16;
    final var managerPfeeSharesAfter = getUInt128LE(_data, i);
    i += 16;
    final var managerMfeeSharesAfter = getUInt128LE(_data, i);
    return new ManagerWithdrawalWithSplit(discriminator,
                                          managerPfeeShares,
                                          managerMfeeShares,
                                          partnerPfeeShares,
                                          partnerMfeeShares,
                                          managerRedemptionAmount,
                                          partnerRedemptionAmount,
                                          bundleAccountKey,
                                          totalSharesAfter,
                                          managerPfeeSharesAfter,
                                          managerMfeeSharesAfter);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt128LE(_data, i, managerPfeeShares);
    i += 16;
    putInt128LE(_data, i, managerMfeeShares);
    i += 16;
    putInt128LE(_data, i, partnerPfeeShares);
    i += 16;
    putInt128LE(_data, i, partnerMfeeShares);
    i += 16;
    putInt64LE(_data, i, managerRedemptionAmount);
    i += 8;
    putInt64LE(_data, i, partnerRedemptionAmount);
    i += 8;
    bundleAccountKey.write(_data, i);
    i += 32;
    putInt128LE(_data, i, totalSharesAfter);
    i += 16;
    putInt128LE(_data, i, managerPfeeSharesAfter);
    i += 16;
    putInt128LE(_data, i, managerMfeeSharesAfter);
    i += 16;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
