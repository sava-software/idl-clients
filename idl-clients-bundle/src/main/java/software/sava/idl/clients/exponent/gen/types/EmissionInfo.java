// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param tokenAccount The token account for the emission where the vault authority is the authority
/// @param finalIndex The final index is used to track the last claimable index after the vault expires
/// @param treasuryTokenAccount The treasury token account for this reward
/// @param feeBps: u16 The fee taken from emission collecting
/// @param treasuryEmission: u64 The lambo fund
public record EmissionInfo(PublicKey tokenAccount,
                           Number initialIndex,
                           Number lastSeenIndex,
                           Number finalIndex,
                           PublicKey treasuryTokenAccount,
                           int feeBps,
                           long treasuryEmission) implements SerDe {

  public static final int BYTES = 170;

  public static final int TOKEN_ACCOUNT_OFFSET = 0;
  public static final int INITIAL_INDEX_OFFSET = 32;
  public static final int LAST_SEEN_INDEX_OFFSET = 64;
  public static final int FINAL_INDEX_OFFSET = 96;
  public static final int TREASURY_TOKEN_ACCOUNT_OFFSET = 128;
  public static final int FEE_BPS_OFFSET = 160;
  public static final int TREASURY_EMISSION_OFFSET = 162;

  public static EmissionInfo read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var tokenAccount = readPubKey(_data, i);
    i += 32;
    final var initialIndex = Number.read(_data, i);
    i += 32;
    final var lastSeenIndex = Number.read(_data, i);
    i += 32;
    final var finalIndex = Number.read(_data, i);
    i += 32;
    final var treasuryTokenAccount = readPubKey(_data, i);
    i += 32;
    final var feeBps = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var treasuryEmission = getInt64LE(_data, i);
    return new EmissionInfo(tokenAccount,
                            initialIndex,
                            lastSeenIndex,
                            finalIndex,
                            treasuryTokenAccount,
                            feeBps,
                            treasuryEmission);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    tokenAccount.write(_data, i);
    i += 32;
    i += initialIndex.write(_data, i);
    i += lastSeenIndex.write(_data, i);
    i += finalIndex.write(_data, i);
    treasuryTokenAccount.write(_data, i);
    i += 32;
    putInt16LE(_data, i, feeBps);
    i += 2;
    putInt64LE(_data, i, treasuryEmission);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
