// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param principalAmount: u64
/// @param remainingPrincipal: u64
/// @param collateralAmount: u64
/// @param remainingCollateral: u64
/// @param apy: u32
/// @param duration: u32
/// @param createdAt: u64
/// @param expiredAt: u64
/// @param updatedAt: u64
/// @param minFillAmount: u64
/// @param fillCounter: u64
/// @param allowPartialFill: u8
/// @param bump: u8
public record Offer(PublicKey _address,
                    Discriminator discriminator,
                    PublicKey creator,
                    OfferSide side,
                    OfferStatus status,
                    byte[] padding,
                    Asset principal,
                    byte[] padding2,
                    Asset collateral,
                    byte[] padding3,
                    AssetFilter filter,
                    byte[] padding4,
                    long principalAmount,
                    long remainingPrincipal,
                    long collateralAmount,
                    long remainingCollateral,
                    byte[] padding5,
                    long apy,
                    long duration,
                    long createdAt,
                    long expiredAt,
                    long updatedAt,
                    long minFillAmount,
                    long fillCounter,
                    int allowPartialFill,
                    int bump,
                    byte[] padding6,
                    PublicKey counteredOffer,
                    byte[] reserved) implements SerDe {

  public static final int PADDING_LEN = 6;
  public static final int PADDING_2_LEN = 7;
  public static final int PADDING_3_LEN = 7;
  public static final int PADDING_4_LEN = 7;
  public static final int PADDING_5_LEN = 8;
  public static final int PADDING_6_LEN = 6;
  public static final int RESERVED_LEN = 232;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(215, 88, 60, 71, 170, 162, 73, 229);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int CREATOR_OFFSET = 8;
  public static final int SIDE_OFFSET = 40;
  public static final int STATUS_OFFSET = 41;
  public static final int PADDING_OFFSET = 42;
  public static final int PRINCIPAL_OFFSET = 48;

  public static Filter createCreatorFilter(final PublicKey creator) {
    return Filter.createMemCompFilter(CREATOR_OFFSET, creator);
  }

  public static Filter createSideFilter(final OfferSide side) {
    return Filter.createMemCompFilter(SIDE_OFFSET, side.write());
  }

  public static Filter createStatusFilter(final OfferStatus status) {
    return Filter.createMemCompFilter(STATUS_OFFSET, status.write());
  }

  public static Filter createPrincipalFilter(final Asset principal) {
    return Filter.createMemCompFilter(PRINCIPAL_OFFSET, principal.write());
  }

  public static Offer read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Offer read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Offer read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Offer> FACTORY = Offer::read;

  public static Offer read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var creator = readPubKey(_data, i);
    i += 32;
    final var side = OfferSide.read(_data, i);
    i += side.l();
    final var status = OfferStatus.read(_data, i);
    i += status.l();
    final var padding = new byte[6];
    i += SerDeUtil.readArray(padding, _data, i);
    final var principal = Asset.read(_data, i);
    i += principal.l();
    final var padding2 = new byte[7];
    i += SerDeUtil.readArray(padding2, _data, i);
    final var collateral = Asset.read(_data, i);
    i += collateral.l();
    final var padding3 = new byte[7];
    i += SerDeUtil.readArray(padding3, _data, i);
    final var filter = AssetFilter.read(_data, i);
    i += filter.l();
    final var padding4 = new byte[7];
    i += SerDeUtil.readArray(padding4, _data, i);
    final var principalAmount = getInt64LE(_data, i);
    i += 8;
    final var remainingPrincipal = getInt64LE(_data, i);
    i += 8;
    final var collateralAmount = getInt64LE(_data, i);
    i += 8;
    final var remainingCollateral = getInt64LE(_data, i);
    i += 8;
    final var padding5 = new byte[8];
    i += SerDeUtil.readArray(padding5, _data, i);
    final var apy = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var duration = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var createdAt = getInt64LE(_data, i);
    i += 8;
    final var expiredAt = getInt64LE(_data, i);
    i += 8;
    final var updatedAt = getInt64LE(_data, i);
    i += 8;
    final var minFillAmount = getInt64LE(_data, i);
    i += 8;
    final var fillCounter = getInt64LE(_data, i);
    i += 8;
    final var allowPartialFill = _data[i] & 0xFF;
    ++i;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var padding6 = new byte[6];
    i += SerDeUtil.readArray(padding6, _data, i);
    final var counteredOffer = readPubKey(_data, i);
    i += 32;
    final var reserved = new byte[232];
    SerDeUtil.readArray(reserved, _data, i);
    return new Offer(_address,
                     discriminator,
                     creator,
                     side,
                     status,
                     padding,
                     principal,
                     padding2,
                     collateral,
                     padding3,
                     filter,
                     padding4,
                     principalAmount,
                     remainingPrincipal,
                     collateralAmount,
                     remainingCollateral,
                     padding5,
                     apy,
                     duration,
                     createdAt,
                     expiredAt,
                     updatedAt,
                     minFillAmount,
                     fillCounter,
                     allowPartialFill,
                     bump,
                     padding6,
                     counteredOffer,
                     reserved);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    creator.write(_data, i);
    i += 32;
    i += side.write(_data, i);
    i += status.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding, 6, _data, i);
    i += principal.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding2, 7, _data, i);
    i += collateral.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding3, 7, _data, i);
    i += filter.write(_data, i);
    i += SerDeUtil.writeArrayChecked(padding4, 7, _data, i);
    putInt64LE(_data, i, principalAmount);
    i += 8;
    putInt64LE(_data, i, remainingPrincipal);
    i += 8;
    putInt64LE(_data, i, collateralAmount);
    i += 8;
    putInt64LE(_data, i, remainingCollateral);
    i += 8;
    i += SerDeUtil.writeArrayChecked(padding5, 8, _data, i);
    putInt32LE(_data, i, (int) apy);
    i += 4;
    putInt32LE(_data, i, (int) duration);
    i += 4;
    putInt64LE(_data, i, createdAt);
    i += 8;
    putInt64LE(_data, i, expiredAt);
    i += 8;
    putInt64LE(_data, i, updatedAt);
    i += 8;
    putInt64LE(_data, i, minFillAmount);
    i += 8;
    putInt64LE(_data, i, fillCounter);
    i += 8;
    _data[i] = (byte) allowPartialFill;
    ++i;
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeArrayChecked(padding6, 6, _data, i);
    counteredOffer.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(reserved, 232, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + side.l()
         + status.l()
         + SerDeUtil.lenArray(padding)
         + principal.l()
         + SerDeUtil.lenArray(padding2)
         + collateral.l()
         + SerDeUtil.lenArray(padding3)
         + filter.l()
         + SerDeUtil.lenArray(padding4)
         + 8
         + 8
         + 8
         + 8
         + SerDeUtil.lenArray(padding5)
         + 4
         + 4
         + 8
         + 8
         + 8
         + 8
         + 8
         + 1
         + 1
         + SerDeUtil.lenArray(padding6)
         + 32
         + SerDeUtil.lenArray(reserved);
  }
}
