// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.math.BigInteger;

import static software.sava.core.encoding.ByteUtil.getInt128LE;

/// Debt leg for `operate_dex` (imperfect).
/// `amounts`   — DEX amounts for smart debt (T3/T4).
/// `new_debt`  — token delta for non-smart debt (T1/T2).
///
/// @param newDebt: Option<i128>
public record OperateDexDebtAmounts(OperateDexAmounts amounts, BigInteger newDebt) implements SerDe {

  public static final int AMOUNTS_OFFSET = 1;

  public static OperateDexDebtAmounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final OperateDexAmounts amounts;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      amounts = null;
      ++i;
    } else {
      ++i;
      amounts = OperateDexAmounts.read(_data, i);
      i += amounts.l();
    }
    final BigInteger newDebt;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      newDebt = null;
    } else {
      ++i;
      newDebt = getInt128LE(_data, i);
    }
    return new OperateDexDebtAmounts(amounts, newDebt);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, amounts, _data, i);
    i += SerDeUtil.write128Optional(1, newDebt, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (amounts == null ? 1 : (1 + amounts.l())) + (newDebt == null ? 1 : (1 + 16));
  }
}
