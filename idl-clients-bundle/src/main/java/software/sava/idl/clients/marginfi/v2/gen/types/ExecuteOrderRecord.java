// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record ExecuteOrderRecord(PublicKey _address,
                                 Discriminator discriminator,
                                 PublicKey order,
                                 PublicKey executor,
                                 ExecuteOrderBalanceRecord[] balanceStates,
                                 int activeBalanceCount,
                                 int inactiveBalanceCount,
                                 byte[] reserved0,
                                 WrappedI80F48 orderStartHealth) implements SerDe {

  public static final int BYTES = 880;
  public static final int BALANCE_STATES_LEN = 14;
  public static final int RESERVED_0_LEN = 6;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(6, 100, 107, 60, 164, 226, 56, 97);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int ORDER_OFFSET = 8;
  public static final int EXECUTOR_OFFSET = 40;
  public static final int BALANCE_STATES_OFFSET = 72;
  public static final int ACTIVE_BALANCE_COUNT_OFFSET = 856;
  public static final int INACTIVE_BALANCE_COUNT_OFFSET = 857;
  public static final int RESERVED_0_OFFSET = 858;
  public static final int ORDER_START_HEALTH_OFFSET = 864;

  public static Filter createOrderFilter(final PublicKey order) {
    return Filter.createMemCompFilter(ORDER_OFFSET, order);
  }

  public static Filter createExecutorFilter(final PublicKey executor) {
    return Filter.createMemCompFilter(EXECUTOR_OFFSET, executor);
  }

  public static Filter createActiveBalanceCountFilter(final int activeBalanceCount) {
    return Filter.createMemCompFilter(ACTIVE_BALANCE_COUNT_OFFSET, new byte[]{(byte) activeBalanceCount});
  }

  public static Filter createInactiveBalanceCountFilter(final int inactiveBalanceCount) {
    return Filter.createMemCompFilter(INACTIVE_BALANCE_COUNT_OFFSET, new byte[]{(byte) inactiveBalanceCount});
  }

  public static Filter createOrderStartHealthFilter(final WrappedI80F48 orderStartHealth) {
    return Filter.createMemCompFilter(ORDER_START_HEALTH_OFFSET, orderStartHealth.write());
  }

  public static ExecuteOrderRecord read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static ExecuteOrderRecord read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static ExecuteOrderRecord read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], ExecuteOrderRecord> FACTORY = ExecuteOrderRecord::read;

  public static ExecuteOrderRecord read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var order = readPubKey(_data, i);
    i += 32;
    final var executor = readPubKey(_data, i);
    i += 32;
    final var balanceStates = new ExecuteOrderBalanceRecord[14];
    i += SerDeUtil.readArray(balanceStates, ExecuteOrderBalanceRecord::read, _data, i);
    final var activeBalanceCount = _data[i] & 0xFF;
    ++i;
    final var inactiveBalanceCount = _data[i] & 0xFF;
    ++i;
    final var reserved0 = new byte[6];
    i += SerDeUtil.readArray(reserved0, _data, i);
    final var orderStartHealth = WrappedI80F48.read(_data, i);
    return new ExecuteOrderRecord(_address,
                                  discriminator,
                                  order,
                                  executor,
                                  balanceStates,
                                  activeBalanceCount,
                                  inactiveBalanceCount,
                                  reserved0,
                                  orderStartHealth);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    order.write(_data, i);
    i += 32;
    executor.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(balanceStates, 14, _data, i);
    _data[i] = (byte) activeBalanceCount;
    ++i;
    _data[i] = (byte) inactiveBalanceCount;
    ++i;
    i += SerDeUtil.writeArrayChecked(reserved0, 6, _data, i);
    i += orderStartHealth.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
