// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.doves.gen;

import software.sava.core.accounts.ProgramDerivedAddress;
import software.sava.core.accounts.PublicKey;

import java.util.List;

import static java.nio.charset.StandardCharsets.US_ASCII;

public final class DovesPDAs {

  public static ProgramDerivedAddress agPriceFeedPDA(final PublicKey program,
                                                     final PublicKey agPriceFeedMintAccount) {
    return PublicKey.findProgramAddress(List.of(
      "ag_feed".getBytes(US_ASCII),
      agPriceFeedMintAccount.toByteArray()
    ), program);
  }

  public static ProgramDerivedAddress feedPDA(final PublicKey program,
                                              final byte[] pair) {
    return PublicKey.findProgramAddress(List.of(
      "feed".getBytes(US_ASCII),
      pair
    ), program);
  }

  public static ProgramDerivedAddress feed1PDA(final PublicKey program,
                                               final PublicKey feedPairAccount) {
    return PublicKey.findProgramAddress(List.of(
      "feed".getBytes(US_ASCII),
      feedPairAccount.toByteArray()
    ), program);
  }

  private DovesPDAs() {
  }
}
