// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.dev.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// Fixed Phoenix Eternal permission delegation account layout.
///
public record PermissionAccount(PublicKey _address,
                                Discriminator discriminator,
                                long discriminant,
                                PublicKey authority,
                                PublicKey user,
                                int bump,
                                byte[] padding1,
                                long permission,
                                long expiresAtTimestamp,
                                long numSignerActionsRemaining,
                                byte[] padding2) implements SerDe {

  public static final int BYTES = 176;
  public static final int PADDING_1_LEN = 7;
  public static final int PADDING_2_LEN = 64;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(180, 172, 109, 215, 129, 165, 97, 38);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int DISCRIMINANT_OFFSET = 8;
  public static final int AUTHORITY_OFFSET = 16;
  public static final int USER_OFFSET = 48;
  public static final int BUMP_OFFSET = 80;
  public static final int PADDING_1_OFFSET = 81;
  public static final int PERMISSION_OFFSET = 88;
  public static final int EXPIRES_AT_TIMESTAMP_OFFSET = 96;
  public static final int NUM_SIGNER_ACTIONS_REMAINING_OFFSET = 104;
  public static final int PADDING_2_OFFSET = 112;

  public static Filter createDiscriminantFilter(final long discriminant) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, discriminant);
    return Filter.createMemCompFilter(DISCRIMINANT_OFFSET, _data);
  }

  public static Filter createAuthorityFilter(final PublicKey authority) {
    return Filter.createMemCompFilter(AUTHORITY_OFFSET, authority);
  }

  public static Filter createUserFilter(final PublicKey user) {
    return Filter.createMemCompFilter(USER_OFFSET, user);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Filter createPermissionFilter(final long permission) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, permission);
    return Filter.createMemCompFilter(PERMISSION_OFFSET, _data);
  }

  public static Filter createExpiresAtTimestampFilter(final long expiresAtTimestamp) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, expiresAtTimestamp);
    return Filter.createMemCompFilter(EXPIRES_AT_TIMESTAMP_OFFSET, _data);
  }

  public static Filter createNumSignerActionsRemainingFilter(final long numSignerActionsRemaining) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, numSignerActionsRemaining);
    return Filter.createMemCompFilter(NUM_SIGNER_ACTIONS_REMAINING_OFFSET, _data);
  }

  public static PermissionAccount read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static PermissionAccount read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static PermissionAccount read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], PermissionAccount> FACTORY = PermissionAccount::read;

  public static PermissionAccount read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var discriminant = getInt64LE(_data, i);
    i += 8;
    final var authority = readPubKey(_data, i);
    i += 32;
    final var user = readPubKey(_data, i);
    i += 32;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var padding1 = new byte[7];
    i += SerDeUtil.readArray(padding1, _data, i);
    final var permission = getInt64LE(_data, i);
    i += 8;
    final var expiresAtTimestamp = getInt64LE(_data, i);
    i += 8;
    final var numSignerActionsRemaining = getInt64LE(_data, i);
    i += 8;
    final var padding2 = new byte[64];
    SerDeUtil.readArray(padding2, _data, i);
    return new PermissionAccount(_address,
                                 discriminator,
                                 discriminant,
                                 authority,
                                 user,
                                 bump,
                                 padding1,
                                 permission,
                                 expiresAtTimestamp,
                                 numSignerActionsRemaining,
                                 padding2);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt64LE(_data, i, discriminant);
    i += 8;
    authority.write(_data, i);
    i += 32;
    user.write(_data, i);
    i += 32;
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeArrayChecked(padding1, 7, _data, i);
    putInt64LE(_data, i, permission);
    i += 8;
    putInt64LE(_data, i, expiresAtTimestamp);
    i += 8;
    putInt64LE(_data, i, numSignerActionsRemaining);
    i += 8;
    i += SerDeUtil.writeArrayChecked(padding2, 64, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
