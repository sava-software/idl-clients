// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.HealthCache;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record HealthPulseEvent(Discriminator discriminator, PublicKey account, HealthCache healthCache) implements MarginfiEvent {

  public static final int BYTES = 344;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(183, 159, 218, 110, 61, 220, 65, 1);

  public static final int ACCOUNT_OFFSET = 8;
  public static final int HEALTH_CACHE_OFFSET = 40;

  public static HealthPulseEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var account = readPubKey(_data, i);
    i += 32;
    final var healthCache = HealthCache.read(_data, i);
    return new HealthPulseEvent(discriminator, account, healthCache);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    account.write(_data, i);
    i += 32;
    i += healthCache.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
