// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.squads.v4.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param multisig The multisig this belongs to.
/// @param creator Member of the Multisig who created the TransactionBuffer.
/// @param bufferIndex: u8 Index to seed address derivation
/// @param vaultIndex: u8 Vault index of the transaction this buffer belongs to.
/// @param finalBufferHash Hash of the final assembled transaction message.
/// @param finalBufferSize: u16 The size of the final assembled transaction message.
/// @param buffer The buffer of the transaction message.
public record TransactionBuffer(PublicKey _address,
                                Discriminator discriminator,
                                PublicKey multisig,
                                PublicKey creator,
                                int bufferIndex,
                                int vaultIndex,
                                byte[] finalBufferHash,
                                int finalBufferSize,
                                byte[] buffer) implements SerDe {

  public static final int FINAL_BUFFER_HASH_LEN = 32;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(90, 36, 35, 219, 93, 225, 110, 96);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int MULTISIG_OFFSET = 8;
  public static final int CREATOR_OFFSET = 40;
  public static final int BUFFER_INDEX_OFFSET = 72;
  public static final int VAULT_INDEX_OFFSET = 73;
  public static final int FINAL_BUFFER_HASH_OFFSET = 74;
  public static final int FINAL_BUFFER_SIZE_OFFSET = 106;
  public static final int BUFFER_OFFSET = 108;

  public static Filter createMultisigFilter(final PublicKey multisig) {
    return Filter.createMemCompFilter(MULTISIG_OFFSET, multisig);
  }

  public static Filter createCreatorFilter(final PublicKey creator) {
    return Filter.createMemCompFilter(CREATOR_OFFSET, creator);
  }

  public static Filter createBufferIndexFilter(final int bufferIndex) {
    return Filter.createMemCompFilter(BUFFER_INDEX_OFFSET, new byte[]{(byte) bufferIndex});
  }

  public static Filter createVaultIndexFilter(final int vaultIndex) {
    return Filter.createMemCompFilter(VAULT_INDEX_OFFSET, new byte[]{(byte) vaultIndex});
  }

  public static Filter createFinalBufferSizeFilter(final int finalBufferSize) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, finalBufferSize);
    return Filter.createMemCompFilter(FINAL_BUFFER_SIZE_OFFSET, _data);
  }

  public static TransactionBuffer read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static TransactionBuffer read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static TransactionBuffer read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static TransactionBuffer readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static TransactionBuffer readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a TransactionBuffer account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], TransactionBuffer> FACTORY = TransactionBuffer::readChecked;

  public static TransactionBuffer read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var multisig = readPubKey(_data, i);
    i += 32;
    final var creator = readPubKey(_data, i);
    i += 32;
    final var bufferIndex = _data[i] & 0xFF;
    ++i;
    final var vaultIndex = _data[i] & 0xFF;
    ++i;
    final var finalBufferHash = new byte[32];
    i += SerDeUtil.readArray(finalBufferHash, _data, i);
    final var finalBufferSize = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var buffer = SerDeUtil.readbyteVector(4, _data, i);
    return new TransactionBuffer(_address,
                                 discriminator,
                                 multisig,
                                 creator,
                                 bufferIndex,
                                 vaultIndex,
                                 finalBufferHash,
                                 finalBufferSize,
                                 buffer);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    multisig.write(_data, i);
    i += 32;
    creator.write(_data, i);
    i += 32;
    _data[i] = (byte) bufferIndex;
    ++i;
    _data[i] = (byte) vaultIndex;
    ++i;
    i += SerDeUtil.writeArrayChecked(finalBufferHash, 32, _data, i);
    putInt16LE(_data, i, finalBufferSize);
    i += 2;
    i += SerDeUtil.writeVector(4, buffer, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 1
         + 1
         + SerDeUtil.lenArray(finalBufferHash)
         + 2
         + SerDeUtil.lenVector(4, buffer);
  }
}
