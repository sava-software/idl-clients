// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.math.BigInteger;

import java.util.Arrays;
import java.util.function.BiFunction;

import static java.nio.charset.StandardCharsets.UTF_8;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param bump: u8
/// @param lpTokenBump: u8
/// @param maxTriggerPriceDiffBps: u64
/// @param maxLpTokenPriceChangeBps: u64
/// @param aumUsdRefreshedAtSlot: u64
public record Pool(PublicKey _address,
                   Discriminator discriminator,
                   String name, byte[] _name,
                   PublicKey[] custodies,
                   BigInteger aumUsd,
                   Limit limit,
                   Fees fees,
                   PoolApr poolApr,
                   long maxRequestExecutionSec,
                   int bump,
                   int lpTokenBump,
                   long inceptionTime,
                   Secp256k1Pubkey parameterUpdateOracle,
                   long aumUsdUpdatedAt,
                   long maxTriggerPriceDiffBps,
                   boolean disableClosePositionRequest,
                   long maxLpTokenPriceChangeBps,
                   long aumUsdRefreshedAtSlot) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(241, 154, 109, 4, 17, 177, 109, 188);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int NAME_OFFSET = 8;

  public static Pool createRecord(final PublicKey _address,
                                  final Discriminator discriminator,
                                  final String name,
                                  final PublicKey[] custodies,
                                  final BigInteger aumUsd,
                                  final Limit limit,
                                  final Fees fees,
                                  final PoolApr poolApr,
                                  final long maxRequestExecutionSec,
                                  final int bump,
                                  final int lpTokenBump,
                                  final long inceptionTime,
                                  final Secp256k1Pubkey parameterUpdateOracle,
                                  final long aumUsdUpdatedAt,
                                  final long maxTriggerPriceDiffBps,
                                  final boolean disableClosePositionRequest,
                                  final long maxLpTokenPriceChangeBps,
                                  final long aumUsdRefreshedAtSlot) {
    return new Pool(_address,
                    discriminator,
                    name, name == null ? null : name.getBytes(UTF_8),
                    custodies,
                    aumUsd,
                    limit,
                    fees,
                    poolApr,
                    maxRequestExecutionSec,
                    bump,
                    lpTokenBump,
                    inceptionTime,
                    parameterUpdateOracle,
                    aumUsdUpdatedAt,
                    maxTriggerPriceDiffBps,
                    disableClosePositionRequest,
                    maxLpTokenPriceChangeBps,
                    aumUsdRefreshedAtSlot);
  }

  public static Pool read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Pool read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Pool read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Pool> FACTORY = Pool::read;

  public static Pool read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final int _nameLength = getInt32LE(_data, i);
    i += 4;
    final byte[] _name = Arrays.copyOfRange(_data, i, i + _nameLength);
    final var name = new String(_name, UTF_8);
    i += _name.length;
    final var custodies = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, custodies);
    final var aumUsd = getUInt128LE(_data, i);
    i += 16;
    final var limit = Limit.read(_data, i);
    i += limit.l();
    final var fees = Fees.read(_data, i);
    i += fees.l();
    final var poolApr = PoolApr.read(_data, i);
    i += poolApr.l();
    final var maxRequestExecutionSec = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var lpTokenBump = _data[i] & 0xFF;
    ++i;
    final var inceptionTime = getInt64LE(_data, i);
    i += 8;
    final var parameterUpdateOracle = Secp256k1Pubkey.read(_data, i);
    i += parameterUpdateOracle.l();
    final var aumUsdUpdatedAt = getInt64LE(_data, i);
    i += 8;
    final var maxTriggerPriceDiffBps = getInt64LE(_data, i);
    i += 8;
    final var disableClosePositionRequest = _data[i] == 1;
    ++i;
    final var maxLpTokenPriceChangeBps = getInt64LE(_data, i);
    i += 8;
    final var aumUsdRefreshedAtSlot = getInt64LE(_data, i);
    return new Pool(_address,
                    discriminator,
                    name, name == null ? null : name.getBytes(UTF_8),
                    custodies,
                    aumUsd,
                    limit,
                    fees,
                    poolApr,
                    maxRequestExecutionSec,
                    bump,
                    lpTokenBump,
                    inceptionTime,
                    parameterUpdateOracle,
                    aumUsdUpdatedAt,
                    maxTriggerPriceDiffBps,
                    disableClosePositionRequest,
                    maxLpTokenPriceChangeBps,
                    aumUsdRefreshedAtSlot);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += SerDeUtil.writeVector(4, _name, _data, i);
    i += SerDeUtil.writeVector(4, custodies, _data, i);
    putInt128LE(_data, i, aumUsd);
    i += 16;
    i += limit.write(_data, i);
    i += fees.write(_data, i);
    i += poolApr.write(_data, i);
    putInt64LE(_data, i, maxRequestExecutionSec);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    _data[i] = (byte) lpTokenBump;
    ++i;
    putInt64LE(_data, i, inceptionTime);
    i += 8;
    i += parameterUpdateOracle.write(_data, i);
    putInt64LE(_data, i, aumUsdUpdatedAt);
    i += 8;
    putInt64LE(_data, i, maxTriggerPriceDiffBps);
    i += 8;
    _data[i] = (byte) (disableClosePositionRequest ? 1 : 0);
    ++i;
    putInt64LE(_data, i, maxLpTokenPriceChangeBps);
    i += 8;
    putInt64LE(_data, i, aumUsdRefreshedAtSlot);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + _name.length
         + SerDeUtil.lenVector(4, custodies)
         + 16
         + limit.l()
         + fees.l()
         + poolApr.l()
         + 8
         + 1
         + 1
         + 8
         + parameterUpdateOracle.l()
         + 8
         + 8
         + 1
         + 8
         + 8;
  }
}
