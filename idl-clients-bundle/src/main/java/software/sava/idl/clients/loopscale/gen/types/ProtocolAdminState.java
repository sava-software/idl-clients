// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.loopscale.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param protocolAdmin Protocol admin has the following permissions:
///                      -- pause/unpause the protocol
///                      -- global protocol additional security authority
/// @param operationsAdmin Operations admin has the following permissions:
///                        -- protocol additional security to make sure market information parameters are valid
/// @param refinanceAdmin Refinance admin has the following permissions:
///                       -- protocol additional security to make sure refinance parameters are valid
/// @param frozen Whether the protocol is frozen. If true, all instructions will be paused.
///               It's expected to pause liquidations and repays as well, this is an emergency switch.
public record ProtocolAdminState(PublicKey _address,
                                 Discriminator discriminator,
                                 PublicKey protocolAdmin,
                                 PublicKey operationsAdmin,
                                 PublicKey refinanceAdmin,
                                 boolean frozen) implements SerDe {

  public static final int BYTES = 105;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(24, 124, 174, 225, 232, 30, 115, 192);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PROTOCOL_ADMIN_OFFSET = 8;
  public static final int OPERATIONS_ADMIN_OFFSET = 40;
  public static final int REFINANCE_ADMIN_OFFSET = 72;
  public static final int FROZEN_OFFSET = 104;

  public static Filter createProtocolAdminFilter(final PublicKey protocolAdmin) {
    return Filter.createMemCompFilter(PROTOCOL_ADMIN_OFFSET, protocolAdmin);
  }

  public static Filter createOperationsAdminFilter(final PublicKey operationsAdmin) {
    return Filter.createMemCompFilter(OPERATIONS_ADMIN_OFFSET, operationsAdmin);
  }

  public static Filter createRefinanceAdminFilter(final PublicKey refinanceAdmin) {
    return Filter.createMemCompFilter(REFINANCE_ADMIN_OFFSET, refinanceAdmin);
  }

  public static Filter createFrozenFilter(final boolean frozen) {
    return Filter.createMemCompFilter(FROZEN_OFFSET, new byte[]{(byte) (frozen ? 1 : 0)});
  }

  public static ProtocolAdminState read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static ProtocolAdminState read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static ProtocolAdminState read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], ProtocolAdminState> FACTORY = ProtocolAdminState::read;

  public static ProtocolAdminState read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var protocolAdmin = readPubKey(_data, i);
    i += 32;
    final var operationsAdmin = readPubKey(_data, i);
    i += 32;
    final var refinanceAdmin = readPubKey(_data, i);
    i += 32;
    final var frozen = _data[i] == 1;
    return new ProtocolAdminState(_address,
                                  discriminator,
                                  protocolAdmin,
                                  operationsAdmin,
                                  refinanceAdmin,
                                  frozen);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    protocolAdmin.write(_data, i);
    i += 32;
    operationsAdmin.write(_data, i);
    i += 32;
    refinanceAdmin.write(_data, i);
    i += 32;
    _data[i] = (byte) (frozen ? 1 : 0);
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
