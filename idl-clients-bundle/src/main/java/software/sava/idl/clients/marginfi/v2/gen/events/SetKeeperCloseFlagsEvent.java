// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.idl.clients.marginfi.v2.gen.types.AccountEventHeader;

import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record SetKeeperCloseFlagsEvent(Discriminator discriminator, AccountEventHeader header, PublicKey[] bankKeys) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(193, 230, 93, 128, 117, 87, 96, 21);

  public static final int HEADER_OFFSET = 8;

  public static SetKeeperCloseFlagsEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = AccountEventHeader.read(_data, i);
    i += header.l();
    final PublicKey[] bankKeys;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      bankKeys = null;
    } else {
      ++i;
      bankKeys = SerDeUtil.readPublicKeyVector(4, _data, i);
    }
    return new SetKeeperCloseFlagsEvent(discriminator, header, bankKeys);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    if (bankKeys == null || bankKeys.length == 0) {
      _data[i++] = 0;
    } else {
      _data[i++] = 1;
      i += SerDeUtil.writeVector(4, bankKeys, _data, i);
    }
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l() + (bankKeys == null || bankKeys.length == 0 ? 1 : (1 + SerDeUtil.lenVector(4, bankKeys)));
  }
}
