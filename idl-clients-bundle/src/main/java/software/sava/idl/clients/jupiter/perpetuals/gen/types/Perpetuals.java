// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param transferAuthorityBump: u8
/// @param perpetualsBump: u8
public record Perpetuals(PublicKey _address,
                         Discriminator discriminator,
                         Permissions permissions,
                         PublicKey[] pools,
                         PublicKey admin,
                         int transferAuthorityBump,
                         int perpetualsBump,
                         long inceptionTime) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(28, 167, 98, 191, 104, 82, 108, 196);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PERMISSIONS_OFFSET = 8;
  public static final int POOLS_OFFSET = 15;

  public static Filter createPermissionsFilter(final Permissions permissions) {
    return Filter.createMemCompFilter(PERMISSIONS_OFFSET, permissions.write());
  }

  public static Perpetuals read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Perpetuals read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Perpetuals read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Perpetuals> FACTORY = Perpetuals::read;

  public static Perpetuals read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var permissions = Permissions.read(_data, i);
    i += permissions.l();
    final var pools = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, pools);
    final var admin = readPubKey(_data, i);
    i += 32;
    final var transferAuthorityBump = _data[i] & 0xFF;
    ++i;
    final var perpetualsBump = _data[i] & 0xFF;
    ++i;
    final var inceptionTime = getInt64LE(_data, i);
    return new Perpetuals(_address,
                          discriminator,
                          permissions,
                          pools,
                          admin,
                          transferAuthorityBump,
                          perpetualsBump,
                          inceptionTime);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += permissions.write(_data, i);
    i += SerDeUtil.writeVector(4, pools, _data, i);
    admin.write(_data, i);
    i += 32;
    _data[i] = (byte) transferAuthorityBump;
    ++i;
    _data[i] = (byte) perpetualsBump;
    ++i;
    putInt64LE(_data, i, inceptionTime);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + permissions.l()
         + SerDeUtil.lenVector(4, pools)
         + 32
         + 1
         + 1
         + 8;
  }
}
