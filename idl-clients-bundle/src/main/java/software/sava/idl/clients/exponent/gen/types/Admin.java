// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param proposedUberAdmin: Option<publicKey>
public record Admin(PublicKey _address,
                    Discriminator discriminator,
                    PublicKey uberAdmin,
                    PublicKey proposedUberAdmin,
                    Principles principles) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(244, 158, 220, 65, 8, 73, 4, 65);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int UBER_ADMIN_OFFSET = 8;
  public static final int PROPOSED_UBER_ADMIN_OFFSET = 41;

  public static Filter createUberAdminFilter(final PublicKey uberAdmin) {
    return Filter.createMemCompFilter(UBER_ADMIN_OFFSET, uberAdmin);
  }

  public static Filter createProposedUberAdminFilter(final PublicKey proposedUberAdmin) {
    final byte[] _data = new byte[33];
    _data[0] = 1;
    proposedUberAdmin.write(_data, 1);
    return Filter.createMemCompFilter(PROPOSED_UBER_ADMIN_OFFSET, _data);
  }

  public static Admin read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Admin read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Admin read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Admin> FACTORY = Admin::read;

  public static Admin read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var uberAdmin = readPubKey(_data, i);
    i += 32;
    final PublicKey proposedUberAdmin;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      proposedUberAdmin = null;
      ++i;
    } else {
      ++i;
      proposedUberAdmin = readPubKey(_data, i);
      i += 32;
    }
    final var principles = Principles.read(_data, i);
    return new Admin(_address, discriminator, uberAdmin, proposedUberAdmin, principles);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    uberAdmin.write(_data, i);
    i += 32;
    i += SerDeUtil.writeOptional(1, proposedUberAdmin, _data, i);
    i += principles.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32 + (proposedUberAdmin == null ? 1 : (1 + 32)) + principles.l();
  }
}
