// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.GroupEventHeader;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record LendingPoolBankCollectFeesEvent(Discriminator discriminator,
                                              GroupEventHeader header,
                                              PublicKey bank,
                                              PublicKey mint,
                                              double groupFeesCollected,
                                              double groupFeesOutstanding,
                                              double insuranceFeesCollected,
                                              double insuranceFeesOutstanding) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(101, 119, 97, 250, 169, 175, 156, 253);

  public static final int HEADER_OFFSET = 8;

  public static LendingPoolBankCollectFeesEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = GroupEventHeader.read(_data, i);
    i += header.l();
    final var bank = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var groupFeesCollected = getFloat64LE(_data, i);
    i += 8;
    final var groupFeesOutstanding = getFloat64LE(_data, i);
    i += 8;
    final var insuranceFeesCollected = getFloat64LE(_data, i);
    i += 8;
    final var insuranceFeesOutstanding = getFloat64LE(_data, i);
    return new LendingPoolBankCollectFeesEvent(discriminator,
                                               header,
                                               bank,
                                               mint,
                                               groupFeesCollected,
                                               groupFeesOutstanding,
                                               insuranceFeesCollected,
                                               insuranceFeesOutstanding);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    bank.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putFloat64LE(_data, i, groupFeesCollected);
    i += 8;
    putFloat64LE(_data, i, groupFeesOutstanding);
    i += 8;
    putFloat64LE(_data, i, insuranceFeesCollected);
    i += 8;
    putFloat64LE(_data, i, insuranceFeesOutstanding);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l()
         + 32
         + 32
         + 8
         + 8
         + 8
         + 8;
  }
}
