// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param outflowUsd Equity-denominated outflow value in USD, rounded to integer.
/// @param currentTimestamp Unix timestamp when the flow was recorded
public record DeleverageWithdrawFlowEvent(Discriminator discriminator,
                                          PublicKey group,
                                          PublicKey bank,
                                          PublicKey mint,
                                          int outflowUsd,
                                          long currentTimestamp) implements MarginfiEvent {

  public static final int BYTES = 116;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(109, 90, 139, 200, 10, 204, 84, 176);

  public static final int GROUP_OFFSET = 8;
  public static final int BANK_OFFSET = 40;
  public static final int MINT_OFFSET = 72;
  public static final int OUTFLOW_USD_OFFSET = 104;
  public static final int CURRENT_TIMESTAMP_OFFSET = 108;

  public static DeleverageWithdrawFlowEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var group = readPubKey(_data, i);
    i += 32;
    final var bank = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var outflowUsd = getInt32LE(_data, i);
    i += 4;
    final var currentTimestamp = getInt64LE(_data, i);
    return new DeleverageWithdrawFlowEvent(discriminator,
                                           group,
                                           bank,
                                           mint,
                                           outflowUsd,
                                           currentTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    group.write(_data, i);
    i += 32;
    bank.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putInt32LE(_data, i, outflowUsd);
    i += 4;
    putInt64LE(_data, i, currentTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
