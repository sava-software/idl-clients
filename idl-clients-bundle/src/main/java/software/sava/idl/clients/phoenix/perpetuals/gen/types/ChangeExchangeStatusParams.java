// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.perpetuals.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

/// Borsh payload patching the exchange-wide status bits; `None` preserves a bit.
///
/// @param active: Option<bool>
/// @param gated: Option<bool>
/// @param maintenance: Option<bool>
public record ChangeExchangeStatusParams(Boolean active,
                                         Boolean gated,
                                         Boolean maintenance) implements SerDe {

  public static final int ACTIVE_OFFSET = 1;

  public static ChangeExchangeStatusParams read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final Boolean active;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      active = null;
      ++i;
    } else {
      ++i;
      active = _data[i] == 1;
      ++i;
    }
    final Boolean gated;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      gated = null;
      ++i;
    } else {
      ++i;
      gated = _data[i] == 1;
      ++i;
    }
    final Boolean maintenance;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maintenance = null;
    } else {
      ++i;
      maintenance = _data[i] == 1;
    }
    return new ChangeExchangeStatusParams(active, gated, maintenance);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, active, _data, i);
    i += SerDeUtil.writeOptional(1, gated, _data, i);
    i += SerDeUtil.writeOptional(1, maintenance, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (active == null ? 1 : (1 + 1)) + (gated == null ? 1 : (1 + 1)) + (maintenance == null ? 1 : (1 + 1));
  }
}
