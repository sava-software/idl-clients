// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;

/// @param remoteDomain: u32
/// @param finalityThresholdExecuted: u32
/// @param authorityBump: u8
public record HandleReceiveMessageParams(long remoteDomain,
                                         PublicKey sender,
                                         long finalityThresholdExecuted,
                                         byte[] messageBody,
                                         int authorityBump) implements SerDe {

  public static final int REMOTE_DOMAIN_OFFSET = 0;
  public static final int SENDER_OFFSET = 4;
  public static final int FINALITY_THRESHOLD_EXECUTED_OFFSET = 36;
  public static final int MESSAGE_BODY_OFFSET = 40;

  public static HandleReceiveMessageParams read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var remoteDomain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var sender = readPubKey(_data, i);
    i += 32;
    final var finalityThresholdExecuted = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var messageBody = SerDeUtil.readbyteVector(4, _data, i);
    i += SerDeUtil.lenVector(4, messageBody);
    final var authorityBump = _data[i] & 0xFF;
    return new HandleReceiveMessageParams(remoteDomain,
                                          sender,
                                          finalityThresholdExecuted,
                                          messageBody,
                                          authorityBump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt32LE(_data, i, (int) remoteDomain);
    i += 4;
    sender.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) finalityThresholdExecuted);
    i += 4;
    i += SerDeUtil.writeVector(4, messageBody, _data, i);
    _data[i] = (byte) authorityBump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return 4
         + 32
         + 4
         + SerDeUtil.lenVector(4, messageBody)
         + 1;
  }
}
