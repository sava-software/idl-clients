// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Cached fee configuration propagated from the global FeeState
///
/// @param globalFeeWallet The wallet that receives program-level fees
/// @param programFeeFixed Fixed fee APR charged to borrowers (program-level)
/// @param programFeeRate Proportional fee rate on interest (program-level)
/// @param lastUpdate Unix timestamp of the last fee state propagation
public record FeeStateCache(PublicKey globalFeeWallet,
                            WrappedI80F48 programFeeFixed,
                            WrappedI80F48 programFeeRate,
                            long lastUpdate) implements SerDe {

  public static final int BYTES = 72;

  public static final int GLOBAL_FEE_WALLET_OFFSET = 0;
  public static final int PROGRAM_FEE_FIXED_OFFSET = 32;
  public static final int PROGRAM_FEE_RATE_OFFSET = 48;
  public static final int LAST_UPDATE_OFFSET = 64;

  public static FeeStateCache read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var globalFeeWallet = readPubKey(_data, i);
    i += 32;
    final var programFeeFixed = WrappedI80F48.read(_data, i);
    i += programFeeFixed.l();
    final var programFeeRate = WrappedI80F48.read(_data, i);
    i += programFeeRate.l();
    final var lastUpdate = getInt64LE(_data, i);
    return new FeeStateCache(globalFeeWallet,
                             programFeeFixed,
                             programFeeRate,
                             lastUpdate);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    globalFeeWallet.write(_data, i);
    i += 32;
    i += programFeeFixed.write(_data, i);
    i += programFeeRate.write(_data, i);
    putInt64LE(_data, i, lastUpdate);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
