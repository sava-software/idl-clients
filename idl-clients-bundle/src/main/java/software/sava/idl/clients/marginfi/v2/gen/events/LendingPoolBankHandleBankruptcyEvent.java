// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.AccountEventHeader;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record LendingPoolBankHandleBankruptcyEvent(Discriminator discriminator,
                                                   AccountEventHeader header,
                                                   PublicKey bank,
                                                   PublicKey mint,
                                                   double badDebt,
                                                   double coveredAmount,
                                                   double socializedAmount) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(166, 77, 41, 140, 36, 94, 10, 57);

  public static final int HEADER_OFFSET = 8;

  public static LendingPoolBankHandleBankruptcyEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = AccountEventHeader.read(_data, i);
    i += header.l();
    final var bank = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var badDebt = getFloat64LE(_data, i);
    i += 8;
    final var coveredAmount = getFloat64LE(_data, i);
    i += 8;
    final var socializedAmount = getFloat64LE(_data, i);
    return new LendingPoolBankHandleBankruptcyEvent(discriminator,
                                                    header,
                                                    bank,
                                                    mint,
                                                    badDebt,
                                                    coveredAmount,
                                                    socializedAmount);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    bank.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putFloat64LE(_data, i, badDebt);
    i += 8;
    putFloat64LE(_data, i, coveredAmount);
    i += 8;
    putFloat64LE(_data, i, socializedAmount);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l()
         + 32
         + 32
         + 8
         + 8
         + 8;
  }
}
