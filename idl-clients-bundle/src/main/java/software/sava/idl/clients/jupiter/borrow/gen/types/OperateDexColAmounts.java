// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.math.BigInteger;

import static software.sava.core.encoding.ByteUtil.getInt128LE;

/// Collateral leg for `operate_dex` (imperfect).
/// `amounts`  — DEX amounts for smart col (T2/T4).
/// `new_col`  — token delta for non-smart col (T1/T3).
///
/// @param newCol: Option<i128>
public record OperateDexColAmounts(OperateDexAmounts amounts, BigInteger newCol) implements SerDe {

  public static final int AMOUNTS_OFFSET = 1;

  public static OperateDexColAmounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final OperateDexAmounts amounts;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      amounts = null;
      ++i;
    } else {
      ++i;
      amounts = OperateDexAmounts.read(_data, i);
      i += amounts.l();
    }
    final BigInteger newCol;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      newCol = null;
    } else {
      ++i;
      newCol = getInt128LE(_data, i);
    }
    return new OperateDexColAmounts(amounts, newCol);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, amounts, _data, i);
    i += SerDeUtil.write128Optional(1, newCol, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (amounts == null ? 1 : (1 + amounts.l())) + (newCol == null ? 1 : (1 + 16));
  }
}
