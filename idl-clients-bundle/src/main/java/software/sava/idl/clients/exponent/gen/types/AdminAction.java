// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.RustEnum;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

public sealed interface AdminAction extends RustEnum permits
  AdminAction.SetVaultStatus,
  AdminAction.ChangeVaultBpsFee,
  AdminAction.ChangeVaultTreasuryTokenAccount,
  AdminAction.ChangeEmissionTreasuryTokenAccount,
  AdminAction.ChangeMinOperationSize,
  AdminAction.ChangeEmissionBpsFee,
  AdminAction.ChangeCpiAccounts,
  AdminAction.ChangeClaimLimits,
  AdminAction.ChangeMaxPySupply,
  AdminAction.ChangeAddressLookupTable,
  AdminAction.RemoveVaultEmission {

  static AdminAction read(final byte[] _data, final int _offset) {
    final int ordinal = _data[_offset] & 0xFF;
    final int i = _offset + 1;
    return switch (ordinal) {
      case 0 -> SetVaultStatus.read(_data, i);
      case 1 -> ChangeVaultBpsFee.read(_data, i);
      case 2 -> ChangeVaultTreasuryTokenAccount.read(_data, i);
      case 3 -> ChangeEmissionTreasuryTokenAccount.read(_data, i);
      case 4 -> ChangeMinOperationSize.read(_data, i);
      case 5 -> ChangeEmissionBpsFee.read(_data, i);
      case 6 -> ChangeCpiAccounts.read(_data, i);
      case 7 -> ChangeClaimLimits.read(_data, i);
      case 8 -> ChangeMaxPySupply.read(_data, i);
      case 9 -> ChangeAddressLookupTable.read(_data, i);
      case 10 -> RemoveVaultEmission.read(_data, i);
      default -> null;
    };
  }

  record SetVaultStatus(int val) implements EnumInt8, AdminAction {

    public static SetVaultStatus read(final byte[] _data, int i) {
      return new SetVaultStatus(_data[i] & 0xFF);
    }

    @Override
    public int ordinal() {
      return 0;
    }
  }

  record ChangeVaultBpsFee(int val) implements EnumInt16, AdminAction {

    public static ChangeVaultBpsFee read(final byte[] _data, int i) {
      return new ChangeVaultBpsFee(Short.toUnsignedInt(getInt16LE(_data, i)));
    }

    @Override
    public int ordinal() {
      return 1;
    }
  }

  record ChangeVaultTreasuryTokenAccount(PublicKey val) implements EnumPublicKey, AdminAction {

    public static ChangeVaultTreasuryTokenAccount read(final byte[] _data, int i) {
      return new ChangeVaultTreasuryTokenAccount(readPubKey(_data, i));
    }

    @Override
    public int ordinal() {
      return 2;
    }
  }

  /// @param emissionIndex: u16
  record ChangeEmissionTreasuryTokenAccount(int emissionIndex, PublicKey newTokenAccount) implements AdminAction {

    public static final int BYTES = 34;

    public static final int EMISSION_INDEX_OFFSET = 0;
    public static final int NEW_TOKEN_ACCOUNT_OFFSET = 2;

    public static ChangeEmissionTreasuryTokenAccount read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var newTokenAccount = readPubKey(_data, i);
      return new ChangeEmissionTreasuryTokenAccount(emissionIndex, newTokenAccount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      putInt16LE(_data, i, emissionIndex);
      i += 2;
      newTokenAccount.write(_data, i);
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 3;
    }
  }

  /// @param newSize: u64
  record ChangeMinOperationSize(boolean isStrip, long newSize) implements AdminAction {

    public static final int BYTES = 9;

    public static final int IS_STRIP_OFFSET = 0;
    public static final int NEW_SIZE_OFFSET = 1;

    public static ChangeMinOperationSize read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var isStrip = _data[i] == 1;
      ++i;
      final var newSize = getInt64LE(_data, i);
      return new ChangeMinOperationSize(isStrip, newSize);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      _data[i] = (byte) (isStrip ? 1 : 0);
      ++i;
      putInt64LE(_data, i, newSize);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 4;
    }
  }

  /// @param emissionIndex: u16
  /// @param newFeeBps: u16
  record ChangeEmissionBpsFee(int emissionIndex, int newFeeBps) implements AdminAction {

    public static final int BYTES = 4;

    public static final int EMISSION_INDEX_OFFSET = 0;
    public static final int NEW_FEE_BPS_OFFSET = 2;

    public static ChangeEmissionBpsFee read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var newFeeBps = Short.toUnsignedInt(getInt16LE(_data, i));
      return new ChangeEmissionBpsFee(emissionIndex, newFeeBps);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      putInt16LE(_data, i, emissionIndex);
      i += 2;
      putInt16LE(_data, i, newFeeBps);
      i += 2;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 5;
    }
  }

  record ChangeCpiAccounts(CpiAccounts val) implements SerDeEnum, AdminAction {

    public static ChangeCpiAccounts read(final byte[] _data, final int _offset) {
      return new ChangeCpiAccounts(CpiAccounts.read(_data, _offset));
    }

    @Override
    public int ordinal() {
      return 6;
    }
  }

  /// @param maxClaimAmountPerWindow: u64
  /// @param claimWindowDurationSeconds: u32
  record ChangeClaimLimits(long maxClaimAmountPerWindow, long claimWindowDurationSeconds) implements AdminAction {

    public static final int BYTES = 12;

    public static final int MAX_CLAIM_AMOUNT_PER_WINDOW_OFFSET = 0;
    public static final int CLAIM_WINDOW_DURATION_SECONDS_OFFSET = 8;

    public static ChangeClaimLimits read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var maxClaimAmountPerWindow = getInt64LE(_data, i);
      i += 8;
      final var claimWindowDurationSeconds = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new ChangeClaimLimits(maxClaimAmountPerWindow, claimWindowDurationSeconds);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      putInt64LE(_data, i, maxClaimAmountPerWindow);
      i += 8;
      putInt32LE(_data, i, (int) claimWindowDurationSeconds);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 7;
    }
  }

  record ChangeMaxPySupply(long val) implements EnumInt64, AdminAction {

    public static ChangeMaxPySupply read(final byte[] _data, int i) {
      return new ChangeMaxPySupply(getInt64LE(_data, i));
    }

    @Override
    public int ordinal() {
      return 8;
    }
  }

  record ChangeAddressLookupTable(PublicKey val) implements EnumPublicKey, AdminAction {

    public static ChangeAddressLookupTable read(final byte[] _data, int i) {
      return new ChangeAddressLookupTable(readPubKey(_data, i));
    }

    @Override
    public int ordinal() {
      return 9;
    }
  }

  record RemoveVaultEmission(int val) implements EnumInt8, AdminAction {

    public static RemoveVaultEmission read(final byte[] _data, int i) {
      return new RemoveVaultEmission(_data[i] & 0xFF);
    }

    @Override
    public int ordinal() {
      return 10;
    }
  }
}
