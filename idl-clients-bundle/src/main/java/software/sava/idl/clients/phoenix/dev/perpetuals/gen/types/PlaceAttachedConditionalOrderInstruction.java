// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.dev.perpetuals.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;

/// @param assetId: u32
public record PlaceAttachedConditionalOrderInstruction(FIFOOrderId orderId,
                                                       long assetId,
                                                       TriggerOrderParams greaterTriggerOrder,
                                                       TriggerOrderParams lessTriggerOrder) implements SerDe {

  public static final int ORDER_ID_OFFSET = 0;
  public static final int ASSET_ID_OFFSET = 16;
  public static final int GREATER_TRIGGER_ORDER_OFFSET = 21;

  public static PlaceAttachedConditionalOrderInstruction read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var orderId = FIFOOrderId.read(_data, i);
    i += orderId.l();
    final var assetId = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final TriggerOrderParams greaterTriggerOrder;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      greaterTriggerOrder = null;
      ++i;
    } else {
      ++i;
      greaterTriggerOrder = TriggerOrderParams.read(_data, i);
      i += greaterTriggerOrder.l();
    }
    final TriggerOrderParams lessTriggerOrder;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      lessTriggerOrder = null;
    } else {
      ++i;
      lessTriggerOrder = TriggerOrderParams.read(_data, i);
    }
    return new PlaceAttachedConditionalOrderInstruction(orderId,
                                                        assetId,
                                                        greaterTriggerOrder,
                                                        lessTriggerOrder);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += orderId.write(_data, i);
    putInt32LE(_data, i, (int) assetId);
    i += 4;
    i += SerDeUtil.writeOptional(1, greaterTriggerOrder, _data, i);
    i += SerDeUtil.writeOptional(1, lessTriggerOrder, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return orderId.l() + 4 + (greaterTriggerOrder == null ? 1 : (1 + greaterTriggerOrder.l())) + (lessTriggerOrder == null ? 1 : (1 + lessTriggerOrder.l()));
  }
}
