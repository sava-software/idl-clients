// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amountIn: u64
/// @param amountOut: u64
/// @param swapUsdAmount: u64
/// @param amountOutAfterFees: u64
/// @param feeBps: u64
public record PoolSwapEvent(Discriminator discriminator,
                            PublicKey receivingCustodyKey,
                            PublicKey dispensingCustodyKey,
                            PublicKey poolKey,
                            long amountIn,
                            long amountOut,
                            long swapUsdAmount,
                            long amountOutAfterFees,
                            long feeBps,
                            PublicKey ownerKey,
                            PublicKey receivingAccountKey) implements PerpetualsEvent {

  public static final int BYTES = 208;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(40, 107, 212, 26, 223, 136, 39, 220);

  public static final int RECEIVING_CUSTODY_KEY_OFFSET = 8;
  public static final int DISPENSING_CUSTODY_KEY_OFFSET = 40;
  public static final int POOL_KEY_OFFSET = 72;
  public static final int AMOUNT_IN_OFFSET = 104;
  public static final int AMOUNT_OUT_OFFSET = 112;
  public static final int SWAP_USD_AMOUNT_OFFSET = 120;
  public static final int AMOUNT_OUT_AFTER_FEES_OFFSET = 128;
  public static final int FEE_BPS_OFFSET = 136;
  public static final int OWNER_KEY_OFFSET = 144;
  public static final int RECEIVING_ACCOUNT_KEY_OFFSET = 176;

  public static PoolSwapEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var receivingCustodyKey = readPubKey(_data, i);
    i += 32;
    final var dispensingCustodyKey = readPubKey(_data, i);
    i += 32;
    final var poolKey = readPubKey(_data, i);
    i += 32;
    final var amountIn = getInt64LE(_data, i);
    i += 8;
    final var amountOut = getInt64LE(_data, i);
    i += 8;
    final var swapUsdAmount = getInt64LE(_data, i);
    i += 8;
    final var amountOutAfterFees = getInt64LE(_data, i);
    i += 8;
    final var feeBps = getInt64LE(_data, i);
    i += 8;
    final var ownerKey = readPubKey(_data, i);
    i += 32;
    final var receivingAccountKey = readPubKey(_data, i);
    return new PoolSwapEvent(discriminator,
                             receivingCustodyKey,
                             dispensingCustodyKey,
                             poolKey,
                             amountIn,
                             amountOut,
                             swapUsdAmount,
                             amountOutAfterFees,
                             feeBps,
                             ownerKey,
                             receivingAccountKey);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    receivingCustodyKey.write(_data, i);
    i += 32;
    dispensingCustodyKey.write(_data, i);
    i += 32;
    poolKey.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amountIn);
    i += 8;
    putInt64LE(_data, i, amountOut);
    i += 8;
    putInt64LE(_data, i, swapUsdAmount);
    i += 8;
    putInt64LE(_data, i, amountOutAfterFees);
    i += 8;
    putInt64LE(_data, i, feeBps);
    i += 8;
    ownerKey.write(_data, i);
    i += 32;
    receivingAccountKey.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
