// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import java.math.BigInteger;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param redemptionAmount: u64
public record ReferrerWithdrawalProcessed(Discriminator discriminator,
                                          PublicKey referrer,
                                          PublicKey bundleAccountKey,
                                          BigInteger sharesRedeemed,
                                          long redemptionAmount,
                                          long timestamp,
                                          BigInteger accruedPfeeSharesAfter,
                                          BigInteger accruedMfeeSharesAfter) implements NtbundleEvent {

  public static final int BYTES = 136;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(118, 14, 179, 109, 134, 95, 211, 194);

  public static final int REFERRER_OFFSET = 8;
  public static final int BUNDLE_ACCOUNT_KEY_OFFSET = 40;
  public static final int SHARES_REDEEMED_OFFSET = 72;
  public static final int REDEMPTION_AMOUNT_OFFSET = 88;
  public static final int TIMESTAMP_OFFSET = 96;
  public static final int ACCRUED_PFEE_SHARES_AFTER_OFFSET = 104;
  public static final int ACCRUED_MFEE_SHARES_AFTER_OFFSET = 120;

  public static ReferrerWithdrawalProcessed read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var referrer = readPubKey(_data, i);
    i += 32;
    final var bundleAccountKey = readPubKey(_data, i);
    i += 32;
    final var sharesRedeemed = getUInt128LE(_data, i);
    i += 16;
    final var redemptionAmount = getInt64LE(_data, i);
    i += 8;
    final var timestamp = getInt64LE(_data, i);
    i += 8;
    final var accruedPfeeSharesAfter = getUInt128LE(_data, i);
    i += 16;
    final var accruedMfeeSharesAfter = getUInt128LE(_data, i);
    return new ReferrerWithdrawalProcessed(discriminator,
                                           referrer,
                                           bundleAccountKey,
                                           sharesRedeemed,
                                           redemptionAmount,
                                           timestamp,
                                           accruedPfeeSharesAfter,
                                           accruedMfeeSharesAfter);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    referrer.write(_data, i);
    i += 32;
    bundleAccountKey.write(_data, i);
    i += 32;
    putInt128LE(_data, i, sharesRedeemed);
    i += 16;
    putInt64LE(_data, i, redemptionAmount);
    i += 8;
    putInt64LE(_data, i, timestamp);
    i += 8;
    putInt128LE(_data, i, accruedPfeeSharesAfter);
    i += 16;
    putInt128LE(_data, i, accruedMfeeSharesAfter);
    i += 16;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
