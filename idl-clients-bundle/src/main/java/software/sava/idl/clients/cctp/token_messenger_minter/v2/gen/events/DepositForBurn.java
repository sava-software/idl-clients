// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amount: u64
/// @param destinationDomain: u32
/// @param maxFee: u64
/// @param minFinalityThreshold: u32
public record DepositForBurn(Discriminator discriminator,
                             PublicKey burnToken,
                             long amount,
                             PublicKey depositor,
                             PublicKey mintRecipient,
                             long destinationDomain,
                             PublicKey destinationTokenMessenger,
                             PublicKey destinationCaller,
                             long maxFee,
                             long minFinalityThreshold,
                             byte[] hookData) implements TokenMessengerMinterV2Event {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(144, 252, 145, 146, 6, 74, 167, 235);

  public static final int BURN_TOKEN_OFFSET = 8;
  public static final int AMOUNT_OFFSET = 40;
  public static final int DEPOSITOR_OFFSET = 48;
  public static final int MINT_RECIPIENT_OFFSET = 80;
  public static final int DESTINATION_DOMAIN_OFFSET = 112;
  public static final int DESTINATION_TOKEN_MESSENGER_OFFSET = 116;
  public static final int DESTINATION_CALLER_OFFSET = 148;
  public static final int MAX_FEE_OFFSET = 180;
  public static final int MIN_FINALITY_THRESHOLD_OFFSET = 188;
  public static final int HOOK_DATA_OFFSET = 192;

  public static DepositForBurn read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var burnToken = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var depositor = readPubKey(_data, i);
    i += 32;
    final var mintRecipient = readPubKey(_data, i);
    i += 32;
    final var destinationDomain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var destinationTokenMessenger = readPubKey(_data, i);
    i += 32;
    final var destinationCaller = readPubKey(_data, i);
    i += 32;
    final var maxFee = getInt64LE(_data, i);
    i += 8;
    final var minFinalityThreshold = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var hookData = SerDeUtil.readbyteVector(4, _data, i);
    return new DepositForBurn(discriminator,
                              burnToken,
                              amount,
                              depositor,
                              mintRecipient,
                              destinationDomain,
                              destinationTokenMessenger,
                              destinationCaller,
                              maxFee,
                              minFinalityThreshold,
                              hookData);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    burnToken.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    depositor.write(_data, i);
    i += 32;
    mintRecipient.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) destinationDomain);
    i += 4;
    destinationTokenMessenger.write(_data, i);
    i += 32;
    destinationCaller.write(_data, i);
    i += 32;
    putInt64LE(_data, i, maxFee);
    i += 8;
    putInt32LE(_data, i, (int) minFinalityThreshold);
    i += 4;
    i += SerDeUtil.writeVector(4, hookData, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 8
         + 32
         + 32
         + 4
         + 32
         + 32
         + 8
         + 4
         + SerDeUtil.lenVector(4, hookData);
  }
}
