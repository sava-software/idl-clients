// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.AcceptOwnershipParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.AddLocalTokenParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.AddRemoteTokenMessengerParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.BurnTokenCustodyParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.DenylistParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.DepositForBurnParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.DepositForBurnWithHookParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.HandleReceiveMessageParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.InitializeParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.LinkTokenPairParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.PauseParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.RemoveLocalTokenParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.RemoveRemoteTokenMessengerParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.SetFeeRecipientParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.SetMaxBurnAmountPerMessageParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.SetMinFeeControllerParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.SetMinFeeParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.SetTokenControllerParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.TransferOwnershipParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.UndenylistParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.UninkTokenPairParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.UnpauseParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.UpdateDenylisterParams;
import software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types.UpdatePauserParams;
import software.sava.idl.clients.core.gen.SerDe;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class TokenMessengerMinterV2Program {

  public static final Discriminator ACCEPT_OWNERSHIP_DISCRIMINATOR = toDiscriminator(172, 23, 43, 13, 238, 213, 85, 150);

  public static List<AccountMeta> acceptOwnershipKeys(final PublicKey pendingOwnerKey,
                                                      final PublicKey tokenMessengerKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(pendingOwnerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction acceptOwnership(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final PublicKey pendingOwnerKey,
                                            final PublicKey tokenMessengerKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final AcceptOwnershipParams params) {
    final var keys = acceptOwnershipKeys(
      pendingOwnerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return acceptOwnership(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction acceptOwnership(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final List<AccountMeta> keys,
                                            final AcceptOwnershipParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = ACCEPT_OWNERSHIP_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record AcceptOwnershipIxData(Discriminator discriminator, AcceptOwnershipParams params) implements SerDe {

    public static AcceptOwnershipIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static AcceptOwnershipIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = AcceptOwnershipParams.read(_data, i);
      return new AcceptOwnershipIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator ADD_LOCAL_TOKEN_DISCRIMINATOR = toDiscriminator(213, 199, 205, 18, 98, 124, 73, 198);

  public static List<AccountMeta> addLocalTokenKeys(final SolanaAccounts solanaAccounts,
                                                    final PublicKey payerKey,
                                                    final PublicKey tokenControllerKey,
                                                    final PublicKey tokenMinterKey,
                                                    final PublicKey localTokenKey,
                                                    final PublicKey custodyTokenAccountKey,
                                                    final PublicKey localTokenMintKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createWrite(custodyTokenAccountKey),
      createRead(localTokenMintKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction addLocalToken(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey payerKey,
                                          final PublicKey tokenControllerKey,
                                          final PublicKey tokenMinterKey,
                                          final PublicKey localTokenKey,
                                          final PublicKey custodyTokenAccountKey,
                                          final PublicKey localTokenMintKey,
                                          final PublicKey tokenProgramKey,
                                          final PublicKey eventAuthorityKey,
                                          final PublicKey programKey,
                                          final AddLocalTokenParams params) {
    final var keys = addLocalTokenKeys(
      solanaAccounts,
      payerKey,
      tokenControllerKey,
      tokenMinterKey,
      localTokenKey,
      custodyTokenAccountKey,
      localTokenMintKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return addLocalToken(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction addLocalToken(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                          final List<AccountMeta> keys,
                                          final AddLocalTokenParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = ADD_LOCAL_TOKEN_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record AddLocalTokenIxData(Discriminator discriminator, AddLocalTokenParams params) implements SerDe {

    public static AddLocalTokenIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static AddLocalTokenIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = AddLocalTokenParams.read(_data, i);
      return new AddLocalTokenIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator ADD_REMOTE_TOKEN_MESSENGER_DISCRIMINATOR = toDiscriminator(12, 149, 172, 165, 111, 202, 24, 33);

  public static List<AccountMeta> addRemoteTokenMessengerKeys(final SolanaAccounts solanaAccounts,
                                                              final PublicKey payerKey,
                                                              final PublicKey ownerKey,
                                                              final PublicKey tokenMessengerKey,
                                                              final PublicKey remoteTokenMessengerKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(ownerKey),
      createRead(tokenMessengerKey),
      createWrite(remoteTokenMessengerKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction addRemoteTokenMessenger(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey payerKey,
                                                    final PublicKey ownerKey,
                                                    final PublicKey tokenMessengerKey,
                                                    final PublicKey remoteTokenMessengerKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey,
                                                    final AddRemoteTokenMessengerParams params) {
    final var keys = addRemoteTokenMessengerKeys(
      solanaAccounts,
      payerKey,
      ownerKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return addRemoteTokenMessenger(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction addRemoteTokenMessenger(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final AddRemoteTokenMessengerParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = ADD_REMOTE_TOKEN_MESSENGER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record AddRemoteTokenMessengerIxData(Discriminator discriminator, AddRemoteTokenMessengerParams params) implements SerDe {

    public static AddRemoteTokenMessengerIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 44;

    public static final int PARAMS_OFFSET = 8;

    public static AddRemoteTokenMessengerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = AddRemoteTokenMessengerParams.read(_data, i);
      return new AddRemoteTokenMessengerIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator BURN_TOKEN_CUSTODY_DISCRIMINATOR = toDiscriminator(233, 136, 180, 175, 112, 41, 62, 71);

  public static List<AccountMeta> burnTokenCustodyKeys(final PublicKey payeeKey,
                                                       final PublicKey tokenControllerKey,
                                                       final PublicKey tokenMinterKey,
                                                       final PublicKey localTokenKey,
                                                       final PublicKey custodyTokenAccountKey,
                                                       final PublicKey custodyTokenMintKey,
                                                       final PublicKey tokenProgramKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey) {
    return List.of(
      createWritableSigner(payeeKey),
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createRead(localTokenKey),
      createWrite(custodyTokenAccountKey),
      createWrite(custodyTokenMintKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction burnTokenCustody(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final PublicKey payeeKey,
                                             final PublicKey tokenControllerKey,
                                             final PublicKey tokenMinterKey,
                                             final PublicKey localTokenKey,
                                             final PublicKey custodyTokenAccountKey,
                                             final PublicKey custodyTokenMintKey,
                                             final PublicKey tokenProgramKey,
                                             final PublicKey eventAuthorityKey,
                                             final PublicKey programKey,
                                             final BurnTokenCustodyParams params) {
    final var keys = burnTokenCustodyKeys(
      payeeKey,
      tokenControllerKey,
      tokenMinterKey,
      localTokenKey,
      custodyTokenAccountKey,
      custodyTokenMintKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return burnTokenCustody(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction burnTokenCustody(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final List<AccountMeta> keys,
                                             final BurnTokenCustodyParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = BURN_TOKEN_CUSTODY_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record BurnTokenCustodyIxData(Discriminator discriminator, BurnTokenCustodyParams params) implements SerDe {

    public static BurnTokenCustodyIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static BurnTokenCustodyIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = BurnTokenCustodyParams.read(_data, i);
      return new BurnTokenCustodyIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DENYLIST_ACCOUNT_DISCRIMINATOR = toDiscriminator(101, 116, 197, 112, 81, 249, 75, 194);

  public static List<AccountMeta> denylistAccountKeys(final SolanaAccounts solanaAccounts,
                                                      final PublicKey payerKey,
                                                      final PublicKey denylisterKey,
                                                      final PublicKey tokenMessengerKey,
                                                      final PublicKey denylistAccountKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(denylisterKey),
      createRead(tokenMessengerKey),
      createWrite(denylistAccountKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction denylistAccount(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final SolanaAccounts solanaAccounts,
                                            final PublicKey payerKey,
                                            final PublicKey denylisterKey,
                                            final PublicKey tokenMessengerKey,
                                            final PublicKey denylistAccountKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final DenylistParams params) {
    final var keys = denylistAccountKeys(
      solanaAccounts,
      payerKey,
      denylisterKey,
      tokenMessengerKey,
      denylistAccountKey,
      eventAuthorityKey,
      programKey
    );
    return denylistAccount(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction denylistAccount(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final List<AccountMeta> keys,
                                            final DenylistParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = DENYLIST_ACCOUNT_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record DenylistAccountIxData(Discriminator discriminator, DenylistParams params) implements SerDe {

    public static DenylistAccountIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static DenylistAccountIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = DenylistParams.read(_data, i);
      return new DenylistAccountIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DEPOSIT_FOR_BURN_DISCRIMINATOR = toDiscriminator(215, 60, 61, 46, 114, 55, 128, 176);

  /// @param denylistAccountKey Account is denylisted if the account exists at the expected PDA.
  public static List<AccountMeta> depositForBurnKeys(final SolanaAccounts solanaAccounts,
                                                     final PublicKey ownerKey,
                                                     final PublicKey eventRentPayerKey,
                                                     final PublicKey senderAuthorityPdaKey,
                                                     final PublicKey burnTokenAccountKey,
                                                     final PublicKey denylistAccountKey,
                                                     final PublicKey messageTransmitterKey,
                                                     final PublicKey tokenMessengerKey,
                                                     final PublicKey remoteTokenMessengerKey,
                                                     final PublicKey tokenMinterKey,
                                                     final PublicKey localTokenKey,
                                                     final PublicKey burnTokenMintKey,
                                                     final PublicKey messageSentEventDataKey,
                                                     final PublicKey messageTransmitterProgramKey,
                                                     final PublicKey tokenMessengerMinterProgramKey,
                                                     final PublicKey tokenProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWritableSigner(eventRentPayerKey),
      createRead(senderAuthorityPdaKey),
      createWrite(burnTokenAccountKey),
      createRead(denylistAccountKey),
      createWrite(messageTransmitterKey),
      createRead(tokenMessengerKey),
      createRead(remoteTokenMessengerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createWrite(burnTokenMintKey),
      createWritableSigner(messageSentEventDataKey),
      createRead(messageTransmitterProgramKey),
      createRead(tokenMessengerMinterProgramKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param denylistAccountKey Account is denylisted if the account exists at the expected PDA.
  public static Instruction depositForBurn(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                           final SolanaAccounts solanaAccounts,
                                           final PublicKey ownerKey,
                                           final PublicKey eventRentPayerKey,
                                           final PublicKey senderAuthorityPdaKey,
                                           final PublicKey burnTokenAccountKey,
                                           final PublicKey denylistAccountKey,
                                           final PublicKey messageTransmitterKey,
                                           final PublicKey tokenMessengerKey,
                                           final PublicKey remoteTokenMessengerKey,
                                           final PublicKey tokenMinterKey,
                                           final PublicKey localTokenKey,
                                           final PublicKey burnTokenMintKey,
                                           final PublicKey messageSentEventDataKey,
                                           final PublicKey messageTransmitterProgramKey,
                                           final PublicKey tokenMessengerMinterProgramKey,
                                           final PublicKey tokenProgramKey,
                                           final PublicKey eventAuthorityKey,
                                           final PublicKey programKey,
                                           final DepositForBurnParams params) {
    final var keys = depositForBurnKeys(
      solanaAccounts,
      ownerKey,
      eventRentPayerKey,
      senderAuthorityPdaKey,
      burnTokenAccountKey,
      denylistAccountKey,
      messageTransmitterKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      tokenMinterKey,
      localTokenKey,
      burnTokenMintKey,
      messageSentEventDataKey,
      messageTransmitterProgramKey,
      tokenMessengerMinterProgramKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return depositForBurn(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction depositForBurn(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                           final List<AccountMeta> keys,
                                           final DepositForBurnParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = DEPOSIT_FOR_BURN_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record DepositForBurnIxData(Discriminator discriminator, DepositForBurnParams params) implements SerDe {

    public static DepositForBurnIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 96;

    public static final int PARAMS_OFFSET = 8;

    public static DepositForBurnIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = DepositForBurnParams.read(_data, i);
      return new DepositForBurnIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DEPOSIT_FOR_BURN_WITH_HOOK_DISCRIMINATOR = toDiscriminator(111, 245, 62, 131, 204, 108, 223, 155);

  /// @param denylistAccountKey Account is denylisted if the account exists at the expected PDA.
  public static List<AccountMeta> depositForBurnWithHookKeys(final SolanaAccounts solanaAccounts,
                                                             final PublicKey ownerKey,
                                                             final PublicKey eventRentPayerKey,
                                                             final PublicKey senderAuthorityPdaKey,
                                                             final PublicKey burnTokenAccountKey,
                                                             final PublicKey denylistAccountKey,
                                                             final PublicKey messageTransmitterKey,
                                                             final PublicKey tokenMessengerKey,
                                                             final PublicKey remoteTokenMessengerKey,
                                                             final PublicKey tokenMinterKey,
                                                             final PublicKey localTokenKey,
                                                             final PublicKey burnTokenMintKey,
                                                             final PublicKey messageSentEventDataKey,
                                                             final PublicKey messageTransmitterProgramKey,
                                                             final PublicKey tokenMessengerMinterProgramKey,
                                                             final PublicKey tokenProgramKey,
                                                             final PublicKey eventAuthorityKey,
                                                             final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWritableSigner(eventRentPayerKey),
      createRead(senderAuthorityPdaKey),
      createWrite(burnTokenAccountKey),
      createRead(denylistAccountKey),
      createWrite(messageTransmitterKey),
      createRead(tokenMessengerKey),
      createRead(remoteTokenMessengerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createWrite(burnTokenMintKey),
      createWritableSigner(messageSentEventDataKey),
      createRead(messageTransmitterProgramKey),
      createRead(tokenMessengerMinterProgramKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param denylistAccountKey Account is denylisted if the account exists at the expected PDA.
  public static Instruction depositForBurnWithHook(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                   final SolanaAccounts solanaAccounts,
                                                   final PublicKey ownerKey,
                                                   final PublicKey eventRentPayerKey,
                                                   final PublicKey senderAuthorityPdaKey,
                                                   final PublicKey burnTokenAccountKey,
                                                   final PublicKey denylistAccountKey,
                                                   final PublicKey messageTransmitterKey,
                                                   final PublicKey tokenMessengerKey,
                                                   final PublicKey remoteTokenMessengerKey,
                                                   final PublicKey tokenMinterKey,
                                                   final PublicKey localTokenKey,
                                                   final PublicKey burnTokenMintKey,
                                                   final PublicKey messageSentEventDataKey,
                                                   final PublicKey messageTransmitterProgramKey,
                                                   final PublicKey tokenMessengerMinterProgramKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey,
                                                   final DepositForBurnWithHookParams params) {
    final var keys = depositForBurnWithHookKeys(
      solanaAccounts,
      ownerKey,
      eventRentPayerKey,
      senderAuthorityPdaKey,
      burnTokenAccountKey,
      denylistAccountKey,
      messageTransmitterKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      tokenMinterKey,
      localTokenKey,
      burnTokenMintKey,
      messageSentEventDataKey,
      messageTransmitterProgramKey,
      tokenMessengerMinterProgramKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return depositForBurnWithHook(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction depositForBurnWithHook(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                   final List<AccountMeta> keys,
                                                   final DepositForBurnWithHookParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = DEPOSIT_FOR_BURN_WITH_HOOK_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record DepositForBurnWithHookIxData(Discriminator discriminator, DepositForBurnWithHookParams params) implements SerDe {

    public static DepositForBurnWithHookIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int PARAMS_OFFSET = 8;

    public static DepositForBurnWithHookIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = DepositForBurnWithHookParams.read(_data, i);
      return new DepositForBurnWithHookIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + params.l();
    }
  }

  public static final Discriminator HANDLE_RECEIVE_FINALIZED_MESSAGE_DISCRIMINATOR = toDiscriminator(186, 252, 239, 70, 86, 180, 110, 95);

  public static List<AccountMeta> handleReceiveFinalizedMessageKeys(final PublicKey authorityPdaKey,
                                                                    final PublicKey tokenMessengerKey,
                                                                    final PublicKey remoteTokenMessengerKey,
                                                                    final PublicKey tokenMinterKey,
                                                                    final PublicKey localTokenKey,
                                                                    final PublicKey tokenPairKey,
                                                                    final PublicKey feeRecipientTokenAccountKey,
                                                                    final PublicKey recipientTokenAccountKey,
                                                                    final PublicKey custodyTokenAccountKey,
                                                                    final PublicKey tokenProgramKey,
                                                                    final PublicKey eventAuthorityKey,
                                                                    final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(authorityPdaKey),
      createRead(tokenMessengerKey),
      createRead(remoteTokenMessengerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createRead(tokenPairKey),
      createWrite(feeRecipientTokenAccountKey),
      createWrite(recipientTokenAccountKey),
      createWrite(custodyTokenAccountKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction handleReceiveFinalizedMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                          final PublicKey authorityPdaKey,
                                                          final PublicKey tokenMessengerKey,
                                                          final PublicKey remoteTokenMessengerKey,
                                                          final PublicKey tokenMinterKey,
                                                          final PublicKey localTokenKey,
                                                          final PublicKey tokenPairKey,
                                                          final PublicKey feeRecipientTokenAccountKey,
                                                          final PublicKey recipientTokenAccountKey,
                                                          final PublicKey custodyTokenAccountKey,
                                                          final PublicKey tokenProgramKey,
                                                          final PublicKey eventAuthorityKey,
                                                          final PublicKey programKey,
                                                          final HandleReceiveMessageParams params) {
    final var keys = handleReceiveFinalizedMessageKeys(
      authorityPdaKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      tokenMinterKey,
      localTokenKey,
      tokenPairKey,
      feeRecipientTokenAccountKey,
      recipientTokenAccountKey,
      custodyTokenAccountKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return handleReceiveFinalizedMessage(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction handleReceiveFinalizedMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                          final List<AccountMeta> keys,
                                                          final HandleReceiveMessageParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = HANDLE_RECEIVE_FINALIZED_MESSAGE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record HandleReceiveFinalizedMessageIxData(Discriminator discriminator, HandleReceiveMessageParams params) implements SerDe {

    public static HandleReceiveFinalizedMessageIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int PARAMS_OFFSET = 8;

    public static HandleReceiveFinalizedMessageIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = HandleReceiveMessageParams.read(_data, i);
      return new HandleReceiveFinalizedMessageIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + params.l();
    }
  }

  public static final Discriminator HANDLE_RECEIVE_UNFINALIZED_MESSAGE_DISCRIMINATOR = toDiscriminator(200, 169, 175, 20, 200, 58, 182, 61);

  public static List<AccountMeta> handleReceiveUnfinalizedMessageKeys(final PublicKey authorityPdaKey,
                                                                      final PublicKey tokenMessengerKey,
                                                                      final PublicKey remoteTokenMessengerKey,
                                                                      final PublicKey tokenMinterKey,
                                                                      final PublicKey localTokenKey,
                                                                      final PublicKey tokenPairKey,
                                                                      final PublicKey feeRecipientTokenAccountKey,
                                                                      final PublicKey recipientTokenAccountKey,
                                                                      final PublicKey custodyTokenAccountKey,
                                                                      final PublicKey tokenProgramKey,
                                                                      final PublicKey eventAuthorityKey,
                                                                      final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(authorityPdaKey),
      createRead(tokenMessengerKey),
      createRead(remoteTokenMessengerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createRead(tokenPairKey),
      createWrite(feeRecipientTokenAccountKey),
      createWrite(recipientTokenAccountKey),
      createWrite(custodyTokenAccountKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction handleReceiveUnfinalizedMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                            final PublicKey authorityPdaKey,
                                                            final PublicKey tokenMessengerKey,
                                                            final PublicKey remoteTokenMessengerKey,
                                                            final PublicKey tokenMinterKey,
                                                            final PublicKey localTokenKey,
                                                            final PublicKey tokenPairKey,
                                                            final PublicKey feeRecipientTokenAccountKey,
                                                            final PublicKey recipientTokenAccountKey,
                                                            final PublicKey custodyTokenAccountKey,
                                                            final PublicKey tokenProgramKey,
                                                            final PublicKey eventAuthorityKey,
                                                            final PublicKey programKey,
                                                            final HandleReceiveMessageParams params) {
    final var keys = handleReceiveUnfinalizedMessageKeys(
      authorityPdaKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      tokenMinterKey,
      localTokenKey,
      tokenPairKey,
      feeRecipientTokenAccountKey,
      recipientTokenAccountKey,
      custodyTokenAccountKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return handleReceiveUnfinalizedMessage(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction handleReceiveUnfinalizedMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                            final List<AccountMeta> keys,
                                                            final HandleReceiveMessageParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = HANDLE_RECEIVE_UNFINALIZED_MESSAGE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record HandleReceiveUnfinalizedMessageIxData(Discriminator discriminator, HandleReceiveMessageParams params) implements SerDe {

    public static HandleReceiveUnfinalizedMessageIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int PARAMS_OFFSET = 8;

    public static HandleReceiveUnfinalizedMessageIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = HandleReceiveMessageParams.read(_data, i);
      return new HandleReceiveUnfinalizedMessageIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + params.l();
    }
  }

  public static final Discriminator INITIALIZE_DISCRIMINATOR = toDiscriminator(175, 175, 109, 31, 13, 152, 155, 237);

  public static List<AccountMeta> initializeKeys(final SolanaAccounts solanaAccounts,
                                                 final PublicKey payerKey,
                                                 final PublicKey upgradeAuthorityKey,
                                                 final PublicKey authorityPdaKey,
                                                 final PublicKey tokenMessengerKey,
                                                 final PublicKey tokenMinterKey,
                                                 final PublicKey tokenMessengerMinterProgramDataKey,
                                                 final PublicKey tokenMessengerMinterProgramKey,
                                                 final PublicKey eventAuthorityKey,
                                                 final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(upgradeAuthorityKey),
      createRead(authorityPdaKey),
      createWrite(tokenMessengerKey),
      createWrite(tokenMinterKey),
      createRead(tokenMessengerMinterProgramDataKey),
      createRead(tokenMessengerMinterProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction initialize(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey payerKey,
                                       final PublicKey upgradeAuthorityKey,
                                       final PublicKey authorityPdaKey,
                                       final PublicKey tokenMessengerKey,
                                       final PublicKey tokenMinterKey,
                                       final PublicKey tokenMessengerMinterProgramDataKey,
                                       final PublicKey tokenMessengerMinterProgramKey,
                                       final PublicKey eventAuthorityKey,
                                       final PublicKey programKey,
                                       final InitializeParams params) {
    final var keys = initializeKeys(
      solanaAccounts,
      payerKey,
      upgradeAuthorityKey,
      authorityPdaKey,
      tokenMessengerKey,
      tokenMinterKey,
      tokenMessengerMinterProgramDataKey,
      tokenMessengerMinterProgramKey,
      eventAuthorityKey,
      programKey
    );
    return initialize(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction initialize(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                       final List<AccountMeta> keys,
                                       final InitializeParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = INITIALIZE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record InitializeIxData(Discriminator discriminator, InitializeParams params) implements SerDe {

    public static InitializeIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 144;

    public static final int PARAMS_OFFSET = 8;

    public static InitializeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = InitializeParams.read(_data, i);
      return new InitializeIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator LINK_TOKEN_PAIR_DISCRIMINATOR = toDiscriminator(68, 162, 24, 104, 125, 46, 130, 12);

  public static List<AccountMeta> linkTokenPairKeys(final SolanaAccounts solanaAccounts,
                                                    final PublicKey payerKey,
                                                    final PublicKey tokenControllerKey,
                                                    final PublicKey tokenMinterKey,
                                                    final PublicKey tokenPairKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createWrite(tokenPairKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction linkTokenPair(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey payerKey,
                                          final PublicKey tokenControllerKey,
                                          final PublicKey tokenMinterKey,
                                          final PublicKey tokenPairKey,
                                          final PublicKey eventAuthorityKey,
                                          final PublicKey programKey,
                                          final LinkTokenPairParams params) {
    final var keys = linkTokenPairKeys(
      solanaAccounts,
      payerKey,
      tokenControllerKey,
      tokenMinterKey,
      tokenPairKey,
      eventAuthorityKey,
      programKey
    );
    return linkTokenPair(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction linkTokenPair(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                          final List<AccountMeta> keys,
                                          final LinkTokenPairParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = LINK_TOKEN_PAIR_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record LinkTokenPairIxData(Discriminator discriminator, LinkTokenPairParams params) implements SerDe {

    public static LinkTokenPairIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 76;

    public static final int PARAMS_OFFSET = 8;

    public static LinkTokenPairIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = LinkTokenPairParams.read(_data, i);
      return new LinkTokenPairIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator PAUSE_DISCRIMINATOR = toDiscriminator(211, 22, 221, 251, 74, 121, 193, 47);

  public static List<AccountMeta> pauseKeys(final PublicKey pauserKey,
                                            final PublicKey tokenMinterKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(pauserKey),
      createWrite(tokenMinterKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction pause(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                  final PublicKey pauserKey,
                                  final PublicKey tokenMinterKey,
                                  final PublicKey eventAuthorityKey,
                                  final PublicKey programKey,
                                  final PauseParams params) {
    final var keys = pauseKeys(
      pauserKey,
      tokenMinterKey,
      eventAuthorityKey,
      programKey
    );
    return pause(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction pause(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                  final List<AccountMeta> keys,
                                  final PauseParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = PAUSE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record PauseIxData(Discriminator discriminator, PauseParams params) implements SerDe {

    public static PauseIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static PauseIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = PauseParams.read(_data, i);
      return new PauseIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator REMOVE_LOCAL_TOKEN_DISCRIMINATOR = toDiscriminator(27, 43, 66, 170, 188, 44, 109, 97);

  public static List<AccountMeta> removeLocalTokenKeys(final PublicKey payeeKey,
                                                       final PublicKey tokenControllerKey,
                                                       final PublicKey tokenMinterKey,
                                                       final PublicKey localTokenKey,
                                                       final PublicKey custodyTokenAccountKey,
                                                       final PublicKey custodyTokenMintKey,
                                                       final PublicKey tokenProgramKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey) {
    return List.of(
      createWritableSigner(payeeKey),
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createWrite(custodyTokenAccountKey),
      createWrite(custodyTokenMintKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction removeLocalToken(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final PublicKey payeeKey,
                                             final PublicKey tokenControllerKey,
                                             final PublicKey tokenMinterKey,
                                             final PublicKey localTokenKey,
                                             final PublicKey custodyTokenAccountKey,
                                             final PublicKey custodyTokenMintKey,
                                             final PublicKey tokenProgramKey,
                                             final PublicKey eventAuthorityKey,
                                             final PublicKey programKey,
                                             final RemoveLocalTokenParams params) {
    final var keys = removeLocalTokenKeys(
      payeeKey,
      tokenControllerKey,
      tokenMinterKey,
      localTokenKey,
      custodyTokenAccountKey,
      custodyTokenMintKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return removeLocalToken(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction removeLocalToken(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final List<AccountMeta> keys,
                                             final RemoveLocalTokenParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = REMOVE_LOCAL_TOKEN_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record RemoveLocalTokenIxData(Discriminator discriminator, RemoveLocalTokenParams params) implements SerDe {

    public static RemoveLocalTokenIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static RemoveLocalTokenIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = RemoveLocalTokenParams.read(_data, i);
      return new RemoveLocalTokenIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator REMOVE_REMOTE_TOKEN_MESSENGER_DISCRIMINATOR = toDiscriminator(65, 114, 66, 85, 169, 98, 177, 146);

  public static List<AccountMeta> removeRemoteTokenMessengerKeys(final PublicKey payeeKey,
                                                                 final PublicKey ownerKey,
                                                                 final PublicKey tokenMessengerKey,
                                                                 final PublicKey remoteTokenMessengerKey,
                                                                 final PublicKey eventAuthorityKey,
                                                                 final PublicKey programKey) {
    return List.of(
      createWritableSigner(payeeKey),
      createReadOnlySigner(ownerKey),
      createRead(tokenMessengerKey),
      createWrite(remoteTokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction removeRemoteTokenMessenger(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                       final PublicKey payeeKey,
                                                       final PublicKey ownerKey,
                                                       final PublicKey tokenMessengerKey,
                                                       final PublicKey remoteTokenMessengerKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey,
                                                       final RemoveRemoteTokenMessengerParams params) {
    final var keys = removeRemoteTokenMessengerKeys(
      payeeKey,
      ownerKey,
      tokenMessengerKey,
      remoteTokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return removeRemoteTokenMessenger(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction removeRemoteTokenMessenger(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                       final List<AccountMeta> keys,
                                                       final RemoveRemoteTokenMessengerParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = REMOVE_REMOTE_TOKEN_MESSENGER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record RemoveRemoteTokenMessengerIxData(Discriminator discriminator, RemoveRemoteTokenMessengerParams params) implements SerDe {

    public static RemoveRemoteTokenMessengerIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static RemoveRemoteTokenMessengerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = RemoveRemoteTokenMessengerParams.read(_data, i);
      return new RemoveRemoteTokenMessengerIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_FEE_RECIPIENT_DISCRIMINATOR = toDiscriminator(227, 18, 215, 42, 237, 246, 151, 66);

  public static List<AccountMeta> setFeeRecipientKeys(final PublicKey ownerKey,
                                                      final PublicKey tokenMessengerKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction setFeeRecipient(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final PublicKey ownerKey,
                                            final PublicKey tokenMessengerKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final SetFeeRecipientParams params) {
    final var keys = setFeeRecipientKeys(
      ownerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return setFeeRecipient(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction setFeeRecipient(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final List<AccountMeta> keys,
                                            final SetFeeRecipientParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = SET_FEE_RECIPIENT_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record SetFeeRecipientIxData(Discriminator discriminator, SetFeeRecipientParams params) implements SerDe {

    public static SetFeeRecipientIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static SetFeeRecipientIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = SetFeeRecipientParams.read(_data, i);
      return new SetFeeRecipientIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_MAX_BURN_AMOUNT_PER_MESSAGE_DISCRIMINATOR = toDiscriminator(30, 128, 145, 240, 70, 237, 109, 207);

  public static List<AccountMeta> setMaxBurnAmountPerMessageKeys(final PublicKey tokenControllerKey,
                                                                 final PublicKey tokenMinterKey,
                                                                 final PublicKey localTokenKey,
                                                                 final PublicKey eventAuthorityKey,
                                                                 final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createWrite(localTokenKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction setMaxBurnAmountPerMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                       final PublicKey tokenControllerKey,
                                                       final PublicKey tokenMinterKey,
                                                       final PublicKey localTokenKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey,
                                                       final SetMaxBurnAmountPerMessageParams params) {
    final var keys = setMaxBurnAmountPerMessageKeys(
      tokenControllerKey,
      tokenMinterKey,
      localTokenKey,
      eventAuthorityKey,
      programKey
    );
    return setMaxBurnAmountPerMessage(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction setMaxBurnAmountPerMessage(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                       final List<AccountMeta> keys,
                                                       final SetMaxBurnAmountPerMessageParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = SET_MAX_BURN_AMOUNT_PER_MESSAGE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record SetMaxBurnAmountPerMessageIxData(Discriminator discriminator, SetMaxBurnAmountPerMessageParams params) implements SerDe {

    public static SetMaxBurnAmountPerMessageIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static SetMaxBurnAmountPerMessageIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = SetMaxBurnAmountPerMessageParams.read(_data, i);
      return new SetMaxBurnAmountPerMessageIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_MIN_FEE_DISCRIMINATOR = toDiscriminator(114, 198, 35, 3, 41, 196, 194, 246);

  public static List<AccountMeta> setMinFeeKeys(final PublicKey minFeeControllerKey,
                                                final PublicKey tokenMessengerKey,
                                                final PublicKey eventAuthorityKey,
                                                final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(minFeeControllerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction setMinFee(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                      final PublicKey minFeeControllerKey,
                                      final PublicKey tokenMessengerKey,
                                      final PublicKey eventAuthorityKey,
                                      final PublicKey programKey,
                                      final SetMinFeeParams params) {
    final var keys = setMinFeeKeys(
      minFeeControllerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return setMinFee(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction setMinFee(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                      final List<AccountMeta> keys,
                                      final SetMinFeeParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = SET_MIN_FEE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record SetMinFeeIxData(Discriminator discriminator, SetMinFeeParams params) implements SerDe {

    public static SetMinFeeIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 12;

    public static final int PARAMS_OFFSET = 8;

    public static SetMinFeeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = SetMinFeeParams.read(_data, i);
      return new SetMinFeeIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_MIN_FEE_CONTROLLER_DISCRIMINATOR = toDiscriminator(195, 142, 74, 84, 234, 94, 180, 113);

  public static List<AccountMeta> setMinFeeControllerKeys(final PublicKey ownerKey,
                                                          final PublicKey tokenMessengerKey,
                                                          final PublicKey eventAuthorityKey,
                                                          final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction setMinFeeController(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                final PublicKey ownerKey,
                                                final PublicKey tokenMessengerKey,
                                                final PublicKey eventAuthorityKey,
                                                final PublicKey programKey,
                                                final SetMinFeeControllerParams params) {
    final var keys = setMinFeeControllerKeys(
      ownerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return setMinFeeController(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction setMinFeeController(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                                final List<AccountMeta> keys,
                                                final SetMinFeeControllerParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = SET_MIN_FEE_CONTROLLER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record SetMinFeeControllerIxData(Discriminator discriminator, SetMinFeeControllerParams params) implements SerDe {

    public static SetMinFeeControllerIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static SetMinFeeControllerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = SetMinFeeControllerParams.read(_data, i);
      return new SetMinFeeControllerIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_TOKEN_CONTROLLER_DISCRIMINATOR = toDiscriminator(88, 6, 98, 10, 79, 59, 15, 24);

  public static List<AccountMeta> setTokenControllerKeys(final PublicKey ownerKey,
                                                         final PublicKey tokenMessengerKey,
                                                         final PublicKey tokenMinterKey,
                                                         final PublicKey eventAuthorityKey,
                                                         final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createRead(tokenMessengerKey),
      createWrite(tokenMinterKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction setTokenController(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                               final PublicKey ownerKey,
                                               final PublicKey tokenMessengerKey,
                                               final PublicKey tokenMinterKey,
                                               final PublicKey eventAuthorityKey,
                                               final PublicKey programKey,
                                               final SetTokenControllerParams params) {
    final var keys = setTokenControllerKeys(
      ownerKey,
      tokenMessengerKey,
      tokenMinterKey,
      eventAuthorityKey,
      programKey
    );
    return setTokenController(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction setTokenController(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                               final List<AccountMeta> keys,
                                               final SetTokenControllerParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = SET_TOKEN_CONTROLLER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record SetTokenControllerIxData(Discriminator discriminator, SetTokenControllerParams params) implements SerDe {

    public static SetTokenControllerIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static SetTokenControllerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = SetTokenControllerParams.read(_data, i);
      return new SetTokenControllerIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator TRANSFER_OWNERSHIP_DISCRIMINATOR = toDiscriminator(65, 177, 215, 73, 53, 45, 99, 47);

  public static List<AccountMeta> transferOwnershipKeys(final PublicKey ownerKey,
                                                        final PublicKey tokenMessengerKey,
                                                        final PublicKey eventAuthorityKey,
                                                        final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction transferOwnership(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                              final PublicKey ownerKey,
                                              final PublicKey tokenMessengerKey,
                                              final PublicKey eventAuthorityKey,
                                              final PublicKey programKey,
                                              final TransferOwnershipParams params) {
    final var keys = transferOwnershipKeys(
      ownerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return transferOwnership(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction transferOwnership(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                              final List<AccountMeta> keys,
                                              final TransferOwnershipParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = TRANSFER_OWNERSHIP_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record TransferOwnershipIxData(Discriminator discriminator, TransferOwnershipParams params) implements SerDe {

    public static TransferOwnershipIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static TransferOwnershipIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = TransferOwnershipParams.read(_data, i);
      return new TransferOwnershipIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UNDENYLIST_ACCOUNT_DISCRIMINATOR = toDiscriminator(57, 36, 43, 168, 62, 172, 33, 39);

  public static List<AccountMeta> undenylistAccountKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey payerKey,
                                                        final PublicKey denylisterKey,
                                                        final PublicKey tokenMessengerKey,
                                                        final PublicKey denylistAccountKey,
                                                        final PublicKey eventAuthorityKey,
                                                        final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(denylisterKey),
      createRead(tokenMessengerKey),
      createWrite(denylistAccountKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction undenylistAccount(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey payerKey,
                                              final PublicKey denylisterKey,
                                              final PublicKey tokenMessengerKey,
                                              final PublicKey denylistAccountKey,
                                              final PublicKey eventAuthorityKey,
                                              final PublicKey programKey,
                                              final UndenylistParams params) {
    final var keys = undenylistAccountKeys(
      solanaAccounts,
      payerKey,
      denylisterKey,
      tokenMessengerKey,
      denylistAccountKey,
      eventAuthorityKey,
      programKey
    );
    return undenylistAccount(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction undenylistAccount(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                              final List<AccountMeta> keys,
                                              final UndenylistParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = UNDENYLIST_ACCOUNT_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record UndenylistAccountIxData(Discriminator discriminator, UndenylistParams params) implements SerDe {

    public static UndenylistAccountIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static UndenylistAccountIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = UndenylistParams.read(_data, i);
      return new UndenylistAccountIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UNLINK_TOKEN_PAIR_DISCRIMINATOR = toDiscriminator(52, 198, 100, 114, 104, 174, 85, 58);

  public static List<AccountMeta> unlinkTokenPairKeys(final PublicKey payeeKey,
                                                      final PublicKey tokenControllerKey,
                                                      final PublicKey tokenMinterKey,
                                                      final PublicKey tokenPairKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey) {
    return List.of(
      createWritableSigner(payeeKey),
      createReadOnlySigner(tokenControllerKey),
      createRead(tokenMinterKey),
      createWrite(tokenPairKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction unlinkTokenPair(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final PublicKey payeeKey,
                                            final PublicKey tokenControllerKey,
                                            final PublicKey tokenMinterKey,
                                            final PublicKey tokenPairKey,
                                            final PublicKey eventAuthorityKey,
                                            final PublicKey programKey,
                                            final UninkTokenPairParams params) {
    final var keys = unlinkTokenPairKeys(
      payeeKey,
      tokenControllerKey,
      tokenMinterKey,
      tokenPairKey,
      eventAuthorityKey,
      programKey
    );
    return unlinkTokenPair(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction unlinkTokenPair(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                            final List<AccountMeta> keys,
                                            final UninkTokenPairParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = UNLINK_TOKEN_PAIR_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record UnlinkTokenPairIxData(Discriminator discriminator, UninkTokenPairParams params) implements SerDe {

    public static UnlinkTokenPairIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static UnlinkTokenPairIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = UninkTokenPairParams.read(_data, i);
      return new UnlinkTokenPairIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UNPAUSE_DISCRIMINATOR = toDiscriminator(169, 144, 4, 38, 10, 141, 188, 255);

  public static List<AccountMeta> unpauseKeys(final PublicKey pauserKey,
                                              final PublicKey tokenMinterKey,
                                              final PublicKey eventAuthorityKey,
                                              final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(pauserKey),
      createWrite(tokenMinterKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction unpause(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                    final PublicKey pauserKey,
                                    final PublicKey tokenMinterKey,
                                    final PublicKey eventAuthorityKey,
                                    final PublicKey programKey,
                                    final UnpauseParams params) {
    final var keys = unpauseKeys(
      pauserKey,
      tokenMinterKey,
      eventAuthorityKey,
      programKey
    );
    return unpause(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction unpause(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                    final List<AccountMeta> keys,
                                    final UnpauseParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = UNPAUSE_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record UnpauseIxData(Discriminator discriminator, UnpauseParams params) implements SerDe {

    public static UnpauseIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 8;

    public static final int PARAMS_OFFSET = 8;

    public static UnpauseIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = UnpauseParams.read(_data, i);
      return new UnpauseIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UPDATE_DENYLISTER_DISCRIMINATOR = toDiscriminator(193, 66, 198, 201, 84, 57, 14, 222);

  public static List<AccountMeta> updateDenylisterKeys(final PublicKey ownerKey,
                                                       final PublicKey tokenMessengerKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createWrite(tokenMessengerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction updateDenylister(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final PublicKey ownerKey,
                                             final PublicKey tokenMessengerKey,
                                             final PublicKey eventAuthorityKey,
                                             final PublicKey programKey,
                                             final UpdateDenylisterParams params) {
    final var keys = updateDenylisterKeys(
      ownerKey,
      tokenMessengerKey,
      eventAuthorityKey,
      programKey
    );
    return updateDenylister(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction updateDenylister(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                             final List<AccountMeta> keys,
                                             final UpdateDenylisterParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = UPDATE_DENYLISTER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record UpdateDenylisterIxData(Discriminator discriminator, UpdateDenylisterParams params) implements SerDe {

    public static UpdateDenylisterIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static UpdateDenylisterIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = UpdateDenylisterParams.read(_data, i);
      return new UpdateDenylisterIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UPDATE_PAUSER_DISCRIMINATOR = toDiscriminator(140, 171, 211, 132, 57, 201, 16, 254);

  public static List<AccountMeta> updatePauserKeys(final PublicKey ownerKey,
                                                   final PublicKey tokenMessengerKey,
                                                   final PublicKey tokenMinterKey,
                                                   final PublicKey eventAuthorityKey,
                                                   final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(ownerKey),
      createRead(tokenMessengerKey),
      createWrite(tokenMinterKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction updatePauser(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                         final PublicKey ownerKey,
                                         final PublicKey tokenMessengerKey,
                                         final PublicKey tokenMinterKey,
                                         final PublicKey eventAuthorityKey,
                                         final PublicKey programKey,
                                         final UpdatePauserParams params) {
    final var keys = updatePauserKeys(
      ownerKey,
      tokenMessengerKey,
      tokenMinterKey,
      eventAuthorityKey,
      programKey
    );
    return updatePauser(invokedTokenMessengerMinterV2ProgramMeta, keys, params);
  }

  public static Instruction updatePauser(final AccountMeta invokedTokenMessengerMinterV2ProgramMeta,
                                         final List<AccountMeta> keys,
                                         final UpdatePauserParams params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = UPDATE_PAUSER_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedTokenMessengerMinterV2ProgramMeta, keys, _data);
  }

  public record UpdatePauserIxData(Discriminator discriminator, UpdatePauserParams params) implements SerDe {

    public static UpdatePauserIxData read(final Instruction instruction) {
      return read(instruction.copyData(), 0);
    }

    public static final int BYTES = 40;

    public static final int PARAMS_OFFSET = 8;

    public static UpdatePauserIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = UpdatePauserParams.read(_data, i);
      return new UpdatePauserIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private TokenMessengerMinterV2Program() {
  }
}
