// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// A sliding window rate limiter that tracks net outflow over a time window.
/// Uses weighted blend of previous and current windows for smooth transitions.
/// 
/// Net outflow = (withdraws + borrows) - (deposits + repays).
/// A negative net outflow increases remaining capacity for subsequent outflows.
///
/// @param maxOutflow Maximum net outflow allowed per window (0 = disabled).
///                   For bank-level: denominated in native tokens.
///                   For group-level: denominated in USD.
/// @param windowDuration Window duration in seconds (e.g., 3600 for hourly, 86400 for daily).
/// @param windowStart Unix timestamp when the current window started.
/// @param prevWindowOutflow Net outflow accumulated in the previous window.
///                          Signed to allow tracking when inflows exceed outflows.
/// @param curWindowOutflow Net outflow accumulated in the current window.
///                         Signed to allow tracking when inflows exceed outflows.
public record RateLimitWindow(long maxOutflow,
                              long windowDuration,
                              long windowStart,
                              long prevWindowOutflow,
                              long curWindowOutflow) implements SerDe {

  public static final int BYTES = 40;

  public static final int MAX_OUTFLOW_OFFSET = 0;
  public static final int WINDOW_DURATION_OFFSET = 8;
  public static final int WINDOW_START_OFFSET = 16;
  public static final int PREV_WINDOW_OUTFLOW_OFFSET = 24;
  public static final int CUR_WINDOW_OUTFLOW_OFFSET = 32;

  public static RateLimitWindow read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var maxOutflow = getInt64LE(_data, i);
    i += 8;
    final var windowDuration = getInt64LE(_data, i);
    i += 8;
    final var windowStart = getInt64LE(_data, i);
    i += 8;
    final var prevWindowOutflow = getInt64LE(_data, i);
    i += 8;
    final var curWindowOutflow = getInt64LE(_data, i);
    return new RateLimitWindow(maxOutflow,
                               windowDuration,
                               windowStart,
                               prevWindowOutflow,
                               curWindowOutflow);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt64LE(_data, i, maxOutflow);
    i += 8;
    putInt64LE(_data, i, windowDuration);
    i += 8;
    putInt64LE(_data, i, windowStart);
    i += 8;
    putInt64LE(_data, i, prevWindowOutflow);
    i += 8;
    putInt64LE(_data, i, curWindowOutflow);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
