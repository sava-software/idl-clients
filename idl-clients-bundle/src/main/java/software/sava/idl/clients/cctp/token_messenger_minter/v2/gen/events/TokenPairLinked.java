// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param remoteDomain: u32
public record TokenPairLinked(Discriminator discriminator,
                              PublicKey localToken,
                              long remoteDomain,
                              PublicKey remoteToken) implements TokenMessengerMinterV2Event {

  public static final int BYTES = 76;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(2, 14, 177, 64, 155, 93, 196, 141);

  public static final int LOCAL_TOKEN_OFFSET = 8;
  public static final int REMOTE_DOMAIN_OFFSET = 40;
  public static final int REMOTE_TOKEN_OFFSET = 44;

  public static TokenPairLinked read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var localToken = readPubKey(_data, i);
    i += 32;
    final var remoteDomain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var remoteToken = readPubKey(_data, i);
    return new TokenPairLinked(discriminator, localToken, remoteDomain, remoteToken);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    localToken.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) remoteDomain);
    i += 4;
    remoteToken.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
