// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param bump: u8
public record TokenMinter(PublicKey _address,
                          Discriminator discriminator,
                          PublicKey tokenController,
                          PublicKey pauser,
                          boolean paused,
                          int bump) implements SerDe {

  public static final int BYTES = 74;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(122, 133, 84, 63, 57, 159, 171, 206);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int TOKEN_CONTROLLER_OFFSET = 8;
  public static final int PAUSER_OFFSET = 40;
  public static final int PAUSED_OFFSET = 72;
  public static final int BUMP_OFFSET = 73;

  public static Filter createTokenControllerFilter(final PublicKey tokenController) {
    return Filter.createMemCompFilter(TOKEN_CONTROLLER_OFFSET, tokenController);
  }

  public static Filter createPauserFilter(final PublicKey pauser) {
    return Filter.createMemCompFilter(PAUSER_OFFSET, pauser);
  }

  public static Filter createPausedFilter(final boolean paused) {
    return Filter.createMemCompFilter(PAUSED_OFFSET, new byte[]{(byte) (paused ? 1 : 0)});
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static TokenMinter read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static TokenMinter read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static TokenMinter read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static TokenMinter readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static TokenMinter readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a TokenMinter account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], TokenMinter> FACTORY = TokenMinter::readChecked;

  public static TokenMinter read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var tokenController = readPubKey(_data, i);
    i += 32;
    final var pauser = readPubKey(_data, i);
    i += 32;
    final var paused = _data[i] == 1;
    ++i;
    final var bump = _data[i] & 0xFF;
    return new TokenMinter(_address,
                           discriminator,
                           tokenController,
                           pauser,
                           paused,
                           bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    tokenController.write(_data, i);
    i += 32;
    pauser.write(_data, i);
    i += 32;
    _data[i] = (byte) (paused ? 1 : 0);
    ++i;
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
