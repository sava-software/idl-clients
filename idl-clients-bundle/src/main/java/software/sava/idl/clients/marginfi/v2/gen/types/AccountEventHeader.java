// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;

public record AccountEventHeader(PublicKey signer,
                                 PublicKey marginfiAccount,
                                 PublicKey marginfiAccountAuthority,
                                 PublicKey marginfiGroup) implements SerDe {

  public static final int SIGNER_OFFSET = 1;

  public static AccountEventHeader read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final PublicKey signer;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      signer = null;
      ++i;
    } else {
      ++i;
      signer = readPubKey(_data, i);
      i += 32;
    }
    final var marginfiAccount = readPubKey(_data, i);
    i += 32;
    final var marginfiAccountAuthority = readPubKey(_data, i);
    i += 32;
    final var marginfiGroup = readPubKey(_data, i);
    return new AccountEventHeader(signer,
                                  marginfiAccount,
                                  marginfiAccountAuthority,
                                  marginfiGroup);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, signer, _data, i);
    marginfiAccount.write(_data, i);
    i += 32;
    marginfiAccountAuthority.write(_data, i);
    i += 32;
    marginfiGroup.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return (signer == null ? 1 : (1 + 32)) + 32 + 32 + 32;
  }
}
