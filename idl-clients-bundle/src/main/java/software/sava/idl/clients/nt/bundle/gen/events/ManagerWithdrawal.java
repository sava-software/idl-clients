// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import java.math.BigInteger;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param redemptionAmount: u64
public record ManagerWithdrawal(Discriminator discriminator,
                                BigInteger managerShares,
                                long redemptionAmount,
                                PublicKey bundleAccountKey,
                                BigInteger totalSharesAfter,
                                BigInteger managerPfeeSharesAfter,
                                BigInteger managerMfeeSharesAfter) implements NtbundleEvent {

  public static final int BYTES = 112;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(167, 6, 24, 193, 128, 74, 94, 207);

  public static final int MANAGER_SHARES_OFFSET = 8;
  public static final int REDEMPTION_AMOUNT_OFFSET = 24;
  public static final int BUNDLE_ACCOUNT_KEY_OFFSET = 32;
  public static final int TOTAL_SHARES_AFTER_OFFSET = 64;
  public static final int MANAGER_PFEE_SHARES_AFTER_OFFSET = 80;
  public static final int MANAGER_MFEE_SHARES_AFTER_OFFSET = 96;

  public static ManagerWithdrawal read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var managerShares = getUInt128LE(_data, i);
    i += 16;
    final var redemptionAmount = getInt64LE(_data, i);
    i += 8;
    final var bundleAccountKey = readPubKey(_data, i);
    i += 32;
    final var totalSharesAfter = getUInt128LE(_data, i);
    i += 16;
    final var managerPfeeSharesAfter = getUInt128LE(_data, i);
    i += 16;
    final var managerMfeeSharesAfter = getUInt128LE(_data, i);
    return new ManagerWithdrawal(discriminator,
                                 managerShares,
                                 redemptionAmount,
                                 bundleAccountKey,
                                 totalSharesAfter,
                                 managerPfeeSharesAfter,
                                 managerMfeeSharesAfter);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt128LE(_data, i, managerShares);
    i += 16;
    putInt64LE(_data, i, redemptionAmount);
    i += 8;
    bundleAccountKey.write(_data, i);
    i += 32;
    putInt128LE(_data, i, totalSharesAfter);
    i += 16;
    putInt128LE(_data, i, managerPfeeSharesAfter);
    i += 16;
    putInt128LE(_data, i, managerMfeeSharesAfter);
    i += 16;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
