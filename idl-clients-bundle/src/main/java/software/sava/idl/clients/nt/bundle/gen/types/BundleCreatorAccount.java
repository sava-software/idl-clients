// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record BundleCreatorAccount(PublicKey _address, Discriminator discriminator, boolean allowed, byte[] padding) implements SerDe {

  public static final int BYTES = 73;
  public static final int PADDING_LEN = 64;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(210, 168, 93, 196, 184, 35, 210, 101);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int ALLOWED_OFFSET = 8;
  public static final int PADDING_OFFSET = 9;

  public static Filter createAllowedFilter(final boolean allowed) {
    return Filter.createMemCompFilter(ALLOWED_OFFSET, new byte[]{(byte) (allowed ? 1 : 0)});
  }

  public static BundleCreatorAccount read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static BundleCreatorAccount read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static BundleCreatorAccount read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], BundleCreatorAccount> FACTORY = BundleCreatorAccount::read;

  public static BundleCreatorAccount read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var allowed = _data[i] == 1;
    ++i;
    final var padding = new byte[64];
    SerDeUtil.readArray(padding, _data, i);
    return new BundleCreatorAccount(_address, discriminator, allowed, padding);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    _data[i] = (byte) (allowed ? 1 : 0);
    ++i;
    i += SerDeUtil.writeArrayChecked(padding, 64, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
