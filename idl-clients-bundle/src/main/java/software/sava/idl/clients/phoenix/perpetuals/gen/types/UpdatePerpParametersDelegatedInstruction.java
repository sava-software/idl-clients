// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.perpetuals.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.util.OptionalLong;

import static software.sava.core.encoding.ByteUtil.getInt64LE;

/// Borsh payload for maintaining price-dependent parameters for a perp asset.
///
/// @param emaDiffRadius: Option<u64>
/// @param bookPriceRadius: Option<u64>
public record UpdatePerpParametersDelegatedInstruction(Symbol perpAssetSymbol,
                                                       OptionalLong emaDiffRadius,
                                                       OptionalLong bookPriceRadius,
                                                       SignedQuoteLotsPerBaseLot maxFundingRate) implements SerDe {

  public static final int PERP_ASSET_SYMBOL_OFFSET = 0;
  public static final int EMA_DIFF_RADIUS_OFFSET = 17;

  public static UpdatePerpParametersDelegatedInstruction read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var perpAssetSymbol = Symbol.read(_data, i);
    i += 16;
    final OptionalLong emaDiffRadius;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      emaDiffRadius = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      emaDiffRadius = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong bookPriceRadius;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      bookPriceRadius = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      bookPriceRadius = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final SignedQuoteLotsPerBaseLot maxFundingRate;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxFundingRate = null;
    } else {
      ++i;
      maxFundingRate = SignedQuoteLotsPerBaseLot.read(_data, i);
    }
    return new UpdatePerpParametersDelegatedInstruction(perpAssetSymbol,
                                                        emaDiffRadius,
                                                        bookPriceRadius,
                                                        maxFundingRate);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += perpAssetSymbol.write(_data, i);
    i += SerDeUtil.writeOptional(1, emaDiffRadius, _data, i);
    i += SerDeUtil.writeOptional(1, bookPriceRadius, _data, i);
    i += SerDeUtil.writeOptional(1, maxFundingRate, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return perpAssetSymbol.l() + (emaDiffRadius == null || emaDiffRadius.isEmpty() ? 1 : (1 + 8)) + (bookPriceRadius == null || bookPriceRadius.isEmpty() ? 1 : (1 + 8)) + (maxFundingRate == null ? 1 : (1 + maxFundingRate.l()));
  }
}
