// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Generic tracker for interest and emissions earned by YT deposits
///
/// @param lastSeenIndex The index is the per-share value of the SY token
///                      Note that the YT balance must be converted to the equivalent SY balance
/// @param staged: u64 Staged tokens that may be withdrawn
public record YieldTokenTracker(Number lastSeenIndex, long staged) implements SerDe {

  public static final int BYTES = 40;

  public static final int LAST_SEEN_INDEX_OFFSET = 0;
  public static final int STAGED_OFFSET = 32;

  public static YieldTokenTracker read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var lastSeenIndex = Number.read(_data, i);
    i += 32;
    final var staged = getInt64LE(_data, i);
    return new YieldTokenTracker(lastSeenIndex, staged);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += lastSeenIndex.write(_data, i);
    putInt64LE(_data, i, staged);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
