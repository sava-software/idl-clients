// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param assetIndex: u32
/// @param amount: u64
/// @param requestedAmount: u64
/// @param newCollateralBalance: u64
/// @param postGlobalCollateral: u64
/// @param traderSequenceNumber: u64
/// @param prevSequenceNumberSlot: u64
public record SpotCollateralDepositedEvent(long assetIndex,
                                           PublicKey trader,
                                           PublicKey authority,
                                           long amount,
                                           long requestedAmount,
                                           SpotCollateralCap bindingCap,
                                           SpotCollateralFlow flow,
                                           long newCollateralBalance,
                                           long postGlobalCollateral,
                                           long traderSequenceNumber,
                                           long prevSequenceNumberSlot) implements SerDe {

  public static final int ASSET_INDEX_OFFSET = 0;
  public static final int TRADER_OFFSET = 4;
  public static final int AUTHORITY_OFFSET = 36;
  public static final int AMOUNT_OFFSET = 68;
  public static final int REQUESTED_AMOUNT_OFFSET = 76;
  public static final int BINDING_CAP_OFFSET = 85;

  public static SpotCollateralDepositedEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var assetIndex = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var trader = readPubKey(_data, i);
    i += 32;
    final var authority = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var requestedAmount = getInt64LE(_data, i);
    i += 8;
    final SpotCollateralCap bindingCap;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      bindingCap = null;
      ++i;
    } else {
      ++i;
      bindingCap = SpotCollateralCap.read(_data, i);
      i += 1;
    }
    final var flow = SpotCollateralFlow.read(_data, i);
    i += 1;
    final var newCollateralBalance = getInt64LE(_data, i);
    i += 8;
    final var postGlobalCollateral = getInt64LE(_data, i);
    i += 8;
    final var traderSequenceNumber = getInt64LE(_data, i);
    i += 8;
    final var prevSequenceNumberSlot = getInt64LE(_data, i);
    return new SpotCollateralDepositedEvent(assetIndex,
                                            trader,
                                            authority,
                                            amount,
                                            requestedAmount,
                                            bindingCap,
                                            flow,
                                            newCollateralBalance,
                                            postGlobalCollateral,
                                            traderSequenceNumber,
                                            prevSequenceNumberSlot);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt32LE(_data, i, (int) assetIndex);
    i += 4;
    trader.write(_data, i);
    i += 32;
    authority.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    putInt64LE(_data, i, requestedAmount);
    i += 8;
    i += SerDeUtil.writeOptional(1, bindingCap, _data, i);
    i += flow.write(_data, i);
    putInt64LE(_data, i, newCollateralBalance);
    i += 8;
    putInt64LE(_data, i, postGlobalCollateral);
    i += 8;
    putInt64LE(_data, i, traderSequenceNumber);
    i += 8;
    putInt64LE(_data, i, prevSequenceNumberSlot);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 4
         + 32
         + 32
         + 8
         + 8
         + (bindingCap == null ? 1 : (1 + bindingCap.l()))
         + flow.l()
         + 8
         + 8
         + 8
         + 8;
  }
}
