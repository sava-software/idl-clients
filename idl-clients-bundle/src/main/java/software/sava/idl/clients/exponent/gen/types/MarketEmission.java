// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param tokenEscrow Escrow account that receives the emissions from the SY program
///                    And then passes them through to the user
/// @param lpShareIndex Index for converting LP shares into earned emissions
/// @param lastSeenStaged: u64 The difference between the staged amount and collected emission amount
public record MarketEmission(PublicKey tokenEscrow,
                             Number lpShareIndex,
                             long lastSeenStaged) implements SerDe {

  public static final int BYTES = 72;

  public static final int TOKEN_ESCROW_OFFSET = 0;
  public static final int LP_SHARE_INDEX_OFFSET = 32;
  public static final int LAST_SEEN_STAGED_OFFSET = 64;

  public static MarketEmission read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var tokenEscrow = readPubKey(_data, i);
    i += 32;
    final var lpShareIndex = Number.read(_data, i);
    i += lpShareIndex.l();
    final var lastSeenStaged = getInt64LE(_data, i);
    return new MarketEmission(tokenEscrow, lpShareIndex, lastSeenStaged);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    tokenEscrow.write(_data, i);
    i += 32;
    i += lpShareIndex.write(_data, i);
    putInt64LE(_data, i, lastSeenStaged);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
