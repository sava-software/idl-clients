// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param admin Broadly able to modify anything, and can set/remove other admins at will.
/// @param groupFlags Bitmask for group settings flags.
///                   * Bit 0 (1): `PROGRAM_FEES_ENABLED` — If set, program-level fees are enabled.
///                   * Bits 1-63: Reserved for future use.
/// @param feeStateCache Caches information from the global `FeeState` so the FeeState can be omitted on certain ixes
/// @param banks For groups initialized in versions 0.1.2 or greater, this is an authoritative count
///              of the number of banks under this group. For groups initialized prior to 0.1.2,
///              a non-authoritative count of the number of banks initiated after 0.1.2 went live.
/// @param emodeAdmin This admin can configure collateral ratios above (but not below) the collateral ratio of
///                   certain banks, e.g. allow SOL to count as 90% collateral when borrowing an LST instead of
///                   the default rate.
/// @param delegateCurveAdmin Can modify the fields in `config.interest_rate_config` but nothing else, for every bank
///                           under this group
/// @param delegateLimitAdmin Can modify the `deposit_limit`, `borrow_limit`, `total_asset_value_init_limit` but nothing
///                           else, for every bank under this group
/// @param delegateEmissionsAdmin Can modify the emissions `flags`, `emissions_rate` and `emissions_mint`, but nothing else,
///                               for every bank under this group
/// @param panicStateCache When program keeper temporarily puts the program into panic mode, information about the
///                        duration of the lockup will be available here.
/// @param deleverageWithdrawWindowCache Keeps track of the liquidity withdrawn from the group over the day as a result of
///                                      deleverages. Used as a protection mechanism against too big (and unwanted) withdrawals (e.g.
///                                      when the risk admin is compromised).
/// @param riskAdmin Can run bankruptcy and forced deleverage ixes to e.g. sunset risky/illiquid assets
/// @param metadataAdmin Can modify a Bank's metadata, and nothing else.
/// @param emodeMaxInitLeverage Maximum leverage allowed for emode positions (initial margin), stored as u32 basis.
///                             Use `u32_to_basis` to convert to I80F48. Range: 1-100.
/// @param emodeMaxMaintLeverage Maximum leverage allowed for emode positions (maintenance margin), stored as u32 basis.
///                              Must be > emode_max_init_leverage. Range: 1-100.
/// @param padding Reserved for future use
/// @param rateLimiter Rate limiter for controlling aggregate withdraw/borrow outflow across all banks.
///                    Tracks net outflow in USD.
/// @param rateLimiterLastAdminUpdateSlot Last slot covered by an admin group rate limiter aggregation update.
/// @param rateLimiterLastAdminUpdateSeq Monotonic sequence number for admin group rate limiter updates.
///                                      This is used to enforce strict ordering and prevent duplicate/replayed batches
///                                      when slot ranges overlap or multiple updates happen in the same slot.
/// @param deleverageWithdrawLastAdminUpdateSlot Last slot covered by an admin deleverage withdraw-limit aggregation update.
/// @param deleverageWithdrawLastAdminUpdateSeq Monotonic sequence number for admin deleverage withdraw-limit updates.
/// @param delegateFlowAdmin Can modify flow-control status for the group, i.e. update the withdraw caches with flow
///                          information from banks. Typically this is a hot wallet that lives in e.g. some cron job. If
///                          compromised, flow control can be effectively disabled until the admin is restored, which
///                          does not itself compromise any funds, and is merely annoying.
public record MarginfiGroup(PublicKey _address,
                            Discriminator discriminator,
                            PublicKey admin,
                            long groupFlags,
                            FeeStateCache feeStateCache,
                            int banks,
                            byte[] pad0,
                            PublicKey emodeAdmin,
                            PublicKey delegateCurveAdmin,
                            PublicKey delegateLimitAdmin,
                            PublicKey delegateEmissionsAdmin,
                            PanicStateCache panicStateCache,
                            WithdrawWindowCache deleverageWithdrawWindowCache,
                            PublicKey riskAdmin,
                            PublicKey metadataAdmin,
                            int emodeMaxInitLeverage,
                            int emodeMaxMaintLeverage,
                            byte[] padding,
                            GroupRateLimiter rateLimiter,
                            long rateLimiterLastAdminUpdateSlot,
                            long rateLimiterLastAdminUpdateSeq,
                            long deleverageWithdrawLastAdminUpdateSlot,
                            long deleverageWithdrawLastAdminUpdateSeq,
                            PublicKey delegateFlowAdmin,
                            long[][] padding0,
                            long[][] padding1) implements SerDe {

  public static final int BYTES = 1064;
  public static final int PAD_0_LEN = 6;
  public static final int PADDING_LEN = 8;
  public static final int PADDING_0_LEN = 2;
  public static final int PADDING_1_LEN = 32;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(182, 23, 173, 240, 151, 206, 182, 67);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int ADMIN_OFFSET = 8;
  public static final int GROUP_FLAGS_OFFSET = 40;
  public static final int FEE_STATE_CACHE_OFFSET = 48;
  public static final int BANKS_OFFSET = 120;
  public static final int PAD_0_OFFSET = 122;
  public static final int EMODE_ADMIN_OFFSET = 128;
  public static final int DELEGATE_CURVE_ADMIN_OFFSET = 160;
  public static final int DELEGATE_LIMIT_ADMIN_OFFSET = 192;
  public static final int DELEGATE_EMISSIONS_ADMIN_OFFSET = 224;
  public static final int PANIC_STATE_CACHE_OFFSET = 256;
  public static final int DELEVERAGE_WITHDRAW_WINDOW_CACHE_OFFSET = 280;
  public static final int RISK_ADMIN_OFFSET = 296;
  public static final int METADATA_ADMIN_OFFSET = 328;
  public static final int EMODE_MAX_INIT_LEVERAGE_OFFSET = 360;
  public static final int EMODE_MAX_MAINT_LEVERAGE_OFFSET = 364;
  public static final int PADDING_OFFSET = 368;
  public static final int RATE_LIMITER_OFFSET = 376;
  public static final int RATE_LIMITER_LAST_ADMIN_UPDATE_SLOT_OFFSET = 456;
  public static final int RATE_LIMITER_LAST_ADMIN_UPDATE_SEQ_OFFSET = 464;
  public static final int DELEVERAGE_WITHDRAW_LAST_ADMIN_UPDATE_SLOT_OFFSET = 472;
  public static final int DELEVERAGE_WITHDRAW_LAST_ADMIN_UPDATE_SEQ_OFFSET = 480;
  public static final int DELEGATE_FLOW_ADMIN_OFFSET = 488;
  public static final int PADDING_0_OFFSET = 520;
  public static final int PADDING_1_OFFSET = 552;

  public static Filter createAdminFilter(final PublicKey admin) {
    return Filter.createMemCompFilter(ADMIN_OFFSET, admin);
  }

  public static Filter createGroupFlagsFilter(final long groupFlags) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, groupFlags);
    return Filter.createMemCompFilter(GROUP_FLAGS_OFFSET, _data);
  }

  public static Filter createFeeStateCacheFilter(final FeeStateCache feeStateCache) {
    return Filter.createMemCompFilter(FEE_STATE_CACHE_OFFSET, feeStateCache.write());
  }

  public static Filter createBanksFilter(final int banks) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, banks);
    return Filter.createMemCompFilter(BANKS_OFFSET, _data);
  }

  public static Filter createEmodeAdminFilter(final PublicKey emodeAdmin) {
    return Filter.createMemCompFilter(EMODE_ADMIN_OFFSET, emodeAdmin);
  }

  public static Filter createDelegateCurveAdminFilter(final PublicKey delegateCurveAdmin) {
    return Filter.createMemCompFilter(DELEGATE_CURVE_ADMIN_OFFSET, delegateCurveAdmin);
  }

  public static Filter createDelegateLimitAdminFilter(final PublicKey delegateLimitAdmin) {
    return Filter.createMemCompFilter(DELEGATE_LIMIT_ADMIN_OFFSET, delegateLimitAdmin);
  }

  public static Filter createDelegateEmissionsAdminFilter(final PublicKey delegateEmissionsAdmin) {
    return Filter.createMemCompFilter(DELEGATE_EMISSIONS_ADMIN_OFFSET, delegateEmissionsAdmin);
  }

  public static Filter createPanicStateCacheFilter(final PanicStateCache panicStateCache) {
    return Filter.createMemCompFilter(PANIC_STATE_CACHE_OFFSET, panicStateCache.write());
  }

  public static Filter createDeleverageWithdrawWindowCacheFilter(final WithdrawWindowCache deleverageWithdrawWindowCache) {
    return Filter.createMemCompFilter(DELEVERAGE_WITHDRAW_WINDOW_CACHE_OFFSET, deleverageWithdrawWindowCache.write());
  }

  public static Filter createRiskAdminFilter(final PublicKey riskAdmin) {
    return Filter.createMemCompFilter(RISK_ADMIN_OFFSET, riskAdmin);
  }

  public static Filter createMetadataAdminFilter(final PublicKey metadataAdmin) {
    return Filter.createMemCompFilter(METADATA_ADMIN_OFFSET, metadataAdmin);
  }

  public static Filter createEmodeMaxInitLeverageFilter(final int emodeMaxInitLeverage) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, emodeMaxInitLeverage);
    return Filter.createMemCompFilter(EMODE_MAX_INIT_LEVERAGE_OFFSET, _data);
  }

  public static Filter createEmodeMaxMaintLeverageFilter(final int emodeMaxMaintLeverage) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, emodeMaxMaintLeverage);
    return Filter.createMemCompFilter(EMODE_MAX_MAINT_LEVERAGE_OFFSET, _data);
  }

  public static Filter createRateLimiterFilter(final GroupRateLimiter rateLimiter) {
    return Filter.createMemCompFilter(RATE_LIMITER_OFFSET, rateLimiter.write());
  }

  public static Filter createRateLimiterLastAdminUpdateSlotFilter(final long rateLimiterLastAdminUpdateSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, rateLimiterLastAdminUpdateSlot);
    return Filter.createMemCompFilter(RATE_LIMITER_LAST_ADMIN_UPDATE_SLOT_OFFSET, _data);
  }

  public static Filter createRateLimiterLastAdminUpdateSeqFilter(final long rateLimiterLastAdminUpdateSeq) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, rateLimiterLastAdminUpdateSeq);
    return Filter.createMemCompFilter(RATE_LIMITER_LAST_ADMIN_UPDATE_SEQ_OFFSET, _data);
  }

  public static Filter createDeleverageWithdrawLastAdminUpdateSlotFilter(final long deleverageWithdrawLastAdminUpdateSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, deleverageWithdrawLastAdminUpdateSlot);
    return Filter.createMemCompFilter(DELEVERAGE_WITHDRAW_LAST_ADMIN_UPDATE_SLOT_OFFSET, _data);
  }

  public static Filter createDeleverageWithdrawLastAdminUpdateSeqFilter(final long deleverageWithdrawLastAdminUpdateSeq) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, deleverageWithdrawLastAdminUpdateSeq);
    return Filter.createMemCompFilter(DELEVERAGE_WITHDRAW_LAST_ADMIN_UPDATE_SEQ_OFFSET, _data);
  }

  public static Filter createDelegateFlowAdminFilter(final PublicKey delegateFlowAdmin) {
    return Filter.createMemCompFilter(DELEGATE_FLOW_ADMIN_OFFSET, delegateFlowAdmin);
  }

  public static MarginfiGroup read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static MarginfiGroup read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static MarginfiGroup read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], MarginfiGroup> FACTORY = MarginfiGroup::read;

  public static MarginfiGroup read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var admin = readPubKey(_data, i);
    i += 32;
    final var groupFlags = getInt64LE(_data, i);
    i += 8;
    final var feeStateCache = FeeStateCache.read(_data, i);
    i += feeStateCache.l();
    final var banks = getInt16LE(_data, i);
    i += 2;
    final var pad0 = new byte[6];
    i += SerDeUtil.readArray(pad0, _data, i);
    final var emodeAdmin = readPubKey(_data, i);
    i += 32;
    final var delegateCurveAdmin = readPubKey(_data, i);
    i += 32;
    final var delegateLimitAdmin = readPubKey(_data, i);
    i += 32;
    final var delegateEmissionsAdmin = readPubKey(_data, i);
    i += 32;
    final var panicStateCache = PanicStateCache.read(_data, i);
    i += panicStateCache.l();
    final var deleverageWithdrawWindowCache = WithdrawWindowCache.read(_data, i);
    i += deleverageWithdrawWindowCache.l();
    final var riskAdmin = readPubKey(_data, i);
    i += 32;
    final var metadataAdmin = readPubKey(_data, i);
    i += 32;
    final var emodeMaxInitLeverage = getInt32LE(_data, i);
    i += 4;
    final var emodeMaxMaintLeverage = getInt32LE(_data, i);
    i += 4;
    final var padding = new byte[8];
    i += SerDeUtil.readArray(padding, _data, i);
    final var rateLimiter = GroupRateLimiter.read(_data, i);
    i += rateLimiter.l();
    final var rateLimiterLastAdminUpdateSlot = getInt64LE(_data, i);
    i += 8;
    final var rateLimiterLastAdminUpdateSeq = getInt64LE(_data, i);
    i += 8;
    final var deleverageWithdrawLastAdminUpdateSlot = getInt64LE(_data, i);
    i += 8;
    final var deleverageWithdrawLastAdminUpdateSeq = getInt64LE(_data, i);
    i += 8;
    final var delegateFlowAdmin = readPubKey(_data, i);
    i += 32;
    final var padding0 = new long[2][2];
    i += SerDeUtil.readArray(padding0, _data, i);
    final var padding1 = new long[32][2];
    SerDeUtil.readArray(padding1, _data, i);
    return new MarginfiGroup(_address,
                             discriminator,
                             admin,
                             groupFlags,
                             feeStateCache,
                             banks,
                             pad0,
                             emodeAdmin,
                             delegateCurveAdmin,
                             delegateLimitAdmin,
                             delegateEmissionsAdmin,
                             panicStateCache,
                             deleverageWithdrawWindowCache,
                             riskAdmin,
                             metadataAdmin,
                             emodeMaxInitLeverage,
                             emodeMaxMaintLeverage,
                             padding,
                             rateLimiter,
                             rateLimiterLastAdminUpdateSlot,
                             rateLimiterLastAdminUpdateSeq,
                             deleverageWithdrawLastAdminUpdateSlot,
                             deleverageWithdrawLastAdminUpdateSeq,
                             delegateFlowAdmin,
                             padding0,
                             padding1);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    admin.write(_data, i);
    i += 32;
    putInt64LE(_data, i, groupFlags);
    i += 8;
    i += feeStateCache.write(_data, i);
    putInt16LE(_data, i, banks);
    i += 2;
    i += SerDeUtil.writeArrayChecked(pad0, 6, _data, i);
    emodeAdmin.write(_data, i);
    i += 32;
    delegateCurveAdmin.write(_data, i);
    i += 32;
    delegateLimitAdmin.write(_data, i);
    i += 32;
    delegateEmissionsAdmin.write(_data, i);
    i += 32;
    i += panicStateCache.write(_data, i);
    i += deleverageWithdrawWindowCache.write(_data, i);
    riskAdmin.write(_data, i);
    i += 32;
    metadataAdmin.write(_data, i);
    i += 32;
    putInt32LE(_data, i, emodeMaxInitLeverage);
    i += 4;
    putInt32LE(_data, i, emodeMaxMaintLeverage);
    i += 4;
    i += SerDeUtil.writeArrayChecked(padding, 8, _data, i);
    i += rateLimiter.write(_data, i);
    putInt64LE(_data, i, rateLimiterLastAdminUpdateSlot);
    i += 8;
    putInt64LE(_data, i, rateLimiterLastAdminUpdateSeq);
    i += 8;
    putInt64LE(_data, i, deleverageWithdrawLastAdminUpdateSlot);
    i += 8;
    putInt64LE(_data, i, deleverageWithdrawLastAdminUpdateSeq);
    i += 8;
    delegateFlowAdmin.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(padding0, 2, 2, _data, i);
    i += SerDeUtil.writeArrayChecked(padding1, 32, 2, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
