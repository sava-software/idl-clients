// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.oracles.wormhole.verify_vaa.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param refundRecipient Payer of this guardian signatures account.
///                        Only they may amend signatures.
///                        Used for reimbursements upon cleanup.
/// @param guardianSetIndexBe Guardian set index that these signatures correspond to.
///                           Storing this simplifies the integrator data.
///                           Using big-endian to match the derivation used by the core bridge.
/// @param guardianSignatures Unverified guardian signatures.
public record GuardianSignatures(PublicKey _address,
                                 Discriminator discriminator,
                                 PublicKey refundRecipient,
                                 byte[] guardianSetIndexBe,
                                 byte[][] guardianSignatures) implements SerDe {

  public static final int GUARDIAN_SET_INDEX_BE_LEN = 4;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(203, 184, 130, 157, 113, 14, 184, 83);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int REFUND_RECIPIENT_OFFSET = 8;
  public static final int GUARDIAN_SET_INDEX_BE_OFFSET = 40;
  public static final int GUARDIAN_SIGNATURES_OFFSET = 44;

  public static Filter createRefundRecipientFilter(final PublicKey refundRecipient) {
    return Filter.createMemCompFilter(REFUND_RECIPIENT_OFFSET, refundRecipient);
  }

  public static GuardianSignatures read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static GuardianSignatures read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static GuardianSignatures read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], GuardianSignatures> FACTORY = GuardianSignatures::read;

  public static GuardianSignatures read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var refundRecipient = readPubKey(_data, i);
    i += 32;
    final var guardianSetIndexBe = new byte[4];
    i += SerDeUtil.readArray(guardianSetIndexBe, _data, i);
    final var guardianSignatures = SerDeUtil.readMultiDimensionbyteVectorArray(4, 66, _data, i);
    return new GuardianSignatures(_address, discriminator, refundRecipient, guardianSetIndexBe, guardianSignatures);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    refundRecipient.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(guardianSetIndexBe, 4, _data, i);
    i += SerDeUtil.writeVectorArrayChecked(4, guardianSignatures, 66, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32 + SerDeUtil.lenArray(guardianSetIndexBe) + SerDeUtil.lenVectorArray(4, guardianSignatures);
  }
}
