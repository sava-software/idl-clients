// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param entirePosition: Option<bool>
/// @param requestChange: u8
/// @param requestType: u8
/// @param side: u8
/// @param amount: u64
public record ClosePositionRequestEvent(Discriminator discriminator,
                                        Boolean entirePosition,
                                        boolean executed,
                                        int requestChange,
                                        int requestType,
                                        int side,
                                        PublicKey positionRequestKey,
                                        PublicKey owner,
                                        PublicKey mint,
                                        long amount,
                                        long openTime) implements PerpetualsEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(21, 34, 92, 158, 224, 29, 180, 243);

  public static final int ENTIRE_POSITION_OFFSET = 9;

  public static ClosePositionRequestEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final Boolean entirePosition;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      entirePosition = null;
      ++i;
    } else {
      ++i;
      entirePosition = _data[i] == 1;
      ++i;
    }
    final var executed = _data[i] == 1;
    ++i;
    final var requestChange = _data[i] & 0xFF;
    ++i;
    final var requestType = _data[i] & 0xFF;
    ++i;
    final var side = _data[i] & 0xFF;
    ++i;
    final var positionRequestKey = readPubKey(_data, i);
    i += 32;
    final var owner = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var openTime = getInt64LE(_data, i);
    return new ClosePositionRequestEvent(discriminator,
                                         entirePosition,
                                         executed,
                                         requestChange,
                                         requestType,
                                         side,
                                         positionRequestKey,
                                         owner,
                                         mint,
                                         amount,
                                         openTime);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += SerDeUtil.writeOptional(1, entirePosition, _data, i);
    _data[i] = (byte) (executed ? 1 : 0);
    ++i;
    _data[i] = (byte) requestChange;
    ++i;
    _data[i] = (byte) requestType;
    ++i;
    _data[i] = (byte) side;
    ++i;
    positionRequestKey.write(_data, i);
    i += 32;
    owner.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    putInt64LE(_data, i, openTime);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + (entirePosition == null ? 1 : (1 + 1))
         + 1
         + 1
         + 1
         + 1
         + 32
         + 32
         + 32
         + 8
         + 8;
  }
}
