// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.GroupEventHeader;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param requestedAmount: u64
/// @param receivedAmount: u64
/// @param sweptAmount: u64
public record DriftClaimBadDebtEvent(Discriminator discriminator,
                                     GroupEventHeader header,
                                     PublicKey bank,
                                     PublicKey claimMint,
                                     PublicKey distributor,
                                     PublicKey claimStatus,
                                     PublicKey liquidityVaultAuthority,
                                     PublicKey globalFeeWallet,
                                     long requestedAmount,
                                     long receivedAmount,
                                     long sweptAmount) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(20, 106, 26, 156, 28, 116, 250, 172);

  public static final int HEADER_OFFSET = 8;

  public static DriftClaimBadDebtEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = GroupEventHeader.read(_data, i);
    i += header.l();
    final var bank = readPubKey(_data, i);
    i += 32;
    final var claimMint = readPubKey(_data, i);
    i += 32;
    final var distributor = readPubKey(_data, i);
    i += 32;
    final var claimStatus = readPubKey(_data, i);
    i += 32;
    final var liquidityVaultAuthority = readPubKey(_data, i);
    i += 32;
    final var globalFeeWallet = readPubKey(_data, i);
    i += 32;
    final var requestedAmount = getInt64LE(_data, i);
    i += 8;
    final var receivedAmount = getInt64LE(_data, i);
    i += 8;
    final var sweptAmount = getInt64LE(_data, i);
    return new DriftClaimBadDebtEvent(discriminator,
                                      header,
                                      bank,
                                      claimMint,
                                      distributor,
                                      claimStatus,
                                      liquidityVaultAuthority,
                                      globalFeeWallet,
                                      requestedAmount,
                                      receivedAmount,
                                      sweptAmount);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    bank.write(_data, i);
    i += 32;
    claimMint.write(_data, i);
    i += 32;
    distributor.write(_data, i);
    i += 32;
    claimStatus.write(_data, i);
    i += 32;
    liquidityVaultAuthority.write(_data, i);
    i += 32;
    globalFeeWallet.write(_data, i);
    i += 32;
    putInt64LE(_data, i, requestedAmount);
    i += 8;
    putInt64LE(_data, i, receivedAmount);
    i += 8;
    putInt64LE(_data, i, sweptAmount);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l()
         + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 8
         + 8
         + 8;
  }
}
