// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

/// An emode configuration. Each bank has one such configuration, but this may also be the
/// intersection of many configurations (see `reconcile_emode_configs`). For example, the risk
/// engine creates such an intersection from all the emode config of all banks the user is borrowing
/// from.
///
public record EmodeConfig(EmodeEntry[] entries) implements SerDe {

  public static final int BYTES = 400;
  public static final int ENTRIES_LEN = 10;

  public static final int ENTRIES_OFFSET = 0;

  public static EmodeConfig read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var entries = new EmodeEntry[10];
    SerDeUtil.readArray(entries, EmodeEntry::read, _data, _offset);
    return new EmodeConfig(entries);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeArrayChecked(entries, 10, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
