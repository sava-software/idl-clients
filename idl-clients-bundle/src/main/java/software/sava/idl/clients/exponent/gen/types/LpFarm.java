// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;

/// @param lastSeenTimestamp: u32
public record LpFarm(long lastSeenTimestamp, FarmEmission[] farmEmissions) implements SerDe {

  public static final int LAST_SEEN_TIMESTAMP_OFFSET = 0;
  public static final int FARM_EMISSIONS_OFFSET = 4;

  public static LpFarm read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var lastSeenTimestamp = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var farmEmissions = SerDeUtil.readVector(4, FarmEmission.class, FarmEmission::read, _data, i);
    return new LpFarm(lastSeenTimestamp, farmEmissions);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt32LE(_data, i, (int) lastSeenTimestamp);
    i += 4;
    i += SerDeUtil.writeVector(4, farmEmissions, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 4 + SerDeUtil.lenVector(4, farmEmissions);
  }
}
