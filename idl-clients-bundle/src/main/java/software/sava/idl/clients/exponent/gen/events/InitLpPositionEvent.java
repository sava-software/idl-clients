// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param numEmissionTrackers: u8
/// @param numFarmEmissions: u8
public record InitLpPositionEvent(Discriminator discriminator,
                                  PublicKey feePayer,
                                  PublicKey owner,
                                  PublicKey market,
                                  PublicKey lpPosition,
                                  int numEmissionTrackers,
                                  int numFarmEmissions,
                                  long timestamp) implements ExponentCoreEvent {

  public static final int BYTES = 146;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(7, 245, 31, 228, 42, 49, 201, 192);

  public static final int FEE_PAYER_OFFSET = 8;
  public static final int OWNER_OFFSET = 40;
  public static final int MARKET_OFFSET = 72;
  public static final int LP_POSITION_OFFSET = 104;
  public static final int NUM_EMISSION_TRACKERS_OFFSET = 136;
  public static final int NUM_FARM_EMISSIONS_OFFSET = 137;
  public static final int TIMESTAMP_OFFSET = 138;

  public static InitLpPositionEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var feePayer = readPubKey(_data, i);
    i += 32;
    final var owner = readPubKey(_data, i);
    i += 32;
    final var market = readPubKey(_data, i);
    i += 32;
    final var lpPosition = readPubKey(_data, i);
    i += 32;
    final var numEmissionTrackers = _data[i] & 0xFF;
    ++i;
    final var numFarmEmissions = _data[i] & 0xFF;
    ++i;
    final var timestamp = getInt64LE(_data, i);
    return new InitLpPositionEvent(discriminator,
                                   feePayer,
                                   owner,
                                   market,
                                   lpPosition,
                                   numEmissionTrackers,
                                   numFarmEmissions,
                                   timestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    feePayer.write(_data, i);
    i += 32;
    owner.write(_data, i);
    i += 32;
    market.write(_data, i);
    i += 32;
    lpPosition.write(_data, i);
    i += 32;
    _data[i] = (byte) numEmissionTrackers;
    ++i;
    _data[i] = (byte) numFarmEmissions;
    ++i;
    putInt64LE(_data, i, timestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
