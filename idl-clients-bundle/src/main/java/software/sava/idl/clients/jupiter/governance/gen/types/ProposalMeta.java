// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.governance.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// Metadata about a proposal.
///
/// @param proposal The Proposal.
/// @param title Title of the proposal.
/// @param descriptionLink Link to a description of the proposal.
public record ProposalMeta(PublicKey _address,
                           Discriminator discriminator,
                           PublicKey proposal,
                           String title, byte[] _title,
                           String descriptionLink, byte[] _descriptionLink) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(50, 100, 46, 24, 151, 174, 216, 78);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PROPOSAL_OFFSET = 8;
  public static final int TITLE_OFFSET = 40;

  public static Filter createProposalFilter(final PublicKey proposal) {
    return Filter.createMemCompFilter(PROPOSAL_OFFSET, proposal);
  }

  public static ProposalMeta createRecord(final PublicKey _address,
                                          final Discriminator discriminator,
                                          final PublicKey proposal,
                                          final String title,
                                          final String descriptionLink) {
    return new ProposalMeta(_address, discriminator, proposal, title, title == null ? null : SerDeUtil.encodeString(title), descriptionLink, descriptionLink == null ? null : SerDeUtil.encodeString(descriptionLink));
  }

  public static ProposalMeta read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static ProposalMeta read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static ProposalMeta read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static ProposalMeta readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static ProposalMeta readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a ProposalMeta account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], ProposalMeta> FACTORY = ProposalMeta::readChecked;

  public static ProposalMeta read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var proposal = readPubKey(_data, i);
    i += 32;
    final byte[] _title = SerDeUtil.readbyteVector(4, _data, i);
    final var title = SerDeUtil.decodeString(_title);
    i += 4 + _title.length;
    final byte[] _descriptionLink = SerDeUtil.readbyteVector(4, _data, i);
    final var descriptionLink = SerDeUtil.decodeString(_descriptionLink);
    return new ProposalMeta(_address, discriminator, proposal, title, title == null ? null : SerDeUtil.encodeString(title), descriptionLink, descriptionLink == null ? null : SerDeUtil.encodeString(descriptionLink));
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    proposal.write(_data, i);
    i += 32;
    i += SerDeUtil.writeVector(4, _title, _data, i);
    i += SerDeUtil.writeVector(4, _descriptionLink, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32 + 4 + _title.length + 4 + _descriptionLink.length;
  }
}
