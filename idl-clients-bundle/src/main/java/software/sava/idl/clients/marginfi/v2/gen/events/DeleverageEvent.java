// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record DeleverageEvent(Discriminator discriminator,
                              PublicKey marginfiAccount,
                              PublicKey riskAdmin,
                              double deleverageeAssetsSeized,
                              double deleverageeLiabilityRepaid) implements MarginfiEvent {

  public static final int BYTES = 88;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(161, 8, 108, 204, 209, 198, 12, 30);

  public static final int MARGINFI_ACCOUNT_OFFSET = 8;
  public static final int RISK_ADMIN_OFFSET = 40;
  public static final int DELEVERAGEE_ASSETS_SEIZED_OFFSET = 72;
  public static final int DELEVERAGEE_LIABILITY_REPAID_OFFSET = 80;

  public static DeleverageEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var marginfiAccount = readPubKey(_data, i);
    i += 32;
    final var riskAdmin = readPubKey(_data, i);
    i += 32;
    final var deleverageeAssetsSeized = getFloat64LE(_data, i);
    i += 8;
    final var deleverageeLiabilityRepaid = getFloat64LE(_data, i);
    return new DeleverageEvent(discriminator,
                               marginfiAccount,
                               riskAdmin,
                               deleverageeAssetsSeized,
                               deleverageeLiabilityRepaid);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    marginfiAccount.write(_data, i);
    i += 32;
    riskAdmin.write(_data, i);
    i += 32;
    putFloat64LE(_data, i, deleverageeAssetsSeized);
    i += 8;
    putFloat64LE(_data, i, deleverageeLiabilityRepaid);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
