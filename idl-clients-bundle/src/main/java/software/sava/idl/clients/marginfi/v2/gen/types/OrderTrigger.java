// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.RustEnum;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;

public sealed interface OrderTrigger extends RustEnum permits
  OrderTrigger.StopLoss,
  OrderTrigger.TakeProfit,
  OrderTrigger.Both {

  static OrderTrigger read(final byte[] _data, final int _offset) {
    final int ordinal = _data[_offset] & 0xFF;
    final int i = _offset + 1;
    return switch (ordinal) {
      case 0 -> StopLoss.read(_data, i);
      case 1 -> TakeProfit.read(_data, i);
      case 2 -> Both.read(_data, i);
      default -> null;
    };
  }

  /// @param maxSlippage: u32
  record StopLoss(WrappedI80F48 threshold, long maxSlippage) implements OrderTrigger {

    public static final int BYTES = 20;

    public static final int THRESHOLD_OFFSET = 0;
    public static final int MAX_SLIPPAGE_OFFSET = 16;

    public static StopLoss read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var threshold = WrappedI80F48.read(_data, i);
      i += threshold.l();
      final var maxSlippage = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new StopLoss(threshold, maxSlippage);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += threshold.write(_data, i);
      putInt32LE(_data, i, (int) maxSlippage);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }

    @Override
    public int ordinal() {
      return 0;
    }
  }

  /// @param maxSlippage: u32
  record TakeProfit(WrappedI80F48 threshold, long maxSlippage) implements OrderTrigger {

    public static final int BYTES = 20;

    public static final int THRESHOLD_OFFSET = 0;
    public static final int MAX_SLIPPAGE_OFFSET = 16;

    public static TakeProfit read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var threshold = WrappedI80F48.read(_data, i);
      i += threshold.l();
      final var maxSlippage = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new TakeProfit(threshold, maxSlippage);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += threshold.write(_data, i);
      putInt32LE(_data, i, (int) maxSlippage);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }

    @Override
    public int ordinal() {
      return 1;
    }
  }

  /// @param maxSlippage: u32
  record Both(WrappedI80F48 stopLoss,
              WrappedI80F48 takeProfit,
              long maxSlippage) implements OrderTrigger {

    public static final int BYTES = 36;

    public static final int STOP_LOSS_OFFSET = 0;
    public static final int TAKE_PROFIT_OFFSET = 16;
    public static final int MAX_SLIPPAGE_OFFSET = 32;

    public static Both read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var stopLoss = WrappedI80F48.read(_data, i);
      i += stopLoss.l();
      final var takeProfit = WrappedI80F48.read(_data, i);
      i += takeProfit.l();
      final var maxSlippage = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new Both(stopLoss, takeProfit, maxSlippage);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += stopLoss.write(_data, i);
      i += takeProfit.write(_data, i);
      putInt32LE(_data, i, (int) maxSlippage);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }

    @Override
    public int ordinal() {
      return 2;
    }
  }
}
