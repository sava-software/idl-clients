// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param bank Bank this metadata corresponds to
/// @param placeholder: u64
/// @param ticker The token's ticker name, e.g. USDC
///               * utf-8
/// @param description The token's plain english description, e.g US Dollar Coin
///                    * utf-8
/// @param dataBlob Reserved for future use. Room for a very small icon or something else cool
/// @param endDescriptionByte: u16 The last data byte in description (padding follows)
/// @param endDataBlob: u16 The last data byte in data_blob (padding follows)
/// @param endTickerByte The last data byte in ticker (padding follows)
public record BankMetadata(PublicKey _address,
                           Discriminator discriminator,
                           PublicKey bank,
                           long placeholder,
                           byte[] ticker,
                           byte[] description,
                           byte[] dataBlob,
                           int endDescriptionByte,
                           int endDataBlob,
                           int endTickerByte,
                           int bump,
                           byte[] pad0) implements SerDe {

  public static final int BYTES = 504;
  public static final int TICKER_LEN = 64;
  public static final int DESCRIPTION_LEN = 128;
  public static final int DATA_BLOB_LEN = 256;
  public static final int PAD_0_LEN = 2;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(49, 207, 31, 34, 67, 225, 169, 186);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int BANK_OFFSET = 8;
  public static final int PLACEHOLDER_OFFSET = 40;
  public static final int TICKER_OFFSET = 48;
  public static final int DESCRIPTION_OFFSET = 112;
  public static final int DATA_BLOB_OFFSET = 240;
  public static final int END_DESCRIPTION_BYTE_OFFSET = 496;
  public static final int END_DATA_BLOB_OFFSET = 498;
  public static final int END_TICKER_BYTE_OFFSET = 500;
  public static final int BUMP_OFFSET = 501;
  public static final int PAD_0_OFFSET = 502;

  public static Filter createBankFilter(final PublicKey bank) {
    return Filter.createMemCompFilter(BANK_OFFSET, bank);
  }

  public static Filter createPlaceholderFilter(final long placeholder) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, placeholder);
    return Filter.createMemCompFilter(PLACEHOLDER_OFFSET, _data);
  }

  public static Filter createEndDescriptionByteFilter(final int endDescriptionByte) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, endDescriptionByte);
    return Filter.createMemCompFilter(END_DESCRIPTION_BYTE_OFFSET, _data);
  }

  public static Filter createEndDataBlobFilter(final int endDataBlob) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, endDataBlob);
    return Filter.createMemCompFilter(END_DATA_BLOB_OFFSET, _data);
  }

  public static Filter createEndTickerByteFilter(final int endTickerByte) {
    return Filter.createMemCompFilter(END_TICKER_BYTE_OFFSET, new byte[]{(byte) endTickerByte});
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static BankMetadata read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static BankMetadata read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static BankMetadata read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], BankMetadata> FACTORY = BankMetadata::read;

  public static BankMetadata read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var bank = readPubKey(_data, i);
    i += 32;
    final var placeholder = getInt64LE(_data, i);
    i += 8;
    final var ticker = new byte[64];
    i += SerDeUtil.readArray(ticker, _data, i);
    final var description = new byte[128];
    i += SerDeUtil.readArray(description, _data, i);
    final var dataBlob = new byte[256];
    i += SerDeUtil.readArray(dataBlob, _data, i);
    final var endDescriptionByte = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var endDataBlob = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var endTickerByte = _data[i] & 0xFF;
    ++i;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var pad0 = new byte[2];
    SerDeUtil.readArray(pad0, _data, i);
    return new BankMetadata(_address,
                            discriminator,
                            bank,
                            placeholder,
                            ticker,
                            description,
                            dataBlob,
                            endDescriptionByte,
                            endDataBlob,
                            endTickerByte,
                            bump,
                            pad0);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    bank.write(_data, i);
    i += 32;
    putInt64LE(_data, i, placeholder);
    i += 8;
    i += SerDeUtil.writeArrayChecked(ticker, 64, _data, i);
    i += SerDeUtil.writeArrayChecked(description, 128, _data, i);
    i += SerDeUtil.writeArrayChecked(dataBlob, 256, _data, i);
    putInt16LE(_data, i, endDescriptionByte);
    i += 2;
    putInt16LE(_data, i, endDataBlob);
    i += 2;
    _data[i] = (byte) endTickerByte;
    ++i;
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeArrayChecked(pad0, 2, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
