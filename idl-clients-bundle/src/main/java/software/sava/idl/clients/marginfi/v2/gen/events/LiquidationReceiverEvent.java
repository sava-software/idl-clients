// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record LiquidationReceiverEvent(Discriminator discriminator,
                                       PublicKey marginfiAccount,
                                       PublicKey liquidationReceiver,
                                       double liquidateeAssetsSeized,
                                       double liquidateeLiabilityRepaid,
                                       int lampsFeePaid) implements MarginfiEvent {

  public static final int BYTES = 92;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(40, 131, 224, 220, 151, 83, 24, 230);

  public static final int MARGINFI_ACCOUNT_OFFSET = 8;
  public static final int LIQUIDATION_RECEIVER_OFFSET = 40;
  public static final int LIQUIDATEE_ASSETS_SEIZED_OFFSET = 72;
  public static final int LIQUIDATEE_LIABILITY_REPAID_OFFSET = 80;
  public static final int LAMPS_FEE_PAID_OFFSET = 88;

  public static LiquidationReceiverEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var marginfiAccount = readPubKey(_data, i);
    i += 32;
    final var liquidationReceiver = readPubKey(_data, i);
    i += 32;
    final var liquidateeAssetsSeized = getFloat64LE(_data, i);
    i += 8;
    final var liquidateeLiabilityRepaid = getFloat64LE(_data, i);
    i += 8;
    final var lampsFeePaid = getInt32LE(_data, i);
    return new LiquidationReceiverEvent(discriminator,
                                        marginfiAccount,
                                        liquidationReceiver,
                                        liquidateeAssetsSeized,
                                        liquidateeLiabilityRepaid,
                                        lampsFeePaid);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    marginfiAccount.write(_data, i);
    i += 32;
    liquidationReceiver.write(_data, i);
    i += 32;
    putFloat64LE(_data, i, liquidateeAssetsSeized);
    i += 8;
    putFloat64LE(_data, i, liquidateeLiabilityRepaid);
    i += 8;
    putInt32LE(_data, i, lampsFeePaid);
    i += 4;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
