// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.idl.clients.exponent.gen.types.Number;
import software.sava.idl.clients.exponent.gen.types.YieldTokenTracker;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param userYtBalance: u64
/// @param userStagedYield: u64
public record StageYieldEventV2(Discriminator discriminator,
                                PublicKey payer,
                                PublicKey vault,
                                PublicKey userYieldPosition,
                                PublicKey vaultYieldPosition,
                                Number syExchangeRate,
                                long userYtBalance,
                                long userStagedYield,
                                long unixTimestamp,
                                YieldTokenTracker userInterest,
                                YieldTokenTracker[] userEmissions) implements ExponentCoreEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(23, 7, 36, 198, 5, 216, 217, 189);

  public static final int PAYER_OFFSET = 8;
  public static final int VAULT_OFFSET = 40;
  public static final int USER_YIELD_POSITION_OFFSET = 72;
  public static final int VAULT_YIELD_POSITION_OFFSET = 104;
  public static final int SY_EXCHANGE_RATE_OFFSET = 136;
  public static final int USER_YT_BALANCE_OFFSET = 168;
  public static final int USER_STAGED_YIELD_OFFSET = 176;
  public static final int UNIX_TIMESTAMP_OFFSET = 184;
  public static final int USER_INTEREST_OFFSET = 192;
  public static final int USER_EMISSIONS_OFFSET = 232;

  public static StageYieldEventV2 read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var payer = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var userYieldPosition = readPubKey(_data, i);
    i += 32;
    final var vaultYieldPosition = readPubKey(_data, i);
    i += 32;
    final var syExchangeRate = Number.read(_data, i);
    i += syExchangeRate.l();
    final var userYtBalance = getInt64LE(_data, i);
    i += 8;
    final var userStagedYield = getInt64LE(_data, i);
    i += 8;
    final var unixTimestamp = getInt64LE(_data, i);
    i += 8;
    final var userInterest = YieldTokenTracker.read(_data, i);
    i += userInterest.l();
    final var userEmissions = SerDeUtil.readVector(4, YieldTokenTracker.class, YieldTokenTracker::read, _data, i);
    return new StageYieldEventV2(discriminator,
                                 payer,
                                 vault,
                                 userYieldPosition,
                                 vaultYieldPosition,
                                 syExchangeRate,
                                 userYtBalance,
                                 userStagedYield,
                                 unixTimestamp,
                                 userInterest,
                                 userEmissions);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    payer.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    userYieldPosition.write(_data, i);
    i += 32;
    vaultYieldPosition.write(_data, i);
    i += 32;
    i += syExchangeRate.write(_data, i);
    putInt64LE(_data, i, userYtBalance);
    i += 8;
    putInt64LE(_data, i, userStagedYield);
    i += 8;
    putInt64LE(_data, i, unixTimestamp);
    i += 8;
    i += userInterest.write(_data, i);
    i += SerDeUtil.writeVector(4, userEmissions, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 32
         + 32
         + syExchangeRate.l()
         + 8
         + 8
         + 8
         + userInterest.l()
         + SerDeUtil.lenVector(4, userEmissions);
  }
}
