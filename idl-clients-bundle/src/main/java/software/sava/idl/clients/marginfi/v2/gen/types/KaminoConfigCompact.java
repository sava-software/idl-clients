// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Used to configure Kamino banks. A simplified version of `BankConfigCompact` which omits most
/// values related to interest since Kamino banks cannot earn interest or be borrowed against.
///
/// @param depositLimit: u64
/// @param oracleSetup Either `KaminoPythPush` or `KaminoSwitchboardPull`
/// @param operationalState Bank operational state - allows starting banks in paused state
/// @param riskTier Risk tier - determines if assets can be borrowed in isolation
/// @param configFlags Config flags for future-proofing
/// @param totalAssetValueInitLimit: u64
/// @param oracleMaxAge: u16 Currently unused: Kamino's oracle age applies to kamino banks.
/// @param oracleMaxConfidence: u32 Oracle confidence threshold (0 = use default 10%)
public record KaminoConfigCompact(PublicKey oracle,
                                  WrappedI80F48 assetWeightInit,
                                  WrappedI80F48 assetWeightMaint,
                                  long depositLimit,
                                  OracleSetup oracleSetup,
                                  BankOperationalState operationalState,
                                  RiskTier riskTier,
                                  int configFlags,
                                  long totalAssetValueInitLimit,
                                  int oracleMaxAge,
                                  long oracleMaxConfidence) implements SerDe {

  public static final int BYTES = 90;

  public static final int ORACLE_OFFSET = 0;
  public static final int ASSET_WEIGHT_INIT_OFFSET = 32;
  public static final int ASSET_WEIGHT_MAINT_OFFSET = 48;
  public static final int DEPOSIT_LIMIT_OFFSET = 64;
  public static final int ORACLE_SETUP_OFFSET = 72;
  public static final int OPERATIONAL_STATE_OFFSET = 73;
  public static final int RISK_TIER_OFFSET = 74;
  public static final int CONFIG_FLAGS_OFFSET = 75;
  public static final int TOTAL_ASSET_VALUE_INIT_LIMIT_OFFSET = 76;
  public static final int ORACLE_MAX_AGE_OFFSET = 84;
  public static final int ORACLE_MAX_CONFIDENCE_OFFSET = 86;

  public static KaminoConfigCompact read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var oracle = readPubKey(_data, i);
    i += 32;
    final var assetWeightInit = WrappedI80F48.read(_data, i);
    i += assetWeightInit.l();
    final var assetWeightMaint = WrappedI80F48.read(_data, i);
    i += assetWeightMaint.l();
    final var depositLimit = getInt64LE(_data, i);
    i += 8;
    final var oracleSetup = OracleSetup.read(_data, i);
    i += oracleSetup.l();
    final var operationalState = BankOperationalState.read(_data, i);
    i += operationalState.l();
    final var riskTier = RiskTier.read(_data, i);
    i += riskTier.l();
    final var configFlags = _data[i] & 0xFF;
    ++i;
    final var totalAssetValueInitLimit = getInt64LE(_data, i);
    i += 8;
    final var oracleMaxAge = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var oracleMaxConfidence = Integer.toUnsignedLong(getInt32LE(_data, i));
    return new KaminoConfigCompact(oracle,
                                   assetWeightInit,
                                   assetWeightMaint,
                                   depositLimit,
                                   oracleSetup,
                                   operationalState,
                                   riskTier,
                                   configFlags,
                                   totalAssetValueInitLimit,
                                   oracleMaxAge,
                                   oracleMaxConfidence);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    oracle.write(_data, i);
    i += 32;
    i += assetWeightInit.write(_data, i);
    i += assetWeightMaint.write(_data, i);
    putInt64LE(_data, i, depositLimit);
    i += 8;
    i += oracleSetup.write(_data, i);
    i += operationalState.write(_data, i);
    i += riskTier.write(_data, i);
    _data[i] = (byte) configFlags;
    ++i;
    putInt64LE(_data, i, totalAssetValueInitLimit);
    i += 8;
    putInt16LE(_data, i, oracleMaxAge);
    i += 2;
    putInt32LE(_data, i, (int) oracleMaxConfidence);
    i += 4;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
