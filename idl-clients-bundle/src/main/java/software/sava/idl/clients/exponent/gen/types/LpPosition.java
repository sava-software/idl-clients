// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param owner Link to address that owns this position
/// @param market Link to market that manages the LP
/// @param lpBalance: u64 Track the LP balance of the user here
/// @param emissions Tracker for emissions earned (paid in emission tokens)
public record LpPosition(PublicKey _address,
                         Discriminator discriminator,
                         PublicKey owner,
                         PublicKey market,
                         long lpBalance,
                         PersonalYieldTrackers emissions,
                         PersonalYieldTrackers farms) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(105, 241, 37, 200, 224, 2, 252, 90);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int OWNER_OFFSET = 8;
  public static final int MARKET_OFFSET = 40;
  public static final int LP_BALANCE_OFFSET = 72;
  public static final int EMISSIONS_OFFSET = 80;

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createMarketFilter(final PublicKey market) {
    return Filter.createMemCompFilter(MARKET_OFFSET, market);
  }

  public static Filter createLpBalanceFilter(final long lpBalance) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lpBalance);
    return Filter.createMemCompFilter(LP_BALANCE_OFFSET, _data);
  }

  public static LpPosition read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static LpPosition read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static LpPosition read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static LpPosition readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static LpPosition readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a LpPosition account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], LpPosition> FACTORY = LpPosition::readChecked;

  public static LpPosition read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var market = readPubKey(_data, i);
    i += 32;
    final var lpBalance = getInt64LE(_data, i);
    i += 8;
    final var emissions = PersonalYieldTrackers.read(_data, i);
    i += emissions.l();
    final var farms = PersonalYieldTrackers.read(_data, i);
    return new LpPosition(_address,
                          discriminator,
                          owner,
                          market,
                          lpBalance,
                          emissions,
                          farms);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    market.write(_data, i);
    i += 32;
    putInt64LE(_data, i, lpBalance);
    i += 8;
    i += emissions.write(_data, i);
    i += farms.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 8
         + emissions.l()
         + farms.l();
  }
}
