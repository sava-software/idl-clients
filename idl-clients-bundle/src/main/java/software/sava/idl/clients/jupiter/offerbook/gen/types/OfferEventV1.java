// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param principalAmount: u64
/// @param remainingPrincipal: u64
/// @param collateralAmount: u64
/// @param remainingCollateral: u64
/// @param apy: u32
/// @param duration: u32
/// @param createdAt: u64
/// @param expiredAt: u64
/// @param updatedAt: u64
/// @param minFillAmount: u64
/// @param fillCounter: u64
public record OfferEventV1(PublicKey creator,
                           OfferSide side,
                           OfferStatus status,
                           EventAsset principal,
                           EventAsset collateral,
                           EventAssetFilter filter,
                           long principalAmount,
                           long remainingPrincipal,
                           long collateralAmount,
                           long remainingCollateral,
                           long apy,
                           long duration,
                           long createdAt,
                           long expiredAt,
                           long updatedAt,
                           long minFillAmount,
                           long fillCounter,
                           int allowPartialFill,
                           int bump,
                           PublicKey counteredOffer) implements SerDe {

  public static final int CREATOR_OFFSET = 0;
  public static final int SIDE_OFFSET = 32;
  public static final int STATUS_OFFSET = 33;
  public static final int PRINCIPAL_OFFSET = 34;

  public static OfferEventV1 read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var creator = readPubKey(_data, i);
    i += 32;
    final var side = OfferSide.read(_data, i);
    i += side.l();
    final var status = OfferStatus.read(_data, i);
    i += status.l();
    final var principal = EventAsset.read(_data, i);
    i += principal.l();
    final var collateral = EventAsset.read(_data, i);
    i += collateral.l();
    final var filter = EventAssetFilter.read(_data, i);
    i += filter.l();
    final var principalAmount = getInt64LE(_data, i);
    i += 8;
    final var remainingPrincipal = getInt64LE(_data, i);
    i += 8;
    final var collateralAmount = getInt64LE(_data, i);
    i += 8;
    final var remainingCollateral = getInt64LE(_data, i);
    i += 8;
    final var apy = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var duration = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var createdAt = getInt64LE(_data, i);
    i += 8;
    final var expiredAt = getInt64LE(_data, i);
    i += 8;
    final var updatedAt = getInt64LE(_data, i);
    i += 8;
    final var minFillAmount = getInt64LE(_data, i);
    i += 8;
    final var fillCounter = getInt64LE(_data, i);
    i += 8;
    final var allowPartialFill = _data[i] & 0xFF;
    ++i;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var counteredOffer = readPubKey(_data, i);
    return new OfferEventV1(creator,
                            side,
                            status,
                            principal,
                            collateral,
                            filter,
                            principalAmount,
                            remainingPrincipal,
                            collateralAmount,
                            remainingCollateral,
                            apy,
                            duration,
                            createdAt,
                            expiredAt,
                            updatedAt,
                            minFillAmount,
                            fillCounter,
                            allowPartialFill,
                            bump,
                            counteredOffer);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    creator.write(_data, i);
    i += 32;
    i += side.write(_data, i);
    i += status.write(_data, i);
    i += principal.write(_data, i);
    i += collateral.write(_data, i);
    i += filter.write(_data, i);
    putInt64LE(_data, i, principalAmount);
    i += 8;
    putInt64LE(_data, i, remainingPrincipal);
    i += 8;
    putInt64LE(_data, i, collateralAmount);
    i += 8;
    putInt64LE(_data, i, remainingCollateral);
    i += 8;
    putInt32LE(_data, i, (int) apy);
    i += 4;
    putInt32LE(_data, i, (int) duration);
    i += 4;
    putInt64LE(_data, i, createdAt);
    i += 8;
    putInt64LE(_data, i, expiredAt);
    i += 8;
    putInt64LE(_data, i, updatedAt);
    i += 8;
    putInt64LE(_data, i, minFillAmount);
    i += 8;
    putInt64LE(_data, i, fillCounter);
    i += 8;
    _data[i] = (byte) allowPartialFill;
    ++i;
    _data[i] = (byte) bump;
    ++i;
    counteredOffer.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return 32
         + side.l()
         + status.l()
         + principal.l()
         + collateral.l()
         + filter.l()
         + 8
         + 8
         + 8
         + 8
         + 4
         + 4
         + 8
         + 8
         + 8
         + 8
         + 8
         + 1
         + 1
         + 32;
  }
}
