// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.StakedSettingsEditConfig;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record EditStakedSettingsEvent(Discriminator discriminator, PublicKey group, StakedSettingsEditConfig settings) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(29, 58, 155, 191, 75, 220, 145, 206);

  public static final int GROUP_OFFSET = 8;
  public static final int SETTINGS_OFFSET = 40;

  public static EditStakedSettingsEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var group = readPubKey(_data, i);
    i += 32;
    final var settings = StakedSettingsEditConfig.read(_data, i);
    return new EditStakedSettingsEvent(discriminator, group, settings);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    group.write(_data, i);
    i += 32;
    i += settings.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32 + settings.l();
  }
}
