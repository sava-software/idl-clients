// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param owner Link to address that owns this position
/// @param vault Link to vault that manages the YT
/// @param ytBalance: u64 Track the YT balance of the user here
/// @param interest Tracker for interest earned (paid in SY)
/// @param emissions Tracker for emissions earned (paid in emission tokens)
public record YieldTokenPosition(PublicKey _address,
                                 Discriminator discriminator,
                                 PublicKey owner,
                                 PublicKey vault,
                                 long ytBalance,
                                 YieldTokenTracker interest,
                                 YieldTokenTracker[] emissions) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(227, 92, 146, 49, 29, 85, 71, 94);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int OWNER_OFFSET = 8;
  public static final int VAULT_OFFSET = 40;
  public static final int YT_BALANCE_OFFSET = 72;
  public static final int INTEREST_OFFSET = 80;
  public static final int EMISSIONS_OFFSET = 120;

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createVaultFilter(final PublicKey vault) {
    return Filter.createMemCompFilter(VAULT_OFFSET, vault);
  }

  public static Filter createYtBalanceFilter(final long ytBalance) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, ytBalance);
    return Filter.createMemCompFilter(YT_BALANCE_OFFSET, _data);
  }

  public static Filter createInterestFilter(final YieldTokenTracker interest) {
    return Filter.createMemCompFilter(INTEREST_OFFSET, interest.write());
  }

  public static YieldTokenPosition read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static YieldTokenPosition read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static YieldTokenPosition read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], YieldTokenPosition> FACTORY = YieldTokenPosition::read;

  public static YieldTokenPosition read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var ytBalance = getInt64LE(_data, i);
    i += 8;
    final var interest = YieldTokenTracker.read(_data, i);
    i += interest.l();
    final var emissions = SerDeUtil.readVector(4, YieldTokenTracker.class, YieldTokenTracker::read, _data, i);
    return new YieldTokenPosition(_address,
                                  discriminator,
                                  owner,
                                  vault,
                                  ytBalance,
                                  interest,
                                  emissions);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    putInt64LE(_data, i, ytBalance);
    i += 8;
    i += interest.write(_data, i);
    i += SerDeUtil.writeVector(4, emissions, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 8
         + interest.l()
         + SerDeUtil.lenVector(4, emissions);
  }
}
