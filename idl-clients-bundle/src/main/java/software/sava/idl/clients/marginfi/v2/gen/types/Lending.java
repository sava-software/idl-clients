// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Minimal representation of the on-chain JupLend `Lending` account.
/// 
/// Notes:
/// - We intentionally use a **zero-copy** layout here to match how other integrations load large
/// external accounts (and to avoid paying Borsh (de)serialization cost on every access).
/// - `repr(C, packed)` keeps the byte layout identical to a field-by-field serialization
/// (i.e. no implicit padding). This is important because `Pubkey` has alignment=1 while `u64`
/// has alignment=8; using plain `repr(C)` would insert padding before the first `u64`.
///
/// @param lendingId: u16
/// @param decimals: u8 number of decimals for the fToken, same as underlying mint
/// @param rewardsRateModel PDA of rewards rate model (LRRM)
/// @param liquidityExchangePrice: u64 exchange price in the liquidity layer (no rewards)
/// @param tokenExchangePrice: u64 exchange price between fToken and underlying (with rewards)
/// @param lastUpdateTimestamp: u64 unix timestamp when exchange prices were updated last
/// @param bump: u8
public record Lending(PublicKey mint,
                      PublicKey fTokenMint,
                      int lendingId,
                      int decimals,
                      PublicKey rewardsRateModel,
                      long liquidityExchangePrice,
                      long tokenExchangePrice,
                      long lastUpdateTimestamp,
                      PublicKey tokenReservesLiquidity,
                      PublicKey supplyPositionOnLiquidity,
                      int bump) implements SerDe {

  public static final int BYTES = 188;

  public static final int MINT_OFFSET = 0;
  public static final int F_TOKEN_MINT_OFFSET = 32;
  public static final int LENDING_ID_OFFSET = 64;
  public static final int DECIMALS_OFFSET = 66;
  public static final int REWARDS_RATE_MODEL_OFFSET = 67;
  public static final int LIQUIDITY_EXCHANGE_PRICE_OFFSET = 99;
  public static final int TOKEN_EXCHANGE_PRICE_OFFSET = 107;
  public static final int LAST_UPDATE_TIMESTAMP_OFFSET = 115;
  public static final int TOKEN_RESERVES_LIQUIDITY_OFFSET = 123;
  public static final int SUPPLY_POSITION_ON_LIQUIDITY_OFFSET = 155;
  public static final int BUMP_OFFSET = 187;

  public static Lending read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var fTokenMint = readPubKey(_data, i);
    i += 32;
    final var lendingId = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var decimals = _data[i] & 0xFF;
    ++i;
    final var rewardsRateModel = readPubKey(_data, i);
    i += 32;
    final var liquidityExchangePrice = getInt64LE(_data, i);
    i += 8;
    final var tokenExchangePrice = getInt64LE(_data, i);
    i += 8;
    final var lastUpdateTimestamp = getInt64LE(_data, i);
    i += 8;
    final var tokenReservesLiquidity = readPubKey(_data, i);
    i += 32;
    final var supplyPositionOnLiquidity = readPubKey(_data, i);
    i += 32;
    final var bump = _data[i] & 0xFF;
    return new Lending(mint,
                       fTokenMint,
                       lendingId,
                       decimals,
                       rewardsRateModel,
                       liquidityExchangePrice,
                       tokenExchangePrice,
                       lastUpdateTimestamp,
                       tokenReservesLiquidity,
                       supplyPositionOnLiquidity,
                       bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    mint.write(_data, i);
    i += 32;
    fTokenMint.write(_data, i);
    i += 32;
    putInt16LE(_data, i, lendingId);
    i += 2;
    _data[i] = (byte) decimals;
    ++i;
    rewardsRateModel.write(_data, i);
    i += 32;
    putInt64LE(_data, i, liquidityExchangePrice);
    i += 8;
    putInt64LE(_data, i, tokenExchangePrice);
    i += 8;
    putInt64LE(_data, i, lastUpdateTimestamp);
    i += 8;
    tokenReservesLiquidity.write(_data, i);
    i += 32;
    supplyPositionOnLiquidity.write(_data, i);
    i += 32;
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
