// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param emissionIndex: u16
/// @param amountCollected: u64
public record MarketCollectEmissionEvent(Discriminator discriminator,
                                         PublicKey owner,
                                         PublicKey market,
                                         PublicKey lpPosition,
                                         PublicKey tokenEmissionEscrow,
                                         PublicKey tokenEmissionDst,
                                         int emissionIndex,
                                         long amountCollected,
                                         long timestamp) implements ExponentCoreEvent {

  public static final int BYTES = 186;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(125, 129, 15, 139, 232, 244, 82, 67);

  public static final int OWNER_OFFSET = 8;
  public static final int MARKET_OFFSET = 40;
  public static final int LP_POSITION_OFFSET = 72;
  public static final int TOKEN_EMISSION_ESCROW_OFFSET = 104;
  public static final int TOKEN_EMISSION_DST_OFFSET = 136;
  public static final int EMISSION_INDEX_OFFSET = 168;
  public static final int AMOUNT_COLLECTED_OFFSET = 170;
  public static final int TIMESTAMP_OFFSET = 178;

  public static MarketCollectEmissionEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var market = readPubKey(_data, i);
    i += 32;
    final var lpPosition = readPubKey(_data, i);
    i += 32;
    final var tokenEmissionEscrow = readPubKey(_data, i);
    i += 32;
    final var tokenEmissionDst = readPubKey(_data, i);
    i += 32;
    final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var amountCollected = getInt64LE(_data, i);
    i += 8;
    final var timestamp = getInt64LE(_data, i);
    return new MarketCollectEmissionEvent(discriminator,
                                          owner,
                                          market,
                                          lpPosition,
                                          tokenEmissionEscrow,
                                          tokenEmissionDst,
                                          emissionIndex,
                                          amountCollected,
                                          timestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    market.write(_data, i);
    i += 32;
    lpPosition.write(_data, i);
    i += 32;
    tokenEmissionEscrow.write(_data, i);
    i += 32;
    tokenEmissionDst.write(_data, i);
    i += 32;
    putInt16LE(_data, i, emissionIndex);
    i += 2;
    putInt64LE(_data, i, amountCollected);
    i += 8;
    putInt64LE(_data, i, timestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
