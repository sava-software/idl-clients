// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param currentStakedAmountLamports: u64
/// @param totalStakingRewardsLamports: u64
/// @param stakeAccountIndex: u64
/// @param bump: u8
public record StakeInfo(PublicKey _address,
                        Discriminator discriminator,
                        PublicKey pool,
                        PublicKey stakeAccount,
                        long currentStakedAmountLamports,
                        long totalStakingRewardsLamports,
                        long lastUpdatedAt,
                        boolean deactivating,
                        long stakeAccountIndex,
                        int bump) implements SerDe {

  public static final int BYTES = 106;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(66, 62, 68, 70, 108, 179, 183, 235);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int POOL_OFFSET = 8;
  public static final int STAKE_ACCOUNT_OFFSET = 40;
  public static final int CURRENT_STAKED_AMOUNT_LAMPORTS_OFFSET = 72;
  public static final int TOTAL_STAKING_REWARDS_LAMPORTS_OFFSET = 80;
  public static final int LAST_UPDATED_AT_OFFSET = 88;
  public static final int DEACTIVATING_OFFSET = 96;
  public static final int STAKE_ACCOUNT_INDEX_OFFSET = 97;
  public static final int BUMP_OFFSET = 105;

  public static Filter createPoolFilter(final PublicKey pool) {
    return Filter.createMemCompFilter(POOL_OFFSET, pool);
  }

  public static Filter createStakeAccountFilter(final PublicKey stakeAccount) {
    return Filter.createMemCompFilter(STAKE_ACCOUNT_OFFSET, stakeAccount);
  }

  public static Filter createCurrentStakedAmountLamportsFilter(final long currentStakedAmountLamports) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, currentStakedAmountLamports);
    return Filter.createMemCompFilter(CURRENT_STAKED_AMOUNT_LAMPORTS_OFFSET, _data);
  }

  public static Filter createTotalStakingRewardsLamportsFilter(final long totalStakingRewardsLamports) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, totalStakingRewardsLamports);
    return Filter.createMemCompFilter(TOTAL_STAKING_REWARDS_LAMPORTS_OFFSET, _data);
  }

  public static Filter createLastUpdatedAtFilter(final long lastUpdatedAt) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lastUpdatedAt);
    return Filter.createMemCompFilter(LAST_UPDATED_AT_OFFSET, _data);
  }

  public static Filter createDeactivatingFilter(final boolean deactivating) {
    return Filter.createMemCompFilter(DEACTIVATING_OFFSET, new byte[]{(byte) (deactivating ? 1 : 0)});
  }

  public static Filter createStakeAccountIndexFilter(final long stakeAccountIndex) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, stakeAccountIndex);
    return Filter.createMemCompFilter(STAKE_ACCOUNT_INDEX_OFFSET, _data);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static StakeInfo read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static StakeInfo read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static StakeInfo read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], StakeInfo> FACTORY = StakeInfo::read;

  public static StakeInfo read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var pool = readPubKey(_data, i);
    i += 32;
    final var stakeAccount = readPubKey(_data, i);
    i += 32;
    final var currentStakedAmountLamports = getInt64LE(_data, i);
    i += 8;
    final var totalStakingRewardsLamports = getInt64LE(_data, i);
    i += 8;
    final var lastUpdatedAt = getInt64LE(_data, i);
    i += 8;
    final var deactivating = _data[i] == 1;
    ++i;
    final var stakeAccountIndex = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    return new StakeInfo(_address,
                         discriminator,
                         pool,
                         stakeAccount,
                         currentStakedAmountLamports,
                         totalStakingRewardsLamports,
                         lastUpdatedAt,
                         deactivating,
                         stakeAccountIndex,
                         bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    pool.write(_data, i);
    i += 32;
    stakeAccount.write(_data, i);
    i += 32;
    putInt64LE(_data, i, currentStakedAmountLamports);
    i += 8;
    putInt64LE(_data, i, totalStakingRewardsLamports);
    i += 8;
    putInt64LE(_data, i, lastUpdatedAt);
    i += 8;
    _data[i] = (byte) (deactivating ? 1 : 0);
    ++i;
    putInt64LE(_data, i, stakeAccountIndex);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
