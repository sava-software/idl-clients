// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amountPyIn: u64
/// @param amountSyRedeemed: u64
/// @param amountBaseOut: u64
public record WrapperMergeEvent(Discriminator discriminator,
                                PublicKey userAddress,
                                PublicKey vaultAddress,
                                long amountPyIn,
                                long amountSyRedeemed,
                                long amountBaseOut) implements ExponentCoreEvent {

  public static final int BYTES = 96;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(97, 26, 173, 147, 157, 218, 37, 102);

  public static final int USER_ADDRESS_OFFSET = 8;
  public static final int VAULT_ADDRESS_OFFSET = 40;
  public static final int AMOUNT_PY_IN_OFFSET = 72;
  public static final int AMOUNT_SY_REDEEMED_OFFSET = 80;
  public static final int AMOUNT_BASE_OUT_OFFSET = 88;

  public static WrapperMergeEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var userAddress = readPubKey(_data, i);
    i += 32;
    final var vaultAddress = readPubKey(_data, i);
    i += 32;
    final var amountPyIn = getInt64LE(_data, i);
    i += 8;
    final var amountSyRedeemed = getInt64LE(_data, i);
    i += 8;
    final var amountBaseOut = getInt64LE(_data, i);
    return new WrapperMergeEvent(discriminator,
                                 userAddress,
                                 vaultAddress,
                                 amountPyIn,
                                 amountSyRedeemed,
                                 amountBaseOut);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    userAddress.write(_data, i);
    i += 32;
    vaultAddress.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amountPyIn);
    i += 8;
    putInt64LE(_data, i, amountSyRedeemed);
    i += 8;
    putInt64LE(_data, i, amountBaseOut);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
