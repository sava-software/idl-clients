// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// Read-only archive of same-asset-emode banks. Enables the emode admin to see, at a glance, which
/// banks are participating in same-asset-emode.
///
/// @param padding0: u64
/// @param key This registry's own key.
/// @param group Group for which this registry applies.
/// @param bankCount: u16
/// @param groupCount: u8
/// @param bump: u8
/// @param groups Describes the same-asset-emode groupings that exist
/// @param banks Describes which bank belongs to which group
public record SameAssetEmodeRegistry(PublicKey _address,
                                     Discriminator discriminator,
                                     long padding0,
                                     PublicKey key,
                                     PublicKey group,
                                     int bankCount,
                                     int groupCount,
                                     int bump,
                                     byte[] padding1,
                                     SameAssetEmodeGroup[] groups,
                                     SameAssetEmodeBank[] banks,
                                     byte[] padding2,
                                     byte[] padding3,
                                     byte[] padding4) implements SerDe {

  public static final int BYTES = 9048;
  public static final int PADDING_1_LEN = 4;
  public static final int GROUPS_LEN = 32;
  public static final int BANKS_LEN = 128;
  public static final int PADDING_2_LEN = 1024;
  public static final int PADDING_3_LEN = 512;
  public static final int PADDING_4_LEN = 256;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(222, 21, 195, 149, 193, 72, 219, 31);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PADDING_0_OFFSET = 8;
  public static final int KEY_OFFSET = 16;
  public static final int GROUP_OFFSET = 48;
  public static final int BANK_COUNT_OFFSET = 80;
  public static final int GROUP_COUNT_OFFSET = 82;
  public static final int BUMP_OFFSET = 83;
  public static final int PADDING_1_OFFSET = 84;
  public static final int GROUPS_OFFSET = 88;
  public static final int BANKS_OFFSET = 2136;
  public static final int PADDING_2_OFFSET = 7256;
  public static final int PADDING_3_OFFSET = 8280;
  public static final int PADDING_4_OFFSET = 8792;

  public static Filter createPadding0Filter(final long padding0) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, padding0);
    return Filter.createMemCompFilter(PADDING_0_OFFSET, _data);
  }

  public static Filter createKeyFilter(final PublicKey key) {
    return Filter.createMemCompFilter(KEY_OFFSET, key);
  }

  public static Filter createGroupFilter(final PublicKey group) {
    return Filter.createMemCompFilter(GROUP_OFFSET, group);
  }

  public static Filter createBankCountFilter(final int bankCount) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, bankCount);
    return Filter.createMemCompFilter(BANK_COUNT_OFFSET, _data);
  }

  public static Filter createGroupCountFilter(final int groupCount) {
    return Filter.createMemCompFilter(GROUP_COUNT_OFFSET, new byte[]{(byte) groupCount});
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static SameAssetEmodeRegistry read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static SameAssetEmodeRegistry read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static SameAssetEmodeRegistry read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static SameAssetEmodeRegistry readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static SameAssetEmodeRegistry readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a SameAssetEmodeRegistry account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], SameAssetEmodeRegistry> FACTORY = SameAssetEmodeRegistry::readChecked;

  public static SameAssetEmodeRegistry read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var padding0 = getInt64LE(_data, i);
    i += 8;
    final var key = readPubKey(_data, i);
    i += 32;
    final var group = readPubKey(_data, i);
    i += 32;
    final var bankCount = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var groupCount = _data[i] & 0xFF;
    ++i;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var padding1 = new byte[4];
    i += SerDeUtil.readArray(padding1, _data, i);
    final var groups = new SameAssetEmodeGroup[32];
    i += SerDeUtil.readArray(groups, SameAssetEmodeGroup::read, _data, i);
    final var banks = new SameAssetEmodeBank[128];
    i += SerDeUtil.readArray(banks, SameAssetEmodeBank::read, _data, i);
    final var padding2 = new byte[1024];
    i += SerDeUtil.readArray(padding2, _data, i);
    final var padding3 = new byte[512];
    i += SerDeUtil.readArray(padding3, _data, i);
    final var padding4 = new byte[256];
    SerDeUtil.readArray(padding4, _data, i);
    return new SameAssetEmodeRegistry(_address,
                                      discriminator,
                                      padding0,
                                      key,
                                      group,
                                      bankCount,
                                      groupCount,
                                      bump,
                                      padding1,
                                      groups,
                                      banks,
                                      padding2,
                                      padding3,
                                      padding4);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt64LE(_data, i, padding0);
    i += 8;
    key.write(_data, i);
    i += 32;
    group.write(_data, i);
    i += 32;
    putInt16LE(_data, i, bankCount);
    i += 2;
    _data[i] = (byte) groupCount;
    ++i;
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeArrayChecked(padding1, 4, _data, i);
    i += SerDeUtil.writeArrayChecked(groups, 32, _data, i);
    i += SerDeUtil.writeArrayChecked(banks, 128, _data, i);
    i += SerDeUtil.writeArrayChecked(padding2, 1024, _data, i);
    i += SerDeUtil.writeArrayChecked(padding3, 512, _data, i);
    i += SerDeUtil.writeArrayChecked(padding4, 256, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
