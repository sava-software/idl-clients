// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.squads.v4.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;

/// @param bufferIndex: u8 Index of the buffer account to seed the account derivation
/// @param vaultIndex: u8 Index of the vault this transaction belongs to.
/// @param finalBufferHash Hash of the final assembled transaction message.
/// @param finalBufferSize: u16 Final size of the buffer.
/// @param buffer Initial slice of the buffer.
public record TransactionBufferCreateArgs(int bufferIndex,
                                          int vaultIndex,
                                          byte[] finalBufferHash,
                                          int finalBufferSize,
                                          byte[] buffer) implements SerDe {

  public static final int FINAL_BUFFER_HASH_LEN = 32;
  public static final int BUFFER_INDEX_OFFSET = 0;
  public static final int VAULT_INDEX_OFFSET = 1;
  public static final int FINAL_BUFFER_HASH_OFFSET = 2;
  public static final int FINAL_BUFFER_SIZE_OFFSET = 34;
  public static final int BUFFER_OFFSET = 36;

  public static TransactionBufferCreateArgs read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var bufferIndex = _data[i] & 0xFF;
    ++i;
    final var vaultIndex = _data[i] & 0xFF;
    ++i;
    final var finalBufferHash = new byte[32];
    i += SerDeUtil.readArray(finalBufferHash, _data, i);
    final var finalBufferSize = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var buffer = SerDeUtil.readbyteVector(4, _data, i);
    return new TransactionBufferCreateArgs(bufferIndex,
                                           vaultIndex,
                                           finalBufferHash,
                                           finalBufferSize,
                                           buffer);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    _data[i] = (byte) bufferIndex;
    ++i;
    _data[i] = (byte) vaultIndex;
    ++i;
    i += SerDeUtil.writeArrayChecked(finalBufferHash, 32, _data, i);
    putInt16LE(_data, i, finalBufferSize);
    i += 2;
    i += SerDeUtil.writeVector(4, buffer, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 1
         + 1
         + SerDeUtil.lenArray(finalBufferHash)
         + 2
         + SerDeUtil.lenVector(4, buffer);
  }
}
