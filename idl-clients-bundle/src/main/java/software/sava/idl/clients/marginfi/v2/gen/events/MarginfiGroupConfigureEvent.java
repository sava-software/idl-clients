// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.idl.clients.marginfi.v2.gen.types.GroupEventHeader;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param flags: u64
public record MarginfiGroupConfigureEvent(Discriminator discriminator,
                                          GroupEventHeader header,
                                          PublicKey admin,
                                          long flags) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(241, 104, 172, 167, 41, 195, 199, 170);

  public static final int HEADER_OFFSET = 8;

  public static MarginfiGroupConfigureEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = GroupEventHeader.read(_data, i);
    i += header.l();
    final PublicKey admin;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      admin = null;
      ++i;
    } else {
      ++i;
      admin = readPubKey(_data, i);
      i += 32;
    }
    final var flags = getInt64LE(_data, i);
    return new MarginfiGroupConfigureEvent(discriminator, header, admin, flags);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    i += SerDeUtil.writeOptional(1, admin, _data, i);
    putInt64LE(_data, i, flags);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l() + (admin == null ? 1 : (1 + 32)) + 8;
  }
}
