// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import java.math.BigInteger;

import static software.sava.core.encoding.ByteUtil.getInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;

/// Smart-side amounts for one imperfect DEX operate leg.
/// `token0` / `token1` — primary token amounts (positive = deposit/borrow, negative = withdraw/payback).
/// `shares_min_max`     — slippage: min shares to receive (deposit/borrow) or max shares to burn (withdraw/payback).
///
public record OperateDexAmounts(BigInteger token0,
                                BigInteger token1,
                                BigInteger sharesMinMax) implements SerDe {

  public static final int BYTES = 48;

  public static final int TOKEN_0_OFFSET = 0;
  public static final int TOKEN_1_OFFSET = 16;
  public static final int SHARES_MIN_MAX_OFFSET = 32;

  public static OperateDexAmounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var token0 = getInt128LE(_data, i);
    i += 16;
    final var token1 = getInt128LE(_data, i);
    i += 16;
    final var sharesMinMax = getInt128LE(_data, i);
    return new OperateDexAmounts(token0, token1, sharesMinMax);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt128LE(_data, i, token0);
    i += 16;
    putInt128LE(_data, i, token1);
    i += 16;
    putInt128LE(_data, i, sharesMinMax);
    i += 16;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
