// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.jupiter.offerbook.gen.types.ReferralType;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amount: u64
/// @param timestamp: u64
public record ReferralReward(Discriminator discriminator,
                             PublicKey referrer,
                             PublicKey referee,
                             PublicKey mint,
                             long amount,
                             long timestamp,
                             ReferralType referralType) implements OfferbookEvent {

  public static final int BYTES = 121;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(30, 218, 84, 175, 188, 85, 219, 99);

  public static final int REFERRER_OFFSET = 8;
  public static final int REFEREE_OFFSET = 40;
  public static final int MINT_OFFSET = 72;
  public static final int AMOUNT_OFFSET = 104;
  public static final int TIMESTAMP_OFFSET = 112;
  public static final int REFERRAL_TYPE_OFFSET = 120;

  public static ReferralReward read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var referrer = readPubKey(_data, i);
    i += 32;
    final var referee = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var timestamp = getInt64LE(_data, i);
    i += 8;
    final var referralType = ReferralType.read(_data, i);
    return new ReferralReward(discriminator,
                              referrer,
                              referee,
                              mint,
                              amount,
                              timestamp,
                              referralType);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    referrer.write(_data, i);
    i += 32;
    referee.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    putInt64LE(_data, i, timestamp);
    i += 8;
    i += referralType.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
