// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.Arrays;
import java.util.OptionalLong;
import java.util.function.BiFunction;

import static java.nio.charset.StandardCharsets.UTF_8;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param seq: Option<u64>
public record BaseAssetV1(PublicKey _address,
                          Key key,
                          PublicKey owner,
                          UpdateAuthority updateAuthority,
                          String name, byte[] _name,
                          String uri, byte[] _uri,
                          OptionalLong seq) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(1);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int KEY_OFFSET = 0;
  public static final int OWNER_OFFSET = 1;
  public static final int UPDATE_AUTHORITY_OFFSET = 33;

  public static Filter createKeyFilter(final Key key) {
    return Filter.createMemCompFilter(KEY_OFFSET, key.write());
  }

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createUpdateAuthorityFilter(final UpdateAuthority updateAuthority) {
    return Filter.createMemCompFilter(UPDATE_AUTHORITY_OFFSET, updateAuthority.write());
  }

  public static BaseAssetV1 createRecord(final PublicKey _address,
                                         final Key key,
                                         final PublicKey owner,
                                         final UpdateAuthority updateAuthority,
                                         final String name,
                                         final String uri,
                                         final OptionalLong seq) {
    return new BaseAssetV1(_address,
                           key,
                           owner,
                           updateAuthority,
                           name, name == null ? null : name.getBytes(UTF_8),
                           uri, uri == null ? null : uri.getBytes(UTF_8),
                           seq);
  }

  public static BaseAssetV1 read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static BaseAssetV1 read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static BaseAssetV1 read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], BaseAssetV1> FACTORY = BaseAssetV1::read;

  public static BaseAssetV1 read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }

    int i = _offset;
    final var key = Key.read(_data, i);
    i += key.l();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var updateAuthority = UpdateAuthority.read(_data, i);
    i += updateAuthority.l();
    final int _nameLength = getInt32LE(_data, i);
    i += 4;
    final byte[] _name = Arrays.copyOfRange(_data, i, i + _nameLength);
    final var name = new String(_name, UTF_8);
    i += _name.length;
    final int _uriLength = getInt32LE(_data, i);
    i += 4;
    final byte[] _uri = Arrays.copyOfRange(_data, i, i + _uriLength);
    final var uri = new String(_uri, UTF_8);
    i += _uri.length;
    final OptionalLong seq;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      seq = OptionalLong.empty();
    } else {
      ++i;
      seq = OptionalLong.of(getInt64LE(_data, i));
    }
    return new BaseAssetV1(_address,
                           key,
                           owner,
                           updateAuthority,
                           name, name == null ? null : name.getBytes(UTF_8),
                           uri, uri == null ? null : uri.getBytes(UTF_8),
                           seq);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += key.write(_data, i);
    owner.write(_data, i);
    i += 32;
    i += updateAuthority.write(_data, i);
    i += SerDeUtil.writeVector(4, _name, _data, i);
    i += SerDeUtil.writeVector(4, _uri, _data, i);
    i += SerDeUtil.writeOptional(1, seq, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return key.l()
         + 32
         + updateAuthority.l()
         + 4 + _name.length
         + 4 + _uri.length
         + (seq == null || seq.isEmpty() ? 1 : (1 + 8));
  }
}
