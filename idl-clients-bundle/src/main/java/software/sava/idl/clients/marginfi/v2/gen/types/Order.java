// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param placeholder: u64 Reserved for future use
/// @param maxSlippage: u32 * a %, as u32, out of 100%, e.g. 50% = .5 * u32::MAX
/// @param tags: u16[] Active tags (currently 2). Remaining capacity is stored in padding for layout compatibility.
///             Padding byte `ORDER_TAG_PADDING - 1` stores the tag count for forward compatibility. (u16 *
///             2 = 4 bytes)
/// @param trigger Stop Loss (0), Take Profit (1), or Both (2)
/// @param bump: u8 Bump to derive this pda
public record Order(PublicKey _address,
                    Discriminator discriminator,
                    PublicKey marginfiAccount,
                    WrappedI80F48 stopLoss,
                    WrappedI80F48 takeProfit,
                    long placeholder,
                    long maxSlippage,
                    byte[] pad0,
                    int[] tags,
                    byte[] pad1,
                    byte[] tagsPadding,
                    OrderTriggerType trigger,
                    int bump,
                    byte[] pad2,
                    byte[][] reserved1) implements SerDe {

  public static final int BYTES = 264;
  public static final int PAD_0_LEN = 4;
  public static final int TAGS_LEN = 2;
  public static final int PAD_1_LEN = 4;
  public static final int TAGS_PADDING_LEN = 32;
  public static final int PAD_2_LEN = 6;
  public static final int RESERVED_1_LEN = 4;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(134, 173, 223, 185, 77, 86, 28, 51);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int MARGINFI_ACCOUNT_OFFSET = 8;
  public static final int STOP_LOSS_OFFSET = 40;
  public static final int TAKE_PROFIT_OFFSET = 56;
  public static final int PLACEHOLDER_OFFSET = 72;
  public static final int MAX_SLIPPAGE_OFFSET = 80;
  public static final int PAD_0_OFFSET = 84;
  public static final int TAGS_OFFSET = 88;
  public static final int PAD_1_OFFSET = 92;
  public static final int TAGS_PADDING_OFFSET = 96;
  public static final int TRIGGER_OFFSET = 128;
  public static final int BUMP_OFFSET = 129;
  public static final int PAD_2_OFFSET = 130;
  public static final int RESERVED_1_OFFSET = 136;

  public static Filter createMarginfiAccountFilter(final PublicKey marginfiAccount) {
    return Filter.createMemCompFilter(MARGINFI_ACCOUNT_OFFSET, marginfiAccount);
  }

  public static Filter createStopLossFilter(final WrappedI80F48 stopLoss) {
    return Filter.createMemCompFilter(STOP_LOSS_OFFSET, stopLoss.write());
  }

  public static Filter createTakeProfitFilter(final WrappedI80F48 takeProfit) {
    return Filter.createMemCompFilter(TAKE_PROFIT_OFFSET, takeProfit.write());
  }

  public static Filter createPlaceholderFilter(final long placeholder) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, placeholder);
    return Filter.createMemCompFilter(PLACEHOLDER_OFFSET, _data);
  }

  public static Filter createMaxSlippageFilter(final long maxSlippage) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) maxSlippage);
    return Filter.createMemCompFilter(MAX_SLIPPAGE_OFFSET, _data);
  }

  public static Filter createTriggerFilter(final OrderTriggerType trigger) {
    return Filter.createMemCompFilter(TRIGGER_OFFSET, trigger.write());
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Order read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Order read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Order read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Order> FACTORY = Order::read;

  public static Order read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var marginfiAccount = readPubKey(_data, i);
    i += 32;
    final var stopLoss = WrappedI80F48.read(_data, i);
    i += stopLoss.l();
    final var takeProfit = WrappedI80F48.read(_data, i);
    i += takeProfit.l();
    final var placeholder = getInt64LE(_data, i);
    i += 8;
    final var maxSlippage = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var pad0 = new byte[4];
    i += SerDeUtil.readArray(pad0, _data, i);
    final var tags = new int[2];
    i += SerDeUtil.readUnsignedShortArray(tags, _data, i);
    final var pad1 = new byte[4];
    i += SerDeUtil.readArray(pad1, _data, i);
    final var tagsPadding = new byte[32];
    i += SerDeUtil.readArray(tagsPadding, _data, i);
    final var trigger = OrderTriggerType.read(_data, i);
    i += trigger.l();
    final var bump = _data[i] & 0xFF;
    ++i;
    final var pad2 = new byte[6];
    i += SerDeUtil.readArray(pad2, _data, i);
    final var reserved1 = new byte[4][32];
    SerDeUtil.readArray(reserved1, _data, i);
    return new Order(_address,
                     discriminator,
                     marginfiAccount,
                     stopLoss,
                     takeProfit,
                     placeholder,
                     maxSlippage,
                     pad0,
                     tags,
                     pad1,
                     tagsPadding,
                     trigger,
                     bump,
                     pad2,
                     reserved1);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    marginfiAccount.write(_data, i);
    i += 32;
    i += stopLoss.write(_data, i);
    i += takeProfit.write(_data, i);
    putInt64LE(_data, i, placeholder);
    i += 8;
    putInt32LE(_data, i, (int) maxSlippage);
    i += 4;
    i += SerDeUtil.writeArrayChecked(pad0, 4, _data, i);
    i += SerDeUtil.writeUnsignedShortArrayChecked(tags, 2, _data, i);
    i += SerDeUtil.writeArrayChecked(pad1, 4, _data, i);
    i += SerDeUtil.writeArrayChecked(tagsPadding, 32, _data, i);
    i += trigger.write(_data, i);
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeArrayChecked(pad2, 6, _data, i);
    i += SerDeUtil.writeArrayChecked(reserved1, 4, 32, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
