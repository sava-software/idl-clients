// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.marginfi.v2.gen.types.AccountEventHeader;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record MarginfiAccountTransferToNewAccount(Discriminator discriminator,
                                                  AccountEventHeader header,
                                                  PublicKey oldAccount,
                                                  PublicKey oldAccountAuthority,
                                                  PublicKey newAccountAuthority) implements MarginfiEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(59, 105, 171, 110, 223, 136, 80, 89);

  public static final int HEADER_OFFSET = 8;

  public static MarginfiAccountTransferToNewAccount read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var header = AccountEventHeader.read(_data, i);
    i += header.l();
    final var oldAccount = readPubKey(_data, i);
    i += 32;
    final var oldAccountAuthority = readPubKey(_data, i);
    i += 32;
    final var newAccountAuthority = readPubKey(_data, i);
    return new MarginfiAccountTransferToNewAccount(discriminator,
                                                   header,
                                                   oldAccount,
                                                   oldAccountAuthority,
                                                   newAccountAuthority);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += header.write(_data, i);
    oldAccount.write(_data, i);
    i += 32;
    oldAccountAuthority.write(_data, i);
    i += 32;
    newAccountAuthority.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + header.l() + 32 + 32 + 32;
  }
}
