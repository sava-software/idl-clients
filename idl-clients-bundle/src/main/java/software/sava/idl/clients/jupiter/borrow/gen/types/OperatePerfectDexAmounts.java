// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import java.math.BigInteger;

import static software.sava.core.encoding.ByteUtil.getInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;

/// Smart-side amounts for one perfect DEX operate leg.
/// `perfect_shares`  — primary: shares to operate (positive = deposit/borrow, negative = withdraw/payback).
/// Pass `i128::MIN` to request max-withdrawal / max-payback.
/// `token0_min_max`  — slippage bound for token0:
/// deposit/payback → max tokens to spend  (positive for deposit, negative for payback)
/// withdraw/borrow → min tokens to receive (negative for withdraw, positive for borrow)
/// Set to 0 to receive the full withdrawal / payback in the *other* token only.
/// `token1_min_max`  — same semantics as `token0_min_max` for token1.
///
public record OperatePerfectDexAmounts(BigInteger perfectShares,
                                       BigInteger token0MinMax,
                                       BigInteger token1MinMax) implements SerDe {

  public static final int BYTES = 48;

  public static final int PERFECT_SHARES_OFFSET = 0;
  public static final int TOKEN_0_MIN_MAX_OFFSET = 16;
  public static final int TOKEN_1_MIN_MAX_OFFSET = 32;

  public static OperatePerfectDexAmounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var perfectShares = getInt128LE(_data, i);
    i += 16;
    final var token0MinMax = getInt128LE(_data, i);
    i += 16;
    final var token1MinMax = getInt128LE(_data, i);
    return new OperatePerfectDexAmounts(perfectShares, token0MinMax, token1MinMax);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt128LE(_data, i, perfectShares);
    i += 16;
    putInt128LE(_data, i, token0MinMax);
    i += 16;
    putInt128LE(_data, i, token1MinMax);
    i += 16;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
