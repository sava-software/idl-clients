// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.doves.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param price: u64
/// @param expo: i8
/// @param bump: u8
public record AgPriceFeed(PublicKey _address,
                          Discriminator discriminator,
                          PublicKey mint,
                          PublicKey edgeFeed,
                          PublicKey clFeed,
                          PublicKey pythFeed,
                          byte[] pythFeedId,
                          long price,
                          int expo,
                          long timestamp,
                          Config config,
                          int bump,
                          PublicKey pythLazerFeed,
                          PublicKey redstoneFeed) implements SerDe {

  public static final int BYTES = 266;
  public static final int PYTH_FEED_ID_LEN = 32;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(112, 249, 139, 217, 215, 208, 249, 54);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int MINT_OFFSET = 8;
  public static final int EDGE_FEED_OFFSET = 40;
  public static final int CL_FEED_OFFSET = 72;
  public static final int PYTH_FEED_OFFSET = 104;
  public static final int PYTH_FEED_ID_OFFSET = 136;
  public static final int PRICE_OFFSET = 168;
  public static final int EXPO_OFFSET = 176;
  public static final int TIMESTAMP_OFFSET = 177;
  public static final int CONFIG_OFFSET = 185;
  public static final int BUMP_OFFSET = 201;
  public static final int PYTH_LAZER_FEED_OFFSET = 202;
  public static final int REDSTONE_FEED_OFFSET = 234;

  public static Filter createMintFilter(final PublicKey mint) {
    return Filter.createMemCompFilter(MINT_OFFSET, mint);
  }

  public static Filter createEdgeFeedFilter(final PublicKey edgeFeed) {
    return Filter.createMemCompFilter(EDGE_FEED_OFFSET, edgeFeed);
  }

  public static Filter createClFeedFilter(final PublicKey clFeed) {
    return Filter.createMemCompFilter(CL_FEED_OFFSET, clFeed);
  }

  public static Filter createPythFeedFilter(final PublicKey pythFeed) {
    return Filter.createMemCompFilter(PYTH_FEED_OFFSET, pythFeed);
  }

  public static Filter createPriceFilter(final long price) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, price);
    return Filter.createMemCompFilter(PRICE_OFFSET, _data);
  }

  public static Filter createExpoFilter(final int expo) {
    return Filter.createMemCompFilter(EXPO_OFFSET, new byte[]{(byte) expo});
  }

  public static Filter createTimestampFilter(final long timestamp) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, timestamp);
    return Filter.createMemCompFilter(TIMESTAMP_OFFSET, _data);
  }

  public static Filter createConfigFilter(final Config config) {
    return Filter.createMemCompFilter(CONFIG_OFFSET, config.write());
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Filter createPythLazerFeedFilter(final PublicKey pythLazerFeed) {
    return Filter.createMemCompFilter(PYTH_LAZER_FEED_OFFSET, pythLazerFeed);
  }

  public static Filter createRedstoneFeedFilter(final PublicKey redstoneFeed) {
    return Filter.createMemCompFilter(REDSTONE_FEED_OFFSET, redstoneFeed);
  }

  public static AgPriceFeed read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static AgPriceFeed read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static AgPriceFeed read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], AgPriceFeed> FACTORY = AgPriceFeed::read;

  public static AgPriceFeed read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var mint = readPubKey(_data, i);
    i += 32;
    final var edgeFeed = readPubKey(_data, i);
    i += 32;
    final var clFeed = readPubKey(_data, i);
    i += 32;
    final var pythFeed = readPubKey(_data, i);
    i += 32;
    final var pythFeedId = new byte[32];
    i += SerDeUtil.readArray(pythFeedId, _data, i);
    final var price = getInt64LE(_data, i);
    i += 8;
    final var expo = _data[i];
    ++i;
    final var timestamp = getInt64LE(_data, i);
    i += 8;
    final var config = Config.read(_data, i);
    i += config.l();
    final var bump = _data[i] & 0xFF;
    ++i;
    final var pythLazerFeed = readPubKey(_data, i);
    i += 32;
    final var redstoneFeed = readPubKey(_data, i);
    return new AgPriceFeed(_address,
                           discriminator,
                           mint,
                           edgeFeed,
                           clFeed,
                           pythFeed,
                           pythFeedId,
                           price,
                           expo,
                           timestamp,
                           config,
                           bump,
                           pythLazerFeed,
                           redstoneFeed);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    mint.write(_data, i);
    i += 32;
    edgeFeed.write(_data, i);
    i += 32;
    clFeed.write(_data, i);
    i += 32;
    pythFeed.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(pythFeedId, 32, _data, i);
    putInt64LE(_data, i, price);
    i += 8;
    _data[i] = (byte) expo;
    ++i;
    putInt64LE(_data, i, timestamp);
    i += 8;
    i += config.write(_data, i);
    _data[i] = (byte) bump;
    ++i;
    pythLazerFeed.write(_data, i);
    i += 32;
    redstoneFeed.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
