// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;

/// The lending account holds up to 16 balance positions for a user.
///
/// @param balances Array of balance positions (max 16). Sorted in descending order by bank_pk.
/// @param lastTagUsed Last allocated balance tag (u16), used to find the next unused tag.
/// @param pad1 Reserved for future use
/// @param padding Reserved for future use
public record LendingAccount(Balance[] balances,
                             int lastTagUsed,
                             byte[] pad1,
                             long[] padding) implements SerDe {

  public static final int BYTES = 1728;
  public static final int BALANCES_LEN = 16;
  public static final int PAD_1_LEN = 6;
  public static final int PADDING_LEN = 7;

  public static final int BALANCES_OFFSET = 0;
  public static final int LAST_TAG_USED_OFFSET = 1664;
  public static final int PAD_1_OFFSET = 1666;
  public static final int PADDING_OFFSET = 1672;

  public static LendingAccount read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var balances = new Balance[16];
    i += SerDeUtil.readArray(balances, Balance::read, _data, i);
    final var lastTagUsed = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var pad1 = new byte[6];
    i += SerDeUtil.readArray(pad1, _data, i);
    final var padding = new long[7];
    SerDeUtil.readArray(padding, _data, i);
    return new LendingAccount(balances,
                              lastTagUsed,
                              pad1,
                              padding);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeArrayChecked(balances, 16, _data, i);
    putInt16LE(_data, i, lastTagUsed);
    i += 2;
    i += SerDeUtil.writeArrayChecked(pad1, 6, _data, i);
    i += SerDeUtil.writeArrayChecked(padding, 7, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
