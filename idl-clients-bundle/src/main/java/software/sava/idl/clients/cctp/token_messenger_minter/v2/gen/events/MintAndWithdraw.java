// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amount: u64
/// @param feeCollected: u64
public record MintAndWithdraw(Discriminator discriminator,
                              PublicKey mintRecipient,
                              long amount,
                              PublicKey mintToken,
                              long feeCollected) implements TokenMessengerMinterV2Event {

  public static final int BYTES = 88;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(75, 67, 229, 70, 162, 126, 0, 71);

  public static final int MINT_RECIPIENT_OFFSET = 8;
  public static final int AMOUNT_OFFSET = 40;
  public static final int MINT_TOKEN_OFFSET = 48;
  public static final int FEE_COLLECTED_OFFSET = 80;

  public static MintAndWithdraw read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var mintRecipient = readPubKey(_data, i);
    i += 32;
    final var amount = getInt64LE(_data, i);
    i += 8;
    final var mintToken = readPubKey(_data, i);
    i += 32;
    final var feeCollected = getInt64LE(_data, i);
    return new MintAndWithdraw(discriminator,
                               mintRecipient,
                               amount,
                               mintToken,
                               feeCollected);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    mintRecipient.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amount);
    i += 8;
    mintToken.write(_data, i);
    i += 32;
    putInt64LE(_data, i, feeCollected);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
