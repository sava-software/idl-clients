// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.oracles.wormhole.post_message.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param sequence: u64
/// @param submissionTime: u32
public record MessageEvent(Discriminator discriminator,
                           PublicKey emitter,
                           long sequence,
                           long submissionTime) implements WormholePostMessageShimEvent {

  public static final int BYTES = 52;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(68, 27, 143, 0, 77, 76, 137, 112);

  public static final int EMITTER_OFFSET = 8;
  public static final int SEQUENCE_OFFSET = 40;
  public static final int SUBMISSION_TIME_OFFSET = 48;

  public static MessageEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var emitter = readPubKey(_data, i);
    i += 32;
    final var sequence = getInt64LE(_data, i);
    i += 8;
    final var submissionTime = Integer.toUnsignedLong(getInt32LE(_data, i));
    return new MessageEvent(discriminator, emitter, sequence, submissionTime);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    emitter.write(_data, i);
    i += 32;
    putInt64LE(_data, i, sequence);
    i += 8;
    putInt32LE(_data, i, (int) submissionTime);
    i += 4;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
