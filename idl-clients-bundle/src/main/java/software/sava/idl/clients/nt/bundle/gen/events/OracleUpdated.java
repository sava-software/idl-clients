// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import java.math.BigInteger;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.getUInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param averageExternalEquity: u64
/// @param bundleUnderlyingBalance: u64
public record OracleUpdated(Discriminator discriminator,
                            long averageExternalEquity,
                            PublicKey bundleAccountKey,
                            BigInteger totalShares,
                            long bundleUnderlyingBalance) implements NtbundleEvent {

  public static final int BYTES = 72;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(138, 9, 51, 219, 228, 198, 11, 147);

  public static final int AVERAGE_EXTERNAL_EQUITY_OFFSET = 8;
  public static final int BUNDLE_ACCOUNT_KEY_OFFSET = 16;
  public static final int TOTAL_SHARES_OFFSET = 48;
  public static final int BUNDLE_UNDERLYING_BALANCE_OFFSET = 64;

  public static OracleUpdated read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var averageExternalEquity = getInt64LE(_data, i);
    i += 8;
    final var bundleAccountKey = readPubKey(_data, i);
    i += 32;
    final var totalShares = getUInt128LE(_data, i);
    i += 16;
    final var bundleUnderlyingBalance = getInt64LE(_data, i);
    return new OracleUpdated(discriminator,
                             averageExternalEquity,
                             bundleAccountKey,
                             totalShares,
                             bundleUnderlyingBalance);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt64LE(_data, i, averageExternalEquity);
    i += 8;
    bundleAccountKey.write(_data, i);
    i += 32;
    putInt128LE(_data, i, totalShares);
    i += 16;
    putInt64LE(_data, i, bundleUnderlyingBalance);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
