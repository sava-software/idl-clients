// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param ytInAmount: u64
/// @param baseOutAmount: u64
public record WrapperSellYtEvent(Discriminator discriminator,
                                 PublicKey seller,
                                 PublicKey market,
                                 long ytInAmount,
                                 long baseOutAmount,
                                 long unixTimestamp) implements ExponentCoreEvent {

  public static final int BYTES = 96;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(252, 140, 45, 253, 6, 94, 95, 135);

  public static final int SELLER_OFFSET = 8;
  public static final int MARKET_OFFSET = 40;
  public static final int YT_IN_AMOUNT_OFFSET = 72;
  public static final int BASE_OUT_AMOUNT_OFFSET = 80;
  public static final int UNIX_TIMESTAMP_OFFSET = 88;

  public static WrapperSellYtEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var seller = readPubKey(_data, i);
    i += 32;
    final var market = readPubKey(_data, i);
    i += 32;
    final var ytInAmount = getInt64LE(_data, i);
    i += 8;
    final var baseOutAmount = getInt64LE(_data, i);
    i += 8;
    final var unixTimestamp = getInt64LE(_data, i);
    return new WrapperSellYtEvent(discriminator,
                                  seller,
                                  market,
                                  ytInAmount,
                                  baseOutAmount,
                                  unixTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    seller.write(_data, i);
    i += 32;
    market.write(_data, i);
    i += 32;
    putInt64LE(_data, i, ytInAmount);
    i += 8;
    putInt64LE(_data, i, baseOutAmount);
    i += 8;
    putInt64LE(_data, i, unixTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
