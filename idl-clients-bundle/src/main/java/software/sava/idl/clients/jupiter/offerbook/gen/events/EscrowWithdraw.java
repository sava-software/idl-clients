// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.jupiter.offerbook.gen.types.EventAsset;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param withdrawAmount: u64
/// @param totalAmount: u64
/// @param timestamp: u64
public record EscrowWithdraw(Discriminator discriminator,
                             PublicKey user,
                             EventAsset asset,
                             long withdrawAmount,
                             long totalAmount,
                             long timestamp) implements OfferbookEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(171, 17, 164, 116, 122, 66, 183, 34);

  public static final int USER_OFFSET = 8;
  public static final int ASSET_OFFSET = 40;

  public static EscrowWithdraw read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var user = readPubKey(_data, i);
    i += 32;
    final var asset = EventAsset.read(_data, i);
    i += asset.l();
    final var withdrawAmount = getInt64LE(_data, i);
    i += 8;
    final var totalAmount = getInt64LE(_data, i);
    i += 8;
    final var timestamp = getInt64LE(_data, i);
    return new EscrowWithdraw(discriminator,
                              user,
                              asset,
                              withdrawAmount,
                              totalAmount,
                              timestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    user.write(_data, i);
    i += 32;
    i += asset.write(_data, i);
    putInt64LE(_data, i, withdrawAmount);
    i += 8;
    putInt64LE(_data, i, totalAmount);
    i += 8;
    putInt64LE(_data, i, timestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + asset.l()
         + 8
         + 8
         + 8;
  }
}
