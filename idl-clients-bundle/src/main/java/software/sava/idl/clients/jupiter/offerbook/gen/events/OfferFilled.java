// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.jupiter.offerbook.gen.types.OfferEventV0;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record OfferFilled(Discriminator discriminator, OfferEventV0 offer, PublicKey pubkey) implements OfferbookEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(173, 104, 95, 161, 144, 206, 72, 57);

  public static final int OFFER_OFFSET = 8;

  public static OfferFilled read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var offer = OfferEventV0.read(_data, i);
    i += offer.l();
    final var pubkey = readPubKey(_data, i);
    return new OfferFilled(discriminator, offer, pubkey);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += offer.write(_data, i);
    pubkey.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + offer.l() + 32;
  }
}
