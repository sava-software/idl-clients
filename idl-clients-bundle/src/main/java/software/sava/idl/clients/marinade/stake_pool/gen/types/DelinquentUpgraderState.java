// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marinade.stake_pool.gen.types;

import software.sava.idl.clients.core.gen.RustEnum;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

public sealed interface DelinquentUpgraderState extends RustEnum permits
  DelinquentUpgraderState.IteratingStakes,
  DelinquentUpgraderState.IteratingValidators,
  DelinquentUpgraderState.Done {

  static DelinquentUpgraderState read(final byte[] _data, final int _offset) {
    final int ordinal = _data[_offset] & 0xFF;
    final int i = _offset + 1;
    return switch (ordinal) {
      case 0 -> IteratingStakes.read(_data, i);
      case 1 -> IteratingValidators.read(_data, i);
      case 2 -> Done.INSTANCE;
      default -> null;
    };
  }

  /// @param visitedCount: u32
  /// @param totalActiveBalance: u64
  /// @param totalDelinquentBalance: u64
  record IteratingStakes(long visitedCount,
                         long totalActiveBalance,
                         long totalDelinquentBalance) implements DelinquentUpgraderState {

    public static final int BYTES = 20;

    public static final int VISITED_COUNT_OFFSET = 0;
    public static final int TOTAL_ACTIVE_BALANCE_OFFSET = 4;
    public static final int TOTAL_DELINQUENT_BALANCE_OFFSET = 12;

    public static IteratingStakes read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var visitedCount = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var totalActiveBalance = getInt64LE(_data, i);
      i += 8;
      final var totalDelinquentBalance = getInt64LE(_data, i);
      return new IteratingStakes(visitedCount, totalActiveBalance, totalDelinquentBalance);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      putInt32LE(_data, i, (int) visitedCount);
      i += 4;
      putInt64LE(_data, i, totalActiveBalance);
      i += 8;
      putInt64LE(_data, i, totalDelinquentBalance);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 0;
    }
  }

  /// @param visitedCount: u32
  /// @param delinquentBalanceLeft: u64
  record IteratingValidators(long visitedCount, long delinquentBalanceLeft) implements DelinquentUpgraderState {

    public static final int BYTES = 12;

    public static final int VISITED_COUNT_OFFSET = 0;
    public static final int DELINQUENT_BALANCE_LEFT_OFFSET = 4;

    public static IteratingValidators read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final var visitedCount = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var delinquentBalanceLeft = getInt64LE(_data, i);
      return new IteratingValidators(visitedCount, delinquentBalanceLeft);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      putInt32LE(_data, i, (int) visitedCount);
      i += 4;
      putInt64LE(_data, i, delinquentBalanceLeft);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + BYTES;
    }

    @Override
    public int ordinal() {
      return 1;
    }
  }

  record Done() implements EnumNone, DelinquentUpgraderState {

    public static final Done INSTANCE = new Done();

    @Override
    public int ordinal() {
      return 2;
    }
  }
}
