// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.OptionalLong;
import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param sizeUsdDelta: u64
/// @param collateralDelta: u64
/// @param priceSlippage: Option<u64>
/// @param jupiterMinimumOut: Option<u64>
/// @param preSwapAmount: Option<u64>
/// @param triggerPrice: Option<u64>
/// @param triggerAboveThreshold: Option<bool>
/// @param entirePosition: Option<bool>
/// @param counter: u64
/// @param bump: u8
/// @param referral: Option<publicKey>
public record PositionRequest(PublicKey _address,
                              Discriminator discriminator,
                              PublicKey owner,
                              PublicKey pool,
                              PublicKey custody,
                              PublicKey position,
                              PublicKey mint,
                              long openTime,
                              long updateTime,
                              long sizeUsdDelta,
                              long collateralDelta,
                              RequestChange requestChange,
                              RequestType requestType,
                              Side side,
                              OptionalLong priceSlippage,
                              OptionalLong jupiterMinimumOut,
                              OptionalLong preSwapAmount,
                              OptionalLong triggerPrice,
                              Boolean triggerAboveThreshold,
                              Boolean entirePosition,
                              boolean executed,
                              long counter,
                              int bump,
                              PublicKey referral) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(12, 38, 250, 199, 46, 154, 32, 216);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int OWNER_OFFSET = 8;
  public static final int POOL_OFFSET = 40;
  public static final int CUSTODY_OFFSET = 72;
  public static final int POSITION_OFFSET = 104;
  public static final int MINT_OFFSET = 136;
  public static final int OPEN_TIME_OFFSET = 168;
  public static final int UPDATE_TIME_OFFSET = 176;
  public static final int SIZE_USD_DELTA_OFFSET = 184;
  public static final int COLLATERAL_DELTA_OFFSET = 192;
  public static final int REQUEST_CHANGE_OFFSET = 200;
  public static final int REQUEST_TYPE_OFFSET = 201;
  public static final int SIDE_OFFSET = 202;
  public static final int PRICE_SLIPPAGE_OFFSET = 204;

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createPoolFilter(final PublicKey pool) {
    return Filter.createMemCompFilter(POOL_OFFSET, pool);
  }

  public static Filter createCustodyFilter(final PublicKey custody) {
    return Filter.createMemCompFilter(CUSTODY_OFFSET, custody);
  }

  public static Filter createPositionFilter(final PublicKey position) {
    return Filter.createMemCompFilter(POSITION_OFFSET, position);
  }

  public static Filter createMintFilter(final PublicKey mint) {
    return Filter.createMemCompFilter(MINT_OFFSET, mint);
  }

  public static Filter createOpenTimeFilter(final long openTime) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, openTime);
    return Filter.createMemCompFilter(OPEN_TIME_OFFSET, _data);
  }

  public static Filter createUpdateTimeFilter(final long updateTime) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, updateTime);
    return Filter.createMemCompFilter(UPDATE_TIME_OFFSET, _data);
  }

  public static Filter createSizeUsdDeltaFilter(final long sizeUsdDelta) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, sizeUsdDelta);
    return Filter.createMemCompFilter(SIZE_USD_DELTA_OFFSET, _data);
  }

  public static Filter createCollateralDeltaFilter(final long collateralDelta) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, collateralDelta);
    return Filter.createMemCompFilter(COLLATERAL_DELTA_OFFSET, _data);
  }

  public static Filter createRequestChangeFilter(final RequestChange requestChange) {
    return Filter.createMemCompFilter(REQUEST_CHANGE_OFFSET, requestChange.write());
  }

  public static Filter createRequestTypeFilter(final RequestType requestType) {
    return Filter.createMemCompFilter(REQUEST_TYPE_OFFSET, requestType.write());
  }

  public static Filter createSideFilter(final Side side) {
    return Filter.createMemCompFilter(SIDE_OFFSET, side.write());
  }

  public static Filter createPriceSlippageFilter(final long priceSlippage) {
    final byte[] _data = new byte[9];
    _data[0] = 1;
    putInt64LE(_data, 1, priceSlippage);
    return Filter.createMemCompFilter(PRICE_SLIPPAGE_OFFSET, _data);
  }

  public static PositionRequest read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static PositionRequest read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static PositionRequest read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], PositionRequest> FACTORY = PositionRequest::read;

  public static PositionRequest read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var pool = readPubKey(_data, i);
    i += 32;
    final var custody = readPubKey(_data, i);
    i += 32;
    final var position = readPubKey(_data, i);
    i += 32;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var openTime = getInt64LE(_data, i);
    i += 8;
    final var updateTime = getInt64LE(_data, i);
    i += 8;
    final var sizeUsdDelta = getInt64LE(_data, i);
    i += 8;
    final var collateralDelta = getInt64LE(_data, i);
    i += 8;
    final var requestChange = RequestChange.read(_data, i);
    i += requestChange.l();
    final var requestType = RequestType.read(_data, i);
    i += requestType.l();
    final var side = Side.read(_data, i);
    i += side.l();
    final OptionalLong priceSlippage;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      priceSlippage = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      priceSlippage = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong jupiterMinimumOut;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      jupiterMinimumOut = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      jupiterMinimumOut = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong preSwapAmount;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      preSwapAmount = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      preSwapAmount = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong triggerPrice;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      triggerPrice = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      triggerPrice = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final Boolean triggerAboveThreshold;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      triggerAboveThreshold = null;
      ++i;
    } else {
      ++i;
      triggerAboveThreshold = _data[i] == 1;
      ++i;
    }
    final Boolean entirePosition;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      entirePosition = null;
      ++i;
    } else {
      ++i;
      entirePosition = _data[i] == 1;
      ++i;
    }
    final var executed = _data[i] == 1;
    ++i;
    final var counter = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    ++i;
    final PublicKey referral;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      referral = null;
    } else {
      ++i;
      referral = readPubKey(_data, i);
    }
    return new PositionRequest(_address,
                               discriminator,
                               owner,
                               pool,
                               custody,
                               position,
                               mint,
                               openTime,
                               updateTime,
                               sizeUsdDelta,
                               collateralDelta,
                               requestChange,
                               requestType,
                               side,
                               priceSlippage,
                               jupiterMinimumOut,
                               preSwapAmount,
                               triggerPrice,
                               triggerAboveThreshold,
                               entirePosition,
                               executed,
                               counter,
                               bump,
                               referral);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    pool.write(_data, i);
    i += 32;
    custody.write(_data, i);
    i += 32;
    position.write(_data, i);
    i += 32;
    mint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, openTime);
    i += 8;
    putInt64LE(_data, i, updateTime);
    i += 8;
    putInt64LE(_data, i, sizeUsdDelta);
    i += 8;
    putInt64LE(_data, i, collateralDelta);
    i += 8;
    i += requestChange.write(_data, i);
    i += requestType.write(_data, i);
    i += side.write(_data, i);
    i += SerDeUtil.writeOptional(1, priceSlippage, _data, i);
    i += SerDeUtil.writeOptional(1, jupiterMinimumOut, _data, i);
    i += SerDeUtil.writeOptional(1, preSwapAmount, _data, i);
    i += SerDeUtil.writeOptional(1, triggerPrice, _data, i);
    i += SerDeUtil.writeOptional(1, triggerAboveThreshold, _data, i);
    i += SerDeUtil.writeOptional(1, entirePosition, _data, i);
    _data[i] = (byte) (executed ? 1 : 0);
    ++i;
    putInt64LE(_data, i, counter);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    i += SerDeUtil.writeOptional(1, referral, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 32
         + 32
         + 32
         + 8
         + 8
         + 8
         + 8
         + requestChange.l()
         + requestType.l()
         + side.l()
         + (priceSlippage == null || priceSlippage.isEmpty() ? 1 : (1 + 8))
         + (jupiterMinimumOut == null || jupiterMinimumOut.isEmpty() ? 1 : (1 + 8))
         + (preSwapAmount == null || preSwapAmount.isEmpty() ? 1 : (1 + 8))
         + (triggerPrice == null || triggerPrice.isEmpty() ? 1 : (1 + 8))
         + (triggerAboveThreshold == null ? 1 : (1 + 1))
         + (entirePosition == null ? 1 : (1 + 1))
         + 1
         + 8
         + 1
         + (referral == null ? 1 : (1 + 32));
  }
}
