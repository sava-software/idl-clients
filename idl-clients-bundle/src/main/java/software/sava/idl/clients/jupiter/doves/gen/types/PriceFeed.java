// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.doves.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param price: u64
/// @param expo: i8
/// @param bump: u8
public record PriceFeed(PublicKey _address,
                        Discriminator discriminator,
                        byte[] pair,
                        byte[] signer,
                        long price,
                        int expo,
                        long timestamp,
                        int bump,
                        PythLazerConfig pythLazerConfig) implements SerDe {

  public static final int BYTES = 96;
  public static final int PAIR_LEN = 32;
  public static final int SIGNER_LEN = 33;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(189, 103, 252, 23, 152, 35, 243, 156);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int PAIR_OFFSET = 8;
  public static final int SIGNER_OFFSET = 40;
  public static final int PRICE_OFFSET = 73;
  public static final int EXPO_OFFSET = 81;
  public static final int TIMESTAMP_OFFSET = 82;
  public static final int BUMP_OFFSET = 90;
  public static final int PYTH_LAZER_CONFIG_OFFSET = 91;

  public static Filter createPriceFilter(final long price) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, price);
    return Filter.createMemCompFilter(PRICE_OFFSET, _data);
  }

  public static Filter createExpoFilter(final int expo) {
    return Filter.createMemCompFilter(EXPO_OFFSET, new byte[]{(byte) expo});
  }

  public static Filter createTimestampFilter(final long timestamp) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, timestamp);
    return Filter.createMemCompFilter(TIMESTAMP_OFFSET, _data);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Filter createPythLazerConfigFilter(final PythLazerConfig pythLazerConfig) {
    return Filter.createMemCompFilter(PYTH_LAZER_CONFIG_OFFSET, pythLazerConfig.write());
  }

  public static PriceFeed read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static PriceFeed read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static PriceFeed read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], PriceFeed> FACTORY = PriceFeed::read;

  public static PriceFeed read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var pair = new byte[32];
    i += SerDeUtil.readArray(pair, _data, i);
    final var signer = new byte[33];
    i += SerDeUtil.readArray(signer, _data, i);
    final var price = getInt64LE(_data, i);
    i += 8;
    final var expo = _data[i];
    ++i;
    final var timestamp = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var pythLazerConfig = PythLazerConfig.read(_data, i);
    return new PriceFeed(_address,
                         discriminator,
                         pair,
                         signer,
                         price,
                         expo,
                         timestamp,
                         bump,
                         pythLazerConfig);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    i += SerDeUtil.writeArrayChecked(pair, 32, _data, i);
    i += SerDeUtil.writeArrayChecked(signer, 33, _data, i);
    putInt64LE(_data, i, price);
    i += 8;
    _data[i] = (byte) expo;
    ++i;
    putInt64LE(_data, i, timestamp);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    i += pythLazerConfig.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
