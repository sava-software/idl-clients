// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.offerbook.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.jupiter.offerbook.gen.types.FillNftCollateralOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.FillNftPrincipalOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.FillTokenCollateralOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.FillTokenPrincipalOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.NftCollateral;
import software.sava.idl.clients.jupiter.offerbook.gen.types.NftCollateralAsset;
import software.sava.idl.clients.jupiter.offerbook.gen.types.NftCollateralOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.NftPrincipalOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.TokenCollateralOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.TokenPrincipalOfferParams;
import software.sava.idl.clients.jupiter.offerbook.gen.types.UpdateConfigAction;

import java.util.List;

import static java.util.Objects.requireNonNullElse;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class OfferbookProgram {

  public static final Discriminator CANCEL_OFFER_DISCRIMINATOR = toDiscriminator(92, 203, 223, 40, 92, 89, 53, 119);

  public static List<AccountMeta> cancelOfferKeys(final PublicKey creatorKey,
                                                  final PublicKey offerKey,
                                                  final PublicKey eventAuthorityKey,
                                                  final PublicKey programKey) {
    return List.of(
      createWritableSigner(creatorKey),
      createWrite(offerKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction cancelOffer(final AccountMeta invokedOfferbookProgramMeta,
                                        final PublicKey creatorKey,
                                        final PublicKey offerKey,
                                        final PublicKey eventAuthorityKey,
                                        final PublicKey programKey) {
    final var keys = cancelOfferKeys(
      creatorKey,
      offerKey,
      eventAuthorityKey,
      programKey
    );
    return cancelOffer(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction cancelOffer(final AccountMeta invokedOfferbookProgramMeta,
                                        final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, CANCEL_OFFER_DISCRIMINATOR);
  }

  public static final Discriminator CLAIM_FEE_DISCRIMINATOR = toDiscriminator(169, 32, 79, 137, 136, 232, 70, 137);

  public static List<AccountMeta> claimFeeKeys(final PublicKey adminKey,
                                               final PublicKey configKey,
                                               final PublicKey feeAuthorityKey,
                                               final PublicKey mintKey,
                                               final PublicKey protocolFeeTokenAccountKey,
                                               final PublicKey destTokenAccountKey,
                                               final PublicKey tokenProgramKey) {
    return List.of(
      createReadOnlySigner(adminKey),
      createRead(configKey),
      createRead(feeAuthorityKey),
      createRead(mintKey),
      createWrite(protocolFeeTokenAccountKey),
      createWrite(destTokenAccountKey),
      createRead(tokenProgramKey)
    );
  }

  public static Instruction claimFee(final AccountMeta invokedOfferbookProgramMeta,
                                     final PublicKey adminKey,
                                     final PublicKey configKey,
                                     final PublicKey feeAuthorityKey,
                                     final PublicKey mintKey,
                                     final PublicKey protocolFeeTokenAccountKey,
                                     final PublicKey destTokenAccountKey,
                                     final PublicKey tokenProgramKey) {
    final var keys = claimFeeKeys(
      adminKey,
      configKey,
      feeAuthorityKey,
      mintKey,
      protocolFeeTokenAccountKey,
      destTokenAccountKey,
      tokenProgramKey
    );
    return claimFee(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction claimFee(final AccountMeta invokedOfferbookProgramMeta,
                                     final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, CLAIM_FEE_DISCRIMINATOR);
  }

  public static final Discriminator CLAIM_NON_FUNGIBLE_LOAN_DISCRIMINATOR = toDiscriminator(152, 212, 201, 200, 206, 13, 149, 210);

  public static List<AccountMeta> claimNonFungibleLoanKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                           final SolanaAccounts solanaAccounts,
                                                           final PublicKey signerKey,
                                                           final PublicKey signerUserKey,
                                                           final PublicKey borrowerKey,
                                                           final PublicKey borrowerUserKey,
                                                           final PublicKey loanKey,
                                                           final PublicKey loanVaultKey,
                                                           final PublicKey configKey,
                                                           final PublicKey eventAuthorityKey,
                                                           final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(borrowerKey),
      createWrite(borrowerUserKey),
      createWrite(loanKey),
      createWrite(requireNonNullElse(loanVaultKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(configKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction claimNonFungibleLoan(final AccountMeta invokedOfferbookProgramMeta,
                                                 final SolanaAccounts solanaAccounts,
                                                 final PublicKey signerKey,
                                                 final PublicKey signerUserKey,
                                                 final PublicKey borrowerKey,
                                                 final PublicKey borrowerUserKey,
                                                 final PublicKey loanKey,
                                                 final PublicKey loanVaultKey,
                                                 final PublicKey configKey,
                                                 final PublicKey eventAuthorityKey,
                                                 final PublicKey programKey) {
    final var keys = claimNonFungibleLoanKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      borrowerKey,
      borrowerUserKey,
      loanKey,
      loanVaultKey,
      configKey,
      eventAuthorityKey,
      programKey
    );
    return claimNonFungibleLoan(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction claimNonFungibleLoan(final AccountMeta invokedOfferbookProgramMeta,
                                                 final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, CLAIM_NON_FUNGIBLE_LOAN_DISCRIMINATOR);
  }

  public static final Discriminator CLAIM_TOKEN_LOAN_DISCRIMINATOR = toDiscriminator(0, 112, 210, 85, 238, 81, 159, 29);

  public static List<AccountMeta> claimTokenLoanKeys(final PublicKey signerKey,
                                                     final PublicKey signerUserKey,
                                                     final PublicKey borrowerKey,
                                                     final PublicKey borrowerUserKey,
                                                     final PublicKey loanKey,
                                                     final PublicKey loanVaultKey,
                                                     final PublicKey configKey,
                                                     final PublicKey collateralMintKey,
                                                     final PublicKey lenderCollateralEscrowKey,
                                                     final PublicKey protocolFeeTokenAccountKey,
                                                     final PublicKey collateralTokenProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(borrowerKey),
      createWrite(borrowerUserKey),
      createWrite(loanKey),
      createWrite(loanVaultKey),
      createRead(configKey),
      createRead(collateralMintKey),
      createWrite(lenderCollateralEscrowKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(collateralTokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction claimTokenLoan(final AccountMeta invokedOfferbookProgramMeta,
                                           final PublicKey signerKey,
                                           final PublicKey signerUserKey,
                                           final PublicKey borrowerKey,
                                           final PublicKey borrowerUserKey,
                                           final PublicKey loanKey,
                                           final PublicKey loanVaultKey,
                                           final PublicKey configKey,
                                           final PublicKey collateralMintKey,
                                           final PublicKey lenderCollateralEscrowKey,
                                           final PublicKey protocolFeeTokenAccountKey,
                                           final PublicKey collateralTokenProgramKey,
                                           final PublicKey eventAuthorityKey,
                                           final PublicKey programKey) {
    final var keys = claimTokenLoanKeys(
      signerKey,
      signerUserKey,
      borrowerKey,
      borrowerUserKey,
      loanKey,
      loanVaultKey,
      configKey,
      collateralMintKey,
      lenderCollateralEscrowKey,
      protocolFeeTokenAccountKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return claimTokenLoan(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction claimTokenLoan(final AccountMeta invokedOfferbookProgramMeta,
                                           final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, CLAIM_TOKEN_LOAN_DISCRIMINATOR);
  }

  public static final Discriminator CREATE_NFT_COLLATERAL_OFFER_DISCRIMINATOR = toDiscriminator(48, 34, 52, 189, 81, 11, 169, 8);

  /// @param signerKey The borrower creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the borrower wants to receive as principal.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Principal), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same principal token. The collateral asset is carried in the IX
  ///                          params rather than as an account, so collateral-side consistency
  ///                          isn't enforceable at the constraint level.
  ///                          When absent, the field stays at Pubkey::default().
  public static List<AccountMeta> createNftCollateralOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                               final SolanaAccounts solanaAccounts,
                                                               final PublicKey signerKey,
                                                               final PublicKey signerUserKey,
                                                               final PublicKey configKey,
                                                               final PublicKey offerKey,
                                                               final PublicKey principalMintKey,
                                                               final PublicKey counteredOfferKey,
                                                               final PublicKey eventAuthorityKey,
                                                               final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createRead(configKey),
      createWrite(offerKey),
      createRead(principalMintKey),
      createRead(requireNonNullElse(counteredOfferKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param signerKey The borrower creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the borrower wants to receive as principal.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Principal), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same principal token. The collateral asset is carried in the IX
  ///                          params rather than as an account, so collateral-side consistency
  ///                          isn't enforceable at the constraint level.
  ///                          When absent, the field stays at Pubkey::default().
  public static Instruction createNftCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                     final SolanaAccounts solanaAccounts,
                                                     final PublicKey signerKey,
                                                     final PublicKey signerUserKey,
                                                     final PublicKey configKey,
                                                     final PublicKey offerKey,
                                                     final PublicKey principalMintKey,
                                                     final PublicKey counteredOfferKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey,
                                                     final NftCollateralOfferParams offerParams,
                                                     final NftCollateralAsset collateral) {
    final var keys = createNftCollateralOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      configKey,
      offerKey,
      principalMintKey,
      counteredOfferKey,
      eventAuthorityKey,
      programKey
    );
    return createNftCollateralOffer(invokedOfferbookProgramMeta, keys, offerParams, collateral);
  }

  public static Instruction createNftCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final NftCollateralOfferParams offerParams,
                                                     final NftCollateralAsset collateral) {
    final byte[] _data = new byte[8 + offerParams.l() + collateral.l()];
    int i = CREATE_NFT_COLLATERAL_OFFER_DISCRIMINATOR.write(_data, 0);
    i += offerParams.write(_data, i);
    collateral.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record CreateNftCollateralOfferIxData(Discriminator discriminator, NftCollateralOfferParams offerParams, NftCollateralAsset collateral) implements SerDe {  

    public static CreateNftCollateralOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int OFFER_PARAMS_OFFSET = 8;
    public static final int COLLATERAL_OFFSET = 28;

    public static CreateNftCollateralOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var offerParams = NftCollateralOfferParams.read(_data, i);
      i += offerParams.l();
      final var collateral = NftCollateralAsset.read(_data, i);
      return new CreateNftCollateralOfferIxData(discriminator, offerParams, collateral);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += offerParams.write(_data, i);
      i += collateral.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + offerParams.l() + collateral.l();
    }
  }

  public static final Discriminator CREATE_NFT_PRINCIPAL_OFFER_DISCRIMINATOR = toDiscriminator(2, 36, 83, 39, 177, 98, 126, 75);

  /// @param signerKey The lender creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the lender is offering to lend.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Collateral), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same principal token. The collateral asset is carried in the IX
  ///                          params rather than as an account, so collateral-side consistency
  ///                          isn't enforceable at the constraint level.
  ///                          When absent, the field stays at Pubkey::default().
  public static List<AccountMeta> createNftPrincipalOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                              final SolanaAccounts solanaAccounts,
                                                              final PublicKey signerKey,
                                                              final PublicKey signerUserKey,
                                                              final PublicKey configKey,
                                                              final PublicKey offerKey,
                                                              final PublicKey principalMintKey,
                                                              final PublicKey counteredOfferKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createRead(configKey),
      createWrite(offerKey),
      createRead(principalMintKey),
      createRead(requireNonNullElse(counteredOfferKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param signerKey The lender creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the lender is offering to lend.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Collateral), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same principal token. The collateral asset is carried in the IX
  ///                          params rather than as an account, so collateral-side consistency
  ///                          isn't enforceable at the constraint level.
  ///                          When absent, the field stays at Pubkey::default().
  public static Instruction createNftPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey signerKey,
                                                    final PublicKey signerUserKey,
                                                    final PublicKey configKey,
                                                    final PublicKey offerKey,
                                                    final PublicKey principalMintKey,
                                                    final PublicKey counteredOfferKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey,
                                                    final NftPrincipalOfferParams offerParams,
                                                    final NftCollateral collateral) {
    final var keys = createNftPrincipalOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      configKey,
      offerKey,
      principalMintKey,
      counteredOfferKey,
      eventAuthorityKey,
      programKey
    );
    return createNftPrincipalOffer(invokedOfferbookProgramMeta, keys, offerParams, collateral);
  }

  public static Instruction createNftPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final NftPrincipalOfferParams offerParams,
                                                    final NftCollateral collateral) {
    final byte[] _data = new byte[8 + offerParams.l() + collateral.l()];
    int i = CREATE_NFT_PRINCIPAL_OFFER_DISCRIMINATOR.write(_data, 0);
    i += offerParams.write(_data, i);
    collateral.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record CreateNftPrincipalOfferIxData(Discriminator discriminator, NftPrincipalOfferParams offerParams, NftCollateral collateral) implements SerDe {  

    public static CreateNftPrincipalOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int OFFER_PARAMS_OFFSET = 8;
    public static final int COLLATERAL_OFFSET = 28;

    public static CreateNftPrincipalOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var offerParams = NftPrincipalOfferParams.read(_data, i);
      i += offerParams.l();
      final var collateral = NftCollateral.read(_data, i);
      return new CreateNftPrincipalOfferIxData(discriminator, offerParams, collateral);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += offerParams.write(_data, i);
      i += collateral.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + offerParams.l() + collateral.l();
    }
  }

  public static final Discriminator CREATE_TOKEN_COLLATERAL_OFFER_DISCRIMINATOR = toDiscriminator(38, 75, 182, 254, 173, 13, 240, 41);

  /// @param signerKey The borrower creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the borrower wants to receive as principal. Must differ
  ///                         from the collateral mint.
  /// @param collateralMintKey The token mint the borrower is offering as collateral. Must differ
  ///                          from the principal mint.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Principal), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same asset pair (same principal mint, same collateral mint).
  ///                          When absent, the field stays at Pubkey::default().
  public static List<AccountMeta> createTokenCollateralOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                 final SolanaAccounts solanaAccounts,
                                                                 final PublicKey signerKey,
                                                                 final PublicKey signerUserKey,
                                                                 final PublicKey configKey,
                                                                 final PublicKey offerKey,
                                                                 final PublicKey principalMintKey,
                                                                 final PublicKey collateralMintKey,
                                                                 final PublicKey counteredOfferKey,
                                                                 final PublicKey eventAuthorityKey,
                                                                 final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createRead(configKey),
      createWrite(offerKey),
      createRead(principalMintKey),
      createRead(collateralMintKey),
      createRead(requireNonNullElse(counteredOfferKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param signerKey The borrower creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the borrower wants to receive as principal. Must differ
  ///                         from the collateral mint.
  /// @param collateralMintKey The token mint the borrower is offering as collateral. Must differ
  ///                          from the principal mint.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Principal), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same asset pair (same principal mint, same collateral mint).
  ///                          When absent, the field stays at Pubkey::default().
  public static Instruction createTokenCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                       final SolanaAccounts solanaAccounts,
                                                       final PublicKey signerKey,
                                                       final PublicKey signerUserKey,
                                                       final PublicKey configKey,
                                                       final PublicKey offerKey,
                                                       final PublicKey principalMintKey,
                                                       final PublicKey collateralMintKey,
                                                       final PublicKey counteredOfferKey,
                                                       final PublicKey eventAuthorityKey,
                                                       final PublicKey programKey,
                                                       final TokenCollateralOfferParams offerParams) {
    final var keys = createTokenCollateralOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      configKey,
      offerKey,
      principalMintKey,
      collateralMintKey,
      counteredOfferKey,
      eventAuthorityKey,
      programKey
    );
    return createTokenCollateralOffer(invokedOfferbookProgramMeta, keys, offerParams);
  }

  public static Instruction createTokenCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                       final List<AccountMeta> keys,
                                                       final TokenCollateralOfferParams offerParams) {
    final byte[] _data = new byte[8 + offerParams.l()];
    int i = CREATE_TOKEN_COLLATERAL_OFFER_DISCRIMINATOR.write(_data, 0);
    offerParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record CreateTokenCollateralOfferIxData(Discriminator discriminator, TokenCollateralOfferParams offerParams) implements SerDe {  

    public static CreateTokenCollateralOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 45;

    public static final int OFFER_PARAMS_OFFSET = 8;

    public static CreateTokenCollateralOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var offerParams = TokenCollateralOfferParams.read(_data, i);
      return new CreateTokenCollateralOfferIxData(discriminator, offerParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += offerParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator CREATE_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR = toDiscriminator(78, 9, 69, 142, 189, 64, 171, 13);

  /// @param signerKey The lender creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the lender is offering to lend. Must differ from
  ///                         the collateral mint.
  /// @param collateralMintKey The token mint the lender expects as collateral. Must differ from
  ///                          the principal mint.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Collateral), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same asset pair (same principal mint, same collateral mint).
  ///                          When absent, the field stays at Pubkey::default().
  public static List<AccountMeta> createTokenPrincipalOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                final SolanaAccounts solanaAccounts,
                                                                final PublicKey signerKey,
                                                                final PublicKey signerUserKey,
                                                                final PublicKey configKey,
                                                                final PublicKey offerKey,
                                                                final PublicKey principalMintKey,
                                                                final PublicKey collateralMintKey,
                                                                final PublicKey counteredOfferKey,
                                                                final PublicKey eventAuthorityKey,
                                                                final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createRead(configKey),
      createWrite(offerKey),
      createRead(principalMintKey),
      createRead(collateralMintKey),
      createRead(requireNonNullElse(counteredOfferKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param signerKey The lender creating the offer; pays for the offer account allocation.
  /// @param signerUserKey Registered user profile for the signer.
  ///                      We want to ensure that the signer is a registered user before creating an offer.
  /// @param configKey Global protocol configuration that defines valid offer parameter ranges
  ///                  (duration, expiry, APY, minimum amounts) and pause state.
  /// @param offerKey Newly initialized offer account that will store all offer terms.
  /// @param principalMintKey The token mint the lender is offering to lend. Must differ from
  ///                         the collateral mint.
  /// @param collateralMintKey The token mint the lender expects as collateral. Must differ from
  ///                          the principal mint.
  /// @param counteredOfferKey Optional: the offer this one is countering. When present, must be
  ///                          on the opposite side (Collateral), created by someone other than
  ///                          the signer, still fillable (Active or PartiallyFilled), and for the
  ///                          same asset pair (same principal mint, same collateral mint).
  ///                          When absent, the field stays at Pubkey::default().
  public static Instruction createTokenPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                      final SolanaAccounts solanaAccounts,
                                                      final PublicKey signerKey,
                                                      final PublicKey signerUserKey,
                                                      final PublicKey configKey,
                                                      final PublicKey offerKey,
                                                      final PublicKey principalMintKey,
                                                      final PublicKey collateralMintKey,
                                                      final PublicKey counteredOfferKey,
                                                      final PublicKey eventAuthorityKey,
                                                      final PublicKey programKey,
                                                      final TokenPrincipalOfferParams offerParams) {
    final var keys = createTokenPrincipalOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      configKey,
      offerKey,
      principalMintKey,
      collateralMintKey,
      counteredOfferKey,
      eventAuthorityKey,
      programKey
    );
    return createTokenPrincipalOffer(invokedOfferbookProgramMeta, keys, offerParams);
  }

  public static Instruction createTokenPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                      final List<AccountMeta> keys,
                                                      final TokenPrincipalOfferParams offerParams) {
    final byte[] _data = new byte[8 + offerParams.l()];
    int i = CREATE_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR.write(_data, 0);
    offerParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record CreateTokenPrincipalOfferIxData(Discriminator discriminator, TokenPrincipalOfferParams offerParams) implements SerDe {  

    public static CreateTokenPrincipalOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 45;

    public static final int OFFER_PARAMS_OFFSET = 8;

    public static CreateTokenPrincipalOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var offerParams = TokenPrincipalOfferParams.read(_data, i);
      return new CreateTokenPrincipalOfferIxData(discriminator, offerParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += offerParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator CREATE_USER_DISCRIMINATOR = toDiscriminator(108, 227, 130, 130, 252, 109, 75, 218);

  public static List<AccountMeta> createUserKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                 final SolanaAccounts solanaAccounts,
                                                 final PublicKey signerKey,
                                                 final PublicKey signerUserKey,
                                                 final PublicKey referrerKey,
                                                 final PublicKey referrerUserKey,
                                                 final PublicKey configKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createRead(requireNonNullElse(referrerKey, invokedOfferbookProgramMeta.publicKey())),
      createWrite(requireNonNullElse(referrerUserKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(configKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  public static Instruction createUser(final AccountMeta invokedOfferbookProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey signerKey,
                                       final PublicKey signerUserKey,
                                       final PublicKey referrerKey,
                                       final PublicKey referrerUserKey,
                                       final PublicKey configKey) {
    final var keys = createUserKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      referrerKey,
      referrerUserKey,
      configKey
    );
    return createUser(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction createUser(final AccountMeta invokedOfferbookProgramMeta,
                                       final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, CREATE_USER_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_CLASSIC_NFT_DEPOSIT_DISCRIMINATOR = toDiscriminator(148, 223, 191, 56, 93, 16, 156, 207);

  public static List<AccountMeta> escrowClassicNftDepositKeys(final SolanaAccounts solanaAccounts,
                                                              final PublicKey signerKey,
                                                              final PublicKey signerNftTokenAccountKey,
                                                              final PublicKey signerUserKey,
                                                              final PublicKey nftMintKey,
                                                              final PublicKey nftMetadataKey,
                                                              final PublicKey nftEditionKey,
                                                              final PublicKey userEscrowTokenAccountKey,
                                                              final PublicKey metadataProgramKey,
                                                              final PublicKey tokenProgramKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerNftTokenAccountKey),
      createRead(signerUserKey),
      createRead(nftMintKey),
      createRead(nftMetadataKey),
      createRead(nftEditionKey),
      createWrite(userEscrowTokenAccountKey),
      createRead(metadataProgramKey),
      createRead(solanaAccounts.associatedTokenAccountProgram()),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowClassicNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey signerKey,
                                                    final PublicKey signerNftTokenAccountKey,
                                                    final PublicKey signerUserKey,
                                                    final PublicKey nftMintKey,
                                                    final PublicKey nftMetadataKey,
                                                    final PublicKey nftEditionKey,
                                                    final PublicKey userEscrowTokenAccountKey,
                                                    final PublicKey metadataProgramKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey) {
    final var keys = escrowClassicNftDepositKeys(
      solanaAccounts,
      signerKey,
      signerNftTokenAccountKey,
      signerUserKey,
      nftMintKey,
      nftMetadataKey,
      nftEditionKey,
      userEscrowTokenAccountKey,
      metadataProgramKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowClassicNftDeposit(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowClassicNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                    final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_CLASSIC_NFT_DEPOSIT_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_CLASSIC_NFT_WITHDRAW_DISCRIMINATOR = toDiscriminator(58, 102, 79, 16, 208, 182, 217, 35);

  public static List<AccountMeta> escrowClassicNftWithdrawKeys(final PublicKey signerKey,
                                                               final PublicKey signerNftTokenAccountKey,
                                                               final PublicKey signerUserKey,
                                                               final PublicKey nftMintKey,
                                                               final PublicKey userEscrowTokenAccountKey,
                                                               final PublicKey tokenProgramKey,
                                                               final PublicKey eventAuthorityKey,
                                                               final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerNftTokenAccountKey),
      createRead(signerUserKey),
      createRead(nftMintKey),
      createWrite(userEscrowTokenAccountKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowClassicNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                     final PublicKey signerKey,
                                                     final PublicKey signerNftTokenAccountKey,
                                                     final PublicKey signerUserKey,
                                                     final PublicKey nftMintKey,
                                                     final PublicKey userEscrowTokenAccountKey,
                                                     final PublicKey tokenProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey) {
    final var keys = escrowClassicNftWithdrawKeys(
      signerKey,
      signerNftTokenAccountKey,
      signerUserKey,
      nftMintKey,
      userEscrowTokenAccountKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowClassicNftWithdraw(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowClassicNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                     final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_CLASSIC_NFT_WITHDRAW_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_CORE_NFT_DEPOSIT_DISCRIMINATOR = toDiscriminator(190, 224, 133, 134, 189, 147, 71, 90);

  public static List<AccountMeta> escrowCoreNftDepositKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                           final SolanaAccounts solanaAccounts,
                                                           final PublicKey signerKey,
                                                           final PublicKey signerUserKey,
                                                           final PublicKey assetKey,
                                                           final PublicKey collectionKey,
                                                           final PublicKey mplCoreProgramKey,
                                                           final PublicKey eventAuthorityKey,
                                                           final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerUserKey),
      createWrite(assetKey),
      createRead(requireNonNullElse(collectionKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(mplCoreProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowCoreNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                 final SolanaAccounts solanaAccounts,
                                                 final PublicKey signerKey,
                                                 final PublicKey signerUserKey,
                                                 final PublicKey assetKey,
                                                 final PublicKey collectionKey,
                                                 final PublicKey mplCoreProgramKey,
                                                 final PublicKey eventAuthorityKey,
                                                 final PublicKey programKey) {
    final var keys = escrowCoreNftDepositKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      assetKey,
      collectionKey,
      mplCoreProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowCoreNftDeposit(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowCoreNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                 final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_CORE_NFT_DEPOSIT_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_CORE_NFT_WITHDRAW_DISCRIMINATOR = toDiscriminator(171, 155, 79, 152, 236, 174, 29, 57);

  public static List<AccountMeta> escrowCoreNftWithdrawKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                            final SolanaAccounts solanaAccounts,
                                                            final PublicKey signerKey,
                                                            final PublicKey signerUserKey,
                                                            final PublicKey assetKey,
                                                            final PublicKey collectionKey,
                                                            final PublicKey mplCoreProgramKey,
                                                            final PublicKey eventAuthorityKey,
                                                            final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerUserKey),
      createWrite(assetKey),
      createRead(requireNonNullElse(collectionKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(mplCoreProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowCoreNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                  final SolanaAccounts solanaAccounts,
                                                  final PublicKey signerKey,
                                                  final PublicKey signerUserKey,
                                                  final PublicKey assetKey,
                                                  final PublicKey collectionKey,
                                                  final PublicKey mplCoreProgramKey,
                                                  final PublicKey eventAuthorityKey,
                                                  final PublicKey programKey) {
    final var keys = escrowCoreNftWithdrawKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      assetKey,
      collectionKey,
      mplCoreProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowCoreNftWithdraw(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowCoreNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                  final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_CORE_NFT_WITHDRAW_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_PROGRAMMABLE_NFT_DEPOSIT_DISCRIMINATOR = toDiscriminator(223, 29, 48, 71, 26, 14, 246, 101);

  public static List<AccountMeta> escrowProgrammableNftDepositKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                   final SolanaAccounts solanaAccounts,
                                                                   final PublicKey signerKey,
                                                                   final PublicKey signerUserKey,
                                                                   final PublicKey nftMintKey,
                                                                   final PublicKey nftMetadataKey,
                                                                   final PublicKey nftEditionKey,
                                                                   final PublicKey signerNftTokenAccountKey,
                                                                   final PublicKey signerTokenRecordKey,
                                                                   final PublicKey userEscrowTokenAccountKey,
                                                                   final PublicKey escrowTokenRecordKey,
                                                                   final PublicKey authorizationRulesKey,
                                                                   final PublicKey metadataProgramKey,
                                                                   final PublicKey authorizationProgramKey,
                                                                   final PublicKey tokenProgramKey,
                                                                   final PublicKey eventAuthorityKey,
                                                                   final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerUserKey),
      createRead(nftMintKey),
      createWrite(nftMetadataKey),
      createRead(nftEditionKey),
      createWrite(signerNftTokenAccountKey),
      createWrite(signerTokenRecordKey),
      createWrite(userEscrowTokenAccountKey),
      createWrite(escrowTokenRecordKey),
      createWrite(requireNonNullElse(authorizationRulesKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(metadataProgramKey),
      createRead(solanaAccounts.instructionsSysVar()),
      createRead(requireNonNullElse(authorizationProgramKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.associatedTokenAccountProgram()),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowProgrammableNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                         final SolanaAccounts solanaAccounts,
                                                         final PublicKey signerKey,
                                                         final PublicKey signerUserKey,
                                                         final PublicKey nftMintKey,
                                                         final PublicKey nftMetadataKey,
                                                         final PublicKey nftEditionKey,
                                                         final PublicKey signerNftTokenAccountKey,
                                                         final PublicKey signerTokenRecordKey,
                                                         final PublicKey userEscrowTokenAccountKey,
                                                         final PublicKey escrowTokenRecordKey,
                                                         final PublicKey authorizationRulesKey,
                                                         final PublicKey metadataProgramKey,
                                                         final PublicKey authorizationProgramKey,
                                                         final PublicKey tokenProgramKey,
                                                         final PublicKey eventAuthorityKey,
                                                         final PublicKey programKey) {
    final var keys = escrowProgrammableNftDepositKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      nftMintKey,
      nftMetadataKey,
      nftEditionKey,
      signerNftTokenAccountKey,
      signerTokenRecordKey,
      userEscrowTokenAccountKey,
      escrowTokenRecordKey,
      authorizationRulesKey,
      metadataProgramKey,
      authorizationProgramKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowProgrammableNftDeposit(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowProgrammableNftDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                                         final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_PROGRAMMABLE_NFT_DEPOSIT_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_PROGRAMMABLE_NFT_WITHDRAW_DISCRIMINATOR = toDiscriminator(28, 157, 21, 234, 59, 222, 170, 178);

  public static List<AccountMeta> escrowProgrammableNftWithdrawKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                    final SolanaAccounts solanaAccounts,
                                                                    final PublicKey signerKey,
                                                                    final PublicKey signerUserKey,
                                                                    final PublicKey nftMintKey,
                                                                    final PublicKey nftMetadataKey,
                                                                    final PublicKey nftEditionKey,
                                                                    final PublicKey signerNftTokenAccountKey,
                                                                    final PublicKey signerTokenRecordKey,
                                                                    final PublicKey userEscrowTokenAccountKey,
                                                                    final PublicKey escrowTokenRecordKey,
                                                                    final PublicKey authorizationRulesKey,
                                                                    final PublicKey metadataProgramKey,
                                                                    final PublicKey authorizationProgramKey,
                                                                    final PublicKey tokenProgramKey,
                                                                    final PublicKey eventAuthorityKey,
                                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerUserKey),
      createRead(nftMintKey),
      createWrite(nftMetadataKey),
      createRead(nftEditionKey),
      createWrite(signerNftTokenAccountKey),
      createWrite(signerTokenRecordKey),
      createWrite(userEscrowTokenAccountKey),
      createWrite(escrowTokenRecordKey),
      createWrite(requireNonNullElse(authorizationRulesKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(metadataProgramKey),
      createRead(solanaAccounts.instructionsSysVar()),
      createRead(requireNonNullElse(authorizationProgramKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.associatedTokenAccountProgram()),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction escrowProgrammableNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                          final SolanaAccounts solanaAccounts,
                                                          final PublicKey signerKey,
                                                          final PublicKey signerUserKey,
                                                          final PublicKey nftMintKey,
                                                          final PublicKey nftMetadataKey,
                                                          final PublicKey nftEditionKey,
                                                          final PublicKey signerNftTokenAccountKey,
                                                          final PublicKey signerTokenRecordKey,
                                                          final PublicKey userEscrowTokenAccountKey,
                                                          final PublicKey escrowTokenRecordKey,
                                                          final PublicKey authorizationRulesKey,
                                                          final PublicKey metadataProgramKey,
                                                          final PublicKey authorizationProgramKey,
                                                          final PublicKey tokenProgramKey,
                                                          final PublicKey eventAuthorityKey,
                                                          final PublicKey programKey) {
    final var keys = escrowProgrammableNftWithdrawKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      nftMintKey,
      nftMetadataKey,
      nftEditionKey,
      signerNftTokenAccountKey,
      signerTokenRecordKey,
      userEscrowTokenAccountKey,
      escrowTokenRecordKey,
      authorizationRulesKey,
      metadataProgramKey,
      authorizationProgramKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowProgrammableNftWithdraw(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction escrowProgrammableNftWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                          final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, ESCROW_PROGRAMMABLE_NFT_WITHDRAW_DISCRIMINATOR);
  }

  public static final Discriminator ESCROW_TOKEN_DEPOSIT_DISCRIMINATOR = toDiscriminator(170, 3, 249, 121, 68, 108, 240, 239);

  public static List<AccountMeta> escrowTokenDepositKeys(final SolanaAccounts solanaAccounts,
                                                         final PublicKey signerKey,
                                                         final PublicKey signerTokenAccountKey,
                                                         final PublicKey signerUserKey,
                                                         final PublicKey userEscrowTokenAccountKey,
                                                         final PublicKey mintKey,
                                                         final PublicKey tokenProgramKey,
                                                         final PublicKey eventAuthorityKey,
                                                         final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerTokenAccountKey),
      createRead(signerUserKey),
      createWrite(userEscrowTokenAccountKey),
      createRead(mintKey),
      createRead(solanaAccounts.associatedTokenAccountProgram()),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param amount: u64
  public static Instruction escrowTokenDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                               final SolanaAccounts solanaAccounts,
                                               final PublicKey signerKey,
                                               final PublicKey signerTokenAccountKey,
                                               final PublicKey signerUserKey,
                                               final PublicKey userEscrowTokenAccountKey,
                                               final PublicKey mintKey,
                                               final PublicKey tokenProgramKey,
                                               final PublicKey eventAuthorityKey,
                                               final PublicKey programKey,
                                               final long amount) {
    final var keys = escrowTokenDepositKeys(
      solanaAccounts,
      signerKey,
      signerTokenAccountKey,
      signerUserKey,
      userEscrowTokenAccountKey,
      mintKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowTokenDeposit(invokedOfferbookProgramMeta, keys, amount);
  }

  /// @param amount: u64
  public static Instruction escrowTokenDeposit(final AccountMeta invokedOfferbookProgramMeta,
                                               final List<AccountMeta> keys,
                                               final long amount) {
    final byte[] _data = new byte[16];
    int i = ESCROW_TOKEN_DEPOSIT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record EscrowTokenDepositIxData(Discriminator discriminator, long amount) implements SerDe {  

    public static EscrowTokenDepositIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int AMOUNT_OFFSET = 8;

    public static EscrowTokenDepositIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new EscrowTokenDepositIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator ESCROW_TOKEN_WITHDRAW_DISCRIMINATOR = toDiscriminator(183, 204, 14, 69, 17, 76, 223, 105);

  public static List<AccountMeta> escrowTokenWithdrawKeys(final PublicKey signerKey,
                                                          final PublicKey signerTokenAccountKey,
                                                          final PublicKey signerUserKey,
                                                          final PublicKey userEscrowTokenAccountKey,
                                                          final PublicKey mintKey,
                                                          final PublicKey tokenProgramKey,
                                                          final PublicKey eventAuthorityKey,
                                                          final PublicKey programKey) {
    return List.of(
      createReadOnlySigner(signerKey),
      createWrite(signerTokenAccountKey),
      createRead(signerUserKey),
      createWrite(userEscrowTokenAccountKey),
      createRead(mintKey),
      createRead(tokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  /// @param amount: u64
  public static Instruction escrowTokenWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                final PublicKey signerKey,
                                                final PublicKey signerTokenAccountKey,
                                                final PublicKey signerUserKey,
                                                final PublicKey userEscrowTokenAccountKey,
                                                final PublicKey mintKey,
                                                final PublicKey tokenProgramKey,
                                                final PublicKey eventAuthorityKey,
                                                final PublicKey programKey,
                                                final long amount) {
    final var keys = escrowTokenWithdrawKeys(
      signerKey,
      signerTokenAccountKey,
      signerUserKey,
      userEscrowTokenAccountKey,
      mintKey,
      tokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return escrowTokenWithdraw(invokedOfferbookProgramMeta, keys, amount);
  }

  /// @param amount: u64
  public static Instruction escrowTokenWithdraw(final AccountMeta invokedOfferbookProgramMeta,
                                                final List<AccountMeta> keys,
                                                final long amount) {
    final byte[] _data = new byte[16];
    int i = ESCROW_TOKEN_WITHDRAW_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  /// @param amount: u64
  public record EscrowTokenWithdrawIxData(Discriminator discriminator, long amount) implements SerDe {  

    public static EscrowTokenWithdrawIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int AMOUNT_OFFSET = 8;

    public static EscrowTokenWithdrawIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      return new EscrowTokenWithdrawIxData(discriminator, amount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator FILL_NON_FUNGIBLE_COLLATERAL_OFFER_DISCRIMINATOR = toDiscriminator(135, 55, 123, 43, 115, 61, 143, 145);

  public static List<AccountMeta> fillNonFungibleCollateralOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                     final SolanaAccounts solanaAccounts,
                                                                     final PublicKey signerKey,
                                                                     final PublicKey signerUserKey,
                                                                     final PublicKey borrowerKey,
                                                                     final PublicKey borrowerUserKey,
                                                                     final PublicKey offerKey,
                                                                     final PublicKey loanKey,
                                                                     final PublicKey loanVaultKey,
                                                                     final PublicKey configKey,
                                                                     final PublicKey principalMintKey,
                                                                     final PublicKey collateralMintKey,
                                                                     final PublicKey lenderPrincipalEscrowKey,
                                                                     final PublicKey borrowerPrincipalTokenAccountKey,
                                                                     final PublicKey protocolFeeTokenAccountKey,
                                                                     final PublicKey principalTokenProgramKey,
                                                                     final PublicKey collateralTokenProgramKey,
                                                                     final PublicKey eventAuthorityKey,
                                                                     final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(borrowerKey),
      createWrite(borrowerUserKey),
      createWrite(offerKey),
      createWrite(loanKey),
      createWrite(requireNonNullElse(loanVaultKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(configKey),
      createRead(principalMintKey),
      createRead(requireNonNullElse(collateralMintKey, invokedOfferbookProgramMeta.publicKey())),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(borrowerPrincipalTokenAccountKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(requireNonNullElse(collateralTokenProgramKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction fillNonFungibleCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                           final SolanaAccounts solanaAccounts,
                                                           final PublicKey signerKey,
                                                           final PublicKey signerUserKey,
                                                           final PublicKey borrowerKey,
                                                           final PublicKey borrowerUserKey,
                                                           final PublicKey offerKey,
                                                           final PublicKey loanKey,
                                                           final PublicKey loanVaultKey,
                                                           final PublicKey configKey,
                                                           final PublicKey principalMintKey,
                                                           final PublicKey collateralMintKey,
                                                           final PublicKey lenderPrincipalEscrowKey,
                                                           final PublicKey borrowerPrincipalTokenAccountKey,
                                                           final PublicKey protocolFeeTokenAccountKey,
                                                           final PublicKey principalTokenProgramKey,
                                                           final PublicKey collateralTokenProgramKey,
                                                           final PublicKey eventAuthorityKey,
                                                           final PublicKey programKey,
                                                           final FillNftCollateralOfferParams fillParams) {
    final var keys = fillNonFungibleCollateralOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      borrowerKey,
      borrowerUserKey,
      offerKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      collateralMintKey,
      lenderPrincipalEscrowKey,
      borrowerPrincipalTokenAccountKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return fillNonFungibleCollateralOffer(invokedOfferbookProgramMeta, keys, fillParams);
  }

  public static Instruction fillNonFungibleCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                           final List<AccountMeta> keys,
                                                           final FillNftCollateralOfferParams fillParams) {
    final byte[] _data = new byte[8 + fillParams.l()];
    int i = FILL_NON_FUNGIBLE_COLLATERAL_OFFER_DISCRIMINATOR.write(_data, 0);
    fillParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record FillNonFungibleCollateralOfferIxData(Discriminator discriminator, FillNftCollateralOfferParams fillParams) implements SerDe {  

    public static FillNonFungibleCollateralOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int FILL_PARAMS_OFFSET = 8;

    public static FillNonFungibleCollateralOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var fillParams = FillNftCollateralOfferParams.read(_data, i);
      return new FillNonFungibleCollateralOfferIxData(discriminator, fillParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += fillParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator FILL_NON_FUNGIBLE_PRINCIPAL_OFFER_DISCRIMINATOR = toDiscriminator(99, 127, 177, 155, 175, 160, 24, 234);

  public static List<AccountMeta> fillNonFungiblePrincipalOfferKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                                    final SolanaAccounts solanaAccounts,
                                                                    final PublicKey signerKey,
                                                                    final PublicKey signerUserKey,
                                                                    final PublicKey lenderKey,
                                                                    final PublicKey lenderUserKey,
                                                                    final PublicKey offerKey,
                                                                    final PublicKey loanKey,
                                                                    final PublicKey loanVaultKey,
                                                                    final PublicKey configKey,
                                                                    final PublicKey principalMintKey,
                                                                    final PublicKey collateralMintKey,
                                                                    final PublicKey lenderPrincipalEscrowKey,
                                                                    final PublicKey borrowerPrincipalTokenAccountKey,
                                                                    final PublicKey protocolFeeTokenAccountKey,
                                                                    final PublicKey principalTokenProgramKey,
                                                                    final PublicKey collateralTokenProgramKey,
                                                                    final PublicKey eventAuthorityKey,
                                                                    final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(lenderKey),
      createWrite(lenderUserKey),
      createWrite(offerKey),
      createWrite(loanKey),
      createWrite(requireNonNullElse(loanVaultKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(configKey),
      createRead(principalMintKey),
      createRead(requireNonNullElse(collateralMintKey, invokedOfferbookProgramMeta.publicKey())),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(borrowerPrincipalTokenAccountKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(requireNonNullElse(collateralTokenProgramKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction fillNonFungiblePrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                          final SolanaAccounts solanaAccounts,
                                                          final PublicKey signerKey,
                                                          final PublicKey signerUserKey,
                                                          final PublicKey lenderKey,
                                                          final PublicKey lenderUserKey,
                                                          final PublicKey offerKey,
                                                          final PublicKey loanKey,
                                                          final PublicKey loanVaultKey,
                                                          final PublicKey configKey,
                                                          final PublicKey principalMintKey,
                                                          final PublicKey collateralMintKey,
                                                          final PublicKey lenderPrincipalEscrowKey,
                                                          final PublicKey borrowerPrincipalTokenAccountKey,
                                                          final PublicKey protocolFeeTokenAccountKey,
                                                          final PublicKey principalTokenProgramKey,
                                                          final PublicKey collateralTokenProgramKey,
                                                          final PublicKey eventAuthorityKey,
                                                          final PublicKey programKey,
                                                          final FillNftPrincipalOfferParams fillParams) {
    final var keys = fillNonFungiblePrincipalOfferKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      lenderKey,
      lenderUserKey,
      offerKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      collateralMintKey,
      lenderPrincipalEscrowKey,
      borrowerPrincipalTokenAccountKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return fillNonFungiblePrincipalOffer(invokedOfferbookProgramMeta, keys, fillParams);
  }

  public static Instruction fillNonFungiblePrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                          final List<AccountMeta> keys,
                                                          final FillNftPrincipalOfferParams fillParams) {
    final byte[] _data = new byte[8 + fillParams.l()];
    int i = FILL_NON_FUNGIBLE_PRINCIPAL_OFFER_DISCRIMINATOR.write(_data, 0);
    fillParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record FillNonFungiblePrincipalOfferIxData(Discriminator discriminator, FillNftPrincipalOfferParams fillParams) implements SerDe {  

    public static FillNonFungiblePrincipalOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int FILL_PARAMS_OFFSET = 8;

    public static FillNonFungiblePrincipalOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var fillParams = FillNftPrincipalOfferParams.read(_data, i);
      return new FillNonFungiblePrincipalOfferIxData(discriminator, fillParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += fillParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator FILL_TOKEN_COLLATERAL_OFFER_DISCRIMINATOR = toDiscriminator(170, 251, 0, 181, 163, 83, 106, 228);

  public static List<AccountMeta> fillTokenCollateralOfferKeys(final SolanaAccounts solanaAccounts,
                                                               final PublicKey signerKey,
                                                               final PublicKey signerUserKey,
                                                               final PublicKey borrowerKey,
                                                               final PublicKey borrowerUserKey,
                                                               final PublicKey offerKey,
                                                               final PublicKey loanKey,
                                                               final PublicKey loanVaultKey,
                                                               final PublicKey configKey,
                                                               final PublicKey principalMintKey,
                                                               final PublicKey collateralMintKey,
                                                               final PublicKey lenderPrincipalEscrowKey,
                                                               final PublicKey borrowerCollateralEscrowKey,
                                                               final PublicKey borrowerPrincipalTokenAccountKey,
                                                               final PublicKey protocolFeeTokenAccountKey,
                                                               final PublicKey principalTokenProgramKey,
                                                               final PublicKey collateralTokenProgramKey,
                                                               final PublicKey eventAuthorityKey,
                                                               final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(borrowerKey),
      createWrite(borrowerUserKey),
      createWrite(offerKey),
      createWrite(loanKey),
      createWrite(loanVaultKey),
      createRead(configKey),
      createRead(principalMintKey),
      createRead(collateralMintKey),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(borrowerCollateralEscrowKey),
      createWrite(borrowerPrincipalTokenAccountKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(collateralTokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction fillTokenCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                     final SolanaAccounts solanaAccounts,
                                                     final PublicKey signerKey,
                                                     final PublicKey signerUserKey,
                                                     final PublicKey borrowerKey,
                                                     final PublicKey borrowerUserKey,
                                                     final PublicKey offerKey,
                                                     final PublicKey loanKey,
                                                     final PublicKey loanVaultKey,
                                                     final PublicKey configKey,
                                                     final PublicKey principalMintKey,
                                                     final PublicKey collateralMintKey,
                                                     final PublicKey lenderPrincipalEscrowKey,
                                                     final PublicKey borrowerCollateralEscrowKey,
                                                     final PublicKey borrowerPrincipalTokenAccountKey,
                                                     final PublicKey protocolFeeTokenAccountKey,
                                                     final PublicKey principalTokenProgramKey,
                                                     final PublicKey collateralTokenProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey,
                                                     final FillTokenCollateralOfferParams fillParams) {
    final var keys = fillTokenCollateralOfferKeys(
      solanaAccounts,
      signerKey,
      signerUserKey,
      borrowerKey,
      borrowerUserKey,
      offerKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      collateralMintKey,
      lenderPrincipalEscrowKey,
      borrowerCollateralEscrowKey,
      borrowerPrincipalTokenAccountKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return fillTokenCollateralOffer(invokedOfferbookProgramMeta, keys, fillParams);
  }

  public static Instruction fillTokenCollateralOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final FillTokenCollateralOfferParams fillParams) {
    final byte[] _data = new byte[8 + fillParams.l()];
    int i = FILL_TOKEN_COLLATERAL_OFFER_DISCRIMINATOR.write(_data, 0);
    fillParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record FillTokenCollateralOfferIxData(Discriminator discriminator, FillTokenCollateralOfferParams fillParams) implements SerDe {  

    public static FillTokenCollateralOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 33;

    public static final int FILL_PARAMS_OFFSET = 8;

    public static FillTokenCollateralOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var fillParams = FillTokenCollateralOfferParams.read(_data, i);
      return new FillTokenCollateralOfferIxData(discriminator, fillParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += fillParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator FILL_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR = toDiscriminator(83, 215, 176, 161, 176, 105, 129, 177);

  public static List<AccountMeta> fillTokenPrincipalOfferKeys(final SolanaAccounts solanaAccounts,
                                                              final PublicKey signerKey,
                                                              final PublicKey signerUserKey,
                                                              final PublicKey lenderKey,
                                                              final PublicKey lenderUserKey,
                                                              final PublicKey offerKey,
                                                              final PublicKey loanKey,
                                                              final PublicKey loanVaultKey,
                                                              final PublicKey configKey,
                                                              final PublicKey principalMintKey,
                                                              final PublicKey collateralMintKey,
                                                              final PublicKey lenderPrincipalEscrowKey,
                                                              final PublicKey borrowerCollateralEscrowKey,
                                                              final PublicKey borrowerPrincipalTokenAccountKey,
                                                              final PublicKey protocolFeeTokenAccountKey,
                                                              final PublicKey principalTokenProgramKey,
                                                              final PublicKey collateralTokenProgramKey,
                                                              final PublicKey eventAuthorityKey,
                                                              final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(lenderKey),
      createWrite(lenderUserKey),
      createWrite(offerKey),
      createWrite(loanKey),
      createWrite(loanVaultKey),
      createRead(configKey),
      createRead(principalMintKey),
      createRead(collateralMintKey),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(borrowerCollateralEscrowKey),
      createWrite(borrowerPrincipalTokenAccountKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(collateralTokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction fillTokenPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey signerKey,
                                                    final PublicKey signerUserKey,
                                                    final PublicKey lenderKey,
                                                    final PublicKey lenderUserKey,
                                                    final PublicKey offerKey,
                                                    final PublicKey loanKey,
                                                    final PublicKey loanVaultKey,
                                                    final PublicKey configKey,
                                                    final PublicKey principalMintKey,
                                                    final PublicKey collateralMintKey,
                                                    final PublicKey lenderPrincipalEscrowKey,
                                                    final PublicKey borrowerCollateralEscrowKey,
                                                    final PublicKey borrowerPrincipalTokenAccountKey,
                                                    final PublicKey protocolFeeTokenAccountKey,
                                                    final PublicKey principalTokenProgramKey,
                                                    final PublicKey collateralTokenProgramKey,
                                                    final PublicKey eventAuthorityKey,
                                                    final PublicKey programKey,
                                                    final FillTokenPrincipalOfferParams fillParams) {
    final var keys = fillTokenPrincipalOfferKeys(
      solanaAccounts,
      signerKey,
      signerUserKey,
      lenderKey,
      lenderUserKey,
      offerKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      collateralMintKey,
      lenderPrincipalEscrowKey,
      borrowerCollateralEscrowKey,
      borrowerPrincipalTokenAccountKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return fillTokenPrincipalOffer(invokedOfferbookProgramMeta, keys, fillParams);
  }

  public static Instruction fillTokenPrincipalOffer(final AccountMeta invokedOfferbookProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final FillTokenPrincipalOfferParams fillParams) {
    final byte[] _data = new byte[8 + fillParams.l()];
    int i = FILL_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR.write(_data, 0);
    fillParams.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record FillTokenPrincipalOfferIxData(Discriminator discriminator, FillTokenPrincipalOfferParams fillParams) implements SerDe {  

    public static FillTokenPrincipalOfferIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 33;

    public static final int FILL_PARAMS_OFFSET = 8;

    public static FillTokenPrincipalOfferIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var fillParams = FillTokenPrincipalOfferParams.read(_data, i);
      return new FillTokenPrincipalOfferIxData(discriminator, fillParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += fillParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator INIT_DISCRIMINATOR = toDiscriminator(220, 59, 207, 236, 108, 250, 47, 100);

  public static List<AccountMeta> initKeys(final SolanaAccounts solanaAccounts,
                                           final PublicKey payerKey,
                                           final PublicKey upgradeAuthorityKey,
                                           final PublicKey configKey,
                                           final PublicKey feeAuthorityKey,
                                           final PublicKey programDataKey,
                                           final PublicKey programKey) {
    return List.of(
      createWritableSigner(payerKey),
      createReadOnlySigner(upgradeAuthorityKey),
      createWrite(configKey),
      createRead(feeAuthorityKey),
      createRead(programDataKey),
      createRead(programKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  public static Instruction init(final AccountMeta invokedOfferbookProgramMeta,
                                 final SolanaAccounts solanaAccounts,
                                 final PublicKey payerKey,
                                 final PublicKey upgradeAuthorityKey,
                                 final PublicKey configKey,
                                 final PublicKey feeAuthorityKey,
                                 final PublicKey programDataKey,
                                 final PublicKey programKey) {
    final var keys = initKeys(
      solanaAccounts,
      payerKey,
      upgradeAuthorityKey,
      configKey,
      feeAuthorityKey,
      programDataKey,
      programKey
    );
    return init(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction init(final AccountMeta invokedOfferbookProgramMeta,
                                 final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, INIT_DISCRIMINATOR);
  }

  public static final Discriminator REPAY_NON_FUNGIBLE_LOAN_DISCRIMINATOR = toDiscriminator(148, 211, 15, 57, 173, 27, 26, 49);

  public static List<AccountMeta> repayNonFungibleLoanKeys(final AccountMeta invokedOfferbookProgramMeta,
                                                           final SolanaAccounts solanaAccounts,
                                                           final PublicKey signerKey,
                                                           final PublicKey signerUserKey,
                                                           final PublicKey lenderKey,
                                                           final PublicKey lenderUserKey,
                                                           final PublicKey loanKey,
                                                           final PublicKey loanVaultKey,
                                                           final PublicKey configKey,
                                                           final PublicKey principalMintKey,
                                                           final PublicKey signerPrincipalTokenAccountKey,
                                                           final PublicKey lenderPrincipalEscrowKey,
                                                           final PublicKey protocolFeeTokenAccountKey,
                                                           final PublicKey principalTokenProgramKey,
                                                           final PublicKey eventAuthorityKey,
                                                           final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(signerUserKey),
      createWrite(lenderKey),
      createWrite(lenderUserKey),
      createWrite(loanKey),
      createWrite(requireNonNullElse(loanVaultKey, invokedOfferbookProgramMeta.publicKey())),
      createRead(configKey),
      createRead(principalMintKey),
      createWrite(signerPrincipalTokenAccountKey),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction repayNonFungibleLoan(final AccountMeta invokedOfferbookProgramMeta,
                                                 final SolanaAccounts solanaAccounts,
                                                 final PublicKey signerKey,
                                                 final PublicKey signerUserKey,
                                                 final PublicKey lenderKey,
                                                 final PublicKey lenderUserKey,
                                                 final PublicKey loanKey,
                                                 final PublicKey loanVaultKey,
                                                 final PublicKey configKey,
                                                 final PublicKey principalMintKey,
                                                 final PublicKey signerPrincipalTokenAccountKey,
                                                 final PublicKey lenderPrincipalEscrowKey,
                                                 final PublicKey protocolFeeTokenAccountKey,
                                                 final PublicKey principalTokenProgramKey,
                                                 final PublicKey eventAuthorityKey,
                                                 final PublicKey programKey) {
    final var keys = repayNonFungibleLoanKeys(
      invokedOfferbookProgramMeta,
      solanaAccounts,
      signerKey,
      signerUserKey,
      lenderKey,
      lenderUserKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      signerPrincipalTokenAccountKey,
      lenderPrincipalEscrowKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return repayNonFungibleLoan(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction repayNonFungibleLoan(final AccountMeta invokedOfferbookProgramMeta,
                                                 final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, REPAY_NON_FUNGIBLE_LOAN_DISCRIMINATOR);
  }

  public static final Discriminator REPAY_TOKEN_LOAN_DISCRIMINATOR = toDiscriminator(208, 50, 208, 39, 189, 153, 25, 251);

  public static List<AccountMeta> repayTokenLoanKeys(final PublicKey signerKey,
                                                     final PublicKey signerUserKey,
                                                     final PublicKey lenderKey,
                                                     final PublicKey lenderUserKey,
                                                     final PublicKey loanKey,
                                                     final PublicKey loanVaultKey,
                                                     final PublicKey configKey,
                                                     final PublicKey principalMintKey,
                                                     final PublicKey collateralMintKey,
                                                     final PublicKey signerPrincipalTokenAccountKey,
                                                     final PublicKey lenderPrincipalEscrowKey,
                                                     final PublicKey borrowerCollateralEscrowKey,
                                                     final PublicKey protocolFeeTokenAccountKey,
                                                     final PublicKey principalTokenProgramKey,
                                                     final PublicKey collateralTokenProgramKey,
                                                     final PublicKey eventAuthorityKey,
                                                     final PublicKey programKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(signerUserKey),
      createWrite(lenderKey),
      createRead(lenderUserKey),
      createWrite(loanKey),
      createWrite(loanVaultKey),
      createRead(configKey),
      createRead(principalMintKey),
      createRead(collateralMintKey),
      createWrite(signerPrincipalTokenAccountKey),
      createWrite(lenderPrincipalEscrowKey),
      createWrite(borrowerCollateralEscrowKey),
      createWrite(protocolFeeTokenAccountKey),
      createRead(principalTokenProgramKey),
      createRead(collateralTokenProgramKey),
      createRead(eventAuthorityKey),
      createRead(programKey)
    );
  }

  public static Instruction repayTokenLoan(final AccountMeta invokedOfferbookProgramMeta,
                                           final PublicKey signerKey,
                                           final PublicKey signerUserKey,
                                           final PublicKey lenderKey,
                                           final PublicKey lenderUserKey,
                                           final PublicKey loanKey,
                                           final PublicKey loanVaultKey,
                                           final PublicKey configKey,
                                           final PublicKey principalMintKey,
                                           final PublicKey collateralMintKey,
                                           final PublicKey signerPrincipalTokenAccountKey,
                                           final PublicKey lenderPrincipalEscrowKey,
                                           final PublicKey borrowerCollateralEscrowKey,
                                           final PublicKey protocolFeeTokenAccountKey,
                                           final PublicKey principalTokenProgramKey,
                                           final PublicKey collateralTokenProgramKey,
                                           final PublicKey eventAuthorityKey,
                                           final PublicKey programKey) {
    final var keys = repayTokenLoanKeys(
      signerKey,
      signerUserKey,
      lenderKey,
      lenderUserKey,
      loanKey,
      loanVaultKey,
      configKey,
      principalMintKey,
      collateralMintKey,
      signerPrincipalTokenAccountKey,
      lenderPrincipalEscrowKey,
      borrowerCollateralEscrowKey,
      protocolFeeTokenAccountKey,
      principalTokenProgramKey,
      collateralTokenProgramKey,
      eventAuthorityKey,
      programKey
    );
    return repayTokenLoan(invokedOfferbookProgramMeta, keys);
  }

  public static Instruction repayTokenLoan(final AccountMeta invokedOfferbookProgramMeta,
                                           final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, REPAY_TOKEN_LOAN_DISCRIMINATOR);
  }

  public static final Discriminator UPDATE_CONFIG_DISCRIMINATOR = toDiscriminator(29, 158, 252, 191, 10, 83, 219, 99);

  public static List<AccountMeta> updateConfigKeys(final PublicKey adminKey,
                                                   final PublicKey configKey) {
    return List.of(
      createReadOnlySigner(adminKey),
      createWrite(configKey)
    );
  }

  public static Instruction updateConfig(final AccountMeta invokedOfferbookProgramMeta,
                                         final PublicKey adminKey,
                                         final PublicKey configKey,
                                         final UpdateConfigAction action) {
    final var keys = updateConfigKeys(
      adminKey,
      configKey
    );
    return updateConfig(invokedOfferbookProgramMeta, keys, action);
  }

  public static Instruction updateConfig(final AccountMeta invokedOfferbookProgramMeta,
                                         final List<AccountMeta> keys,
                                         final UpdateConfigAction action) {
    final byte[] _data = new byte[8 + action.l()];
    int i = UPDATE_CONFIG_DISCRIMINATOR.write(_data, 0);
    action.write(_data, i);

    return Instruction.createInstruction(invokedOfferbookProgramMeta, keys, _data);
  }

  public record UpdateConfigIxData(Discriminator discriminator, UpdateConfigAction action) implements SerDe {  

    public static UpdateConfigIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int ACTION_OFFSET = 8;

    public static UpdateConfigIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var action = UpdateConfigAction.read(_data, i);
      return new UpdateConfigIxData(discriminator, action);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += action.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + action.l();
    }
  }

  private OfferbookProgram() {
  }
}
