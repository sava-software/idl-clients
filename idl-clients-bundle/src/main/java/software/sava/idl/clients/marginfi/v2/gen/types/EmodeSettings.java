// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Controls the bank's e-mode configuration, allowing certain collateral sources to be treated more
/// favorably as collateral when used to borrow from this bank.
///
/// @param emodeTag This bank's NON-unique id that other banks will use to determine what emode rate to use when
///                 this bank is offered as collateral.
///                 
///                 For example, all stablecoin banks might share the same emode_tag, and in their entries, each
///                 such stablecoin bank will recognize that collateral sources with this "stable" tag get
///                 preferential weights. When a new stablecoin is added that is considered riskier, it may get
///                 a new, less favorable emode tag, and eventually get upgraded to the same one as the other
///                 stables
///                 
///                 * 0 is in an invalid tag and will do nothing.
/// @param timestamp Unix timestamp from the system clock when emode state was last updated
/// @param flags EMODE_ON (1) - If set, at least one entry is configured
///              2, 4, 8, etc, Reserved for future use
public record EmodeSettings(int emodeTag,
                            byte[] pad0,
                            long timestamp,
                            long flags,
                            EmodeConfig emodeConfig) implements SerDe {

  public static final int BYTES = 424;
  public static final int PAD_0_LEN = 6;

  public static final int EMODE_TAG_OFFSET = 0;
  public static final int PAD_0_OFFSET = 2;
  public static final int TIMESTAMP_OFFSET = 8;
  public static final int FLAGS_OFFSET = 16;
  public static final int EMODE_CONFIG_OFFSET = 24;

  public static EmodeSettings read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var emodeTag = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var pad0 = new byte[6];
    i += SerDeUtil.readArray(pad0, _data, i);
    final var timestamp = getInt64LE(_data, i);
    i += 8;
    final var flags = getInt64LE(_data, i);
    i += 8;
    final var emodeConfig = EmodeConfig.read(_data, i);
    return new EmodeSettings(emodeTag,
                             pad0,
                             timestamp,
                             flags,
                             emodeConfig);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt16LE(_data, i, emodeTag);
    i += 2;
    i += SerDeUtil.writeArrayChecked(pad0, 6, _data, i);
    putInt64LE(_data, i, timestamp);
    i += 8;
    putInt64LE(_data, i, flags);
    i += 8;
    i += emodeConfig.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
