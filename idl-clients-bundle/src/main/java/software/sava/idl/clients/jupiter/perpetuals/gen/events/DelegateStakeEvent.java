// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param custodyTotalStakedAmount: u64
/// @param stakeAmount: u64
public record DelegateStakeEvent(Discriminator discriminator,
                                 PublicKey custody,
                                 PublicKey stakeAccount,
                                 PublicKey stakeInfo,
                                 long custodyTotalStakedAmount,
                                 long stakeAmount,
                                 PublicKey validatorVoteAccount,
                                 long delegateTime) implements PerpetualsEvent {

  public static final int BYTES = 160;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(109, 50, 19, 133, 150, 191, 244, 85);

  public static final int CUSTODY_OFFSET = 8;
  public static final int STAKE_ACCOUNT_OFFSET = 40;
  public static final int STAKE_INFO_OFFSET = 72;
  public static final int CUSTODY_TOTAL_STAKED_AMOUNT_OFFSET = 104;
  public static final int STAKE_AMOUNT_OFFSET = 112;
  public static final int VALIDATOR_VOTE_ACCOUNT_OFFSET = 120;
  public static final int DELEGATE_TIME_OFFSET = 152;

  public static DelegateStakeEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createDiscriminator(_data, _offset, 8);
    int i = _offset + discriminator.length();
    final var custody = readPubKey(_data, i);
    i += 32;
    final var stakeAccount = readPubKey(_data, i);
    i += 32;
    final var stakeInfo = readPubKey(_data, i);
    i += 32;
    final var custodyTotalStakedAmount = getInt64LE(_data, i);
    i += 8;
    final var stakeAmount = getInt64LE(_data, i);
    i += 8;
    final var validatorVoteAccount = readPubKey(_data, i);
    i += 32;
    final var delegateTime = getInt64LE(_data, i);
    return new DelegateStakeEvent(discriminator,
                                  custody,
                                  stakeAccount,
                                  stakeInfo,
                                  custodyTotalStakedAmount,
                                  stakeAmount,
                                  validatorVoteAccount,
                                  delegateTime);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    custody.write(_data, i);
    i += 32;
    stakeAccount.write(_data, i);
    i += 32;
    stakeInfo.write(_data, i);
    i += 32;
    putInt64LE(_data, i, custodyTotalStakedAmount);
    i += 8;
    putInt64LE(_data, i, stakeAmount);
    i += 8;
    validatorVoteAccount.write(_data, i);
    i += 32;
    putInt64LE(_data, i, delegateTime);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
