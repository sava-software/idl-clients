// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param key This account's own key. A PDA derived from `marginfi_account`
/// @param marginfiAccount Account this record tracks
/// @param recordPayer The key that paid to create this account. At some point, we may allow this wallet to reclaim
///                    the rent paid to open a record.
/// @param liquidationReceiver The liquidator taking receivership of the `marginfi_account` to complete a liquidation. Pays
///                            the liquidation fee.
///                            * Always pubkey default unless actively within a liquidation event.
/// @param entries Basic historical data for the last few liquidation events on this account
public record LiquidationRecord(PublicKey _address,
                                Discriminator discriminator,
                                PublicKey key,
                                PublicKey marginfiAccount,
                                PublicKey recordPayer,
                                PublicKey liquidationReceiver,
                                LiquidationEntry[] entries,
                                LiquidationCache cache,
                                byte[] reserved0,
                                byte[] reserved2,
                                byte[] reserved3) implements SerDe {

  public static final int BYTES = 520;
  public static final int ENTRIES_LEN = 4;
  public static final int RESERVED_0_LEN = 64;
  public static final int RESERVED_2_LEN = 16;
  public static final int RESERVED_3_LEN = 8;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(95, 116, 23, 132, 89, 210, 245, 162);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int KEY_OFFSET = 8;
  public static final int MARGINFI_ACCOUNT_OFFSET = 40;
  public static final int RECORD_PAYER_OFFSET = 72;
  public static final int LIQUIDATION_RECEIVER_OFFSET = 104;
  public static final int ENTRIES_OFFSET = 136;
  public static final int CACHE_OFFSET = 328;
  public static final int RESERVED_0_OFFSET = 432;
  public static final int RESERVED_2_OFFSET = 496;
  public static final int RESERVED_3_OFFSET = 512;

  public static Filter createKeyFilter(final PublicKey key) {
    return Filter.createMemCompFilter(KEY_OFFSET, key);
  }

  public static Filter createMarginfiAccountFilter(final PublicKey marginfiAccount) {
    return Filter.createMemCompFilter(MARGINFI_ACCOUNT_OFFSET, marginfiAccount);
  }

  public static Filter createRecordPayerFilter(final PublicKey recordPayer) {
    return Filter.createMemCompFilter(RECORD_PAYER_OFFSET, recordPayer);
  }

  public static Filter createLiquidationReceiverFilter(final PublicKey liquidationReceiver) {
    return Filter.createMemCompFilter(LIQUIDATION_RECEIVER_OFFSET, liquidationReceiver);
  }

  public static Filter createCacheFilter(final LiquidationCache cache) {
    return Filter.createMemCompFilter(CACHE_OFFSET, cache.write());
  }

  public static LiquidationRecord read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static LiquidationRecord read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static LiquidationRecord read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], LiquidationRecord> FACTORY = LiquidationRecord::read;

  public static LiquidationRecord read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var key = readPubKey(_data, i);
    i += 32;
    final var marginfiAccount = readPubKey(_data, i);
    i += 32;
    final var recordPayer = readPubKey(_data, i);
    i += 32;
    final var liquidationReceiver = readPubKey(_data, i);
    i += 32;
    final var entries = new LiquidationEntry[4];
    i += SerDeUtil.readArray(entries, LiquidationEntry::read, _data, i);
    final var cache = LiquidationCache.read(_data, i);
    i += cache.l();
    final var reserved0 = new byte[64];
    i += SerDeUtil.readArray(reserved0, _data, i);
    final var reserved2 = new byte[16];
    i += SerDeUtil.readArray(reserved2, _data, i);
    final var reserved3 = new byte[8];
    SerDeUtil.readArray(reserved3, _data, i);
    return new LiquidationRecord(_address,
                                 discriminator,
                                 key,
                                 marginfiAccount,
                                 recordPayer,
                                 liquidationReceiver,
                                 entries,
                                 cache,
                                 reserved0,
                                 reserved2,
                                 reserved3);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    key.write(_data, i);
    i += 32;
    marginfiAccount.write(_data, i);
    i += 32;
    recordPayer.write(_data, i);
    i += 32;
    liquidationReceiver.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(entries, 4, _data, i);
    i += cache.write(_data, i);
    i += SerDeUtil.writeArrayChecked(reserved0, 64, _data, i);
    i += SerDeUtil.writeArrayChecked(reserved2, 16, _data, i);
    i += SerDeUtil.writeArrayChecked(reserved3, 8, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
