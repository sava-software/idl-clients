// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.doves.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.idl.clients.jupiter.doves.gen.types.UpdateMessage;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class DovesProgram {

  public static final Discriminator INITIALIZE_DISCRIMINATOR = toDiscriminator(175, 175, 109, 31, 13, 152, 155, 237);

  public static List<AccountMeta> initializeKeys(final SolanaAccounts solanaAccounts,
                                                 final PublicKey adminKey,
                                                 final PublicKey feedKey) {
    return List.of(
      createWritableSigner(adminKey),
      createWrite(feedKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  public static Instruction initialize(final AccountMeta invokedDovesProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey adminKey,
                                       final PublicKey feedKey,
                                       final byte[] pair,
                                       final byte[] feedSigner) {
    final var keys = initializeKeys(
      solanaAccounts,
      adminKey,
      feedKey
    );
    return initialize(invokedDovesProgramMeta, keys, pair, feedSigner);
  }

  public static Instruction initialize(final AccountMeta invokedDovesProgramMeta,
                                       final List<AccountMeta> keys,
                                       final byte[] pair,
                                       final byte[] feedSigner) {
    final byte[] _data = new byte[8 + SerDeUtil.lenArray(pair) + SerDeUtil.lenArray(feedSigner)];
    int i = INITIALIZE_DISCRIMINATOR.write(_data, 0);
    i += SerDeUtil.writeArrayChecked(pair, 32, _data, i);
    SerDeUtil.writeArrayChecked(feedSigner, 33, _data, i);

    return Instruction.createInstruction(invokedDovesProgramMeta, keys, _data);
  }

  public record InitializeIxData(Discriminator discriminator, byte[] pair, byte[] feedSigner) implements SerDe {  

    public static InitializeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 73;
    public static final int PAIR_LEN = 32;
    public static final int FEED_SIGNER_LEN = 33;

    public static final int PAIR_OFFSET = 8;
    public static final int FEED_SIGNER_OFFSET = 40;

    public static InitializeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var pair = new byte[32];
      i += SerDeUtil.readArray(pair, _data, i);
      final var feedSigner = new byte[33];
      SerDeUtil.readArray(feedSigner, _data, i);
      return new InitializeIxData(discriminator, pair, feedSigner);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeArrayChecked(pair, 32, _data, i);
      i += SerDeUtil.writeArrayChecked(feedSigner, 33, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator INITIALIZE_PYTH_LAZER_PRICE_FEED_DISCRIMINATOR = toDiscriminator(244, 187, 210, 9, 84, 50, 255, 137);

  public static List<AccountMeta> initializePythLazerPriceFeedKeys(final SolanaAccounts solanaAccounts,
                                                                   final PublicKey signerKey,
                                                                   final PublicKey feedKey) {
    return List.of(
      createWritableSigner(signerKey),
      createWrite(feedKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// @param feedId: u32
  /// @param channelId: u8
  public static Instruction initializePythLazerPriceFeed(final AccountMeta invokedDovesProgramMeta,
                                                         final SolanaAccounts solanaAccounts,
                                                         final PublicKey signerKey,
                                                         final PublicKey feedKey,
                                                         final byte[] pair,
                                                         final long feedId,
                                                         final int channelId) {
    final var keys = initializePythLazerPriceFeedKeys(
      solanaAccounts,
      signerKey,
      feedKey
    );
    return initializePythLazerPriceFeed(
      invokedDovesProgramMeta,
      keys,
      pair,
      feedId,
      channelId
    );
  }

  /// @param feedId: u32
  /// @param channelId: u8
  public static Instruction initializePythLazerPriceFeed(final AccountMeta invokedDovesProgramMeta,
                                                         final List<AccountMeta> keys,
                                                         final byte[] pair,
                                                         final long feedId,
                                                         final int channelId) {
    final byte[] _data = new byte[13 + SerDeUtil.lenArray(pair)];
    int i = INITIALIZE_PYTH_LAZER_PRICE_FEED_DISCRIMINATOR.write(_data, 0);
    i += SerDeUtil.writeArrayChecked(pair, 32, _data, i);
    putInt32LE(_data, i, (int) feedId);
    i += 4;
    _data[i] = (byte) channelId;

    return Instruction.createInstruction(invokedDovesProgramMeta, keys, _data);
  }

  /// @param feedId: u32
  /// @param channelId: u8
  public record InitializePythLazerPriceFeedIxData(Discriminator discriminator,
                                                   byte[] pair,
                                                   long feedId,
                                                   int channelId) implements SerDe {  

    public static InitializePythLazerPriceFeedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 45;
    public static final int PAIR_LEN = 32;

    public static final int PAIR_OFFSET = 8;
    public static final int FEED_ID_OFFSET = 40;
    public static final int CHANNEL_ID_OFFSET = 44;

    public static InitializePythLazerPriceFeedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var pair = new byte[32];
      i += SerDeUtil.readArray(pair, _data, i);
      final var feedId = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var channelId = _data[i] & 0xFF;
      return new InitializePythLazerPriceFeedIxData(discriminator, pair, feedId, channelId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeArrayChecked(pair, 32, _data, i);
      putInt32LE(_data, i, (int) feedId);
      i += 4;
      _data[i] = (byte) channelId;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UPDATE_AG_PRICE_2_DISCRIMINATOR = toDiscriminator(61, 65, 60, 229, 235, 142, 250, 117);

  public static List<AccountMeta> updateAgPrice2Keys(final PublicKey signerKey,
                                                     final PublicKey agPriceFeedKey,
                                                     final PublicKey pythLazerFeedKey,
                                                     final PublicKey clFeedKey,
                                                     final PublicKey redstoneFeedKey) {
    return List.of(
      createReadOnlySigner(signerKey),
      createWrite(agPriceFeedKey),
      createRead(pythLazerFeedKey),
      createRead(clFeedKey),
      createRead(redstoneFeedKey)
    );
  }

  public static Instruction updateAgPrice2(final AccountMeta invokedDovesProgramMeta,
                                           final PublicKey signerKey,
                                           final PublicKey agPriceFeedKey,
                                           final PublicKey pythLazerFeedKey,
                                           final PublicKey clFeedKey,
                                           final PublicKey redstoneFeedKey) {
    final var keys = updateAgPrice2Keys(
      signerKey,
      agPriceFeedKey,
      pythLazerFeedKey,
      clFeedKey,
      redstoneFeedKey
    );
    return updateAgPrice2(invokedDovesProgramMeta, keys);
  }

  public static Instruction updateAgPrice2(final AccountMeta invokedDovesProgramMeta,
                                           final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedDovesProgramMeta, keys, UPDATE_AG_PRICE_2_DISCRIMINATOR);
  }

  public static final Discriminator UPDATE_AG_PRICE_FEED_DISCRIMINATOR = toDiscriminator(181, 230, 146, 98, 116, 172, 249, 27);

  public static List<AccountMeta> updateAgPriceFeedKeys(final PublicKey signerKey,
                                                        final PublicKey agPriceFeedKey,
                                                        final PublicKey edgeFeedKey,
                                                        final PublicKey clFeedKey,
                                                        final PublicKey pythFeedKey) {
    return List.of(
      createReadOnlySigner(signerKey),
      createWrite(agPriceFeedKey),
      createRead(edgeFeedKey),
      createRead(clFeedKey),
      createRead(pythFeedKey)
    );
  }

  public static Instruction updateAgPriceFeed(final AccountMeta invokedDovesProgramMeta,
                                              final PublicKey signerKey,
                                              final PublicKey agPriceFeedKey,
                                              final PublicKey edgeFeedKey,
                                              final PublicKey clFeedKey,
                                              final PublicKey pythFeedKey) {
    final var keys = updateAgPriceFeedKeys(
      signerKey,
      agPriceFeedKey,
      edgeFeedKey,
      clFeedKey,
      pythFeedKey
    );
    return updateAgPriceFeed(invokedDovesProgramMeta, keys);
  }

  public static Instruction updateAgPriceFeed(final AccountMeta invokedDovesProgramMeta,
                                              final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedDovesProgramMeta, keys, UPDATE_AG_PRICE_FEED_DISCRIMINATOR);
  }

  public static final Discriminator UPDATE_AG_PRICE_FEED_TO_V_2_DISCRIMINATOR = toDiscriminator(166, 45, 37, 18, 198, 105, 232, 53);

  public static List<AccountMeta> updateAgPriceFeedToV2Keys(final PublicKey signerKey,
                                                            final PublicKey agPriceFeedKey,
                                                            final PublicKey pythLazerFeedKey,
                                                            final PublicKey redstoneFeedKey) {
    return List.of(
      createReadOnlySigner(signerKey),
      createWrite(agPriceFeedKey),
      createRead(pythLazerFeedKey),
      createRead(redstoneFeedKey)
    );
  }

  public static Instruction updateAgPriceFeedToV2(final AccountMeta invokedDovesProgramMeta,
                                                  final PublicKey signerKey,
                                                  final PublicKey agPriceFeedKey,
                                                  final PublicKey pythLazerFeedKey,
                                                  final PublicKey redstoneFeedKey) {
    final var keys = updateAgPriceFeedToV2Keys(
      signerKey,
      agPriceFeedKey,
      pythLazerFeedKey,
      redstoneFeedKey
    );
    return updateAgPriceFeedToV2(invokedDovesProgramMeta, keys);
  }

  public static Instruction updateAgPriceFeedToV2(final AccountMeta invokedDovesProgramMeta,
                                                  final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedDovesProgramMeta, keys, UPDATE_AG_PRICE_FEED_TO_V_2_DISCRIMINATOR);
  }

  public static final Discriminator UPDATE_MANY_WITH_PYTH_LAZER_DISCRIMINATOR = toDiscriminator(203, 59, 95, 176, 236, 21, 73, 153);

  public static List<AccountMeta> updateManyWithPythLazerKeys(final SolanaAccounts solanaAccounts,
                                                              final PublicKey signerKey,
                                                              final PublicKey pythLazerStorageKey,
                                                              final PublicKey pythLazerTreasuryKey,
                                                              final PublicKey pythLazerProgramKey) {
    return List.of(
      createWritableSigner(signerKey),
      createRead(pythLazerStorageKey),
      createWrite(pythLazerTreasuryKey),
      createRead(pythLazerProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(solanaAccounts.instructionsSysVar())
    );
  }

  /// @param ed25519InstructionIndex: u16
  /// @param signatureIndex: u8
  public static Instruction updateManyWithPythLazer(final AccountMeta invokedDovesProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey signerKey,
                                                    final PublicKey pythLazerStorageKey,
                                                    final PublicKey pythLazerTreasuryKey,
                                                    final PublicKey pythLazerProgramKey,
                                                    final byte[] messageData,
                                                    final int ed25519InstructionIndex,
                                                    final int signatureIndex,
                                                    final boolean raise) {
    final var keys = updateManyWithPythLazerKeys(
      solanaAccounts,
      signerKey,
      pythLazerStorageKey,
      pythLazerTreasuryKey,
      pythLazerProgramKey
    );
    return updateManyWithPythLazer(
      invokedDovesProgramMeta,
      keys,
      messageData,
      ed25519InstructionIndex,
      signatureIndex,
      raise
    );
  }

  /// @param ed25519InstructionIndex: u16
  /// @param signatureIndex: u8
  public static Instruction updateManyWithPythLazer(final AccountMeta invokedDovesProgramMeta,
                                                    final List<AccountMeta> keys,
                                                    final byte[] messageData,
                                                    final int ed25519InstructionIndex,
                                                    final int signatureIndex,
                                                    final boolean raise) {
    final byte[] _data = new byte[12 + SerDeUtil.lenVector(4, messageData)];
    int i = UPDATE_MANY_WITH_PYTH_LAZER_DISCRIMINATOR.write(_data, 0);
    i += SerDeUtil.writeVector(4, messageData, _data, i);
    putInt16LE(_data, i, ed25519InstructionIndex);
    i += 2;
    _data[i] = (byte) signatureIndex;
    ++i;
    _data[i] = (byte) (raise ? 1 : 0);

    return Instruction.createInstruction(invokedDovesProgramMeta, keys, _data);
  }

  /// @param ed25519InstructionIndex: u16
  /// @param signatureIndex: u8
  public record UpdateManyWithPythLazerIxData(Discriminator discriminator,
                                              byte[] messageData,
                                              int ed25519InstructionIndex,
                                              int signatureIndex,
                                              boolean raise) implements SerDe {  

    public static UpdateManyWithPythLazerIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int MESSAGE_DATA_OFFSET = 8;

    public static UpdateManyWithPythLazerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var messageData = SerDeUtil.readbyteVector(4, _data, i);
      i += SerDeUtil.lenVector(4, messageData);
      final var ed25519InstructionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var signatureIndex = _data[i] & 0xFF;
      ++i;
      final var raise = _data[i] == 1;
      return new UpdateManyWithPythLazerIxData(discriminator,
                                               messageData,
                                               ed25519InstructionIndex,
                                               signatureIndex,
                                               raise);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeVector(4, messageData, _data, i);
      putInt16LE(_data, i, ed25519InstructionIndex);
      i += 2;
      _data[i] = (byte) signatureIndex;
      ++i;
      _data[i] = (byte) (raise ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + SerDeUtil.lenVector(4, messageData) + 2 + 1 + 1;
    }
  }

  public static final Discriminator UPDATE_WITH_SIGNER_DISCRIMINATOR = toDiscriminator(117, 159, 82, 197, 207, 85, 237, 45);

  public static List<AccountMeta> updateWithSignerKeys(final SolanaAccounts solanaAccounts,
                                                       final PublicKey signerKey,
                                                       final PublicKey feedKey) {
    return List.of(
      createReadOnlySigner(signerKey),
      createWrite(feedKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  public static Instruction updateWithSigner(final AccountMeta invokedDovesProgramMeta,
                                             final SolanaAccounts solanaAccounts,
                                             final PublicKey signerKey,
                                             final PublicKey feedKey,
                                             final UpdateMessage update,
                                             final boolean raise) {
    final var keys = updateWithSignerKeys(
      solanaAccounts,
      signerKey,
      feedKey
    );
    return updateWithSigner(invokedDovesProgramMeta, keys, update, raise);
  }

  public static Instruction updateWithSigner(final AccountMeta invokedDovesProgramMeta,
                                             final List<AccountMeta> keys,
                                             final UpdateMessage update,
                                             final boolean raise) {
    final byte[] _data = new byte[9 + update.l()];
    int i = UPDATE_WITH_SIGNER_DISCRIMINATOR.write(_data, 0);
    i += update.write(_data, i);
    _data[i] = (byte) (raise ? 1 : 0);

    return Instruction.createInstruction(invokedDovesProgramMeta, keys, _data);
  }

  public record UpdateWithSignerIxData(Discriminator discriminator, UpdateMessage update, boolean raise) implements SerDe {  

    public static UpdateWithSignerIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 91;

    public static final int UPDATE_OFFSET = 8;
    public static final int RAISE_OFFSET = 90;

    public static UpdateWithSignerIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var update = UpdateMessage.read(_data, i);
      i += update.l();
      final var raise = _data[i] == 1;
      return new UpdateWithSignerIxData(discriminator, update, raise);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += update.write(_data, i);
      _data[i] = (byte) (raise ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private DovesProgram() {
  }
}
