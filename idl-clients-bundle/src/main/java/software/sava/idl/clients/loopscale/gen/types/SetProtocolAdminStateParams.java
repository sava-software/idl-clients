// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.loopscale.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;

/// @param protocolAdmin: Option<publicKey>
/// @param operationsAdmin: Option<publicKey>
/// @param refinanceAdmin: Option<publicKey>
/// @param frozen: Option<bool>
public record SetProtocolAdminStateParams(PublicKey protocolAdmin,
                                          PublicKey operationsAdmin,
                                          PublicKey refinanceAdmin,
                                          Boolean frozen) implements SerDe {

  public static final int PROTOCOL_ADMIN_OFFSET = 1;

  public static SetProtocolAdminStateParams read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final PublicKey protocolAdmin;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      protocolAdmin = null;
      ++i;
    } else {
      ++i;
      protocolAdmin = readPubKey(_data, i);
      i += 32;
    }
    final PublicKey operationsAdmin;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      operationsAdmin = null;
      ++i;
    } else {
      ++i;
      operationsAdmin = readPubKey(_data, i);
      i += 32;
    }
    final PublicKey refinanceAdmin;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      refinanceAdmin = null;
      ++i;
    } else {
      ++i;
      refinanceAdmin = readPubKey(_data, i);
      i += 32;
    }
    final Boolean frozen;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      frozen = null;
    } else {
      ++i;
      frozen = _data[i] == 1;
    }
    return new SetProtocolAdminStateParams(protocolAdmin,
                                           operationsAdmin,
                                           refinanceAdmin,
                                           frozen);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, protocolAdmin, _data, i);
    i += SerDeUtil.writeOptional(1, operationsAdmin, _data, i);
    i += SerDeUtil.writeOptional(1, refinanceAdmin, _data, i);
    i += SerDeUtil.writeOptional(1, frozen, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (protocolAdmin == null ? 1 : (1 + 32)) + (operationsAdmin == null ? 1 : (1 + 32)) + (refinanceAdmin == null ? 1 : (1 + 32)) + (frozen == null ? 1 : (1 + 1));
  }
}
