// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amountBaseCollected: u64
public record WrapperCollectInterestEvent(Discriminator discriminator,
                                          PublicKey depositor,
                                          PublicKey vault,
                                          long amountBaseCollected,
                                          long unixTimestamp) implements ExponentCoreEvent {

  public static final int BYTES = 88;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(177, 39, 249, 210, 72, 117, 16, 192);

  public static final int DEPOSITOR_OFFSET = 8;
  public static final int VAULT_OFFSET = 40;
  public static final int AMOUNT_BASE_COLLECTED_OFFSET = 72;
  public static final int UNIX_TIMESTAMP_OFFSET = 80;

  public static WrapperCollectInterestEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var depositor = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var amountBaseCollected = getInt64LE(_data, i);
    i += 8;
    final var unixTimestamp = getInt64LE(_data, i);
    return new WrapperCollectInterestEvent(discriminator,
                                           depositor,
                                           vault,
                                           amountBaseCollected,
                                           unixTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    depositor.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amountBaseCollected);
    i += 8;
    putInt64LE(_data, i, unixTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
