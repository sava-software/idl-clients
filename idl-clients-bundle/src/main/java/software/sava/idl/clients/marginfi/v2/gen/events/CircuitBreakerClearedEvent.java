// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.marginfi.v2.gen.events;

import software.sava.core.programs.Discriminator;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param priorTier: u8
/// @param reason: u8 One of the `CB_CLEAR_REASON_*` constants.
public record CircuitBreakerClearedEvent(Discriminator discriminator,
                                         int priorTier,
                                         int reason,
                                         long currentTimestamp) implements MarginfiEvent {

  public static final int BYTES = 18;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(97, 244, 85, 48, 246, 155, 29, 219);

  public static final int PRIOR_TIER_OFFSET = 8;
  public static final int REASON_OFFSET = 9;
  public static final int CURRENT_TIMESTAMP_OFFSET = 10;

  public static CircuitBreakerClearedEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var priorTier = _data[i] & 0xFF;
    ++i;
    final var reason = _data[i] & 0xFF;
    ++i;
    final var currentTimestamp = getInt64LE(_data, i);
    return new CircuitBreakerClearedEvent(discriminator, priorTier, reason, currentTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    _data[i] = (byte) priorTier;
    ++i;
    _data[i] = (byte) reason;
    ++i;
    putInt64LE(_data, i, currentTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
