// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

/// Account lists for validating CPI calls to the SY program
///
/// @param getSyState Fetch SY state
/// @param depositSy Deposit SY into personal account owned by vault
/// @param withdrawSy Withdraw SY from personal account owned by vault
/// @param claimEmission Settle rewards for vault to accounts owned by the vault
/// @param getPositionState Get personal yield position
public record CpiAccounts(CpiInterfaceContext[] getSyState,
                          CpiInterfaceContext[] depositSy,
                          CpiInterfaceContext[] withdrawSy,
                          CpiInterfaceContext[][] claimEmission,
                          CpiInterfaceContext[] getPositionState) implements SerDe {

  public static final int GET_SY_STATE_OFFSET = 0;

  public static CpiAccounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var getSyState = SerDeUtil.readVector(4, CpiInterfaceContext.class, CpiInterfaceContext::read, _data, i);
    i += SerDeUtil.lenVector(4, getSyState);
    final var depositSy = SerDeUtil.readVector(4, CpiInterfaceContext.class, CpiInterfaceContext::read, _data, i);
    i += SerDeUtil.lenVector(4, depositSy);
    final var withdrawSy = SerDeUtil.readVector(4, CpiInterfaceContext.class, CpiInterfaceContext::read, _data, i);
    i += SerDeUtil.lenVector(4, withdrawSy);
    final var claimEmission = SerDeUtil.readMultiDimensionVector(4, CpiInterfaceContext.class, CpiInterfaceContext::read, _data, i);
    i += SerDeUtil.lenVector(4, claimEmission);
    final var getPositionState = SerDeUtil.readVector(4, CpiInterfaceContext.class, CpiInterfaceContext::read, _data, i);
    return new CpiAccounts(getSyState,
                           depositSy,
                           withdrawSy,
                           claimEmission,
                           getPositionState);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeVector(4, getSyState, _data, i);
    i += SerDeUtil.writeVector(4, depositSy, _data, i);
    i += SerDeUtil.writeVector(4, withdrawSy, _data, i);
    i += SerDeUtil.writeVector(4, claimEmission, _data, i);
    i += SerDeUtil.writeVector(4, getPositionState, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return SerDeUtil.lenVector(4, getSyState)
         + SerDeUtil.lenVector(4, depositSy)
         + SerDeUtil.lenVector(4, withdrawSy)
         + SerDeUtil.lenVector(4, claimEmission)
         + SerDeUtil.lenVector(4, getPositionState);
  }
}
