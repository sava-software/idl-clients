// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// T2/T4 smart collateral: minimum token amounts per collateral share (1e18 precision).
/// After the core liquidation, the vault calls DEX `withdraw_perfect` (or
/// `withdraw_perfect_in_one_token`) with per-share slippage bounds computed as
/// `token_per_unit_shares * actual_col_shares / 1e18`.
/// Set a field to `0` to receive the full withdrawal in the *other* token only.
///
/// @param token0PerUnitShares: u64
/// @param token1PerUnitShares: u64
public record LiquidateDexColAmounts(long token0PerUnitShares, long token1PerUnitShares) implements SerDe {

  public static final int BYTES = 16;

  public static final int TOKEN_0_PER_UNIT_SHARES_OFFSET = 0;
  public static final int TOKEN_1_PER_UNIT_SHARES_OFFSET = 8;

  public static LiquidateDexColAmounts read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var token0PerUnitShares = getInt64LE(_data, i);
    i += 8;
    final var token1PerUnitShares = getInt64LE(_data, i);
    return new LiquidateDexColAmounts(token0PerUnitShares, token1PerUnitShares);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt64LE(_data, i, token0PerUnitShares);
    i += 8;
    putInt64LE(_data, i, token1PerUnitShares);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
