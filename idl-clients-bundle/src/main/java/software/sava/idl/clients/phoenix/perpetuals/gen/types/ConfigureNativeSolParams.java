// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.perpetuals.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.util.OptionalLong;

import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;

/// @param maxPerTraderBalance: Option<u64>
/// @param maxGlobalBalance: Option<u64>
/// @param minMarginDiscount: Option<u32>
/// @param maxMarginDiscount: Option<u32>
/// @param maxLiquidationDiscount: Option<u32>
/// @param minLiquidationSlippage: Option<u32>
/// @param maxLiquidationSize: Option<u64>
/// @param postLiquidationBuffer: Option<u64>
/// @param quoteLotCollateralShortfallBuffer: Option<u64>
/// @param disablePositionAuthoritySwap: Option<bool>
public record ConfigureNativeSolParams(OptionalLong maxPerTraderBalance,
                                       OptionalLong maxGlobalBalance,
                                       OptionalLong minMarginDiscount,
                                       OptionalLong maxMarginDiscount,
                                       OptionalLong maxLiquidationDiscount,
                                       OptionalLong minLiquidationSlippage,
                                       OptionalLong maxLiquidationSize,
                                       OptionalLong postLiquidationBuffer,
                                       OptionalLong quoteLotCollateralShortfallBuffer,
                                       Boolean disablePositionAuthoritySwap) implements SerDe {

  public static final int MAX_PER_TRADER_BALANCE_OFFSET = 1;

  public static ConfigureNativeSolParams read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final OptionalLong maxPerTraderBalance;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxPerTraderBalance = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      maxPerTraderBalance = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong maxGlobalBalance;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxGlobalBalance = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      maxGlobalBalance = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong minMarginDiscount;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      minMarginDiscount = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      minMarginDiscount = OptionalLong.of(Integer.toUnsignedLong(getInt32LE(_data, i)));
      i += 4;
    }
    final OptionalLong maxMarginDiscount;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxMarginDiscount = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      maxMarginDiscount = OptionalLong.of(Integer.toUnsignedLong(getInt32LE(_data, i)));
      i += 4;
    }
    final OptionalLong maxLiquidationDiscount;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxLiquidationDiscount = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      maxLiquidationDiscount = OptionalLong.of(Integer.toUnsignedLong(getInt32LE(_data, i)));
      i += 4;
    }
    final OptionalLong minLiquidationSlippage;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      minLiquidationSlippage = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      minLiquidationSlippage = OptionalLong.of(Integer.toUnsignedLong(getInt32LE(_data, i)));
      i += 4;
    }
    final OptionalLong maxLiquidationSize;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      maxLiquidationSize = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      maxLiquidationSize = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong postLiquidationBuffer;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      postLiquidationBuffer = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      postLiquidationBuffer = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong quoteLotCollateralShortfallBuffer;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      quoteLotCollateralShortfallBuffer = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      quoteLotCollateralShortfallBuffer = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final Boolean disablePositionAuthoritySwap;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      disablePositionAuthoritySwap = null;
    } else {
      ++i;
      disablePositionAuthoritySwap = _data[i] == 1;
    }
    return new ConfigureNativeSolParams(maxPerTraderBalance,
                                        maxGlobalBalance,
                                        minMarginDiscount,
                                        maxMarginDiscount,
                                        maxLiquidationDiscount,
                                        minLiquidationSlippage,
                                        maxLiquidationSize,
                                        postLiquidationBuffer,
                                        quoteLotCollateralShortfallBuffer,
                                        disablePositionAuthoritySwap);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, maxPerTraderBalance, _data, i);
    i += SerDeUtil.writeOptional(1, maxGlobalBalance, _data, i);
    i += SerDeUtil.writeOptionalUnsignedInt(1, minMarginDiscount, _data, i);
    i += SerDeUtil.writeOptionalUnsignedInt(1, maxMarginDiscount, _data, i);
    i += SerDeUtil.writeOptionalUnsignedInt(1, maxLiquidationDiscount, _data, i);
    i += SerDeUtil.writeOptionalUnsignedInt(1, minLiquidationSlippage, _data, i);
    i += SerDeUtil.writeOptional(1, maxLiquidationSize, _data, i);
    i += SerDeUtil.writeOptional(1, postLiquidationBuffer, _data, i);
    i += SerDeUtil.writeOptional(1, quoteLotCollateralShortfallBuffer, _data, i);
    i += SerDeUtil.writeOptional(1, disablePositionAuthoritySwap, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (maxPerTraderBalance == null || maxPerTraderBalance.isEmpty() ? 1 : (1 + 8))
         + (maxGlobalBalance == null || maxGlobalBalance.isEmpty() ? 1 : (1 + 8))
         + (minMarginDiscount == null || minMarginDiscount.isEmpty() ? 1 : (1 + 4))
         + (maxMarginDiscount == null || maxMarginDiscount.isEmpty() ? 1 : (1 + 4))
         + (maxLiquidationDiscount == null || maxLiquidationDiscount.isEmpty() ? 1 : (1 + 4))
         + (minLiquidationSlippage == null || minLiquidationSlippage.isEmpty() ? 1 : (1 + 4))
         + (maxLiquidationSize == null || maxLiquidationSize.isEmpty() ? 1 : (1 + 8))
         + (postLiquidationBuffer == null || postLiquidationBuffer.isEmpty() ? 1 : (1 + 8))
         + (quoteLotCollateralShortfallBuffer == null || quoteLotCollateralShortfallBuffer.isEmpty() ? 1 : (1 + 8))
         + (disablePositionAuthoritySwap == null ? 1 : (1 + 1));
  }
}
