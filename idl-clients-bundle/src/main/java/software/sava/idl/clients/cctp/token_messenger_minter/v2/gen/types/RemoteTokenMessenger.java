// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.cctp.token_messenger_minter.v2.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param domain: u32
public record RemoteTokenMessenger(PublicKey _address, Discriminator discriminator, long domain, PublicKey tokenMessenger) implements SerDe {

  public static final int BYTES = 44;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(105, 115, 174, 34, 95, 233, 138, 252);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int DOMAIN_OFFSET = 8;
  public static final int TOKEN_MESSENGER_OFFSET = 12;

  public static Filter createDomainFilter(final long domain) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) domain);
    return Filter.createMemCompFilter(DOMAIN_OFFSET, _data);
  }

  public static Filter createTokenMessengerFilter(final PublicKey tokenMessenger) {
    return Filter.createMemCompFilter(TOKEN_MESSENGER_OFFSET, tokenMessenger);
  }

  public static RemoteTokenMessenger read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static RemoteTokenMessenger read(final AccountInfo<byte[]> accountInfo) {
    return readChecked(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static RemoteTokenMessenger read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static RemoteTokenMessenger readChecked(final PublicKey _address, final byte[] _data) {
    return readChecked(_address, _data, 0);
  }

  public static RemoteTokenMessenger readChecked(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    if (!DISCRIMINATOR.equals(_data, _offset)) {
      throw new IllegalArgumentException("Not a RemoteTokenMessenger account.");
    }
    return read(_address, _data, _offset);
  }

  public static final BiFunction<PublicKey, byte[], RemoteTokenMessenger> FACTORY = RemoteTokenMessenger::readChecked;

  public static RemoteTokenMessenger read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var domain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var tokenMessenger = readPubKey(_data, i);
    return new RemoteTokenMessenger(_address, discriminator, domain, tokenMessenger);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt32LE(_data, i, (int) domain);
    i += 4;
    tokenMessenger.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
