// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param mint Mint for the emission token
/// @param tokenRate: u64 Rate at which the emission token is emitted per second
/// @param expiryTimestamp: u32 Expiration timestamp for the emission token
/// @param index Index for converting LP shares into earned emissions
public record FarmEmission(PublicKey mint,
                           long tokenRate,
                           long expiryTimestamp,
                           Number index) implements SerDe {

  public static final int BYTES = 76;

  public static final int MINT_OFFSET = 0;
  public static final int TOKEN_RATE_OFFSET = 32;
  public static final int EXPIRY_TIMESTAMP_OFFSET = 40;
  public static final int INDEX_OFFSET = 44;

  public static FarmEmission read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var mint = readPubKey(_data, i);
    i += 32;
    final var tokenRate = getInt64LE(_data, i);
    i += 8;
    final var expiryTimestamp = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var index = Number.read(_data, i);
    return new FarmEmission(mint,
                            tokenRate,
                            expiryTimestamp,
                            index);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    mint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, tokenRate);
    i += 8;
    putInt32LE(_data, i, (int) expiryTimestamp);
    i += 4;
    i += index.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
