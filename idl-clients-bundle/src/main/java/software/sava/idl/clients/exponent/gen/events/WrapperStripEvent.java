// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param amountBaseIn: u64
/// @param amountSyStripped: u64
/// @param amountPyOut: u64
public record WrapperStripEvent(Discriminator discriminator,
                                PublicKey userAddress,
                                PublicKey vaultAddress,
                                long amountBaseIn,
                                long amountSyStripped,
                                long amountPyOut) implements ExponentCoreEvent {

  public static final int BYTES = 96;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(70, 128, 226, 182, 17, 99, 235, 18);

  public static final int USER_ADDRESS_OFFSET = 8;
  public static final int VAULT_ADDRESS_OFFSET = 40;
  public static final int AMOUNT_BASE_IN_OFFSET = 72;
  public static final int AMOUNT_SY_STRIPPED_OFFSET = 80;
  public static final int AMOUNT_PY_OUT_OFFSET = 88;

  public static WrapperStripEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var userAddress = readPubKey(_data, i);
    i += 32;
    final var vaultAddress = readPubKey(_data, i);
    i += 32;
    final var amountBaseIn = getInt64LE(_data, i);
    i += 8;
    final var amountSyStripped = getInt64LE(_data, i);
    i += 8;
    final var amountPyOut = getInt64LE(_data, i);
    return new WrapperStripEvent(discriminator,
                                 userAddress,
                                 vaultAddress,
                                 amountBaseIn,
                                 amountSyStripped,
                                 amountPyOut);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    userAddress.write(_data, i);
    i += 32;
    vaultAddress.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amountBaseIn);
    i += 8;
    putInt64LE(_data, i, amountSyStripped);
    i += 8;
    putInt64LE(_data, i, amountPyOut);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
