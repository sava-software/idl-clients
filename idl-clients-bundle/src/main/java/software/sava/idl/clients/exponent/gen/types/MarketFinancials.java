// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// Financial parameters for the market
///
/// @param expirationTs: u64 Expiration timestamp, which is copied from the vault associated with the PT
/// @param ptBalance: u64 Balance of PT in the market
///                  This amount is tracked separately to prevent bugs from token transfers directly to the market
/// @param syBalance: u64 Balance of SY in the market
///                  This amount is tracked separately to prevent bugs from token transfers directly to the market
/// @param lnFeeRateRoot Initial log of fee rate, which decreases over time
/// @param lastLnImpliedRate Last seen log of implied rate (APY) for PT
///                          Used to maintain continuity of the APY between trades over time
/// @param rateScalarRoot Initial rate scalar, which increases over time
public record MarketFinancials(long expirationTs,
                               long ptBalance,
                               long syBalance,
                               double lnFeeRateRoot,
                               double lastLnImpliedRate,
                               double rateScalarRoot) implements SerDe {

  public static final int BYTES = 48;

  public static final int EXPIRATION_TS_OFFSET = 0;
  public static final int PT_BALANCE_OFFSET = 8;
  public static final int SY_BALANCE_OFFSET = 16;
  public static final int LN_FEE_RATE_ROOT_OFFSET = 24;
  public static final int LAST_LN_IMPLIED_RATE_OFFSET = 32;
  public static final int RATE_SCALAR_ROOT_OFFSET = 40;

  public static MarketFinancials read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var expirationTs = getInt64LE(_data, i);
    i += 8;
    final var ptBalance = getInt64LE(_data, i);
    i += 8;
    final var syBalance = getInt64LE(_data, i);
    i += 8;
    final var lnFeeRateRoot = getFloat64LE(_data, i);
    i += 8;
    final var lastLnImpliedRate = getFloat64LE(_data, i);
    i += 8;
    final var rateScalarRoot = getFloat64LE(_data, i);
    return new MarketFinancials(expirationTs,
                                ptBalance,
                                syBalance,
                                lnFeeRateRoot,
                                lastLnImpliedRate,
                                rateScalarRoot);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt64LE(_data, i, expirationTs);
    i += 8;
    putInt64LE(_data, i, ptBalance);
    i += 8;
    putInt64LE(_data, i, syBalance);
    i += 8;
    putFloat64LE(_data, i, lnFeeRateRoot);
    i += 8;
    putFloat64LE(_data, i, lastLnImpliedRate);
    i += 8;
    putFloat64LE(_data, i, rateScalarRoot);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
