// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.nt.bundle.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param clearMask: u8
/// @param previousFlags: u8
/// @param newFlags: u8
public record UserWithdrawalTimingOverrideCleared(Discriminator discriminator,
                                                  PublicKey manager,
                                                  PublicKey user,
                                                  PublicKey bundleAccountKey,
                                                  int clearMask,
                                                  int previousFlags,
                                                  int newFlags,
                                                  long timestamp) implements NtbundleEvent {

  public static final int BYTES = 115;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(78, 111, 250, 12, 64, 54, 226, 251);

  public static final int MANAGER_OFFSET = 8;
  public static final int USER_OFFSET = 40;
  public static final int BUNDLE_ACCOUNT_KEY_OFFSET = 72;
  public static final int CLEAR_MASK_OFFSET = 104;
  public static final int PREVIOUS_FLAGS_OFFSET = 105;
  public static final int NEW_FLAGS_OFFSET = 106;
  public static final int TIMESTAMP_OFFSET = 107;

  public static UserWithdrawalTimingOverrideCleared read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var manager = readPubKey(_data, i);
    i += 32;
    final var user = readPubKey(_data, i);
    i += 32;
    final var bundleAccountKey = readPubKey(_data, i);
    i += 32;
    final var clearMask = _data[i] & 0xFF;
    ++i;
    final var previousFlags = _data[i] & 0xFF;
    ++i;
    final var newFlags = _data[i] & 0xFF;
    ++i;
    final var timestamp = getInt64LE(_data, i);
    return new UserWithdrawalTimingOverrideCleared(discriminator,
                                                   manager,
                                                   user,
                                                   bundleAccountKey,
                                                   clearMask,
                                                   previousFlags,
                                                   newFlags,
                                                   timestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    manager.write(_data, i);
    i += 32;
    user.write(_data, i);
    i += 32;
    bundleAccountKey.write(_data, i);
    i += 32;
    _data[i] = (byte) clearMask;
    ++i;
    _data[i] = (byte) previousFlags;
    ++i;
    _data[i] = (byte) newFlags;
    ++i;
    putInt64LE(_data, i, timestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
