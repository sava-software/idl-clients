// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param userAddress User who withdrew liquidity
/// @param marketAddress Market that the user withdrew liquidity from
/// @param amountBaseOut: u64 Amount of base token out
/// @param amountLpIn: u64 Amount of LP token in
/// @param amountPtOut: u64 Amount of PT token out
/// @param lpPrice LP price in asset
public record WrapperWithdrawLiquidityClassicEvent(Discriminator discriminator,
                                                   PublicKey userAddress,
                                                   PublicKey marketAddress,
                                                   long amountBaseOut,
                                                   long amountLpIn,
                                                   long amountPtOut,
                                                   double lpPrice) implements ExponentCoreEvent {

  public static final int BYTES = 104;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(18, 154, 212, 39, 36, 23, 158, 124);

  public static final int USER_ADDRESS_OFFSET = 8;
  public static final int MARKET_ADDRESS_OFFSET = 40;
  public static final int AMOUNT_BASE_OUT_OFFSET = 72;
  public static final int AMOUNT_LP_IN_OFFSET = 80;
  public static final int AMOUNT_PT_OUT_OFFSET = 88;
  public static final int LP_PRICE_OFFSET = 96;

  public static WrapperWithdrawLiquidityClassicEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var userAddress = readPubKey(_data, i);
    i += 32;
    final var marketAddress = readPubKey(_data, i);
    i += 32;
    final var amountBaseOut = getInt64LE(_data, i);
    i += 8;
    final var amountLpIn = getInt64LE(_data, i);
    i += 8;
    final var amountPtOut = getInt64LE(_data, i);
    i += 8;
    final var lpPrice = getFloat64LE(_data, i);
    return new WrapperWithdrawLiquidityClassicEvent(discriminator,
                                                    userAddress,
                                                    marketAddress,
                                                    amountBaseOut,
                                                    amountLpIn,
                                                    amountPtOut,
                                                    lpPrice);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    userAddress.write(_data, i);
    i += 32;
    marketAddress.write(_data, i);
    i += 32;
    putInt64LE(_data, i, amountBaseOut);
    i += 8;
    putInt64LE(_data, i, amountLpIn);
    i += 8;
    putInt64LE(_data, i, amountPtOut);
    i += 8;
    putFloat64LE(_data, i, lpPrice);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
