// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param addressLookupTable Address to ALT
/// @param mintPt Mint of the vault's PT token
/// @param mintSy Mint of the SY program's SY token
/// @param vault Link to yield-stripping vault
/// @param mintLp Mint for the market's LP tokens
/// @param tokenLpEscrow Holds the LP tokens that are earning emissions
///                      This is where LP holders "stake" their LP tokens
/// @param tokenPtEscrow Token account that holds PT liquidity
/// @param tokenSyEscrow Pass-through token account for SY moving from the depositor to the SY program
/// @param tokenFeeTreasurySy Token account that holds SY fees from trade_pt
/// @param feeTreasurySyBps: u16 Fee treasury SY BPS
/// @param selfAddress Authority for CPI calls owned by the market struct
/// @param signerBump Bump for signing the PDA
/// @param statusFlags: u8
/// @param syProgram Link to the SY program ID
/// @param maxLpSupply: u64
/// @param lpEscrowAmount: u64
/// @param cpiAccounts Record of CPI accounts
/// @param seedId Unique seed id for the market
public record MarketTwo(PublicKey _address,
                        Discriminator discriminator,
                        PublicKey addressLookupTable,
                        PublicKey mintPt,
                        PublicKey mintSy,
                        PublicKey vault,
                        PublicKey mintLp,
                        PublicKey tokenLpEscrow,
                        PublicKey tokenPtEscrow,
                        PublicKey tokenSyEscrow,
                        PublicKey tokenFeeTreasurySy,
                        int feeTreasurySyBps,
                        PublicKey selfAddress,
                        byte[] signerBump,
                        int statusFlags,
                        PublicKey syProgram,
                        MarketFinancials financials,
                        MarketEmissions emissions,
                        LpFarm lpFarm,
                        long maxLpSupply,
                        long lpEscrowAmount,
                        CpiAccounts cpiAccounts,
                        boolean isCurrentFlashSwap,
                        LiquidityNetBalanceLimits liquidityNetBalanceLimits,
                        byte[] seedId) implements SerDe {

  public static final int SIGNER_BUMP_LEN = 1;
  public static final int SEED_ID_LEN = 1;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(212, 4, 132, 126, 169, 121, 121, 20);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int ADDRESS_LOOKUP_TABLE_OFFSET = 8;
  public static final int MINT_PT_OFFSET = 40;
  public static final int MINT_SY_OFFSET = 72;
  public static final int VAULT_OFFSET = 104;
  public static final int MINT_LP_OFFSET = 136;
  public static final int TOKEN_LP_ESCROW_OFFSET = 168;
  public static final int TOKEN_PT_ESCROW_OFFSET = 200;
  public static final int TOKEN_SY_ESCROW_OFFSET = 232;
  public static final int TOKEN_FEE_TREASURY_SY_OFFSET = 264;
  public static final int FEE_TREASURY_SY_BPS_OFFSET = 296;
  public static final int SELF_ADDRESS_OFFSET = 298;
  public static final int SIGNER_BUMP_OFFSET = 330;
  public static final int STATUS_FLAGS_OFFSET = 331;
  public static final int SY_PROGRAM_OFFSET = 332;
  public static final int FINANCIALS_OFFSET = 364;
  public static final int EMISSIONS_OFFSET = 412;

  public static Filter createAddressLookupTableFilter(final PublicKey addressLookupTable) {
    return Filter.createMemCompFilter(ADDRESS_LOOKUP_TABLE_OFFSET, addressLookupTable);
  }

  public static Filter createMintPtFilter(final PublicKey mintPt) {
    return Filter.createMemCompFilter(MINT_PT_OFFSET, mintPt);
  }

  public static Filter createMintSyFilter(final PublicKey mintSy) {
    return Filter.createMemCompFilter(MINT_SY_OFFSET, mintSy);
  }

  public static Filter createVaultFilter(final PublicKey vault) {
    return Filter.createMemCompFilter(VAULT_OFFSET, vault);
  }

  public static Filter createMintLpFilter(final PublicKey mintLp) {
    return Filter.createMemCompFilter(MINT_LP_OFFSET, mintLp);
  }

  public static Filter createTokenLpEscrowFilter(final PublicKey tokenLpEscrow) {
    return Filter.createMemCompFilter(TOKEN_LP_ESCROW_OFFSET, tokenLpEscrow);
  }

  public static Filter createTokenPtEscrowFilter(final PublicKey tokenPtEscrow) {
    return Filter.createMemCompFilter(TOKEN_PT_ESCROW_OFFSET, tokenPtEscrow);
  }

  public static Filter createTokenSyEscrowFilter(final PublicKey tokenSyEscrow) {
    return Filter.createMemCompFilter(TOKEN_SY_ESCROW_OFFSET, tokenSyEscrow);
  }

  public static Filter createTokenFeeTreasurySyFilter(final PublicKey tokenFeeTreasurySy) {
    return Filter.createMemCompFilter(TOKEN_FEE_TREASURY_SY_OFFSET, tokenFeeTreasurySy);
  }

  public static Filter createFeeTreasurySyBpsFilter(final int feeTreasurySyBps) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, feeTreasurySyBps);
    return Filter.createMemCompFilter(FEE_TREASURY_SY_BPS_OFFSET, _data);
  }

  public static Filter createSelfAddressFilter(final PublicKey selfAddress) {
    return Filter.createMemCompFilter(SELF_ADDRESS_OFFSET, selfAddress);
  }

  public static Filter createStatusFlagsFilter(final int statusFlags) {
    return Filter.createMemCompFilter(STATUS_FLAGS_OFFSET, new byte[]{(byte) statusFlags});
  }

  public static Filter createSyProgramFilter(final PublicKey syProgram) {
    return Filter.createMemCompFilter(SY_PROGRAM_OFFSET, syProgram);
  }

  public static Filter createFinancialsFilter(final MarketFinancials financials) {
    return Filter.createMemCompFilter(FINANCIALS_OFFSET, financials.write());
  }

  public static MarketTwo read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static MarketTwo read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static MarketTwo read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], MarketTwo> FACTORY = MarketTwo::read;

  public static MarketTwo read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var addressLookupTable = readPubKey(_data, i);
    i += 32;
    final var mintPt = readPubKey(_data, i);
    i += 32;
    final var mintSy = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var mintLp = readPubKey(_data, i);
    i += 32;
    final var tokenLpEscrow = readPubKey(_data, i);
    i += 32;
    final var tokenPtEscrow = readPubKey(_data, i);
    i += 32;
    final var tokenSyEscrow = readPubKey(_data, i);
    i += 32;
    final var tokenFeeTreasurySy = readPubKey(_data, i);
    i += 32;
    final var feeTreasurySyBps = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var selfAddress = readPubKey(_data, i);
    i += 32;
    final var signerBump = new byte[1];
    i += SerDeUtil.readArray(signerBump, _data, i);
    final var statusFlags = _data[i] & 0xFF;
    ++i;
    final var syProgram = readPubKey(_data, i);
    i += 32;
    final var financials = MarketFinancials.read(_data, i);
    i += 48;
    final var emissions = MarketEmissions.read(_data, i);
    i += emissions.l();
    final var lpFarm = LpFarm.read(_data, i);
    i += lpFarm.l();
    final var maxLpSupply = getInt64LE(_data, i);
    i += 8;
    final var lpEscrowAmount = getInt64LE(_data, i);
    i += 8;
    final var cpiAccounts = CpiAccounts.read(_data, i);
    i += cpiAccounts.l();
    final var isCurrentFlashSwap = _data[i] == 1;
    ++i;
    final var liquidityNetBalanceLimits = LiquidityNetBalanceLimits.read(_data, i);
    i += 22;
    final var seedId = new byte[1];
    SerDeUtil.readArray(seedId, _data, i);
    return new MarketTwo(_address,
                         discriminator,
                         addressLookupTable,
                         mintPt,
                         mintSy,
                         vault,
                         mintLp,
                         tokenLpEscrow,
                         tokenPtEscrow,
                         tokenSyEscrow,
                         tokenFeeTreasurySy,
                         feeTreasurySyBps,
                         selfAddress,
                         signerBump,
                         statusFlags,
                         syProgram,
                         financials,
                         emissions,
                         lpFarm,
                         maxLpSupply,
                         lpEscrowAmount,
                         cpiAccounts,
                         isCurrentFlashSwap,
                         liquidityNetBalanceLimits,
                         seedId);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    addressLookupTable.write(_data, i);
    i += 32;
    mintPt.write(_data, i);
    i += 32;
    mintSy.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    mintLp.write(_data, i);
    i += 32;
    tokenLpEscrow.write(_data, i);
    i += 32;
    tokenPtEscrow.write(_data, i);
    i += 32;
    tokenSyEscrow.write(_data, i);
    i += 32;
    tokenFeeTreasurySy.write(_data, i);
    i += 32;
    putInt16LE(_data, i, feeTreasurySyBps);
    i += 2;
    selfAddress.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(signerBump, 1, _data, i);
    _data[i] = (byte) statusFlags;
    ++i;
    syProgram.write(_data, i);
    i += 32;
    i += financials.write(_data, i);
    i += emissions.write(_data, i);
    i += lpFarm.write(_data, i);
    putInt64LE(_data, i, maxLpSupply);
    i += 8;
    putInt64LE(_data, i, lpEscrowAmount);
    i += 8;
    i += cpiAccounts.write(_data, i);
    _data[i] = (byte) (isCurrentFlashSwap ? 1 : 0);
    ++i;
    i += liquidityNetBalanceLimits.write(_data, i);
    i += SerDeUtil.writeArrayChecked(seedId, 1, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 2
         + 32
         + SerDeUtil.lenArray(signerBump)
         + 1
         + 32
         + financials.l()
         + emissions.l()
         + lpFarm.l()
         + 8
         + 8
         + cpiAccounts.l()
         + 1
         + liquidityNetBalanceLimits.l()
         + SerDeUtil.lenArray(seedId);
  }
}
