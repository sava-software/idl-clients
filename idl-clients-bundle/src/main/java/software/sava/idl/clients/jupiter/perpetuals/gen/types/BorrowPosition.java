// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.rpc.json.http.response.AccountInfo;

import java.math.BigInteger;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt128LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param lockedCollateral: u64
/// @param bump: u8
public record BorrowPosition(PublicKey _address,
                             Discriminator discriminator,
                             PublicKey owner,
                             PublicKey pool,
                             PublicKey custody,
                             long openTime,
                             long updateTime,
                             BigInteger borrowSize,
                             BigInteger cumulativeCompoundedInterestSnapshot,
                             long lockedCollateral,
                             int bump,
                             long lastBorrowed) implements SerDe {

  public static final int BYTES = 169;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(243, 140, 20, 139, 32, 243, 114, 55);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int OWNER_OFFSET = 8;
  public static final int POOL_OFFSET = 40;
  public static final int CUSTODY_OFFSET = 72;
  public static final int OPEN_TIME_OFFSET = 104;
  public static final int UPDATE_TIME_OFFSET = 112;
  public static final int BORROW_SIZE_OFFSET = 120;
  public static final int CUMULATIVE_COMPOUNDED_INTEREST_SNAPSHOT_OFFSET = 136;
  public static final int LOCKED_COLLATERAL_OFFSET = 152;
  public static final int BUMP_OFFSET = 160;
  public static final int LAST_BORROWED_OFFSET = 161;

  public static Filter createOwnerFilter(final PublicKey owner) {
    return Filter.createMemCompFilter(OWNER_OFFSET, owner);
  }

  public static Filter createPoolFilter(final PublicKey pool) {
    return Filter.createMemCompFilter(POOL_OFFSET, pool);
  }

  public static Filter createCustodyFilter(final PublicKey custody) {
    return Filter.createMemCompFilter(CUSTODY_OFFSET, custody);
  }

  public static Filter createOpenTimeFilter(final long openTime) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, openTime);
    return Filter.createMemCompFilter(OPEN_TIME_OFFSET, _data);
  }

  public static Filter createUpdateTimeFilter(final long updateTime) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, updateTime);
    return Filter.createMemCompFilter(UPDATE_TIME_OFFSET, _data);
  }

  public static Filter createBorrowSizeFilter(final BigInteger borrowSize) {
    final byte[] _data = new byte[16];
    putInt128LE(_data, 0, borrowSize);
    return Filter.createMemCompFilter(BORROW_SIZE_OFFSET, _data);
  }

  public static Filter createCumulativeCompoundedInterestSnapshotFilter(final BigInteger cumulativeCompoundedInterestSnapshot) {
    final byte[] _data = new byte[16];
    putInt128LE(_data, 0, cumulativeCompoundedInterestSnapshot);
    return Filter.createMemCompFilter(CUMULATIVE_COMPOUNDED_INTEREST_SNAPSHOT_OFFSET, _data);
  }

  public static Filter createLockedCollateralFilter(final long lockedCollateral) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lockedCollateral);
    return Filter.createMemCompFilter(LOCKED_COLLATERAL_OFFSET, _data);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static Filter createLastBorrowedFilter(final long lastBorrowed) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lastBorrowed);
    return Filter.createMemCompFilter(LAST_BORROWED_OFFSET, _data);
  }

  public static BorrowPosition read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static BorrowPosition read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static BorrowPosition read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], BorrowPosition> FACTORY = BorrowPosition::read;

  public static BorrowPosition read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var pool = readPubKey(_data, i);
    i += 32;
    final var custody = readPubKey(_data, i);
    i += 32;
    final var openTime = getInt64LE(_data, i);
    i += 8;
    final var updateTime = getInt64LE(_data, i);
    i += 8;
    final var borrowSize = getInt128LE(_data, i);
    i += 16;
    final var cumulativeCompoundedInterestSnapshot = getInt128LE(_data, i);
    i += 16;
    final var lockedCollateral = getInt64LE(_data, i);
    i += 8;
    final var bump = _data[i] & 0xFF;
    ++i;
    final var lastBorrowed = getInt64LE(_data, i);
    return new BorrowPosition(_address,
                              discriminator,
                              owner,
                              pool,
                              custody,
                              openTime,
                              updateTime,
                              borrowSize,
                              cumulativeCompoundedInterestSnapshot,
                              lockedCollateral,
                              bump,
                              lastBorrowed);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    pool.write(_data, i);
    i += 32;
    custody.write(_data, i);
    i += 32;
    putInt64LE(_data, i, openTime);
    i += 8;
    putInt64LE(_data, i, updateTime);
    i += 8;
    putInt128LE(_data, i, borrowSize);
    i += 16;
    putInt128LE(_data, i, cumulativeCompoundedInterestSnapshot);
    i += 16;
    putInt64LE(_data, i, lockedCollateral);
    i += 8;
    _data[i] = (byte) bump;
    ++i;
    putInt64LE(_data, i, lastBorrowed);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
