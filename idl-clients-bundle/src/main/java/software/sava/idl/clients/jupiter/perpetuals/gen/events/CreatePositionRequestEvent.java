// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.perpetuals.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.util.OptionalLong;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param positionSide: u8
/// @param sizeUsdDelta: u64
/// @param collateralDelta: u64
/// @param priceSlippage: Option<u64>
/// @param jupiterMinimumOut: Option<u64>
/// @param preSwapAmount: Option<u64>
/// @param requestChange: u8
/// @param referral: Option<publicKey>
public record CreatePositionRequestEvent(Discriminator discriminator,
                                         PublicKey owner,
                                         PublicKey pool,
                                         PublicKey positionKey,
                                         int positionSide,
                                         PublicKey positionMint,
                                         PublicKey positionCustody,
                                         PublicKey positionCollateralMint,
                                         PublicKey positionCollateralCustody,
                                         PublicKey positionRequestKey,
                                         PublicKey positionRequestMint,
                                         long sizeUsdDelta,
                                         long collateralDelta,
                                         OptionalLong priceSlippage,
                                         OptionalLong jupiterMinimumOut,
                                         OptionalLong preSwapAmount,
                                         int requestChange,
                                         long openTime,
                                         PublicKey referral) implements PerpetualsEvent {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(2, 238, 94, 53, 105, 211, 46, 186);

  public static final int OWNER_OFFSET = 8;
  public static final int POOL_OFFSET = 40;
  public static final int POSITION_KEY_OFFSET = 72;
  public static final int POSITION_SIDE_OFFSET = 104;
  public static final int POSITION_MINT_OFFSET = 105;
  public static final int POSITION_CUSTODY_OFFSET = 137;
  public static final int POSITION_COLLATERAL_MINT_OFFSET = 169;
  public static final int POSITION_COLLATERAL_CUSTODY_OFFSET = 201;
  public static final int POSITION_REQUEST_KEY_OFFSET = 233;
  public static final int POSITION_REQUEST_MINT_OFFSET = 265;
  public static final int SIZE_USD_DELTA_OFFSET = 297;
  public static final int COLLATERAL_DELTA_OFFSET = 305;
  public static final int PRICE_SLIPPAGE_OFFSET = 314;

  public static CreatePositionRequestEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var owner = readPubKey(_data, i);
    i += 32;
    final var pool = readPubKey(_data, i);
    i += 32;
    final var positionKey = readPubKey(_data, i);
    i += 32;
    final var positionSide = _data[i] & 0xFF;
    ++i;
    final var positionMint = readPubKey(_data, i);
    i += 32;
    final var positionCustody = readPubKey(_data, i);
    i += 32;
    final var positionCollateralMint = readPubKey(_data, i);
    i += 32;
    final var positionCollateralCustody = readPubKey(_data, i);
    i += 32;
    final var positionRequestKey = readPubKey(_data, i);
    i += 32;
    final var positionRequestMint = readPubKey(_data, i);
    i += 32;
    final var sizeUsdDelta = getInt64LE(_data, i);
    i += 8;
    final var collateralDelta = getInt64LE(_data, i);
    i += 8;
    final OptionalLong priceSlippage;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      priceSlippage = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      priceSlippage = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong jupiterMinimumOut;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      jupiterMinimumOut = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      jupiterMinimumOut = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong preSwapAmount;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      preSwapAmount = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      preSwapAmount = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final var requestChange = _data[i] & 0xFF;
    ++i;
    final var openTime = getInt64LE(_data, i);
    i += 8;
    final PublicKey referral;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      referral = null;
    } else {
      ++i;
      referral = readPubKey(_data, i);
    }
    return new CreatePositionRequestEvent(discriminator,
                                          owner,
                                          pool,
                                          positionKey,
                                          positionSide,
                                          positionMint,
                                          positionCustody,
                                          positionCollateralMint,
                                          positionCollateralCustody,
                                          positionRequestKey,
                                          positionRequestMint,
                                          sizeUsdDelta,
                                          collateralDelta,
                                          priceSlippage,
                                          jupiterMinimumOut,
                                          preSwapAmount,
                                          requestChange,
                                          openTime,
                                          referral);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    owner.write(_data, i);
    i += 32;
    pool.write(_data, i);
    i += 32;
    positionKey.write(_data, i);
    i += 32;
    _data[i] = (byte) positionSide;
    ++i;
    positionMint.write(_data, i);
    i += 32;
    positionCustody.write(_data, i);
    i += 32;
    positionCollateralMint.write(_data, i);
    i += 32;
    positionCollateralCustody.write(_data, i);
    i += 32;
    positionRequestKey.write(_data, i);
    i += 32;
    positionRequestMint.write(_data, i);
    i += 32;
    putInt64LE(_data, i, sizeUsdDelta);
    i += 8;
    putInt64LE(_data, i, collateralDelta);
    i += 8;
    i += SerDeUtil.writeOptional(1, priceSlippage, _data, i);
    i += SerDeUtil.writeOptional(1, jupiterMinimumOut, _data, i);
    i += SerDeUtil.writeOptional(1, preSwapAmount, _data, i);
    _data[i] = (byte) requestChange;
    ++i;
    putInt64LE(_data, i, openTime);
    i += 8;
    i += SerDeUtil.writeOptional(1, referral, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 32
         + 32
         + 32
         + 1
         + 32
         + 32
         + 32
         + 32
         + 32
         + 32
         + 8
         + 8
         + (priceSlippage == null || priceSlippage.isEmpty() ? 1 : (1 + 8))
         + (jupiterMinimumOut == null || jupiterMinimumOut.isEmpty() ? 1 : (1 + 8))
         + (preSwapAmount == null || preSwapAmount.isEmpty() ? 1 : (1 + 8))
         + 1
         + 8
         + (referral == null ? 1 : (1 + 32));
  }
}
