// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.exponent.gen.events;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

/// @param emissionIndex: u16
/// @param amountToUser: u64
/// @param amountToTreasury: u64
public record CollectEmissionEvent(Discriminator discriminator,
                                   PublicKey user,
                                   PublicKey vault,
                                   PublicKey position,
                                   int emissionIndex,
                                   long amountToUser,
                                   long amountToTreasury,
                                   long unixTimestamp) implements ExponentCoreEvent {

  public static final int BYTES = 130;
  public static final Discriminator DISCRIMINATOR = toDiscriminator(220, 173, 217, 52, 133, 253, 5, 114);

  public static final int USER_OFFSET = 8;
  public static final int VAULT_OFFSET = 40;
  public static final int POSITION_OFFSET = 72;
  public static final int EMISSION_INDEX_OFFSET = 104;
  public static final int AMOUNT_TO_USER_OFFSET = 106;
  public static final int AMOUNT_TO_TREASURY_OFFSET = 114;
  public static final int UNIX_TIMESTAMP_OFFSET = 122;

  public static CollectEmissionEvent read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var user = readPubKey(_data, i);
    i += 32;
    final var vault = readPubKey(_data, i);
    i += 32;
    final var position = readPubKey(_data, i);
    i += 32;
    final var emissionIndex = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final var amountToUser = getInt64LE(_data, i);
    i += 8;
    final var amountToTreasury = getInt64LE(_data, i);
    i += 8;
    final var unixTimestamp = getInt64LE(_data, i);
    return new CollectEmissionEvent(discriminator,
                                    user,
                                    vault,
                                    position,
                                    emissionIndex,
                                    amountToUser,
                                    amountToTreasury,
                                    unixTimestamp);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    user.write(_data, i);
    i += 32;
    vault.write(_data, i);
    i += 32;
    position.write(_data, i);
    i += 32;
    putInt16LE(_data, i, emissionIndex);
    i += 2;
    putInt64LE(_data, i, amountToUser);
    i += 8;
    putInt64LE(_data, i, amountToTreasury);
    i += 8;
    putInt64LE(_data, i, unixTimestamp);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
