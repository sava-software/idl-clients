# Audited timeout-detected mutants for the 'scope' suite — line-less
# class,method,mutator keys with a reviewed cause category. Currently empty:
# the suite has no timeout-detected mutants. ScopeReaderRecord.entry's memo-cache
# mutant was the one member and is now killed by an assertion instead of by the
# watchdog (README.md, 'Audited timeout-detected mutants'). The file exists to
# arm the verify's audit — a first timed-out mutant here should warn
# (reviewer-stop), not pass as an unaudited count.
