// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.orca.whirlpools.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record TickArray(PublicKey _address,
                        Discriminator discriminator,
                        int startTickIndex,
                        Tick[] ticks,
                        PublicKey whirlpool) implements SerDe {

  public static final int BYTES = 9988;
  public static final int TICKS_LEN = 88;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(69, 97, 189, 190, 110, 7, 66, 187);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int START_TICK_INDEX_OFFSET = 8;
  public static final int TICKS_OFFSET = 12;
  public static final int WHIRLPOOL_OFFSET = 9956;

  public static Filter createStartTickIndexFilter(final int startTickIndex) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, startTickIndex);
    return Filter.createMemCompFilter(START_TICK_INDEX_OFFSET, _data);
  }

  public static Filter createWhirlpoolFilter(final PublicKey whirlpool) {
    return Filter.createMemCompFilter(WHIRLPOOL_OFFSET, whirlpool);
  }

  public static TickArray read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static TickArray read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static TickArray read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], TickArray> FACTORY = TickArray::read;

  public static TickArray read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var startTickIndex = getInt32LE(_data, i);
    i += 4;
    final var ticks = new Tick[88];
    i += SerDeUtil.readArray(ticks, Tick::read, _data, i);
    final var whirlpool = readPubKey(_data, i);
    return new TickArray(_address, discriminator, startTickIndex, ticks, whirlpool);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt32LE(_data, i, startTickIndex);
    i += 4;
    i += SerDeUtil.writeArrayChecked(ticks, 88, _data, i);
    whirlpool.write(_data, i);
    i += 32;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
