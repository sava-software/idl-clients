// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.jupiter.borrow.gen.types;

import java.util.function.BiFunction;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record Oracle(PublicKey _address,
                     Discriminator discriminator,
                     int nonce,
                     Sources[] sources,
                     int bump) implements SerDe {

  public static final Discriminator DISCRIMINATOR = toDiscriminator(139, 194, 131, 179, 140, 179, 229, 244);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int NONCE_OFFSET = 8;
  public static final int SOURCES_OFFSET = 10;

  public static Filter createNonceFilter(final int nonce) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, nonce);
    return Filter.createMemCompFilter(NONCE_OFFSET, _data);
  }

  public static Oracle read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Oracle read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Oracle read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Oracle> FACTORY = Oracle::read;

  public static Oracle read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var nonce = getInt16LE(_data, i);
    i += 2;
    final var sources = SerDeUtil.readVector(4, Sources.class, Sources::read, _data, i);
    i += SerDeUtil.lenVector(4, sources);
    final var bump = _data[i] & 0xFF;
    return new Oracle(_address, discriminator, nonce, sources, bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    putInt16LE(_data, i, nonce);
    i += 2;
    i += SerDeUtil.writeVector(4, sources, _data, i);
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + 2 + SerDeUtil.lenVector(4, sources) + 1;
  }
}
