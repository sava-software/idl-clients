// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.lut.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class AddressLookupTableProgram {

  public static final Discriminator CREATE_LOOKUP_TABLE_DISCRIMINATOR = toDiscriminator(0, 0, 0, 0);

  public static List<AccountMeta> createLookupTableKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey addressKey,
                                                        final PublicKey authorityKey,
                                                        final PublicKey payerKey) {
    return List.of(
      createWrite(addressKey),
      createRead(authorityKey),
      createWritableSigner(payerKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// @param recentSlot: u64
  /// @param bump: u8
  public static Instruction createLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey addressKey,
                                              final PublicKey authorityKey,
                                              final PublicKey payerKey,
                                              final long recentSlot,
                                              final int bump) {
    final var keys = createLookupTableKeys(
      solanaAccounts,
      addressKey,
      authorityKey,
      payerKey
    );
    return createLookupTable(
      invokedAddressLookupTableProgramMeta,
      keys,
      recentSlot,
      bump
    );
  }

  /// @param recentSlot: u64
  /// @param bump: u8
  public static Instruction createLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final List<AccountMeta> keys,
                                              final long recentSlot,
                                              final int bump) {
    final byte[] _data = new byte[13];
    int i = CREATE_LOOKUP_TABLE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, recentSlot);
    i += 8;
    _data[i] = (byte) bump;

    return Instruction.createInstruction(invokedAddressLookupTableProgramMeta, keys, _data);
  }

  /// @param recentSlot: u64
  /// @param bump: u8
  public record CreateLookupTableIxData(long discriminator,
                                        long recentSlot,
                                        int bump) implements SerDe {  

    public static CreateLookupTableIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 13;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int RECENT_SLOT_OFFSET = 4;
    public static final int BUMP_OFFSET = 12;

    public static CreateLookupTableIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var recentSlot = getInt64LE(_data, i);
      i += 8;
      final var bump = _data[i] & 0xFF;
      return new CreateLookupTableIxData(discriminator, recentSlot, bump);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, (int) discriminator);
      i += 4;
      putInt64LE(_data, i, recentSlot);
      i += 8;
      _data[i] = (byte) bump;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator FREEZE_LOOKUP_TABLE_DISCRIMINATOR = toDiscriminator(1, 0, 0, 0);

  public static List<AccountMeta> freezeLookupTableKeys(final PublicKey addressKey,
                                                        final PublicKey authorityKey) {
    return List.of(
      createWrite(addressKey),
      createReadOnlySigner(authorityKey)
    );
  }

  public static Instruction freezeLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final PublicKey addressKey,
                                              final PublicKey authorityKey) {
    final var keys = freezeLookupTableKeys(
      addressKey,
      authorityKey
    );
    return freezeLookupTable(invokedAddressLookupTableProgramMeta, keys);
  }

  public static Instruction freezeLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    FREEZE_LOOKUP_TABLE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedAddressLookupTableProgramMeta, keys, _data);
  }

  public record FreezeLookupTableIxData(long discriminator) implements SerDe {  

    public static FreezeLookupTableIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static FreezeLookupTableIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, _offset));
      return new FreezeLookupTableIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, (int) discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator EXTEND_LOOKUP_TABLE_DISCRIMINATOR = toDiscriminator(2, 0, 0, 0);

  public static List<AccountMeta> extendLookupTableKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey addressKey,
                                                        final PublicKey authorityKey,
                                                        final PublicKey payerKey) {
    return List.of(
      createWrite(addressKey),
      createReadOnlySigner(authorityKey),
      createWritableSigner(payerKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  public static Instruction extendLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey addressKey,
                                              final PublicKey authorityKey,
                                              final PublicKey payerKey,
                                              final PublicKey[] addresses) {
    final var keys = extendLookupTableKeys(
      solanaAccounts,
      addressKey,
      authorityKey,
      payerKey
    );
    return extendLookupTable(invokedAddressLookupTableProgramMeta, keys, addresses);
  }

  public static Instruction extendLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                              final List<AccountMeta> keys,
                                              final PublicKey[] addresses) {
    final byte[] _data = new byte[4 + SerDeUtil.lenVector(8, addresses)];
    int i = EXTEND_LOOKUP_TABLE_DISCRIMINATOR.write(_data, 0);
    SerDeUtil.writeVector(8, addresses, _data, i);

    return Instruction.createInstruction(invokedAddressLookupTableProgramMeta, keys, _data);
  }

  public record ExtendLookupTableIxData(long discriminator, PublicKey[] addresses) implements SerDe {  

    public static ExtendLookupTableIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ADDRESSES_OFFSET = 4;

    public static ExtendLookupTableIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, i));
      i += 4;
      final var addresses = SerDeUtil.readPublicKeyVector(8, _data, i);
      return new ExtendLookupTableIxData(discriminator, addresses);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, (int) discriminator);
      i += 4;
      i += SerDeUtil.writeVector(8, addresses, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 4 + SerDeUtil.lenVector(8, addresses);
    }
  }

  public static final Discriminator DEACTIVATE_LOOKUP_TABLE_DISCRIMINATOR = toDiscriminator(3, 0, 0, 0);

  public static List<AccountMeta> deactivateLookupTableKeys(final PublicKey addressKey,
                                                            final PublicKey authorityKey) {
    return List.of(
      createWrite(addressKey),
      createReadOnlySigner(authorityKey)
    );
  }

  public static Instruction deactivateLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                                  final PublicKey addressKey,
                                                  final PublicKey authorityKey) {
    final var keys = deactivateLookupTableKeys(
      addressKey,
      authorityKey
    );
    return deactivateLookupTable(invokedAddressLookupTableProgramMeta, keys);
  }

  public static Instruction deactivateLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                                  final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    DEACTIVATE_LOOKUP_TABLE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedAddressLookupTableProgramMeta, keys, _data);
  }

  public record DeactivateLookupTableIxData(long discriminator) implements SerDe {  

    public static DeactivateLookupTableIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static DeactivateLookupTableIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, _offset));
      return new DeactivateLookupTableIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, (int) discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator CLOSE_LOOKUP_TABLE_DISCRIMINATOR = toDiscriminator(4, 0, 0, 0);

  public static List<AccountMeta> closeLookupTableKeys(final PublicKey addressKey,
                                                       final PublicKey authorityKey,
                                                       final PublicKey recipientKey) {
    return List.of(
      createWrite(addressKey),
      createReadOnlySigner(authorityKey),
      createWrite(recipientKey)
    );
  }

  public static Instruction closeLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                             final PublicKey addressKey,
                                             final PublicKey authorityKey,
                                             final PublicKey recipientKey) {
    final var keys = closeLookupTableKeys(
      addressKey,
      authorityKey,
      recipientKey
    );
    return closeLookupTable(invokedAddressLookupTableProgramMeta, keys);
  }

  public static Instruction closeLookupTable(final AccountMeta invokedAddressLookupTableProgramMeta,
                                             final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    CLOSE_LOOKUP_TABLE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedAddressLookupTableProgramMeta, keys, _data);
  }

  public record CloseLookupTableIxData(long discriminator) implements SerDe {  

    public static CloseLookupTableIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static CloseLookupTableIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, _offset));
      return new CloseLookupTableIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, (int) discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private AddressLookupTableProgram() {
  }
}
