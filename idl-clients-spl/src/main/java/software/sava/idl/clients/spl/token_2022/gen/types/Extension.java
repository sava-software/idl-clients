// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.token_2022.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.encoding.ByteUtil;
import software.sava.idl.clients.core.gen.RustEnum;
import software.sava.idl.clients.core.gen.SerDeUtil;

import java.util.Arrays;
import java.util.Map;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getFloat64LE;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putFloat64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

public sealed interface Extension extends RustEnum permits
  Extension.uninitialized,
  Extension.transferFeeConfig,
  Extension.transferFeeAmount,
  Extension.mintCloseAuthority,
  Extension.confidentialTransferMint,
  Extension.confidentialTransferAccount,
  Extension.defaultAccountState,
  Extension.immutableOwner,
  Extension.memoTransfer,
  Extension.nonTransferable,
  Extension.interestBearingConfig,
  Extension.cpiGuard,
  Extension.permanentDelegate,
  Extension.nonTransferableAccount,
  Extension.transferHook,
  Extension.transferHookAccount,
  Extension.confidentialTransferFee,
  Extension.confidentialTransferFeeAmount,
  Extension.metadataPointer,
  Extension.tokenMetadata,
  Extension.groupPointer,
  Extension.tokenGroup,
  Extension.groupMemberPointer,
  Extension.tokenGroupMember,
  Extension.confidentialMintBurn,
  Extension.scaledUiAmountConfig,
  Extension.pausableConfig,
  Extension.pausableAccount,
  Extension.permissionedBurn {

  @Override
  default int ordinalBytes() {
    return 2;
  }

  static Extension read(final byte[] _data, final int _offset) {
    final int ordinal = Short.toUnsignedInt(ByteUtil.getInt16LE(_data, _offset));
    final int i = _offset + 2;
    return switch (ordinal) {
      case 0 -> uninitialized.INSTANCE;
      case 1 -> transferFeeConfig.read(_data, i);
      case 2 -> transferFeeAmount.read(_data, i);
      case 3 -> mintCloseAuthority.read(_data, i);
      case 4 -> confidentialTransferMint.read(_data, i);
      case 5 -> confidentialTransferAccount.read(_data, i);
      case 6 -> defaultAccountState.read(_data, i);
      case 7 -> immutableOwner.read(_data, i);
      case 8 -> memoTransfer.read(_data, i);
      case 9 -> nonTransferable.read(_data, i);
      case 10 -> interestBearingConfig.read(_data, i);
      case 11 -> cpiGuard.read(_data, i);
      case 12 -> permanentDelegate.read(_data, i);
      case 13 -> nonTransferableAccount.read(_data, i);
      case 14 -> transferHook.read(_data, i);
      case 15 -> transferHookAccount.read(_data, i);
      case 16 -> confidentialTransferFee.read(_data, i);
      case 17 -> confidentialTransferFeeAmount.read(_data, i);
      case 18 -> metadataPointer.read(_data, i);
      case 19 -> tokenMetadata.read(_data, i);
      case 20 -> groupPointer.read(_data, i);
      case 21 -> tokenGroup.read(_data, i);
      case 22 -> groupMemberPointer.read(_data, i);
      case 23 -> tokenGroupMember.read(_data, i);
      case 24 -> confidentialMintBurn.read(_data, i);
      case 25 -> scaledUiAmountConfig.read(_data, i);
      case 26 -> pausableConfig.read(_data, i);
      case 27 -> pausableAccount.read(_data, i);
      case 28 -> permissionedBurn.read(_data, i);
      default -> null;
    };
  }

  record uninitialized() implements EnumNone, Extension {

    public static final uninitialized INSTANCE = new uninitialized();

    @Override
    public int ordinal() {
      return 0;
    }
  }

  /// @param transferFeeConfigAuthority Optional authority to set the fee.
  /// @param withdrawWithheldAuthority Withdraw from mint instructions must be signed by this key.
  /// @param withheldAmount: u64 Withheld transfer fee tokens that have been moved to the mint for withdrawal.
  /// @param olderTransferFee Older transfer fee, used if the current epoch < newerTransferFee.epoch.
  /// @param newerTransferFee Newer transfer fee, used if the current epoch >= newerTransferFee.epoch.
  record transferFeeConfig(PublicKey transferFeeConfigAuthority,
                           PublicKey withdrawWithheldAuthority,
                           long withheldAmount,
                           TransferFee olderTransferFee,
                           TransferFee newerTransferFee) implements Extension {

    public static final int BYTES = 108;

    public static final int TRANSFER_FEE_CONFIG_AUTHORITY_OFFSET = 2;
    public static final int WITHDRAW_WITHHELD_AUTHORITY_OFFSET = 34;
    public static final int WITHHELD_AMOUNT_OFFSET = 66;
    public static final int OLDER_TRANSFER_FEE_OFFSET = 74;
    public static final int NEWER_TRANSFER_FEE_OFFSET = 92;

    public static transferFeeConfig read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("transferFeeConfig claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var transferFeeConfigAuthority = readPubKey(_data, i);
      i += 32;
      final var withdrawWithheldAuthority = readPubKey(_data, i);
      i += 32;
      final var withheldAmount = getInt64LE(_data, i);
      i += 8;
      final var olderTransferFee = TransferFee.read(_data, i);
      i += 18;
      final var newerTransferFee = TransferFee.read(_data, i);
      return new transferFeeConfig(transferFeeConfigAuthority,
                                   withdrawWithheldAuthority,
                                   withheldAmount,
                                   olderTransferFee,
                                   newerTransferFee);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      transferFeeConfigAuthority.write(_data, i);
      i += 32;
      withdrawWithheldAuthority.write(_data, i);
      i += 32;
      putInt64LE(_data, i, withheldAmount);
      i += 8;
      i += olderTransferFee.write(_data, i);
      i += newerTransferFee.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 1;
    }
  }

  /// @param withheldAmount: u64 Withheld transfer fee tokens that can be claimed by the fee authority.
  record transferFeeAmount(long withheldAmount) implements Extension {

    public static final int BYTES = 8;

    public static final int WITHHELD_AMOUNT_OFFSET = 2;

    public static transferFeeAmount read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("transferFeeAmount claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var withheldAmount = getInt64LE(_data, i);
      return new transferFeeAmount(withheldAmount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      putInt64LE(_data, i, withheldAmount);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 2;
    }
  }

  record mintCloseAuthority(PublicKey closeAuthority) implements Extension {

    public static final int BYTES = 32;

    public static final int CLOSE_AUTHORITY_OFFSET = 2;

    public static mintCloseAuthority read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("mintCloseAuthority claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var closeAuthority = readPubKey(_data, i);
      return new mintCloseAuthority(closeAuthority);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      closeAuthority.write(_data, i);
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 3;
    }
  }

  /// @param authority Authority to modify the `ConfidentialTransferMint` configuration and to
  ///                  approve new accounts (if `auto_approve_new_accounts` is true).
  ///
  ///                  The legacy Token Multisig account is not supported as the authority.
  /// @param autoApproveNewAccounts Indicate if newly configured accounts must be approved by the
  ///                               `authority` before they may be used by the user.
  ///
  ///                               * If `true`, no approval is required and new accounts may be used immediately.
  ///                               * If `false`, the authority must approve newly configured accounts (see
  ///                               `ConfidentialTransferInstruction::ConfigureAccount`).
  /// @param auditorElgamalPubkey Authority to decode any transfer amount in a confidential transfer.
  record confidentialTransferMint(PublicKey authority,
                                  boolean autoApproveNewAccounts,
                                  PublicKey auditorElgamalPubkey) implements Extension {

    public static final int BYTES = 65;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int AUTO_APPROVE_NEW_ACCOUNTS_OFFSET = 34;
    public static final int AUDITOR_ELGAMAL_PUBKEY_OFFSET = 35;

    public static confidentialTransferMint read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("confidentialTransferMint claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var autoApproveNewAccounts = _data[i] == 1;
      ++i;
      final var auditorElgamalPubkey = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      return new confidentialTransferMint(authority, autoApproveNewAccounts, auditorElgamalPubkey);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      _data[i] = (byte) (autoApproveNewAccounts ? 1 : 0);
      ++i;
      if (auditorElgamalPubkey != null) {
        auditorElgamalPubkey.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 4;
    }
  }

  /// @param approved `true` if this account has been approved for use. All confidential
  ///                 transfer operations for the account will fail until approval is granted.
  /// @param elgamalPubkey The public key associated with ElGamal encryption.
  /// @param pendingBalanceLow The low 16 bits of the pending balance (encrypted by `elgamal_pubkey`).
  /// @param pendingBalanceHigh The high 32 bits of the pending balance (encrypted by `elgamal_pubkey`).
  /// @param availableBalance The available balance (encrypted by `encrypiton_pubkey`).
  /// @param decryptableAvailableBalance The decryptable available balance.
  /// @param allowConfidentialCredits If `false`, the extended account rejects any incoming confidential transfers.
  /// @param allowNonConfidentialCredits If `false`, the base account rejects any incoming transfers.
  /// @param pendingBalanceCreditCounter: u64 The total number of `Deposit` and `Transfer` instructions that have credited `pending_balance`.
  /// @param maximumPendingBalanceCreditCounter: u64 The maximum number of `Deposit` and `Transfer` instructions that can
  ///                                           credit `pending_balance` before the `ApplyPendingBalance`
  ///                                           instruction is executed.
  /// @param expectedPendingBalanceCreditCounter: u64 The `expected_pending_balance_credit_counter` value that was included in
  ///                                            the last `ApplyPendingBalance` instruction.
  /// @param actualPendingBalanceCreditCounter: u64 The actual `pending_balance_credit_counter` when the last
  ///                                          `ApplyPendingBalance` instruction was executed.
  record confidentialTransferAccount(boolean approved,
                                     PublicKey elgamalPubkey,
                                     EncryptedBalance pendingBalanceLow,
                                     EncryptedBalance pendingBalanceHigh,
                                     EncryptedBalance availableBalance,
                                     DecryptableBalance decryptableAvailableBalance,
                                     boolean allowConfidentialCredits,
                                     boolean allowNonConfidentialCredits,
                                     long pendingBalanceCreditCounter,
                                     long maximumPendingBalanceCreditCounter,
                                     long expectedPendingBalanceCreditCounter,
                                     long actualPendingBalanceCreditCounter) implements Extension {

    public static final int BYTES = 295;

    public static final int APPROVED_OFFSET = 2;
    public static final int ELGAMAL_PUBKEY_OFFSET = 3;
    public static final int PENDING_BALANCE_LOW_OFFSET = 35;
    public static final int PENDING_BALANCE_HIGH_OFFSET = 99;
    public static final int AVAILABLE_BALANCE_OFFSET = 163;
    public static final int DECRYPTABLE_AVAILABLE_BALANCE_OFFSET = 227;
    public static final int ALLOW_CONFIDENTIAL_CREDITS_OFFSET = 263;
    public static final int ALLOW_NON_CONFIDENTIAL_CREDITS_OFFSET = 264;
    public static final int PENDING_BALANCE_CREDIT_COUNTER_OFFSET = 265;
    public static final int MAXIMUM_PENDING_BALANCE_CREDIT_COUNTER_OFFSET = 273;
    public static final int EXPECTED_PENDING_BALANCE_CREDIT_COUNTER_OFFSET = 281;
    public static final int ACTUAL_PENDING_BALANCE_CREDIT_COUNTER_OFFSET = 289;

    public static confidentialTransferAccount read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("confidentialTransferAccount claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var approved = _data[i] == 1;
      ++i;
      final var elgamalPubkey = readPubKey(_data, i);
      i += 32;
      final var pendingBalanceLow = EncryptedBalance.read(_data, i);
      i += 64;
      final var pendingBalanceHigh = EncryptedBalance.read(_data, i);
      i += 64;
      final var availableBalance = EncryptedBalance.read(_data, i);
      i += 64;
      final var decryptableAvailableBalance = DecryptableBalance.read(_data, i);
      i += 36;
      final var allowConfidentialCredits = _data[i] == 1;
      ++i;
      final var allowNonConfidentialCredits = _data[i] == 1;
      ++i;
      final var pendingBalanceCreditCounter = getInt64LE(_data, i);
      i += 8;
      final var maximumPendingBalanceCreditCounter = getInt64LE(_data, i);
      i += 8;
      final var expectedPendingBalanceCreditCounter = getInt64LE(_data, i);
      i += 8;
      final var actualPendingBalanceCreditCounter = getInt64LE(_data, i);
      return new confidentialTransferAccount(approved,
                                             elgamalPubkey,
                                             pendingBalanceLow,
                                             pendingBalanceHigh,
                                             availableBalance,
                                             decryptableAvailableBalance,
                                             allowConfidentialCredits,
                                             allowNonConfidentialCredits,
                                             pendingBalanceCreditCounter,
                                             maximumPendingBalanceCreditCounter,
                                             expectedPendingBalanceCreditCounter,
                                             actualPendingBalanceCreditCounter);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      _data[i] = (byte) (approved ? 1 : 0);
      ++i;
      elgamalPubkey.write(_data, i);
      i += 32;
      i += pendingBalanceLow.write(_data, i);
      i += pendingBalanceHigh.write(_data, i);
      i += availableBalance.write(_data, i);
      i += decryptableAvailableBalance.write(_data, i);
      _data[i] = (byte) (allowConfidentialCredits ? 1 : 0);
      ++i;
      _data[i] = (byte) (allowNonConfidentialCredits ? 1 : 0);
      ++i;
      putInt64LE(_data, i, pendingBalanceCreditCounter);
      i += 8;
      putInt64LE(_data, i, maximumPendingBalanceCreditCounter);
      i += 8;
      putInt64LE(_data, i, expectedPendingBalanceCreditCounter);
      i += 8;
      putInt64LE(_data, i, actualPendingBalanceCreditCounter);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 5;
    }
  }

  record defaultAccountState(AccountState state) implements Extension {

    public static final int BYTES = 1;

    public static final int STATE_OFFSET = 2;

    public static defaultAccountState read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("defaultAccountState claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var state = AccountState.read(_data, i);
      return new defaultAccountState(state);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      i += state.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 6;
    }
  }

  record immutableOwner() implements Extension {

    public static final immutableOwner INSTANCE = new immutableOwner();

    public static immutableOwner read(final byte[] _data, final int _offset) {
      final int _sizePrefix = SerDeUtil.readLen(2, _data, _offset);
      if (_sizePrefix != 0) {
        throw new IllegalArgumentException("immutableOwner claims " + _sizePrefix + " bytes, but has no fields.");
      }
      return INSTANCE;
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, 0, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2;
    }

    @Override
    public int ordinal() {
      return 7;
    }
  }

  /// @param requireIncomingTransferMemos Require transfers into this account to be accompanied by a memo.
  record memoTransfer(boolean requireIncomingTransferMemos) implements Extension {

    public static final int BYTES = 1;

    public static final int REQUIRE_INCOMING_TRANSFER_MEMOS_OFFSET = 2;

    public static memoTransfer read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("memoTransfer claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var requireIncomingTransferMemos = _data[i] == 1;
      return new memoTransfer(requireIncomingTransferMemos);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      _data[i] = (byte) (requireIncomingTransferMemos ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 8;
    }
  }

  record nonTransferable() implements Extension {

    public static final nonTransferable INSTANCE = new nonTransferable();

    public static nonTransferable read(final byte[] _data, final int _offset) {
      final int _sizePrefix = SerDeUtil.readLen(2, _data, _offset);
      if (_sizePrefix != 0) {
        throw new IllegalArgumentException("nonTransferable claims " + _sizePrefix + " bytes, but has no fields.");
      }
      return INSTANCE;
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, 0, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2;
    }

    @Override
    public int ordinal() {
      return 9;
    }
  }

  /// @param initializationTimestamp: u64
  /// @param lastUpdateTimestamp: u64
  record interestBearingConfig(PublicKey rateAuthority,
                               long initializationTimestamp,
                               int preUpdateAverageRate,
                               long lastUpdateTimestamp,
                               int currentRate) implements Extension {

    public static final int BYTES = 52;

    public static final int RATE_AUTHORITY_OFFSET = 2;
    public static final int INITIALIZATION_TIMESTAMP_OFFSET = 34;
    public static final int PRE_UPDATE_AVERAGE_RATE_OFFSET = 42;
    public static final int LAST_UPDATE_TIMESTAMP_OFFSET = 44;
    public static final int CURRENT_RATE_OFFSET = 52;

    public static interestBearingConfig read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("interestBearingConfig claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var rateAuthority = readPubKey(_data, i);
      i += 32;
      final var initializationTimestamp = getInt64LE(_data, i);
      i += 8;
      final var preUpdateAverageRate = getInt16LE(_data, i);
      i += 2;
      final var lastUpdateTimestamp = getInt64LE(_data, i);
      i += 8;
      final var currentRate = getInt16LE(_data, i);
      return new interestBearingConfig(rateAuthority,
                                       initializationTimestamp,
                                       preUpdateAverageRate,
                                       lastUpdateTimestamp,
                                       currentRate);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      rateAuthority.write(_data, i);
      i += 32;
      putInt64LE(_data, i, initializationTimestamp);
      i += 8;
      putInt16LE(_data, i, preUpdateAverageRate);
      i += 2;
      putInt64LE(_data, i, lastUpdateTimestamp);
      i += 8;
      putInt16LE(_data, i, currentRate);
      i += 2;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 10;
    }
  }

  /// @param lockCpi Lock certain token operations from taking place within CPI for this account.
  record cpiGuard(boolean lockCpi) implements Extension {

    public static final int BYTES = 1;

    public static final int LOCK_CPI_OFFSET = 2;

    public static cpiGuard read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("cpiGuard claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var lockCpi = _data[i] == 1;
      return new cpiGuard(lockCpi);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      _data[i] = (byte) (lockCpi ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 11;
    }
  }

  record permanentDelegate(PublicKey delegate) implements Extension {

    public static final int BYTES = 32;

    public static final int DELEGATE_OFFSET = 2;

    public static permanentDelegate read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("permanentDelegate claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var delegate = readPubKey(_data, i);
      return new permanentDelegate(delegate);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      delegate.write(_data, i);
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 12;
    }
  }

  record nonTransferableAccount() implements Extension {

    public static final nonTransferableAccount INSTANCE = new nonTransferableAccount();

    public static nonTransferableAccount read(final byte[] _data, final int _offset) {
      final int _sizePrefix = SerDeUtil.readLen(2, _data, _offset);
      if (_sizePrefix != 0) {
        throw new IllegalArgumentException("nonTransferableAccount claims " + _sizePrefix + " bytes, but has no fields.");
      }
      return INSTANCE;
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, 0, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2;
    }

    @Override
    public int ordinal() {
      return 13;
    }
  }

  /// @param authority The transfer hook update authority.
  /// @param programId The transfer hook program account.
  record transferHook(PublicKey authority, PublicKey programId) implements Extension {

    public static final int BYTES = 64;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int PROGRAM_ID_OFFSET = 34;

    public static transferHook read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("transferHook claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = readPubKey(_data, i);
      i += 32;
      final var programId = readPubKey(_data, i);
      return new transferHook(authority, programId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      authority.write(_data, i);
      i += 32;
      programId.write(_data, i);
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 14;
    }
  }

  /// @param transferring Whether or not this account is currently transferring tokens
  ///                     True during the transfer hook cpi, otherwise false.
  record transferHookAccount(boolean transferring) implements Extension {

    public static final int BYTES = 1;

    public static final int TRANSFERRING_OFFSET = 2;

    public static transferHookAccount read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("transferHookAccount claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var transferring = _data[i] == 1;
      return new transferHookAccount(transferring);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      _data[i] = (byte) (transferring ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 15;
    }
  }

  /// @param authority Optional authority to set the withdraw withheld authority ElGamal key.
  /// @param elgamalPubkey Withheld fees from accounts must be encrypted with this ElGamal key.
  ///
  ///                      Note that whoever holds the ElGamal private key for this ElGamal public
  ///                      key has the ability to decode any withheld fee amount that are
  ///                      associated with accounts. When combined with the fee parameters, the
  ///                      withheld fee amounts can reveal information about transfer amounts.
  /// @param harvestToMintEnabled If `false`, the harvest of withheld tokens to mint is rejected.
  /// @param withheldAmount Withheld confidential transfer fee tokens that have been moved to the
  ///                       mint for withdrawal.
  record confidentialTransferFee(PublicKey authority,
                                 PublicKey elgamalPubkey,
                                 boolean harvestToMintEnabled,
                                 EncryptedBalance withheldAmount) implements Extension {

    public static final int BYTES = 129;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int ELGAMAL_PUBKEY_OFFSET = 34;
    public static final int HARVEST_TO_MINT_ENABLED_OFFSET = 66;
    public static final int WITHHELD_AMOUNT_OFFSET = 67;

    public static confidentialTransferFee read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("confidentialTransferFee claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var elgamalPubkey = readPubKey(_data, i);
      i += 32;
      final var harvestToMintEnabled = _data[i] == 1;
      ++i;
      final var withheldAmount = EncryptedBalance.read(_data, i);
      return new confidentialTransferFee(authority,
                                         elgamalPubkey,
                                         harvestToMintEnabled,
                                         withheldAmount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      elgamalPubkey.write(_data, i);
      i += 32;
      _data[i] = (byte) (harvestToMintEnabled ? 1 : 0);
      ++i;
      i += withheldAmount.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 16;
    }
  }

  /// @param withheldAmount Amount withheld during confidential transfers, to be harvest to the mint.
  record confidentialTransferFeeAmount(EncryptedBalance withheldAmount) implements Extension {

    public static final int BYTES = 64;

    public static final int WITHHELD_AMOUNT_OFFSET = 2;

    public static confidentialTransferFeeAmount read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("confidentialTransferFeeAmount claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var withheldAmount = EncryptedBalance.read(_data, i);
      return new confidentialTransferFeeAmount(withheldAmount);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      i += withheldAmount.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 17;
    }
  }

  /// @param authority Optional authority that can set the metadata address.
  /// @param metadataAddress Optional Account Address that holds the metadata.
  record metadataPointer(PublicKey authority, PublicKey metadataAddress) implements Extension {

    public static final int BYTES = 64;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int METADATA_ADDRESS_OFFSET = 34;

    public static metadataPointer read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("metadataPointer claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var metadataAddress = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      return new metadataPointer(authority, metadataAddress);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      if (metadataAddress != null) {
        metadataAddress.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 18;
    }
  }

  /// @param updateAuthority The authority that can sign to update the metadata.
  /// @param mint The associated mint, used to counter spoofing to be sure that metadata belongs to a particular mint.
  /// @param name The longer name of the token.
  /// @param symbol The shortened symbol for the token.
  /// @param uri The URI pointing to richer metadata.
  /// @param additionalMetadata Any additional metadata about the token as key-value pairs.
  record tokenMetadata(PublicKey updateAuthority,
                       PublicKey mint,
                       String name, byte[] _name,
                       String symbol, byte[] _symbol,
                       String uri, byte[] _uri,
                       Map<String, String> additionalMetadata) implements Extension {

    public static final int UPDATE_AUTHORITY_OFFSET = 2;
    public static final int MINT_OFFSET = 34;
    public static final int NAME_OFFSET = 66;

    public static tokenMetadata createRecord(final PublicKey updateAuthority,
                                             final PublicKey mint,
                                             final String name,
                                             final String symbol,
                                             final String uri,
                                             final Map<String, String> additionalMetadata) {
      return new tokenMetadata(updateAuthority,
                               mint,
                               name, SerDeUtil.encodeString(name),
                               symbol, SerDeUtil.encodeString(symbol),
                               uri, SerDeUtil.encodeString(uri),
                               additionalMetadata);
    }

    public static tokenMetadata read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      final var updateAuthority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var mint = readPubKey(_data, i);
      i += 32;
      final byte[] _name = SerDeUtil.readbyteVector(4, _data, i);
      final var name = SerDeUtil.decodeString(_name);
      i += 4 + _name.length;
      final byte[] _symbol = SerDeUtil.readbyteVector(4, _data, i);
      final var symbol = SerDeUtil.decodeString(_symbol);
      i += 4 + _symbol.length;
      final byte[] _uri = SerDeUtil.readbyteVector(4, _data, i);
      final var uri = SerDeUtil.decodeString(_uri);
      i += 4 + _uri.length;
      final var additionalMetadata = MapSerDe.readStringMap(4, 4, 4, _data, i);
      i += MapSerDe.lenStringMap(4, 4, 4, additionalMetadata);
      final int _size = i - _offset - 2;
      if (_size != _sizePrefix) {
        throw new IllegalArgumentException("tokenMetadata claims " + _sizePrefix + " bytes, but its fields occupy " + _size + ".");
      }
      return new tokenMetadata(updateAuthority,
                               mint,
                               name, _name,
                               symbol, _symbol,
                               uri, _uri,
                               additionalMetadata);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, l() - ordinalBytes() - 2, _data, i);
      if (updateAuthority != null) {
        updateAuthority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      mint.write(_data, i);
      i += 32;
      putInt32LE(_data, i, (int) _name.length);
      i += 4;
      System.arraycopy(_name, 0, _data, i, _name.length);
      i += _name.length;
      putInt32LE(_data, i, (int) _symbol.length);
      i += 4;
      System.arraycopy(_symbol, 0, _data, i, _symbol.length);
      i += _symbol.length;
      putInt32LE(_data, i, (int) _uri.length);
      i += 4;
      System.arraycopy(_uri, 0, _data, i, _uri.length);
      i += _uri.length;
      i += MapSerDe.writeStringMapInOrder(4, 4, 4, additionalMetadata, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + 32
           + 32
           + 4 + _name.length
           + 4 + _symbol.length
           + 4 + _uri.length
           + MapSerDe.lenStringMap(4, 4, 4, additionalMetadata);
    }

    @Override
    public int ordinal() {
      return 19;
    }
  }

  /// @param authority Optional authority that can set the group address.
  /// @param groupAddress Optional account address that holds the group.
  record groupPointer(PublicKey authority, PublicKey groupAddress) implements Extension {

    public static final int BYTES = 64;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int GROUP_ADDRESS_OFFSET = 34;

    public static groupPointer read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("groupPointer claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var groupAddress = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      return new groupPointer(authority, groupAddress);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      if (groupAddress != null) {
        groupAddress.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 20;
    }
  }

  /// @param updateAuthority The authority that can sign to update the group.
  /// @param mint The associated mint, used to counter spoofing to be sure that group belongs to a particular mint.
  /// @param size: u64 The current number of group members.
  /// @param maxSize: u64 The maximum number of group members.
  record tokenGroup(PublicKey updateAuthority,
                    PublicKey mint,
                    long size,
                    long maxSize) implements Extension {

    public static final int BYTES = 80;

    public static final int UPDATE_AUTHORITY_OFFSET = 2;
    public static final int MINT_OFFSET = 34;
    public static final int SIZE_OFFSET = 66;
    public static final int MAX_SIZE_OFFSET = 74;

    public static tokenGroup read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("tokenGroup claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var updateAuthority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var mint = readPubKey(_data, i);
      i += 32;
      final var size = getInt64LE(_data, i);
      i += 8;
      final var maxSize = getInt64LE(_data, i);
      return new tokenGroup(updateAuthority,
                            mint,
                            size,
                            maxSize);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (updateAuthority != null) {
        updateAuthority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      mint.write(_data, i);
      i += 32;
      putInt64LE(_data, i, size);
      i += 8;
      putInt64LE(_data, i, maxSize);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 21;
    }
  }

  /// @param authority Optional authority that can set the member address.
  /// @param memberAddress Optional account address that holds the member.
  record groupMemberPointer(PublicKey authority, PublicKey memberAddress) implements Extension {

    public static final int BYTES = 64;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int MEMBER_ADDRESS_OFFSET = 34;

    public static groupMemberPointer read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("groupMemberPointer claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var memberAddress = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      return new groupMemberPointer(authority, memberAddress);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      if (memberAddress != null) {
        memberAddress.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 22;
    }
  }

  /// @param mint The associated mint, used to counter spoofing to be sure that member belongs to a particular mint.
  /// @param group The pubkey of the `TokenGroup`.
  /// @param memberNumber: u64 The member number.
  record tokenGroupMember(PublicKey mint,
                          PublicKey group,
                          long memberNumber) implements Extension {

    public static final int BYTES = 72;

    public static final int MINT_OFFSET = 2;
    public static final int GROUP_OFFSET = 34;
    public static final int MEMBER_NUMBER_OFFSET = 66;

    public static tokenGroupMember read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("tokenGroupMember claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var mint = readPubKey(_data, i);
      i += 32;
      final var group = readPubKey(_data, i);
      i += 32;
      final var memberNumber = getInt64LE(_data, i);
      return new tokenGroupMember(mint, group, memberNumber);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      mint.write(_data, i);
      i += 32;
      group.write(_data, i);
      i += 32;
      putInt64LE(_data, i, memberNumber);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 23;
    }
  }

  /// @param confidentialSupply The confidential supply of the mint (encrypted by `supply_elgamal_pubkey`).
  /// @param decryptableSupply The decryptable confidential supply of the mint.
  /// @param supplyElgamalPubkey The ElGamal pubkey used to encrypt the confidential supply.
  /// @param pendingBurn The amount of burnt tokens that have not yet been applied to the confidential supply (encrypted by `supply_elgamal_pubkey`).
  record confidentialMintBurn(EncryptedBalance confidentialSupply,
                              DecryptableBalance decryptableSupply,
                              PublicKey supplyElgamalPubkey,
                              EncryptedBalance pendingBurn) implements Extension {

    public static final int BYTES = 196;

    public static final int CONFIDENTIAL_SUPPLY_OFFSET = 2;
    public static final int DECRYPTABLE_SUPPLY_OFFSET = 66;
    public static final int SUPPLY_ELGAMAL_PUBKEY_OFFSET = 102;
    public static final int PENDING_BURN_OFFSET = 134;

    public static confidentialMintBurn read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("confidentialMintBurn claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var confidentialSupply = EncryptedBalance.read(_data, i);
      i += 64;
      final var decryptableSupply = DecryptableBalance.read(_data, i);
      i += 36;
      final var supplyElgamalPubkey = readPubKey(_data, i);
      i += 32;
      final var pendingBurn = EncryptedBalance.read(_data, i);
      return new confidentialMintBurn(confidentialSupply,
                                      decryptableSupply,
                                      supplyElgamalPubkey,
                                      pendingBurn);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      i += confidentialSupply.write(_data, i);
      i += decryptableSupply.write(_data, i);
      supplyElgamalPubkey.write(_data, i);
      i += 32;
      i += pendingBurn.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 24;
    }
  }

  /// @param newMultiplierEffectiveTimestamp: u64
  record scaledUiAmountConfig(PublicKey authority,
                              double multiplier,
                              long newMultiplierEffectiveTimestamp,
                              double newMultiplier) implements Extension {

    public static final int BYTES = 56;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int MULTIPLIER_OFFSET = 34;
    public static final int NEW_MULTIPLIER_EFFECTIVE_TIMESTAMP_OFFSET = 42;
    public static final int NEW_MULTIPLIER_OFFSET = 50;

    public static scaledUiAmountConfig read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("scaledUiAmountConfig claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = readPubKey(_data, i);
      i += 32;
      final var multiplier = getFloat64LE(_data, i);
      i += 8;
      final var newMultiplierEffectiveTimestamp = getInt64LE(_data, i);
      i += 8;
      final var newMultiplier = getFloat64LE(_data, i);
      return new scaledUiAmountConfig(authority,
                                      multiplier,
                                      newMultiplierEffectiveTimestamp,
                                      newMultiplier);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      authority.write(_data, i);
      i += 32;
      putFloat64LE(_data, i, multiplier);
      i += 8;
      putInt64LE(_data, i, newMultiplierEffectiveTimestamp);
      i += 8;
      putFloat64LE(_data, i, newMultiplier);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 25;
    }
  }

  record pausableConfig(PublicKey authority, boolean paused) implements Extension {

    public static final int BYTES = 33;

    public static final int AUTHORITY_OFFSET = 2;
    public static final int PAUSED_OFFSET = 34;

    public static pausableConfig read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("pausableConfig claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      i += 32;
      final var paused = _data[i] == 1;
      return new pausableConfig(authority, paused);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      _data[i] = (byte) (paused ? 1 : 0);
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 26;
    }
  }

  record pausableAccount() implements Extension {

    public static final pausableAccount INSTANCE = new pausableAccount();

    public static pausableAccount read(final byte[] _data, final int _offset) {
      final int _sizePrefix = SerDeUtil.readLen(2, _data, _offset);
      if (_sizePrefix != 0) {
        throw new IllegalArgumentException("pausableAccount claims " + _sizePrefix + " bytes, but has no fields.");
      }
      return INSTANCE;
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, 0, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2;
    }

    @Override
    public int ordinal() {
      return 27;
    }
  }

  /// @param authority Authority that is required for burning
  record permissionedBurn(PublicKey authority) implements Extension {

    public static final int BYTES = 32;

    public static final int AUTHORITY_OFFSET = 2;

    public static permissionedBurn read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      int i = _offset;
      final int _sizePrefix = SerDeUtil.readLen(2, _data, i);
      i += 2;
      if (_sizePrefix != BYTES) {
        throw new IllegalArgumentException("permissionedBurn claims " + _sizePrefix + " bytes, expected " + BYTES + ".");
      }
      final var authority = SerDeUtil.isZeroed(_data, i, 32) ? null : readPubKey(_data, i);
      return new permissionedBurn(authority);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + writeOrdinal(_data, _offset);
      i += SerDeUtil.writeLen(2, BYTES, _data, i);
      if (authority != null) {
        authority.write(_data, i);
      } else {
        Arrays.fill(_data, i, i + 32, (byte) 0);
      }
      i += 32;
      return i - _offset;
    }

    @Override
    public int l() {
      return ordinalBytes() + 2 + BYTES;
    }

    @Override
    public int ordinal() {
      return 28;
    }
  }
}
