// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.token_2022.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Objects;
import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param mintAuthority Optional authority used to mint new tokens. The mint authority may only
///                      be provided during mint creation. If no mint authority is present
///                      then the mint has a fixed supply and no further tokens may be minted.
/// @param supply: u64 Total supply of tokens.
/// @param decimals: u8 Number of base 10 digits to the right of the decimal place.
/// @param isInitialized Is `true` if this structure has been initialized.
/// @param freezeAuthority Optional authority to freeze token accounts.
/// @param extensions The extensions activated on the mint account.
public record Mint(PublicKey _address,
                   PublicKey mintAuthority,
                   long supply,
                   int decimals,
                   boolean isInitialized,
                   PublicKey freezeAuthority,
                   Extension[] extensions) implements SerDe {

  public static final int MINT_AUTHORITY_OPTION_OFFSET = 0;
  public static final int MINT_AUTHORITY_OFFSET = 4;
  public static final int SUPPLY_OFFSET = 36;
  public static final int DECIMALS_OFFSET = 44;
  public static final int IS_INITIALIZED_OFFSET = 45;
  public static final int FREEZE_AUTHORITY_OPTION_OFFSET = 46;
  public static final int FREEZE_AUTHORITY_OFFSET = 50;
  public static final int EXTENSIONS_OFFSET = 82;

  public static Filter createMintAuthorityFilter(final PublicKey mintAuthority) {
    final byte[] _data = new byte[36];
    _data[0] = 1;
    mintAuthority.write(_data, 4);
    return Filter.createMemCompFilter(MINT_AUTHORITY_OPTION_OFFSET, _data);
  }

  public static Filter createMintAuthorityAbsentFilter() {
    return Filter.createMemCompFilter(MINT_AUTHORITY_OPTION_OFFSET, new byte[4]);
  }

  public static Filter createSupplyFilter(final long supply) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, supply);
    return Filter.createMemCompFilter(SUPPLY_OFFSET, _data);
  }

  public static Filter createDecimalsFilter(final int decimals) {
    return Filter.createMemCompFilter(DECIMALS_OFFSET, new byte[]{(byte) decimals});
  }

  public static Filter createIsInitializedFilter(final boolean isInitialized) {
    return Filter.createMemCompFilter(IS_INITIALIZED_OFFSET, new byte[]{(byte) (isInitialized ? 1 : 0)});
  }

  public static Filter createFreezeAuthorityFilter(final PublicKey freezeAuthority) {
    final byte[] _data = new byte[36];
    _data[0] = 1;
    freezeAuthority.write(_data, 4);
    return Filter.createMemCompFilter(FREEZE_AUTHORITY_OPTION_OFFSET, _data);
  }

  public static Filter createFreezeAuthorityAbsentFilter() {
    return Filter.createMemCompFilter(FREEZE_AUTHORITY_OPTION_OFFSET, new byte[4]);
  }

  public static Mint read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static Mint read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static Mint read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], Mint> FACTORY = Mint::read;

  public static Mint read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }

    int i = _offset;
    final PublicKey mintAuthority;
    if (SerDeUtil.isAbsent(4, _data, i)) {
      Objects.checkFromIndexSize(i, 36, _data.length);
      mintAuthority = null;
      i += 32 + 4;
    } else {
      i += 4;
      mintAuthority = readPubKey(_data, i);
      i += 32;
    }
    final var supply = getInt64LE(_data, i);
    i += 8;
    final var decimals = _data[i] & 0xFF;
    ++i;
    final var isInitialized = _data[i] == 1;
    ++i;
    final PublicKey freezeAuthority;
    if (SerDeUtil.isAbsent(4, _data, i)) {
      Objects.checkFromIndexSize(i, 36, _data.length);
      freezeAuthority = null;
      i += 32 + 4;
    } else {
      i += 4;
      freezeAuthority = readPubKey(_data, i);
      i += 32;
    }
    final Extension[] extensions;
    if (i < _data.length) {
      if (i + 84 > _data.length) {
        throw new IllegalArgumentException("A hidden prefix of 84 bytes at offset " + i + " runs past the " + _data.length + " byte buffer.");
      }
      if (!SerDeUtil.isZeroed(_data, i, 83)) {
        throw new IllegalArgumentException("Expected 83 zero bytes of padding at offset " + i + ".");
      }
      i += 83;
      if ((_data[i] & 0xFF) != 1) {
        throw new IllegalArgumentException("Expected the hidden prefix constant 1 at offset " + i + ", found " + (_data[i] & 0xFF) + ".");
      }
      i += 1;
      final var _extensionsList = new ArrayList<Extension>();
      for (int _i = i, _len; _i + 2 <= _data.length; _i += _len) {
        final var _extensionsItem = Extension.read(_data, _i);
        if (_extensionsItem == null) {
          throw new IllegalArgumentException("Unknown Extension at offset " + _i + ".");
        }
        _len = _extensionsItem.l();
        if (_len <= 0) {
          throw new IllegalStateException("Extension at offset " + _i + " occupies no bytes.");
        }
        _extensionsList.add(_extensionsItem);
      }
      final var _extensions = _extensionsList.toArray(Extension[]::new);
      extensions = _extensions;
    } else {
      extensions = null;
    }
    return new Mint(_address,
                    mintAuthority,
                    supply,
                    decimals,
                    isInitialized,
                    freezeAuthority,
                    extensions);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    if (mintAuthority != null) {
      putInt32LE(_data, i, (int) 1);
      i += 4;
      mintAuthority.write(_data, i);
    } else {
      putInt32LE(_data, i, (int) 0);
      i += 4;
    }
    i += 32;
    putInt64LE(_data, i, supply);
    i += 8;
    _data[i] = (byte) decimals;
    ++i;
    _data[i] = (byte) (isInitialized ? 1 : 0);
    ++i;
    if (freezeAuthority != null) {
      putInt32LE(_data, i, (int) 1);
      i += 4;
      freezeAuthority.write(_data, i);
    } else {
      putInt32LE(_data, i, (int) 0);
      i += 4;
    }
    i += 32;
    if (extensions != null) {
      Arrays.fill(_data, i, i + 83, (byte) 0);
      i += 83;
      _data[i] = (byte) 1;
      ++i;
      i += SerDeUtil.writeArray(extensions, _data, i);
    }
    return i - _offset;
  }

  @Override
  public int l() {
    return 36
         + 8
         + 1
         + 1
         + 36
         + (extensions == null ? 0 : 84 + SerDeUtil.lenArray(extensions));
  }
}
