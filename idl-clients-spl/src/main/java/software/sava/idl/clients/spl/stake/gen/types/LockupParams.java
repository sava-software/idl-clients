// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.stake.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;

public record LockupParams(UnixTimestamp unixTimestamp,
                           Epoch epoch,
                           PublicKey custodian) implements SerDe {

  public static final int UNIX_TIMESTAMP_OFFSET = 1;

  public static LockupParams read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final UnixTimestamp unixTimestamp;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      unixTimestamp = null;
      ++i;

    } else {
      ++i;
      unixTimestamp = UnixTimestamp.read(_data, i);
      i += unixTimestamp.l();
    }
    final Epoch epoch;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      epoch = null;
      ++i;

    } else {
      ++i;
      epoch = Epoch.read(_data, i);
      i += epoch.l();
    }
    final PublicKey custodian;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      custodian = null;
    } else {
      ++i;
      custodian = readPubKey(_data, i);
    }
    return new LockupParams(unixTimestamp, epoch, custodian);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, unixTimestamp, _data, i);
    i += SerDeUtil.writeOptional(1, epoch, _data, i);
    i += SerDeUtil.writeOptional(1, custodian, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (unixTimestamp == null ? 1 : (1 + unixTimestamp.l())) + (epoch == null ? 1 : (1 + epoch.l())) + (custodian == null ? 1 : (1 + 32));
  }
}
