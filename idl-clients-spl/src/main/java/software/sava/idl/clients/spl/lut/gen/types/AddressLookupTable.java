// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.lut.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param discriminator: u32
/// @param deactivationSlot: u64
/// @param lastExtendedSlot: u64
/// @param padding: u16
public record AddressLookupTable(PublicKey _address,
                                 long discriminator,
                                 long deactivationSlot,
                                 long lastExtendedSlot,
                                 int lastExtendedSlotStartIndex,
                                 PublicKey authority,
                                 int padding,
                                 PublicKey[] addresses) implements SerDe {

  public static final int DISCRIMINATOR_OFFSET = 0;
  public static final int DEACTIVATION_SLOT_OFFSET = 4;
  public static final int LAST_EXTENDED_SLOT_OFFSET = 12;
  public static final int LAST_EXTENDED_SLOT_START_INDEX_OFFSET = 20;
  public static final int AUTHORITY_OFFSET = 22;
  public static final int PADDING_OFFSET = 54;
  public static final int ADDRESSES_OFFSET = 56;

  public static Filter createDiscriminatorFilter() {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, (int) 1);
    return Filter.createMemCompFilter(DISCRIMINATOR_OFFSET, _data);
  }

  public static Filter createDeactivationSlotFilter(final long deactivationSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, deactivationSlot);
    return Filter.createMemCompFilter(DEACTIVATION_SLOT_OFFSET, _data);
  }

  public static Filter createLastExtendedSlotFilter(final long lastExtendedSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lastExtendedSlot);
    return Filter.createMemCompFilter(LAST_EXTENDED_SLOT_OFFSET, _data);
  }

  public static Filter createLastExtendedSlotStartIndexFilter(final int lastExtendedSlotStartIndex) {
    return Filter.createMemCompFilter(LAST_EXTENDED_SLOT_START_INDEX_OFFSET, new byte[]{(byte) lastExtendedSlotStartIndex});
  }

  public static Filter createAuthorityFilter(final PublicKey authority) {
    return Filter.createMemCompFilter(AUTHORITY_OFFSET, authority);
  }

  public static Filter createPaddingFilter() {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, 0);
    return Filter.createMemCompFilter(PADDING_OFFSET, _data);
  }

  public static AddressLookupTable read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static AddressLookupTable read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static AddressLookupTable read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], AddressLookupTable> FACTORY = AddressLookupTable::read;

  public static AddressLookupTable read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }

    int i = _offset;
    final var discriminator = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var deactivationSlot = getInt64LE(_data, i);
    i += 8;
    final var lastExtendedSlot = getInt64LE(_data, i);
    i += 8;
    final var lastExtendedSlotStartIndex = _data[i] & 0xFF;
    ++i;
    final PublicKey authority;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      authority = null;
      i += 32 + 1;
    } else {
      ++i;
      authority = readPubKey(_data, i);
      i += 32;
    }
    final var padding = Short.toUnsignedInt(getInt16LE(_data, i));
    i += 2;
    final int _addressesCount = (_data.length - i) / 32;
    final var addresses = new PublicKey[_addressesCount];
    for (int _i = 0; i < _data.length; ++_i) {
      final var _addressesItem = readPubKey(_data, i);
      i += 32;
      addresses[_i] = _addressesItem;
    }
    return new AddressLookupTable(_address,
                                  discriminator,
                                  deactivationSlot,
                                  lastExtendedSlot,
                                  lastExtendedSlotStartIndex,
                                  authority,
                                  padding,
                                  addresses);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt32LE(_data, i, (int) discriminator);
    i += 4;
    putInt64LE(_data, i, deactivationSlot);
    i += 8;
    putInt64LE(_data, i, lastExtendedSlot);
    i += 8;
    _data[i] = (byte) lastExtendedSlotStartIndex;
    ++i;
    if (authority != null) {
      _data[i] = (byte) 1;
      ++i;
      authority.write(_data, i);
    } else {
      i += 1;
    }
    i += 32;
    putInt16LE(_data, i, padding);
    i += 2;
    i += SerDeUtil.writeArray(addresses, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 4
         + 8
         + 8
         + 1
         + 33
         + 2
         + SerDeUtil.lenArray(addresses);
  }
}
