// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.stake.gen.types;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

public record LockupCheckedArgs(UnixTimestamp unixTimestamp, Epoch epoch) implements SerDe {

  public static final int UNIX_TIMESTAMP_OFFSET = 1;

  public static LockupCheckedArgs read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final UnixTimestamp unixTimestamp;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      unixTimestamp = null;
      ++i;

    } else {
      ++i;
      unixTimestamp = UnixTimestamp.read(_data, i);
      i += unixTimestamp.l();
    }
    final Epoch epoch;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      epoch = null;
    } else {
      ++i;
      epoch = Epoch.read(_data, i);
    }
    return new LockupCheckedArgs(unixTimestamp, epoch);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeOptional(1, unixTimestamp, _data, i);
    i += SerDeUtil.writeOptional(1, epoch, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return (unixTimestamp == null ? 1 : (1 + unixTimestamp.l())) + (epoch == null ? 1 : (1 + epoch.l()));
  }
}
