// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.stake.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.spl.stake.gen.types.AuthorizeCheckedWithSeedArgs;
import software.sava.idl.clients.spl.stake.gen.types.AuthorizeWithSeedArgs;
import software.sava.idl.clients.spl.stake.gen.types.Authorized;
import software.sava.idl.clients.spl.stake.gen.types.Lockup;
import software.sava.idl.clients.spl.stake.gen.types.LockupArgs;
import software.sava.idl.clients.spl.stake.gen.types.LockupCheckedArgs;
import software.sava.idl.clients.spl.stake.gen.types.StakeAuthorize;

import java.util.ArrayList;
import java.util.List;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class SolanaStakeInterfaceProgram {

  public static final Discriminator INITIALIZE_DISCRIMINATOR = toDiscriminator(0, 0, 0, 0);

  /// @param stakeKey Uninitialized stake account
  public static List<AccountMeta> initializeKeys(final SolanaAccounts solanaAccounts,
                                                 final PublicKey stakeKey) {
    return List.of(
      createWrite(stakeKey),
      createRead(solanaAccounts.rentSysVar())
    );
  }

  /// @param stakeKey Uninitialized stake account
  public static Instruction initialize(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey stakeKey,
                                       final Authorized arg0,
                                       final Lockup arg1) {
    final var keys = initializeKeys(
      solanaAccounts,
      stakeKey
    );
    return initialize(
      invokedSolanaStakeInterfaceProgramMeta,
      keys,
      arg0,
      arg1
    );
  }

  public static Instruction initialize(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                       final List<AccountMeta> keys,
                                       final Authorized arg0,
                                       final Lockup arg1) {
    final byte[] _data = new byte[4 + arg0.l() + arg1.l()];
    int i = INITIALIZE_DISCRIMINATOR.write(_data, 0);
    i += arg0.write(_data, i);
    arg1.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record InitializeIxData(int discriminator,
                                 Authorized arg0,
                                 Lockup arg1) implements SerDe {  

    public static InitializeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 116;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;
    public static final int ARG_1_OFFSET = 68;

    public static InitializeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = Authorized.read(_data, i);
      i += arg0.l();
      final var arg1 = Lockup.read(_data, i);
      return new InitializeIxData(discriminator, arg0, arg1);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += arg0.write(_data, i);
      i += arg1.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator AUTHORIZE_DISCRIMINATOR = toDiscriminator(1, 0, 0, 0);

  /// @param stakeKey Stake account to be updated
  /// @param authorityKey The stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static List<AccountMeta> authorizeKeys(final SolanaAccounts solanaAccounts,
                                                final PublicKey stakeKey,
                                                final PublicKey authorityKey,
                                                final PublicKey lockupAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(4);
    keys.add(createWrite(stakeKey));
    keys.add(createRead(solanaAccounts.clockSysVar()));
    keys.add(createReadOnlySigner(authorityKey));
    if (lockupAuthorityKey != null) {
      keys.add(createReadOnlySigner(lockupAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Stake account to be updated
  /// @param authorityKey The stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static Instruction authorize(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final SolanaAccounts solanaAccounts,
                                      final PublicKey stakeKey,
                                      final PublicKey authorityKey,
                                      final PublicKey lockupAuthorityKey,
                                      final PublicKey arg0,
                                      final StakeAuthorize arg1) {
    final var keys = authorizeKeys(
      solanaAccounts,
      stakeKey,
      authorityKey,
      lockupAuthorityKey
    );
    return authorize(
      invokedSolanaStakeInterfaceProgramMeta,
      keys,
      arg0,
      arg1
    );
  }

  public static Instruction authorize(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final List<AccountMeta> keys,
                                      final PublicKey arg0,
                                      final StakeAuthorize arg1) {
    final byte[] _data = new byte[36 + arg1.l()];
    int i = AUTHORIZE_DISCRIMINATOR.write(_data, 0);
    arg0.write(_data, i);
    i += 32;
    arg1.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record AuthorizeIxData(int discriminator,
                                PublicKey arg0,
                                StakeAuthorize arg1) implements SerDe {  

    public static AuthorizeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 40;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;
    public static final int ARG_1_OFFSET = 36;

    public static AuthorizeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = readPubKey(_data, i);
      i += 32;
      final var arg1 = StakeAuthorize.read(_data, i);
      return new AuthorizeIxData(discriminator, arg0, arg1);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      arg0.write(_data, i);
      i += 32;
      i += arg1.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DELEGATE_STAKE_DISCRIMINATOR = toDiscriminator(2, 0, 0, 0);

  /// @param stakeKey Initialized stake account to be delegated
  /// @param voteKey Vote account to which this stake will be delegated
  /// @param unusedKey Unused account, formerly the stake config
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> delegateStakeKeys(final SolanaAccounts solanaAccounts,
                                                    final PublicKey stakeKey,
                                                    final PublicKey voteKey,
                                                    final PublicKey unusedKey,
                                                    final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(stakeKey),
      createRead(voteKey),
      createRead(solanaAccounts.clockSysVar()),
      createRead(solanaAccounts.stakeHistorySysVar()),
      createRead(unusedKey),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param stakeKey Initialized stake account to be delegated
  /// @param voteKey Vote account to which this stake will be delegated
  /// @param unusedKey Unused account, formerly the stake config
  /// @param stakeAuthorityKey Stake authority
  public static Instruction delegateStake(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey stakeKey,
                                          final PublicKey voteKey,
                                          final PublicKey unusedKey,
                                          final PublicKey stakeAuthorityKey) {
    final var keys = delegateStakeKeys(
      solanaAccounts,
      stakeKey,
      voteKey,
      unusedKey,
      stakeAuthorityKey
    );
    return delegateStake(invokedSolanaStakeInterfaceProgramMeta, keys);
  }

  public static Instruction delegateStake(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                          final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    DELEGATE_STAKE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record DelegateStakeIxData(int discriminator) implements SerDe {  

    public static DelegateStakeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static DelegateStakeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new DelegateStakeIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SPLIT_DISCRIMINATOR = toDiscriminator(3, 0, 0, 0);

  /// @param stakeKey Stake account to be split; must be in the Initialized or Stake state
  /// @param splitStakeKey Uninitialized stake account that will take the split-off amount
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> splitKeys(final PublicKey stakeKey,
                                            final PublicKey splitStakeKey,
                                            final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(stakeKey),
      createWrite(splitStakeKey),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param stakeKey Stake account to be split; must be in the Initialized or Stake state
  /// @param splitStakeKey Uninitialized stake account that will take the split-off amount
  /// @param stakeAuthorityKey Stake authority
  public static Instruction split(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                  final PublicKey stakeKey,
                                  final PublicKey splitStakeKey,
                                  final PublicKey stakeAuthorityKey,
                                  final long args) {
    final var keys = splitKeys(
      stakeKey,
      splitStakeKey,
      stakeAuthorityKey
    );
    return split(invokedSolanaStakeInterfaceProgramMeta, keys, args);
  }

  public static Instruction split(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                  final List<AccountMeta> keys,
                                  final long args) {
    final byte[] _data = new byte[12];
    int i = SPLIT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, args);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record SplitIxData(int discriminator, long args) implements SerDe {  

    public static SplitIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 12;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARGS_OFFSET = 4;

    public static SplitIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var args = getInt64LE(_data, i);
      return new SplitIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      putInt64LE(_data, i, args);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WITHDRAW_DISCRIMINATOR = toDiscriminator(4, 0, 0, 0);

  /// @param stakeKey Stake account from which to withdraw
  /// @param recipientKey Recipient account
  /// @param withdrawAuthorityKey Withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if before lockup expiration
  public static List<AccountMeta> withdrawKeys(final SolanaAccounts solanaAccounts,
                                               final PublicKey stakeKey,
                                               final PublicKey recipientKey,
                                               final PublicKey withdrawAuthorityKey,
                                               final PublicKey lockupAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(6);
    keys.add(createWrite(stakeKey));
    keys.add(createWrite(recipientKey));
    keys.add(createRead(solanaAccounts.clockSysVar()));
    keys.add(createRead(solanaAccounts.stakeHistorySysVar()));
    keys.add(createReadOnlySigner(withdrawAuthorityKey));
    if (lockupAuthorityKey != null) {
      keys.add(createReadOnlySigner(lockupAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Stake account from which to withdraw
  /// @param recipientKey Recipient account
  /// @param withdrawAuthorityKey Withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if before lockup expiration
  public static Instruction withdraw(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                     final SolanaAccounts solanaAccounts,
                                     final PublicKey stakeKey,
                                     final PublicKey recipientKey,
                                     final PublicKey withdrawAuthorityKey,
                                     final PublicKey lockupAuthorityKey,
                                     final long args) {
    final var keys = withdrawKeys(
      solanaAccounts,
      stakeKey,
      recipientKey,
      withdrawAuthorityKey,
      lockupAuthorityKey
    );
    return withdraw(invokedSolanaStakeInterfaceProgramMeta, keys, args);
  }

  public static Instruction withdraw(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                     final List<AccountMeta> keys,
                                     final long args) {
    final byte[] _data = new byte[12];
    int i = WITHDRAW_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, args);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record WithdrawIxData(int discriminator, long args) implements SerDe {  

    public static WithdrawIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 12;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARGS_OFFSET = 4;

    public static WithdrawIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var args = getInt64LE(_data, i);
      return new WithdrawIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      putInt64LE(_data, i, args);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DEACTIVATE_DISCRIMINATOR = toDiscriminator(5, 0, 0, 0);

  /// @param stakeKey Delegated stake account to be deactivated
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> deactivateKeys(final SolanaAccounts solanaAccounts,
                                                 final PublicKey stakeKey,
                                                 final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(stakeKey),
      createRead(solanaAccounts.clockSysVar()),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param stakeKey Delegated stake account to be deactivated
  /// @param stakeAuthorityKey Stake authority
  public static Instruction deactivate(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey stakeKey,
                                       final PublicKey stakeAuthorityKey) {
    final var keys = deactivateKeys(
      solanaAccounts,
      stakeKey,
      stakeAuthorityKey
    );
    return deactivate(invokedSolanaStakeInterfaceProgramMeta, keys);
  }

  public static Instruction deactivate(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                       final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    DEACTIVATE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record DeactivateIxData(int discriminator) implements SerDe {  

    public static DeactivateIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static DeactivateIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new DeactivateIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_LOCKUP_DISCRIMINATOR = toDiscriminator(6, 0, 0, 0);

  /// @param stakeKey Initialized stake account
  /// @param authorityKey Lockup authority or withdraw authority
  public static List<AccountMeta> setLockupKeys(final PublicKey stakeKey,
                                                final PublicKey authorityKey) {
    return List.of(
      createWrite(stakeKey),
      createReadOnlySigner(authorityKey)
    );
  }

  /// @param stakeKey Initialized stake account
  /// @param authorityKey Lockup authority or withdraw authority
  public static Instruction setLockup(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final PublicKey stakeKey,
                                      final PublicKey authorityKey,
                                      final LockupArgs arg0) {
    final var keys = setLockupKeys(
      stakeKey,
      authorityKey
    );
    return setLockup(invokedSolanaStakeInterfaceProgramMeta, keys, arg0);
  }

  public static Instruction setLockup(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final List<AccountMeta> keys,
                                      final LockupArgs arg0) {
    final byte[] _data = new byte[4 + arg0.l()];
    int i = SET_LOCKUP_DISCRIMINATOR.write(_data, 0);
    arg0.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record SetLockupIxData(int discriminator, LockupArgs arg0) implements SerDe {  

    public static SetLockupIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;

    public static SetLockupIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = LockupArgs.read(_data, i);
      return new SetLockupIxData(discriminator, arg0);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += arg0.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 4 + arg0.l();
    }
  }

  public static final Discriminator MERGE_DISCRIMINATOR = toDiscriminator(7, 0, 0, 0);

  /// @param destinationStakeKey Destination stake account for the merge
  /// @param sourceStakeKey Source stake account for to merge.  This account will be drained
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> mergeKeys(final SolanaAccounts solanaAccounts,
                                            final PublicKey destinationStakeKey,
                                            final PublicKey sourceStakeKey,
                                            final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(destinationStakeKey),
      createWrite(sourceStakeKey),
      createRead(solanaAccounts.clockSysVar()),
      createRead(solanaAccounts.stakeHistorySysVar()),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param destinationStakeKey Destination stake account for the merge
  /// @param sourceStakeKey Source stake account for to merge.  This account will be drained
  /// @param stakeAuthorityKey Stake authority
  public static Instruction merge(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                  final SolanaAccounts solanaAccounts,
                                  final PublicKey destinationStakeKey,
                                  final PublicKey sourceStakeKey,
                                  final PublicKey stakeAuthorityKey) {
    final var keys = mergeKeys(
      solanaAccounts,
      destinationStakeKey,
      sourceStakeKey,
      stakeAuthorityKey
    );
    return merge(invokedSolanaStakeInterfaceProgramMeta, keys);
  }

  public static Instruction merge(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                  final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    MERGE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record MergeIxData(int discriminator) implements SerDe {  

    public static MergeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static MergeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new MergeIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator AUTHORIZE_WITH_SEED_DISCRIMINATOR = toDiscriminator(8, 0, 0, 0);

  /// @param stakeKey Stake account to be updated
  /// @param baseKey Base key of stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static List<AccountMeta> authorizeWithSeedKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey stakeKey,
                                                        final PublicKey baseKey,
                                                        final PublicKey lockupAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(4);
    keys.add(createWrite(stakeKey));
    keys.add(createReadOnlySigner(baseKey));
    keys.add(createRead(solanaAccounts.clockSysVar()));
    if (lockupAuthorityKey != null) {
      keys.add(createReadOnlySigner(lockupAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Stake account to be updated
  /// @param baseKey Base key of stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static Instruction authorizeWithSeed(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey stakeKey,
                                              final PublicKey baseKey,
                                              final PublicKey lockupAuthorityKey,
                                              final AuthorizeWithSeedArgs arg0) {
    final var keys = authorizeWithSeedKeys(
      solanaAccounts,
      stakeKey,
      baseKey,
      lockupAuthorityKey
    );
    return authorizeWithSeed(invokedSolanaStakeInterfaceProgramMeta, keys, arg0);
  }

  public static Instruction authorizeWithSeed(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                              final List<AccountMeta> keys,
                                              final AuthorizeWithSeedArgs arg0) {
    final byte[] _data = new byte[4 + arg0.l()];
    int i = AUTHORIZE_WITH_SEED_DISCRIMINATOR.write(_data, 0);
    arg0.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record AuthorizeWithSeedIxData(int discriminator, AuthorizeWithSeedArgs arg0) implements SerDe {  

    public static AuthorizeWithSeedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;

    public static AuthorizeWithSeedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = AuthorizeWithSeedArgs.read(_data, i);
      return new AuthorizeWithSeedIxData(discriminator, arg0);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += arg0.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 4 + arg0.l();
    }
  }

  public static final Discriminator INITIALIZE_CHECKED_DISCRIMINATOR = toDiscriminator(9, 0, 0, 0);

  /// @param stakeKey Uninitialized stake account
  /// @param stakeAuthorityKey The stake authority
  /// @param withdrawAuthorityKey The withdraw authority
  public static List<AccountMeta> initializeCheckedKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey stakeKey,
                                                        final PublicKey stakeAuthorityKey,
                                                        final PublicKey withdrawAuthorityKey) {
    return List.of(
      createWrite(stakeKey),
      createRead(solanaAccounts.rentSysVar()),
      createRead(stakeAuthorityKey),
      createReadOnlySigner(withdrawAuthorityKey)
    );
  }

  /// @param stakeKey Uninitialized stake account
  /// @param stakeAuthorityKey The stake authority
  /// @param withdrawAuthorityKey The withdraw authority
  public static Instruction initializeChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey stakeKey,
                                              final PublicKey stakeAuthorityKey,
                                              final PublicKey withdrawAuthorityKey) {
    final var keys = initializeCheckedKeys(
      solanaAccounts,
      stakeKey,
      stakeAuthorityKey,
      withdrawAuthorityKey
    );
    return initializeChecked(invokedSolanaStakeInterfaceProgramMeta, keys);
  }

  public static Instruction initializeChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                              final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    INITIALIZE_CHECKED_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record InitializeCheckedIxData(int discriminator) implements SerDe {  

    public static InitializeCheckedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static InitializeCheckedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new InitializeCheckedIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator AUTHORIZE_CHECKED_DISCRIMINATOR = toDiscriminator(10, 0, 0, 0);

  /// @param stakeKey Stake account to be updated
  /// @param authorityKey The stake or withdraw authority
  /// @param newAuthorityKey The new stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static List<AccountMeta> authorizeCheckedKeys(final SolanaAccounts solanaAccounts,
                                                       final PublicKey stakeKey,
                                                       final PublicKey authorityKey,
                                                       final PublicKey newAuthorityKey,
                                                       final PublicKey lockupAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(5);
    keys.add(createWrite(stakeKey));
    keys.add(createRead(solanaAccounts.clockSysVar()));
    keys.add(createReadOnlySigner(authorityKey));
    keys.add(createReadOnlySigner(newAuthorityKey));
    if (lockupAuthorityKey != null) {
      keys.add(createReadOnlySigner(lockupAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Stake account to be updated
  /// @param authorityKey The stake or withdraw authority
  /// @param newAuthorityKey The new stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static Instruction authorizeChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                             final SolanaAccounts solanaAccounts,
                                             final PublicKey stakeKey,
                                             final PublicKey authorityKey,
                                             final PublicKey newAuthorityKey,
                                             final PublicKey lockupAuthorityKey,
                                             final StakeAuthorize stakeAuthorize) {
    final var keys = authorizeCheckedKeys(
      solanaAccounts,
      stakeKey,
      authorityKey,
      newAuthorityKey,
      lockupAuthorityKey
    );
    return authorizeChecked(invokedSolanaStakeInterfaceProgramMeta, keys, stakeAuthorize);
  }

  public static Instruction authorizeChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                             final List<AccountMeta> keys,
                                             final StakeAuthorize stakeAuthorize) {
    final byte[] _data = new byte[4 + stakeAuthorize.l()];
    int i = AUTHORIZE_CHECKED_DISCRIMINATOR.write(_data, 0);
    stakeAuthorize.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record AuthorizeCheckedIxData(int discriminator, StakeAuthorize stakeAuthorize) implements SerDe {  

    public static AuthorizeCheckedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 8;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int STAKE_AUTHORIZE_OFFSET = 4;

    public static AuthorizeCheckedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var stakeAuthorize = StakeAuthorize.read(_data, i);
      return new AuthorizeCheckedIxData(discriminator, stakeAuthorize);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += stakeAuthorize.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator AUTHORIZE_CHECKED_WITH_SEED_DISCRIMINATOR = toDiscriminator(11, 0, 0, 0);

  /// @param stakeKey Stake account to be updated
  /// @param baseKey Base key of stake or withdraw authority
  /// @param newAuthorityKey The new stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static List<AccountMeta> authorizeCheckedWithSeedKeys(final SolanaAccounts solanaAccounts,
                                                               final PublicKey stakeKey,
                                                               final PublicKey baseKey,
                                                               final PublicKey newAuthorityKey,
                                                               final PublicKey lockupAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(5);
    keys.add(createWrite(stakeKey));
    keys.add(createReadOnlySigner(baseKey));
    keys.add(createRead(solanaAccounts.clockSysVar()));
    keys.add(createReadOnlySigner(newAuthorityKey));
    if (lockupAuthorityKey != null) {
      keys.add(createReadOnlySigner(lockupAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Stake account to be updated
  /// @param baseKey Base key of stake or withdraw authority
  /// @param newAuthorityKey The new stake or withdraw authority
  /// @param lockupAuthorityKey Lockup authority, if updating `StakeAuthorize::Withdrawer` before lockup expiration
  public static Instruction authorizeCheckedWithSeed(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                                     final SolanaAccounts solanaAccounts,
                                                     final PublicKey stakeKey,
                                                     final PublicKey baseKey,
                                                     final PublicKey newAuthorityKey,
                                                     final PublicKey lockupAuthorityKey,
                                                     final AuthorizeCheckedWithSeedArgs arg0) {
    final var keys = authorizeCheckedWithSeedKeys(
      solanaAccounts,
      stakeKey,
      baseKey,
      newAuthorityKey,
      lockupAuthorityKey
    );
    return authorizeCheckedWithSeed(invokedSolanaStakeInterfaceProgramMeta, keys, arg0);
  }

  public static Instruction authorizeCheckedWithSeed(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final AuthorizeCheckedWithSeedArgs arg0) {
    final byte[] _data = new byte[4 + arg0.l()];
    int i = AUTHORIZE_CHECKED_WITH_SEED_DISCRIMINATOR.write(_data, 0);
    arg0.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record AuthorizeCheckedWithSeedIxData(int discriminator, AuthorizeCheckedWithSeedArgs arg0) implements SerDe {  

    public static AuthorizeCheckedWithSeedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;

    public static AuthorizeCheckedWithSeedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = AuthorizeCheckedWithSeedArgs.read(_data, i);
      return new AuthorizeCheckedWithSeedIxData(discriminator, arg0);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += arg0.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 4 + arg0.l();
    }
  }

  public static final Discriminator SET_LOCKUP_CHECKED_DISCRIMINATOR = toDiscriminator(12, 0, 0, 0);

  /// @param stakeKey Initialized stake account
  /// @param authorityKey Lockup authority or withdraw authority
  /// @param newAuthorityKey New lockup authority
  public static List<AccountMeta> setLockupCheckedKeys(final PublicKey stakeKey,
                                                       final PublicKey authorityKey,
                                                       final PublicKey newAuthorityKey) {
    final var keys = new ArrayList<AccountMeta>(3);
    keys.add(createWrite(stakeKey));
    keys.add(createReadOnlySigner(authorityKey));
    if (newAuthorityKey != null) {
      keys.add(createReadOnlySigner(newAuthorityKey));
    }
    return keys;
  }

  /// @param stakeKey Initialized stake account
  /// @param authorityKey Lockup authority or withdraw authority
  /// @param newAuthorityKey New lockup authority
  public static Instruction setLockupChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                             final PublicKey stakeKey,
                                             final PublicKey authorityKey,
                                             final PublicKey newAuthorityKey,
                                             final LockupCheckedArgs arg0) {
    final var keys = setLockupCheckedKeys(
      stakeKey,
      authorityKey,
      newAuthorityKey
    );
    return setLockupChecked(invokedSolanaStakeInterfaceProgramMeta, keys, arg0);
  }

  public static Instruction setLockupChecked(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                             final List<AccountMeta> keys,
                                             final LockupCheckedArgs arg0) {
    final byte[] _data = new byte[4 + arg0.l()];
    int i = SET_LOCKUP_CHECKED_DISCRIMINATOR.write(_data, 0);
    arg0.write(_data, i);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record SetLockupCheckedIxData(int discriminator, LockupCheckedArgs arg0) implements SerDe {  

    public static SetLockupCheckedIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARG_0_OFFSET = 4;

    public static SetLockupCheckedIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var arg0 = LockupCheckedArgs.read(_data, i);
      return new SetLockupCheckedIxData(discriminator, arg0);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      i += arg0.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 4 + arg0.l();
    }
  }

  public static final Discriminator GET_MINIMUM_DELEGATION_DISCRIMINATOR = toDiscriminator(13, 0, 0, 0);

  public static Instruction getMinimumDelegation(final AccountMeta invokedSolanaStakeInterfaceProgramMeta) {
    final byte[] _data = new byte[4];
    GET_MINIMUM_DELEGATION_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, List.of(), _data);
  }

  public record GetMinimumDelegationIxData(int discriminator) implements SerDe {  

    public static GetMinimumDelegationIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static GetMinimumDelegationIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new GetMinimumDelegationIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DEACTIVATE_DELINQUENT_DISCRIMINATOR = toDiscriminator(14, 0, 0, 0);

  /// @param stakeKey Delegated stake account
  /// @param delinquentVoteKey Delinquent vote account for the delegated stake account
  /// @param referenceVoteKey Reference vote account that has voted at least once in the last `MINIMUM_DELINQUENT_EPOCHS_FOR_DEACTIVATION` epochs
  public static List<AccountMeta> deactivateDelinquentKeys(final PublicKey stakeKey,
                                                           final PublicKey delinquentVoteKey,
                                                           final PublicKey referenceVoteKey) {
    return List.of(
      createWrite(stakeKey),
      createRead(delinquentVoteKey),
      createRead(referenceVoteKey)
    );
  }

  /// @param stakeKey Delegated stake account
  /// @param delinquentVoteKey Delinquent vote account for the delegated stake account
  /// @param referenceVoteKey Reference vote account that has voted at least once in the last `MINIMUM_DELINQUENT_EPOCHS_FOR_DEACTIVATION` epochs
  public static Instruction deactivateDelinquent(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                                 final PublicKey stakeKey,
                                                 final PublicKey delinquentVoteKey,
                                                 final PublicKey referenceVoteKey) {
    final var keys = deactivateDelinquentKeys(
      stakeKey,
      delinquentVoteKey,
      referenceVoteKey
    );
    return deactivateDelinquent(invokedSolanaStakeInterfaceProgramMeta, keys);
  }

  public static Instruction deactivateDelinquent(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                                 final List<AccountMeta> keys) {
    final byte[] _data = new byte[4];
    DEACTIVATE_DELINQUENT_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record DeactivateDelinquentIxData(int discriminator) implements SerDe {  

    public static DeactivateDelinquentIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static DeactivateDelinquentIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new DeactivateDelinquentIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator REDELEGATE_DISCRIMINATOR = toDiscriminator(15, 0, 0, 0);

  public static Instruction redelegate(final AccountMeta invokedSolanaStakeInterfaceProgramMeta) {
    final byte[] _data = new byte[4];
    REDELEGATE_DISCRIMINATOR.write(_data, 0);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, List.of(), _data);
  }

  public record RedelegateIxData(int discriminator) implements SerDe {  

    public static RedelegateIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 4;

    public static final int DISCRIMINATOR_OFFSET = 0;

    public static RedelegateIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = getInt32LE(_data, _offset);
      return new RedelegateIxData(discriminator);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MOVE_STAKE_DISCRIMINATOR = toDiscriminator(16, 0, 0, 0);

  /// @param sourceStakeKey Active source stake account
  /// @param destinationStakeKey Active or inactive destination stake account
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> moveStakeKeys(final PublicKey sourceStakeKey,
                                                final PublicKey destinationStakeKey,
                                                final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(sourceStakeKey),
      createWrite(destinationStakeKey),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param sourceStakeKey Active source stake account
  /// @param destinationStakeKey Active or inactive destination stake account
  /// @param stakeAuthorityKey Stake authority
  public static Instruction moveStake(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final PublicKey sourceStakeKey,
                                      final PublicKey destinationStakeKey,
                                      final PublicKey stakeAuthorityKey,
                                      final long args) {
    final var keys = moveStakeKeys(
      sourceStakeKey,
      destinationStakeKey,
      stakeAuthorityKey
    );
    return moveStake(invokedSolanaStakeInterfaceProgramMeta, keys, args);
  }

  public static Instruction moveStake(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                      final List<AccountMeta> keys,
                                      final long args) {
    final byte[] _data = new byte[12];
    int i = MOVE_STAKE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, args);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record MoveStakeIxData(int discriminator, long args) implements SerDe {  

    public static MoveStakeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 12;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARGS_OFFSET = 4;

    public static MoveStakeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var args = getInt64LE(_data, i);
      return new MoveStakeIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      putInt64LE(_data, i, args);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator MOVE_LAMPORTS_DISCRIMINATOR = toDiscriminator(17, 0, 0, 0);

  /// @param sourceStakeKey Active or inactive source stake account
  /// @param destinationStakeKey Mergeable destination stake account
  /// @param stakeAuthorityKey Stake authority
  public static List<AccountMeta> moveLamportsKeys(final PublicKey sourceStakeKey,
                                                   final PublicKey destinationStakeKey,
                                                   final PublicKey stakeAuthorityKey) {
    return List.of(
      createWrite(sourceStakeKey),
      createWrite(destinationStakeKey),
      createReadOnlySigner(stakeAuthorityKey)
    );
  }

  /// @param sourceStakeKey Active or inactive source stake account
  /// @param destinationStakeKey Mergeable destination stake account
  /// @param stakeAuthorityKey Stake authority
  public static Instruction moveLamports(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                         final PublicKey sourceStakeKey,
                                         final PublicKey destinationStakeKey,
                                         final PublicKey stakeAuthorityKey,
                                         final long args) {
    final var keys = moveLamportsKeys(
      sourceStakeKey,
      destinationStakeKey,
      stakeAuthorityKey
    );
    return moveLamports(invokedSolanaStakeInterfaceProgramMeta, keys, args);
  }

  public static Instruction moveLamports(final AccountMeta invokedSolanaStakeInterfaceProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long args) {
    final byte[] _data = new byte[12];
    int i = MOVE_LAMPORTS_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, args);

    return Instruction.createInstruction(invokedSolanaStakeInterfaceProgramMeta, keys, _data);
  }

  public record MoveLamportsIxData(int discriminator, long args) implements SerDe {  

    public static MoveLamportsIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 12;

    public static final int DISCRIMINATOR_OFFSET = 0;
    public static final int ARGS_OFFSET = 4;

    public static MoveLamportsIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }

      int i = _offset;
      final var discriminator = getInt32LE(_data, i);
      i += 4;
      final var args = getInt64LE(_data, i);
      return new MoveLamportsIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset;
      putInt32LE(_data, i, discriminator);
      i += 4;
      putInt64LE(_data, i, args);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private SolanaStakeInterfaceProgram() {
  }
}
