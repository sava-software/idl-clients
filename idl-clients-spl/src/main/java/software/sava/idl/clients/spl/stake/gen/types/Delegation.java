// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.spl.stake.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param stake: u64
public record Delegation(PublicKey voterPubkey,
                         long stake,
                         Epoch activationEpoch,
                         Epoch deactivationEpoch,
                         byte[] reserved) implements SerDe {

  public static final int BYTES = 64;
  public static final int RESERVED_LEN = 8;

  public static final int VOTER_PUBKEY_OFFSET = 0;
  public static final int STAKE_OFFSET = 32;
  public static final int ACTIVATION_EPOCH_OFFSET = 40;
  public static final int DEACTIVATION_EPOCH_OFFSET = 48;
  public static final int RESERVED_OFFSET = 56;

  public static Delegation read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var voterPubkey = readPubKey(_data, i);
    i += 32;
    final var stake = getInt64LE(_data, i);
    i += 8;
    final var activationEpoch = Epoch.read(_data, i);
    i += activationEpoch.l();
    final var deactivationEpoch = Epoch.read(_data, i);
    i += deactivationEpoch.l();
    final var reserved = new byte[8];
    SerDeUtil.readArray(reserved, _data, i);
    return new Delegation(voterPubkey,
                          stake,
                          activationEpoch,
                          deactivationEpoch,
                          reserved);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    voterPubkey.write(_data, i);
    i += 32;
    putInt64LE(_data, i, stake);
    i += 8;
    i += activationEpoch.write(_data, i);
    i += deactivationEpoch.write(_data, i);
    i += SerDeUtil.writeArrayChecked(reserved, 8, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
