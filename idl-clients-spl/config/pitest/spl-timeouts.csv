# Audited timeout-detected mutants for the 'spl' suite — line-less
# class,method,mutator keys (HARDENING.md, the audited-set bullet). Currently
# empty: the suite has no timeout-detected mutants. The file exists to arm the
# verify's audit — a first timed-out mutant here should warn (reviewer-stop),
# not pass as an unaudited count.
