// @generated by sava-software idl-src-gen — DO NOT EDIT
package software.sava.idl.clients.phoenix.dev.perpetuals.gen.types;

import java.util.OptionalLong;

import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

public record SetPermissionInstruction(long permission,
                                       OptionalLong expiresAtTimestamp,
                                       OptionalLong allowedSignerActions) implements SerDe {

  public static final int PERMISSION_OFFSET = 0;
  public static final int EXPIRES_AT_TIMESTAMP_OFFSET = 9;

  public static SetPermissionInstruction read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var permission = getInt64LE(_data, i);
    i += 8;
    final OptionalLong expiresAtTimestamp;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      expiresAtTimestamp = OptionalLong.empty();
      ++i;
    } else {
      ++i;
      expiresAtTimestamp = OptionalLong.of(getInt64LE(_data, i));
      i += 8;
    }
    final OptionalLong allowedSignerActions;
    if (SerDeUtil.isAbsent(1, _data, i)) {
      allowedSignerActions = OptionalLong.empty();
    } else {
      ++i;
      allowedSignerActions = OptionalLong.of(getInt64LE(_data, i));
    }
    return new SetPermissionInstruction(permission, expiresAtTimestamp, allowedSignerActions);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    putInt64LE(_data, i, permission);
    i += 8;
    i += SerDeUtil.writeOptional(1, expiresAtTimestamp, _data, i);
    i += SerDeUtil.writeOptional(1, allowedSignerActions, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 8 + (expiresAtTimestamp == null || expiresAtTimestamp.isEmpty() ? 1 : (1 + 8)) + (allowedSignerActions == null || allowedSignerActions.isEmpty() ? 1 : (1 + 8));
  }
}
